{
  "scenario": "fp-shell-subscription",
  "doc": "A shell subscription (kind = shell, every = 30s) is the kernel's only cron: it runs the command once per period with no model turn.",
  "status": "break",
  "breaks": [
    {
      "rule": "bare-subscription-silently-ignored",
      "detail": "a hand-written subscription without id/created/next_due ran 0 time(s) and the kernel never named 0002-bare.toml in kernel.log or stderr; the file system is the interface, a file the kernel cannot use must be said",
      "where": "arbos-core/src/subscription.rs load(): read(&p).ok() drops parse errors"
    }
  ],
  "notes": {
    "ticks_in_40s": 2,
    "bare_ticks_in_40s": 0,
    "user_told": 4
  },
  "duration_s": 40.4,
  "place": "/tmp/arbos-qa-fp-shell-subscription-saxtz8p8/place",
  "with_model": true,
  "kernel": "/home/ubuntu/arbos-qa/target-track-cursor-ask-parks-b027/release/arbos-kernel",
  "rollout": "/home/ubuntu/arbos-qa/loop/rollouts/20260913T214330Z-fp-shell-subscription",
  "branch": "cursor/ask-parks-b027",
  "usd": 0.0
}