# Arbos

- [ ] A job wrote 164 GB into a deleted file and filled a disk — the trace names a trap we built: the pid `jobs` displays is the wrapper holding the leash, so when the model did the obvious thing and killed what `jobs` showed, it disarmed the safety and orphaned the writer, which then ran 4.7 hours against a size cap that read 0 because its folder was gone. Closed in [#377](https://github.com/unarbos/arbos/pull/377): a signal at that pid now ends the whole group and says so, the `bash` tool routes a literal `kill` on a running job to the kernel's own kill, the wrapper watches its own parent, and an archived child's job follows its folder with the cap holding there rather than going blind; eight tests cover each route, including both shells killed and the next kernel reaping the group. It was held out of `main` for an hour on a measurement that turned out to be a commit mismatch — QA's head predated the child-reaping fix — and cleared by a re-measurement at `1a0003a2` with a control in the same minute: delivery in 0.2 s three runs of three, the trap still closing, the old commit still held. Merged, so the trap is closed on `main` and the next dev build carries it; the steward re-read the kill path at the merged head and found the routing narrow — only pids that are currently running jobs of that agent, with `kill 1`, `kill -- -N` and compound lines untouched. Two habits came out of it and are now rules: every bug file names the commit and the kernel's version line for both finding and control, and a hold placed on a measurement lifts only on a re-measurement by whoever made the original

- [ ] Rewind is closed out — nine scenarios now stand on it, each proven against a control that fails, and the family that started with the file deletion ran to eight bugs: the last was the subtlest, where a retry meant to save a missing checkpoint produced a **wrong** one instead, so a rewind restored the turn's own work and said it had succeeded. Fixed at the source: a tool whose plan writes now waits for the turn's snapshot, so the "before" tree is always from before. What remains is its face — the wait is charged to the command, so a person on a large repository sees `echo` take six seconds and blames their machine

- [ ] Rewind deletes untracked files when a project's git has no identity — the checkpoint's `commit-tree` fails silently, so every checkpoint holds HEAD alone, and the restore's `reset --hard` plus `clean -fd` wipes everything the kept turns made while reporting success (`qal-j08`, pinned by `rw-04`, failing twice of twice on both kernels with a passing control). Worse beside it: `undo` had the same hole on a shorter path, deleting every untracked file including ones the user had before Arbos ever ran, with no rewind and no fresh place needed. [#390](https://github.com/unarbos/arbos/pull/390) answers all four — the checkpoint writes under Arbos's own identity, a checkpoint that cannot be written records why instead of looking empty, a restore refuses `files: true` on a record it knows is missing, and `undo` restores the tree it used to delete — verified by QA at `9ade320f` with the `undo` case pinned too, and the steward told to get it onto his Update rather than only onto `main`; expect old checkpoints to be refused, which is correct, though it currently arrives as an error rather than a notice and should say when it stops applying. The hunt for the family found two more destructive members open on it — one unreadable read of the coordinator's page and the next `plan` writes an empty one over it, and a failed turn-start mark makes `undo` delete a commit — against a census of 34 best-effort writes and 12 reads that turn failure into an empty value; the fix that closes the class is [#392](https://github.com/unarbos/arbos/pull/392): reads answer `Present`, `Absent` or `Unknown(why)` and never a default, writes land whole or not at all, and nothing destructive — no reset, no clean, no rewrite over old bytes — acts on a record we could not confirm, with the rule written into the design and the smell to grep for beside it; both further bugs fall out of it, an unread page refuses to be saved over and a failed undo mark leaves no mark, and #390's blunt refusal becomes a notice saying what was rewound, why the files were not, and that turns from now on restore theirs. QA closed both against it and checked the refusals had not eaten the ordinary path, then found one the fix misses and Jacob is the exact population for: `qal-j12`, where the one-time migration's completion is recorded only by a rename whose failure is discarded, so a single slip makes it migrate again on the next start and every standing cron and pending task exists twice for ever — it triggers on the first start after an upgrade of any place that still has a `plan.jsonl`, which is his older projects, and he is about to update from 1185, so it ships in the same build: the completion record is now written first, and a migration that cannot start says so in words a person can act on. All six of the family are filed and closed against named commits, the last being the same lesson one turn later — a migration cut halfway kept its file but told nobody, now finished properly with the person told what was carried over. The hunt has moved to the states nobody tests, the ones after something has already gone wrong: two windows on one place passes cleanly, while renaming a project folder under a running kernel tells the person their agent is archived, which is untrue, and swallows the line they had typed (`qal-j14`). The suspicion that rewind also eats the transcript did not reproduce in 32 rewinds under three load shapes

- [ ] Our own tools have been reporting results they never established — four in one night, all found by asking rather than by failure: the benchmark loop scoring rollouts where the agent downloaded the upstream fix (headline number withdrawn), 29 QA scenarios printing `pass` while skipping themselves, a subscription test matching the kernel's own echo instead of a delivered line, and a parity kernel measuring Sep 14 code for two and a half days because nothing restarts a process whose binary is gone. A fifth was the record of what shipped: a PR's closed state was read as its fate, so a merged PR was reported as lost and I passed that to Jacob — the steward now ends every pass by testing each closed PR's head against `main`'s tree, a check that found nothing lost across the whole period but whose first draft failed silently and reported all clear, so an empty listing now refuses to pass. Each loop is now stating what its rig cannot see — the parity loop audited ten ways its gate could pass on a lie and fixed three, including a rig that clicked elements nobody could see, which is what F-123 turned out to be, then went through all 119 of its assertions in one sitting and found eight that could never fail, now marked `unverified` rather than passing — and [#385](https://github.com/unarbos/arbos/pull/385) ends the hub's habit of reporting a build it does not know: a `builds` list with one entry per process, a live `binary_gone` on `hello`, `/healthz` and the update line, and a machine-wide build only when every process agrees. Asking whether that detector works on a machine we did not choose found that it did not: it was a path check, and on macOS the path still exists after our own installer stages a build and renames it over the same file, so it would have answered "not gone" for Jacob's Mac — the case it exists for. It became an identity comparison, and a review against the real incident found that blind too: our updater renames the whole bundle aside, so the running kernel's inode travels with it, `current_exe()` names the backup, and the identity matched itself while the start path held a new build. It now remembers the path taken at start and asks what is there now, so an unlink-and-write, a bundle rename and a moved file all read as gone. Three times tonight a safeguard turned out not to cover the case it was built for, each caught by reading code against the incident rather than by a test — both clients told, since an empty build field now means "they differ" rather than "unknown". A lead worth watching under it: the flake everyone read past for days may be a real rewind race, where a rewind with `files: true` empties the transcript it is rewinding ([sweep](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/internal/mesh-stale-binary-sweep-2026-09-17.md))

- [ ] **We deleted the store ourselves, all seven times — the report must not be sent.** The QA loop's attack list contained `cd / && rm -rf *` annotated "not caught, decide whether it should be"; its agent read that and ran it, and as a normal user it fails on system directories and succeeds on the first writable tree, which was the store. The proof is that machine's own file-operation log: delete bursts from that client at exactly the seven episode times and at no other time. The restores then wrote 06:41 mirror copies over other people's newer files, which is the phone loop's "older version being served". Closed at the source: the loop's kernels now run in a namespace where the store is an empty directory, destructive commands in attack notes are rewritten before an agent reads them, and the mirror guard is absolute — a directory that does not list, or any fall in file count, refuses the pass. The last unexplained observation is now explained too, and it is the one genuine finding for the store's engineers: **a client whose credential is missing lists the store as empty with no error at all.** QA proved it by accident — its own guard test wiped the grant file out of a world-writable directory, and from that moment its client read the store as empty while another machine read it whole. That single behaviour is why a deleted credential looked twice like a deleted store. One sentence, certainly theirs, and worth more than the long document we nearly sent. Lost for good: anything written into `docs/`, the inbox, `parity/` or the bug drafts between 06:41 and 06:52 that exists nowhere else

- [ ] **An agent can wipe a whole tree with no ask and no refusal** (`qal-j15`) — the guard splits a command on `&&` and never reads the `cd`, so `cd / && rm -rf *`, `cd /usr && rm -rf *` and `rm -rf "$PWD"/*` all run. This is what deleted the store seven times, and Jacob runs in full auto, so the same command against his home directory would not be stopped. Closed in [#410](https://github.com/unarbos/arbos/pull/410) and merged: the guard now reads the command's effective directory rather than one fragment, and a removal whose target resolves to a root, a home or a system tree is refused in every mode rather than asked about. It ran once more at 08:13 before that landed, and with the store hidden the walk moved on to the next writable tree and took half of the QA machine's home directory — its keys, its cargo and cursor folders — because the first defuser had left the command in the prompt with "do not run this, only check whether the kernel asks", and the only way to check that is to run it. The loop's kernels now see a read-only home, destructive commands are removed from notes outright, and QA is asserting the guard itself on a kernel with no namespace to save it, since that is the protection Jacob actually has

- [ ] Superseded by the finding above, kept for the record — what we believed while it was happening: the store drops the same files again and again, six episodes, each restored from the mirror within minutes, 174 files this time and back within eight; the newest was caught mid-deletion at 03:26, three minutes after a clean push, and the removal sweeps the tree over 30 to 60 seconds in the same directory order with nothing ever coming back, which is the sharpest evidence yet; the [report for Cursor](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/store-fault-report-2026-09-17.md) is written and waiting on Jacob to forward it, since only the service side can see what selects the files ([record](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/internal/store-docs-loss-2026-09-16.md)); the selection is deterministic — `docs/`, the inbox, the rig tooling and the hash-named bug drafts go, while `notes.md`, `media/`, the QA tooling and human-named files never have — which is the fact worth sending them; a third phenomenon was caught live at 05:57 and is now its own section: the same store answered three clients differently in the same minute — empty and unwritable to one VM since 05:35, whole and writable to this machine and to QA's, which pushed two complete mirrors inside that blackout — which points a service engineer at per-session state rather than at deletion. Our alarm could not have fired for it by construction, since the mirror judges only what its own client sees, and that limitation is now written down with its remedies; the mirror runs every fifteen minutes, refuses to push a damaged view, and stages rather than restores unless I say otherwise; the question of whether any episode was actually us is now answered in the report itself, under "Could the client have done it? — checked, and no": our own bug truncates to a smaller valid file and cannot remove one, every loss was an absence, no file across 108 snapshots of the day shrank by more than half, and the one file shaped like our bug's target is the one file never lost — so it is ready to send

- [ ] The mesh's weakest point is now its address, not its code — everything dials one process on ArbosLife through three stitched URLs: the pod's tunnel, ArbosLife's own quick tunnel whose URL changes whenever it restarts, and `hub-api.arbos.life`, which still points at another of Jacob's services and answers 404 — that last one is why his feedback looked unreachable while the hub was healthy, though his own Mac was repointed at 01:37 and all four of his reports did arrive — nothing of his was dropped, and the two still stranded are from a closed project on build 1185, which predates the fix that drains from a registry rather than from open tabs. Underneath, the hub and three kernels run in `while true` loops in tmux with no reboot entry, so one reboot of that box takes the mesh down while the phone still shows cached rows. reboot survival is done — six user units installed, enabled and proven by restarting each through systemd rather than by rebooting his box, with the quick tunnels deliberately left alone since restarting them rotates their addresses. The pod's 20 September deadline was real — recorded correctly from the provider on 13 September, briefly and wrongly declared fictional when the wrong field was read — and on Jacob's choice of C it is **extended to 17 October** at the same $1.19/h, about $860 for the month, with the auto-termination kept so forgetting costs a month at most. What remains is the CNAMEs, or DNS-edit on the token so the records stop needing him

Goals, principles, benchmark: [project-context](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/project-context.md) — restored at half its size after this morning's store loss, so treat it as incomplete until its gaps are refilled

<tldr>
- [Ship Arbos to main with README](bc-71eb0fc3-658e-5b64-8b2b-9854416c9baf) — [append_line](https://github.com/unarbos/arbos/pull/721) on `main`; take [tool fold](https://github.com/unarbos/arbos/pull/722) if green
- [Add in-app update bar](bc-37bdb830-611b-5b03-8528-e12912f71b59) — Mac **2183**; next zip names [headless records](https://github.com/unarbos/arbos/pull/720) and [append_line](https://github.com/unarbos/arbos/pull/721)
- [Run iOS app loop on AWS Mac](bc-7c66cfa8-381e-5700-9d78-3129f338a4fa) — cycle 148; [Stop ends the turn](https://github.com/unarbos/arbos/pull/719) on `main`
- [Run SWE-bench through Arbos harness](bc-bfb2cd63-da09-5a42-920b-3410d3337c9c) — cycle 38 sent
</tldr>

- [x] [Side panel drawer](https://github.com/unarbos/arbos/pull/476) — merged on `1b51d97`; next Update after 1534 carries it

- [ ] [Fix project panel layout](bc-33718272-0f05-555b-946f-f08db1b9ee4b) — [#544](https://github.com/unarbos/arbos/pull/544) on `main`; in **1765**
- [ ] [Make settings an inline tab](bc-6d9c3785-7bed-5cb3-9eb7-bca86aad4ee5) — his ask: a full inline tab rather than a floating window, with the gear still the way in and real routes back, and without reintroducing a separate band across the top; boundaries written down against the parity loop after two duplications today

## Voice and phone (end goal)

- [ ] [Hold Live then speak kernel](bc-32d10b66-6bef-50c3-9ccf-4350ba54f23a) — [voice path latency](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/voice-path-latency.md); kernel 8–11 s is the wait; official slug on `main` in [Jev controller](https://github.com/unarbos/arbos/pull/647)
  - [x] [Mirror call text in chat](bc-5691d9b7-e5a5-578a-a19f-a7be750d7671) — [#501](https://github.com/unarbos/arbos/pull/501) on `main`
  - [x] [Give Live full chat context](bc-4e8d547d-fa8a-535d-a931-0d11d7b4fdbb) — [#500](https://github.com/unarbos/arbos/pull/500) on `main`
- [ ] A call goes silent while the agent works, and Jacob could not tell that from a crash — he asked "what are you working on", heard "let me get that information", and nothing came back, with the strip returning to Listening as though the exchange were over; the cause is being found on the desktop path, with the filler settling the turn as the first suspicion. His ask beside it is the real fix: a sound while commands are running, driven by the actual running state rather than a timer, tolerable for minutes rather than seconds, and ducking out of the way the moment either of them speaks

- [ ] [iOS app scaffold PR](https://github.com/unarbos/arbos/pull/5) — voice call, text mode, and Main chat all live against the real kernel and speech server ([screenshot](/opt/cursor/artifacts/screenshots/ios/live-kernel-chat.png)); installed on Jacob's iPhone
- [ ] Hosting move — the backend now runs on ArbosLife: hub, Jacob's phone kernel and the voice gateway all served from there behind a named tunnel, with the pod merely forwarding its old addresses so nothing broke under his phone ([plan](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/arbos-mesh-design.md)); releasing the pod now needs only his two CNAMEs, the next phone build installed, his Mac's `hub.toml` repointed whenever he lets us on that machine again, and his GPU choice for the voice model (~$220–500 against ~$860, pod expires 20 September); a crontab line for reboot survival also waits on him

- [ ] [Voice server PR](https://github.com/unarbos/arbos/pull/56) — live on rented Lium GPU ($1.19/h, ends 09-20) via temp tunnel with supervisor publishing voice, kernel, and hub URLs; server-side echo gate and output normalisation on (both engines at −3 dBFS peak; speakerphone talk-over in duplex limited without client AEC); Jacob to add DNS CNAMEs and decide GPU hosting; PAT on pod should be swapped for a fine-grained token
- [ ] The caller's first word now survives, both halves — the phone was dropping 320 to 400 ms of every opening sentence before its socket was up ([#402](https://github.com/unarbos/arbos/pull/402), proven by frame counts), and the remaining clip was the duplex model's own transcription dropping the onset regardless of what preceded it, proven by streaming into the container with no gateway in the path. The uplink is now segmented and transcribed by the Whisper model already on that GPU, and that text is what the app shows and the kernel is asked ([#56](https://github.com/unarbos/arbos/pull/56), live on the pod): "Hello Arbos, what are we working on right now?" arrives whole, 757 ms after speech ends, and barge-in cuts a reply in 232 ms where it took up to 2.4 s. Verified independently on the real microphone path, with small talk still answering whole at 559 ms, and the verification found one more of its own: a caller who pauses mid-sentence used to see the agent drop back to listening as though it had given up, now zero occurrences ([#409](https://github.com/unarbos/arbos/pull/409)). One left with the speech server — a long pause makes the answer text arrive twice

- [ ] Voice in the call — both faults fixed and live on the pod: every non-small-talk turn now goes to the kernel and its answer is spoken (the model had called the agent once in 101 turns, so it was made structural), and barge-in cuts a reply in 0.35–0.41 s instead of nearly two seconds; the phone must stop forwarding finals itself and re-measure over AirPods, and a loud speakerphone without echo cancellation is still the open limit

- [x] [Clean desktop chrome clutter](bc-1c45e4f0-9522-56f6-a7b6-f8a1aa864105) — [Jacob's chrome](https://github.com/unarbos/arbos/pull/688) on `main` (`27c77664`)
- [x] [Speed terminal and browser](bc-fddc4e9f-b8ac-5d93-a0fa-0cb3ec84a180) — [prompt opens](https://github.com/unarbos/arbos/pull/692) in Mac **2117**
- [ ] [Fix top bar chrome](bc-85782d4e-78b3-55d7-80ba-a25615cd0690) — [top bar](https://github.com/unarbos/arbos/pull/654) in Mac **2056**
- [ ] [Remove composer branch chip](bc-97ab6331-b8f3-5844-b20d-2004eb4e2b9d) — [branch chip](https://github.com/unarbos/arbos/pull/656) in Mac **2056**
- [ ] [Fix Mac fullscreen chrome](bc-0df916de-0e66-5368-b418-08c4a8340ec4) — [#542](https://github.com/unarbos/arbos/pull/542) on `main`; still not a real Space; [chrome on 2019](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/desktop-chrome-2019.md) owns the next pass
- [ ] [Architect desktop call mode](bc-9590b6c7-3ece-5b78-bb08-21ae0191cf3f) — slice 1 live: [design](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/desktop-call-mode-design.md), [gateway narrator #100](https://github.com/unarbos/arbos/pull/100), [inbox channel #99](https://github.com/unarbos/arbos/pull/99), [desktop call UI #101](https://github.com/unarbos/arbos/pull/101), slices 1–4 shipped: spoken approvals with re-ask, reconnect, drill-down by phrase, hub attach per call, 11/11 harness, live hub call into `mac/.arbos`; desktop call now matches the phone in [#101 `eb5f930`](https://github.com/unarbos/arbos/pull/101): in-process playback, continuous uplink, model voice for small talk + narrator, gateway ASR for Fn (0.7 s first word); Mac live check pending; QA owns perfecting
- [ ] [Run iOS app loop on AWS Mac](bc-7c66cfa8-381e-5700-9d78-3129f338a4fa) — cycle 148; [Stop ends the turn](https://github.com/unarbos/arbos/pull/719) on `main` (`9821dc90`); TestFlight **2160**; leftover `bc-08d8261b` stays asleep
- [ ] [Build in-app feedback for desktop](bc-0d55088a-e9bd-57ba-bbdd-3a893272675e) — the whole chain is merged and proven with a real report crossing the hub: the thumbs-down opens a review sheet showing every part as readable lines, the report is on his disk before Send returns, delivery drains itself into a place of its own on ArbosLife where the credential cannot touch his projects, and the parity loop picks it up within fifteen minutes and tells him which build fixed it ([design](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/desktop-feedback-design.md)); building it found more than it shipped — a sheet that froze the app on the first real click, a screenshot that would silently never attach, his own words never redacted at all, a retry promise the code did not keep, and a test failing one run in forty that had been holding other workers' PRs; the missing Send button was the status sentence shoving it sideways off the window, measured at x=1216 in his exact state and fixed in [#370](https://github.com/unarbos/arbos/pull/370) along with Send vanishing after a failure, an old kernel's refusal being dropped silently, and reports in closed projects never retrying; four of his reports have now crossed and are in the feedback store with their screenshots, after his `url` was pointed at the tunnel directly — `hub-api.arbos.life` goes to another app until he adds the CNAME; two more wait on his Mac because the drain only sweeps open projects; and after his update the report itself is complete, with the earlier "nothing to send" rows explained by a kernel older than the frame

- [ ] Jacob's TestFlight feedback is an input — the poller runs every fifteen minutes and picked up eight reports with screenshots; today's four fixes are merged and on his TestFlight as build 994, including the serious one where lines typed in a disconnected project were run by the next project he opened — that one was reasoned from his screenshots rather than reproduced, so the loop is proving it live with the network cut; waiting on his word about two older reports, and his `subnet120` kernel needs updating since it is his own project

- [ ] [Kernels update themselves](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/kernel-self-update-design.md) — Jacob's ask after his own `subnet120` served days-old code: [#318](https://github.com/unarbos/arbos/pull/318) gives a verified `arbos-kernel update`, proven against the live feed and pulling the kernel alone, and it has fixed his `subnet120` kernel — its binary was current since 15 September while the process still served 14 September code, which no file check could see; hub-registered kernels track `dev`, updating is on by default with four refusals, and it re-execs in place so a bad binary leaves the old kernel serving rather than none; the running process's own commit is the only honest staleness check, since replacing the file leaves the old image running; driving the gate's assumptions then found a second false one — the boot reap killed every detached job rather than only orphans, so an update would have ended the jobs it was told to work around, fixed in [#353](https://github.com/unarbos/arbos/pull/353) along with making job survival and the gate's refusal readable from the log and `/healthz`; the automatic moment is next and will not run unattended until six behaviours are watched on a real kernel; four of the six are now readable straight from the kernel, the last one confirming a claim rather than disproving it, and the other two belong to the updater — after three "facts" about restart behaviour turned out to be correct readings of the code and wrong about what happens

- [ ] Push to a sleeping phone — everything but Jacob's APNs key is done: the hub registers device tokens, posts a push per notification and a silent badge on `seen`, and [#333](https://github.com/unarbos/arbos/pull/333) makes the handoff safe and visible — a bad key path no longer stops the hub serving, `GET /push` reports why push is off, and `GET /push/test` proves a buzz the minute the key lands

- [ ] iPhone on-device pass — build 13 is installed and running on Jacob's iPhone Air (replacing build 12), pointed at the live hub; his 17e is unpaired if that is the phone he meant; three findings with the iOS worker — the reply drawn twice, identical globes instead of each project's icon, and the name repeated three times per row ([stills](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/media/mobile/device-build-13)); earlier build 12 findings: loudness fixed (duplex model output was 14 dB low, normalised on device; gateway-side normalisation requested), round trip 0.5 s, no self-hearing, hub picker; open: barge-in over speaker (iOS mutes mic in playback), background audio check by Jacob
- [ ] Reach agents from anywhere — fixed: CI builds had been shipping with no settings file at all, so [#276](https://github.com/unarbos/arbos/pull/276) bakes the phone's own rotated token, replaces a stale keychain copy on launch, and blocks any upload whose hub address or token the hub rejects — build 920 is the first guarded upload and reaches his real projects; the pod hub is redeployed and relays frames raw so it can no longer drop fields silently; Jacob to add CNAMEs for `hub-api` and `kernel-api.arbos.life`, which is why the phone still uses a throwaway tunnel URL
- [ ] [Build Arbos mesh: workers everywhere](bc-22d20d79-de36-524a-ae31-3e1c44c03b98) — both watchers green; Jacob's Mac joined at 10:10 UTC as `mac` (five kernels); live gateway serving [pause-split](https://github.com/unarbos/arbos/pull/562) (`1e0e9cde`)

## Agent architecture

- [ ] [Multitasking audit](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/multitasking-audit-2026-09-13.md) — verdict: kernel right, desktop not (typed text waits for turn end, lost on relaunch), push-back noisy, child cap counts finished workers; 12 fixes dispatched to layout (steer-by-default, ask typing, strip contents, reconnect storm) and features (child cap, one report per child, done batching, steer vs tool batch); 26 QA scenarios added

- [ ] [Match Cursor coordinator process in Arbos](bc-fe947dc6-3057-5855-9e6e-2e28531794d6) — process parity track from the [Cursor coordinator spec](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/cursor-coordinator-spec.md) and [tools appendix](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/cursor-coordinator-tools-appendix.md): slices 1–9 merged ([#198](https://github.com/unarbos/arbos/pull/198) say title/rename and placement, [#200](https://github.com/unarbos/arbos/pull/200)–[#207](https://github.com/unarbos/arbos/pull/207): helper kinds and area coordinators, todo checklist, chat subscriptions, project-page algorithm with archive and tldr, missing-file wake, user store in Cursor's shape, context inventory, agents/transcript/delete tools); [#208 spend cap](https://github.com/unarbos/arbos/pull/208) and [#211 pr tool](https://github.com/unarbos/arbos/pull/211) merged; gap table complete, no missing rows; [acceptance run](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/media/process-parity/acceptance-2026-09-15) passed live on OpenRouter; desktop asks with the style loop; earlier [kernel protocol #103](https://github.com/unarbos/arbos/pull/103) and [Project panel #105](https://github.com/unarbos/arbos/pull/105) merged

- [ ] [Cursor vs Arbos agent model](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/cursor-vs-arbos-agent-model.md) — all slices shipped: [#98](https://github.com/unarbos/arbos/pull/98) GOALS.md + coordinator root + done files, [#104](https://github.com/unarbos/arbos/pull/104) subscriptions replace plan engine, [#106](https://github.com/unarbos/arbos/pull/106) ask parks, [#107](https://github.com/unarbos/arbos/pull/107) root notes at `.arbos/notes.md` + gc subscription + lints; all merged and migrated live on Jacob's places

- [ ] [Filesystem state design](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/filesystem-state-design.md) — [Phase 1 #89](https://github.com/unarbos/arbos/pull/89) and [Phase 2 inbox files #91](https://github.com/unarbos/arbos/pull/91) done, [fixtures #87](https://github.com/unarbos/arbos/pull/87)/[#90](https://github.com/unarbos/arbos/pull/90) done; Phase 4 replaced by the Cursor-model plan (subscriptions, `plan.md`, delete plan node); all design questions now decided by "take Cursor's shape"
- [ ] [Match Cursor chat view exactly](bc-2a1318aa-e675-52f4-b3ab-94cb9415aa39) — [relaunch probe](https://github.com/unarbos/arbos/pull/703) draft held (F-206–F-210, F-197); [keyless and nest](https://github.com/unarbos/arbos/pull/696) ready and held; [search accent](https://github.com/unarbos/arbos/pull/676) held; next cycle after the poll error; do not revert [Clear / This Mac](https://github.com/unarbos/arbos/pull/629)
- [x] [Cursor-style lines + dark default PR](https://github.com/unarbos/arbos/pull/175) — merged: inline "1 Working · <step>" lines, link chips, dark by default incl. title band
- [x] [Mac fixups PR](https://github.com/unarbos/arbos/pull/181) — merged with a macOS CI job ([#180](https://github.com/unarbos/arbos/pull/180)); Mac worker rebuilding and re-checking permissions dialogs and dictation finals
- [ ] [Cursor Projects research](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/cursor-projects-research.md) — official post folded in, 26-row gap list done: no change of direction, three small gaps with features (repo-wide PR following, a learned-correction nudge, higher worker cap); the one real hole — a remote worker never seeing the Project store — is now Jacob's federation decision, owned by the mesh worker

## Full-time loops

- [ ] End-to-end journeys, not moments — eleven runs scored, 8/8 at best, with notifications now passing both ways against the notification daemon after the closed-app badge bug was fixed; the newest finding is the worst kind — the nudge that made a worker move the user's own file is fixed and closed by [#340](https://github.com/unarbos/arbos/pull/340), with two clean 8/8 runs on it; every action now also sends a one-second liveness pulse, so a frozen window fails the step instead of passing; the capped-turn wording is fixed and closed, and the newest catch is in the feedback chain — a report that waited never retried by itself; fixed in [#356](https://github.com/unarbos/arbos/pull/356) and now asserted every cycle — a report filed, credentials appearing while another place is on screen, nothing else sent, and it delivers itself within the minute; a skipped scenario, a bound budget or a failed import now shouts at the end of every cycle, after two cycles silently measured nothing; definition and per-step history live in [acceptance journeys](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/acceptance-journeys.md), and the desktop, phone and QA loops each run it every cycle — new project on a real folder, a challenge that can fail, follow-ups mid-flight and after, steer and interrupt, close and reopen, work verified on disk, then dropped links and kernel restarts — scored per step so flakiness reads as a rate and two failures in a row become a named bug; it has already paid off — [#317](https://github.com/unarbos/arbos/pull/317) makes a mid-flight follow-up reach the coordinator in about two seconds instead of waiting minutes for a worker to finish, and [#316](https://github.com/unarbos/arbos/pull/316) pins killing the kernel mid-command as a test

- [ ] OpenRouter blocks OpenAI models — 403 "user blocked for a previous policy violation" on `openai/*`; it cost Jacob a new project's first turn (raw provider text as the opening line, then 98 silent seconds), fixed in [#298](https://github.com/unarbos/arbos/pull/298): a kickoff probes the key before writing a word, a refusal marks that family blocked for later turns, the switch reads as one plain sentence with the provider's text confined to the log, and a model that sends no first byte in 30 seconds is replaced; loops run on Gemini and Sonnet, and the SWE-bench measurements were never OpenAI so they stay comparable; QA now records a provider refusal as an environment fault rather than an Arbos bug; the pod's voice gateway and every mesh kernel are moved to Gemini and running; [#283](https://github.com/unarbos/arbos/pull/283) and [#284](https://github.com/unarbos/arbos/pull/284) are merged, so the kernel's fallbacks start with the primary's own family, a 403 falls through to the next model, and vision and voice defaults are off OpenAI; only Jacob's own `subnet120` kernel still names a blocked model — offered to switch it; Jacob to query the block on his account

- [ ] [Run SWE-bench through Arbos harness](bc-bfb2cd63-da09-5a42-920b-3410d3337c9c) — cycle 38 sent; Jev stays off ([loop](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/swebench-loop.md))
- [ ] [SWE-bench harness PR](https://github.com/unarbos/arbos/pull/94) — Arbos as a verifiers Harness; [first run](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/swebench-run-2026-09-13.md) 12/16; [comparison](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/swebench-harness-comparison-2026-09-13.md) Arbos 8 vs Codex 10 on shared 12, 5x cheaper, 2x more calls; comparison overspent $73 vs $40 cap

- [ ] [Route harness work through Jev](bc-7330e6eb-85be-5448-944c-6ed6225064ce) — [#546](https://github.com/unarbos/arbos/pull/546) on `main`; next is full integration
  - [x] [Jev in the harness](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/jev-harness.md) — on in **1765**; `jev = false` turns it off
  - [x] [Design full Jev integration](bc-1f7d62b4-04dd-557c-9d9e-0260ea321bda) — [full integration](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/jev-full-integration.md) written; slices A–G wait
  - [x] [Build Jev controller](bc-1f7d62b4-04dd-557c-9d9e-0260ea321bda) — [Jev controller](https://github.com/unarbos/arbos/pull/647) on `main`; every Jev PR merged; leftover open: none
  - [ ] [Trace Jev step latency](bc-1f7d62b4-04dd-557c-9d9e-0260ea321bda) — [decisions API](https://github.com/unarbos/arbos/pull/667) in Mac **2060**; [timings](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/jev-choosing-next-step.md)
  - [x] [Fix Jev folder-list face](bc-c1b06b11-d50d-5287-93cd-bffca4a7becc) — [quiet Jev hop](https://github.com/unarbos/arbos/pull/690) on `main`
  - [x] [Verify Jev on Mac](bc-f1fe4ab6-c6fb-5255-8cc1-6415870c30e4) — works on **2060** (decisions 200, `ls`)
  - [x] [Check Jev on Jacob Mac](bc-ed875167-505e-5e93-a181-df7b20f96895) — **2083**, Jev on, project Clawdbot, `ls` ×400+
  - [x] [Study Maxblade Jev usage](bc-66d7ed04-b9b6-5fa2-88c4-5097a1d82ffc) — [CNVS lessons](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/docs/jev-maxblade-lessons.md) written
  - [ ] [Read failing Jev rollout](bc-5d4f4e4e-bda9-5656-bee9-5994aa65cf7c) — claimed sent; body not in chat, queue, or attachments
- [x] [Load last-chat before restore](bc-0b17760f-ba67-58b7-b443-5471e314147b) — `qal-j35` closed by [restore first](https://github.com/unarbos/arbos/pull/679); [load before focus](https://github.com/unarbos/arbos/pull/682) not needed
- [ ] [Run features agent for Cursor parity](bc-dcc57cf8-d8e5-575b-8c61-7a42eda2b027) — [append_line](https://github.com/unarbos/arbos/pull/721) on `main`; do not pick j35
- [ ] Feature PRs ready for review — [#6 OpenRouter](https://github.com/unarbos/arbos/pull/6) (possible key exposure, rotation is Jacob's call), [#8 steer child](https://github.com/unarbos/arbos/pull/8) (now includes #28's drain fix), [#9 worktree isolation](https://github.com/unarbos/arbos/pull/9), [#15 headless run](https://github.com/unarbos/arbos/pull/15), [#16 screenshot tool](https://github.com/unarbos/arbos/pull/16), [#17 cwd confinement](https://github.com/unarbos/arbos/pull/17), [#18 git guard](https://github.com/unarbos/arbos/pull/18), [#21 sub-agent panel](https://github.com/unarbos/arbos/pull/21), [#23 composer pills](https://github.com/unarbos/arbos/pull/23), [#27 user message card](https://github.com/unarbos/arbos/pull/27), [#29 see/commit contract rules](https://github.com/unarbos/arbos/pull/29), [#30 secrets door](https://github.com/unarbos/arbos/pull/30), [#32 GitHub door](https://github.com/unarbos/arbos/pull/32) (stacked on #30), [#33 remote spawn](https://github.com/unarbos/arbos/pull/33), [#34 fallback models](https://github.com/unarbos/arbos/pull/34), [#35 spawn wait](https://github.com/unarbos/arbos/pull/35), [#36 per-child remote places](https://github.com/unarbos/arbos/pull/36), [#37 permission modes](https://github.com/unarbos/arbos/pull/37), [#38 agent definitions](https://github.com/unarbos/arbos/pull/38) (on #37), [#39 recording tool](https://github.com/unarbos/arbos/pull/39) (on #16), [#40 artifact cards](https://github.com/unarbos/arbos/pull/40), [#41 PR tracking](https://github.com/unarbos/arbos/pull/41) (on #23), [#42 skills](https://github.com/unarbos/arbos/pull/42), [#43 memory](https://github.com/unarbos/arbos/pull/43), [#44 cost per turn](https://github.com/unarbos/arbos/pull/44), [#45 hooks policy](https://github.com/unarbos/arbos/pull/45), [#46 search citations](https://github.com/unarbos/arbos/pull/46), [#47 job streaming](https://github.com/unarbos/arbos/pull/47), [#48 worked timing](https://github.com/unarbos/arbos/pull/48), [#49 MCP servers](https://github.com/unarbos/arbos/pull/49), [#50 under-composer row](https://github.com/unarbos/arbos/pull/50), [#51 desktop voice client](https://github.com/unarbos/arbos/pull/51), [#52 sandbox](https://github.com/unarbos/arbos/pull/52), [#53 attach replay + deltas](https://github.com/unarbos/arbos/pull/53)
- [ ] [Run QA break-and-fix loop](bc-b4f4cdba-0146-5dea-9731-24ea2538adcd) — cycle 14 verified; leftover `bc-f2e2f30d` stays asleep
- [ ] QA fix PRs ready for review — [#7](https://github.com/unarbos/arbos/pull/7), [#10](https://github.com/unarbos/arbos/pull/10), [#11](https://github.com/unarbos/arbos/pull/11), [#12](https://github.com/unarbos/arbos/pull/12), [#13](https://github.com/unarbos/arbos/pull/13), [#14](https://github.com/unarbos/arbos/pull/14), [#19](https://github.com/unarbos/arbos/pull/19), [#20](https://github.com/unarbos/arbos/pull/20), [#22](https://github.com/unarbos/arbos/pull/22), [#24](https://github.com/unarbos/arbos/pull/24), [#25](https://github.com/unarbos/arbos/pull/25), [#26](https://github.com/unarbos/arbos/pull/26), [#28](https://github.com/unarbos/arbos/pull/28), [#31](https://github.com/unarbos/arbos/pull/31); #12 and #13 stack on #10; #28 now folded into #8; #7 shrinks to test-only after #6
## Release and website

- [ ] [Add in-app update bar and channel](bc-37bdb830-611b-5b03-8528-e12912f71b59) — Mac **2183**; builds 2181–2183 carry [Stop ends the turn](https://github.com/unarbos/arbos/pull/719); next zip names [headless records](https://github.com/unarbos/arbos/pull/720) and [append_line](https://github.com/unarbos/arbos/pull/721)

- [ ] [Ship Arbos to main with README](bc-71eb0fc3-658e-5b64-8b2b-9854416c9baf) — Mac **2183**; TestFlight **2160**; take [tool fold](https://github.com/unarbos/arbos/pull/722) if green; [append_line](https://github.com/unarbos/arbos/pull/721) on `main` (`d6842abd`); leave [orb revert](https://github.com/unarbos/arbos/pull/715); hold draft [relaunch probe](https://github.com/unarbos/arbos/pull/703); hold ready [keyless and nest](https://github.com/unarbos/arbos/pull/696); hold [#640](https://github.com/unarbos/arbos/pull/640) [#661](https://github.com/unarbos/arbos/pull/661) [#663](https://github.com/unarbos/arbos/pull/663) [#666](https://github.com/unarbos/arbos/pull/666) [#676](https://github.com/unarbos/arbos/pull/676); leave [#650](https://github.com/unarbos/arbos/pull/650) [#652](https://github.com/unarbos/arbos/pull/652) [#653](https://github.com/unarbos/arbos/pull/653) [#655](https://github.com/unarbos/arbos/pull/655) [#660](https://github.com/unarbos/arbos/pull/660) [#684](https://github.com/unarbos/arbos/pull/684); `v0.2.0` stays a draft
- [x] [append_line](https://github.com/unarbos/arbos/pull/721) — on `main` (`d6842abd`)
- [x] [headless records](https://github.com/unarbos/arbos/pull/720) — on `main` (`b820fb34`)
- [x] [Stop ends the turn](https://github.com/unarbos/arbos/pull/719) — on `main` (`9821dc90`)

## Running Arbos

- [ ] [Mac release polish PR](https://github.com/unarbos/arbos/pull/173) — signing, notarize/staple, icon, Info.plist, kernel in bundle, DMG background all done; done: the DMG is signed, notarised by Apple and stapled, and a quarantined double-click opens with no dialog at all; the steward is swapping it into the draft release, and CI can notarise too now that the Developer ID `.p12` is in the vault

- [x] [Check if the GitHub token leaked publicly](bc-dee5ecf2-a13f-5f19-9a52-9ec12c19bce8) — not exposed: the value itself was searched in seven encodings across all history, branches, PRs, CI logs, releases and feeds with zero hits, and GitHub raised no secret-scanning alert; two findings outrank the leak though — the token is admin on `AffineFoundation/affine` with `repo` scope everywhere, and Jacob's GitHub account has no two-factor authentication

- [ ] Our own update broke Jacob's kernels — the in-app update swapped the bundle and left his kernels running the deleted binary, 223 commits behind, and the new app attached to them anyway: that is why five workers each produced their number and reported nothing, and why his feedback sheet came back empty; the cause was the updater stopping only the kernels of open tabs while a closed tab's kernel served on through several updates ([#368](https://github.com/unarbos/arbos/pull/368) widens that list); the layer that closes the class is [#372](https://github.com/unarbos/arbos/pull/372): the app still attaches, since refusing would cut him off from work already running, but the bar shouts — both builds named, what the kernel is busy with in its own words, and what the click will end — with the kernel's gate deciding and `unknown` treated as busy; with the version-skew display now blocking a safe update rather than merely wanted; [#374](https://github.com/unarbos/arbos/pull/374) answers the class in general — a silent turn says what it is waiting on and since when, and a panic on a turn's own task no longer strands an agent in "running" for ever, both now pinned as QA scenarios that fail on the old kernel and pass on the new one; the deeper cause is worse and separate: on macOS the kernel was deaf to its own children, so every command finished unnoticed and returned "still running" after the full ten-minute wait — his five workers and this afternoon's stall are both that, fixed in [#371](https://github.com/unarbos/arbos/pull/371) by trusting the command's exit file and unblocking the signal at startup; no Linux rig could ever have seen it — proven by a control run where the same blocked signal is harmless on Linux — so the QA design now states plainly which classes a green cycle cannot vouch for, and a Mac back in the loop is the open ask

- [ ] Rotate leaked secrets — four leaks today, every one from a worker masking output it had chosen to print, so masking is now banned outright and no agent may fetch a whole vault item: Jacob to change his Apple ID password (exposed twice), revoke the app-specific password, re-export the `.p12`, rotate the GitHub PAT and turn on GitHub two-factor; the AWS keys, wallet seed and API tokens sitting in two Cursor helper processes on his Mac are worth rotating too

- [ ] [Launch Arbos desktop on Macbook](bc-b337f0b1-4e0b-5dcf-ae7f-b040ac2a52cd) — visit done and stopped again: his app runs `181b657` with the bar showing `0.2.0 (879)`, no button because he is on the newest build, so the click is still unproven; the notarised DMG upload and the macOS capture leg wait for his word
- [ ] [Linux launch fix PR](https://github.com/unarbos/arbos/pull/4) — ready for review; QA and parity builds already use it

[Archived](/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/archived.md) — finished work moved out of this page.
