# Ralph worklist

Highest priority first. The loop does one unchecked `- [ ]` item per iteration,
then marks it `- [x]`. Add new items as `- [ ]` where they belong by priority.

## Tasks

- [ ] (example) Add a package-level doc comment to any internal package missing one.
