---
cursor:
  subagentId: "bc-f2e2f30d-1298-59f1-a24c-55113322de28"
---
# QA cycle notes, 2026-09-16 (main `43d8569d` → `b49e6163`)

These notes would have gone into `docs/qa-loop-design.md`; that folder was not in the store when this cycle ended (see the last section). Bugs are in `internal/qa/bugs/`, scenarios in `internal/qa/*.py`.

## Mesh federated store (`fs-01-federated-store-read-write-cas`)

A local `arbos-hub`; kernel B registered as `qa-b/beta`; node A drives `arbos-kernel store read|ls|put arbos://qa-b/beta/…` with A's machine token. All of these hold: read with hash; `ls`; a put into `docs/` (allowed, hash returned); a put with a stale `--base` (conflict, file kept); the same with the right hash (lands); a put to B's root-owned `notes.md` (refused with the ownership rule, file unchanged); a `..` address (refused); an unknown machine (the hub's own refusal at once). Filed **qa-039** (low): the CLI's conflict line drops the current hash the frame carries, and the peer refusal says `say to=root` — A's own root — instead of `say to=qa-b/beta/root`.

## Long-project behaviours from the desktop loop

Archived children visible to a coordinator (`bt-07`) and worktree re-spawn under a used name (`bt-03`) pass on `main`. "A reply that restates itself" is now a standing detector in `check_duplicate_assistant`: `reply-restates-itself` fires on a paragraph repeated within one message, or a message repeated within one turn.

## Regular cycle on `43d8569d` (Gemini 2.5 Flash, first full cycle since the route change)

Kickoff **10/12** (items 4 and 7 open in that run). Harness fixes: a coordinator root delegates first, so `model_turn` now waits for the workers (`settle_children`) before reading results — `kill-mid-turn-restart` green again; `spawn-storm` counted archived children against the live cap (fixed). Real reds: `secrets-leak-hunt` (qa-016 class — the secret reaches the transcript spaced/reversed/hex; Gemini complies more readily with the adversarial ask) and `bench-fix-commit-branch` (the worker ran `git init` in the place around toy-repo and treated it as a submodule; the git guard correctly refused the commit on main; model behaviour).

## Loop mechanics

The 90-minute coordinator timer wakes the VM between turns; the inter-cycle wait is resume-safe (30 s polls). The `arbos-qa` user on ArbosLife still waits on Jacob.

## Store observation, 2026-09-16

At 07:22 UTC the store root listed `artifacts/ docs/ internal/ media/ notes.md`. At 09:10 UTC `docs/` and `artifacts/` are gone (`stat` says no such file) while `notes.md` still links 17 times into `docs/` (including `docs/qa-loop-design.md` and `docs/project-context.md`). Nothing in the QA loop writes or removes anything outside `internal/qa/`. The design doc's content up to 09:00 is only in this worker's history; if `docs/` does not come back, say so and it can be reconstructed from the bug files, `internal/qa/*.py` docstrings, and these notes.
