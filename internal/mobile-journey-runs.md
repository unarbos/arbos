---
cursor:
  subagentId: "bc-08d8261b-fea2-5075-9949-d45f6f9d4acc"
---

# The phone journey — one run, start to finish, scored per step

Jacob (2026-09-16): "The upgrade loops need to actually run full cycles of creating a project, running a challenge, doing follow up etc. Right now these things are flaky at best." From cycle 14 every cycle runs this journey once at least; per-screen checks live inside it. Scores accumulate here so flakiness reads as a rate; a step that fails twice running becomes a named bug (bottom).

**Definition.** QA's `docs/acceptance-journeys.md` (13:52 UTC) is the spine: steps **J1–J8** below carry QA's ids and assertions in the phone's version (J1 = open the project in the list and have the kernel seed the failing project; J6 = background and return; J8c = the link cut, which the phone owns). Runs 1–3 used the earlier 13-step numbering (table kept below for the record). Phone-only steps Jacob asked for — dictate, photo, call — are **P1–P3**. History lines in QA's shape: `internal/mobile-journey-history.jsonl`.

**Runner.** `~/mac-journey.sh <target>` (`pod` direct, or `<machine>/<project>` through the hub) on the loop's Mac (iPhone 15 Pro simulator, iOS 27.0, ArbosLife hub, phone token). Each run writes `~/mobile-out/journey/<run>/` with a still per step, the app console, and `transcript-tail.txt` — the project's root transcript read back through the hub by `~/kernel.py`, which is how PASS/FAIL is decided: the step passes when the kernel's own record shows the expected line after the challenge's sequence number, or when the kernel's `read` frame returns the file — never when the screen merely looks right. `U` = unverified, said so, per QA rule 2. Steps marked EYE are scored from the still. Copies of the run folders: `media/mobile/journey/<run>/`.

## Steps (QA ids, phone version)

| step | the phone | pass when |
| --- | --- | --- |
| J1 | cold open, open the project from the list; ask the kernel (no workers) to seed `journey_<id>/` — `mathlib.area` wrong, a failing unittest, one commit on main, no CHANGELOG | the row is listed; the `read` frame returns `journey_<id>/tests/test_math.py` |
| J2 | QA's challenge prompt scoped to the folder | busy on screen ≤ 20 s; a `spawn` ≤ 120 s — `U` when the root does it itself (QA rule) |
| J3 | watch honestly | the challenge turn ends (`turn_complete`); no two identical assistant lines in a row; no `status "…"` written as prose (#320) |
| J4 | mid-flight: "Also add a line to the CHANGELOG saying who asked for this: QA-<id>."; after: "Summarise what you changed in two lines." | `read CHANGELOG.md` carries `QA-<id>`; no second worker spawned after the line; the summary gets a reply |
| J5 | steer ("British spelling"), read-only ask ("What time is it, roughly?") while it works; Stop | the ask is answered; **Stop: the phone has no Stop control → `U` until it has one** |
| J6 | background 40 s while it works, return; kill and reopen at the end (J6k) | chat intact; badge / away card when the kernel sends `notify` (`U` on kernels that send none) |
| J7 | the result on disk through the `read` frame + one verification command | `mathlib.py` has `w * h`; `CHANGELOG.md` mentions the fix; unittest `OK`; a branch other than main with commits |
| J8 | a: kernel restart mid-turn — `U` (hosted kernel, the phone cannot kill it); b: second project — a tagged line in another project (P-runs); c: **link cut 25 s mid-turn** — the phone's own | c: calm "not answering — waiting" line during the cut; the turn finishes after the link returns |
| P1 | dictate a follow-up (clip); his tap sends | the words arrive as a user line |
| P2 | attach a photo, ask what is in it | the reply describes it; "didn't arrive / nothing at that path" fails |
| P3 | "Call <project>", ask a question (clip) | the spoken turn lands in **this** project's transcript |

## Runs (QA ids)

| run (UTC) | target | build | J1 | J2 | J3 | J4 | J5 | J6 | J7 | J8 | P1 | P2 | P3 | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 09-17 01:40 | `arboslife/demo` — **on `b6e7098` at last** (hello with `git_sha`, raw frames, transcript 1416+) | `main` 0fede124 (= TestFlight 1194) | P (seed 125 s) | **F — JB-6**: the spawn record is `arboslife: start arbos-kernel serve: No such file or directory (os error 2)`; the runner scored P on the record's presence and now reads its `error` | P | P (the root did the CHANGELOG line itself: no worker) | **F** (model, 11/11) | **P — away card in the run** ("root replied · I do not have access to real-time clock information") | **P on the merits by the runner** (BRANCHES `fix/J014026_challenge`, RETURNS `w * h`, TESTS `OK`, AHEAD 1) — but the root's own work in the checkout, **not a #357 worktree test** | U/U/**c: P** | P | P (photo answered: ice plants) | P (scoped call answered; connect 0.50 s, duplex, speaker) | PUSH U with reason. Stills and console: `media/mobile/journey/0917-014026/` |
| 09-16 23:05 | `arboslife/demo` — **served by `b6e7098` at 23:06 (seed line at seq 1338), by an old image from ~23:15** (hello without `store`/`git_sha`, frames it does not parse answered `unknown frame type "unknown"`, transcript 433 lines ending at the 16:2x photo turn) | `main` 1168 | P (line seen) then **F** (seed never finished) | U (no worker) | F (no turn end) | F (no line landed) | F | – | F (verify line never landed) | – | – | – | – | **void — not a #357 test, twice over**: the kernel under it changed mid-run (M-119), and I swapped `~/kernel.py` on the Mac at 23:19–23:24 for the F15 feedback command (M-118), which blanked the runner's history reads for about two minutes while J4m/J5 were being scored. Rerun when the mesh worker confirms which `demo` the hub points at |
| 09-16 22:31 | `arboslife/demo` — **the kernel was redeployed to `b6e7098` (#357) mid-run** | `main` 1168 | P | P (worker at 83 s) | P | U | **F** (model) | EYE | **not a #357 test**: the root said "the kernel restarted while the spawn call was running" (#316's cut record, honest), found the worker's uncommitted half-work (`area` renamed to `add`), spawned again, then lost `mathlib.py` altogether and rebuilt the files by hand | U/U/**c: P** | P | P | **F** (the call's turn did not land — the gateway redialling a restarting kernel) | 14 m. The one J8a-like observation the phone has: a kernel restart mid-spawn is reported plainly and the root carries on |
| 09-16 22:46 | `arboslife/demo` (`b6e7098`) | `main` 1168 | **F/U — muddled**: the root was still rebuilding run 27's files when the seed line arrived, absorbed it into that turn ("seeded. Now for the J224618 challenge…") and did the challenge itself | U (no worker — so **still not a #357 test**) | P | U | **F** (model) | EYE | F/U (root did it in place) | U/U/**c: P** | P | P | P | 17 m. Lesson for the runner: wait for the root to be idle before seeding |
| 09-16 21:56 | `arboslife/demo` — **before #357 is deployed** (its spawn record carries no base note; the PR is open) | `main` 1148 | P (seed, git identity set again — M-113) | P (worker at 86 s) | P | U | **F** (model, 10/10) | EYE | **F — JB-5, 5/5**: the worker's whole report was "I have updated the project notes. The task is complete." — the empty-worktree shape in its purest form; the root did the work itself and fought imports | U/U/**c: P** | P | P | P | PUSH U with reason. 14 m 47 s. **The run that tests #357 is the first one after ArbosLife serves it** — the spawn record's output will carry the base-replaced note with both file counts |
| 09-16 21:47 | `pod` (direct) | `main` 1148 | P | P (worker at 23 s) | P | U (turn ended first) | U | EYE | **P by the runner** at last (BRANCHES `fix-mathlib-J214750`, `w * h`, `OK`, ahead 1) | U/U/**c: P** | P | P | P | PUSH U with reason. 6 m 48 s |
| 09-16 21:31 | `arboslife/demo` | `main` 1148 | P (seed 155 s) | P (worker at 43 s) | P | U (turn ended first) | **F** (model, 9/9) | EYE | **F on the merits — JB-5, 4/4**: "the worker again did not successfully implement the changes or commit them properly … No CHANGELOG.md file found … mathlib.py is still in its initial state" | U/U/**c: P** | P | P | P | PUSH U with reason. 11 m 14 s |
| 09-16 21:08 | `pod` (direct) | `main` 1148 | P | P (worker at 22 s) | P | U (turn ended first) | U | EYE | **P by the paste** (BRANCHES `fix-mathlib-J210834`, RETURNS `w * h`, TESTS `OK`, AHEAD 1) — the runner's branch regex on the Mac was still an old one and rejected the BRANCHES heading; fixed | U/U/**c: P** | P | P | P | PUSH U with reason. 7 m 0 s |
| 09-16 20:50 | `arboslife/demo` | **`main` 1148** (Jacob's next build) | P (seed 135 s) | P (worker at 76 s) | P | U (turn ended first; root: "I did not instruct it … before it completed") | **F** (model, 8/8) | EYE | **F on the merits — JB-5 a third time**: the worker reported `feature/task-c39192-3` "committed locally"; the project repo has no such branch; the root redid the work in the checkout and then fought `ModuleNotFoundError` in its own throwaway worktree | U/U/**c: P** | P | P | P | PUSH U with reason. 12 m 58 s; the phone's half clean on `main` |
| 09-16 20:38 | `pod` (direct) | #339 branch | P | P (worker at 17 s) | P | U (turn ended first) | U | EYE | **P by the full paste** (BRANCHES `fix-mathlib-J203857`, RETURNS `w * h`, TESTS `OK`, AHEAD 1); the runner read the reply before it had settled and scored F — it now waits 8 s | U/U/**c: P** | P | P | P | PUSH U with reason. 6 m 30 s |
| 09-16 20:22 | `arboslife/demo` | #339 branch | P (seed 150 s) | P (worker at 96 s) | P | U (turn ended before the line; root answered from the worker's report) | **F** (model, 7/7) | EYE | **F on the merits — JB-5 repeats**: the worker reported branch `fix/test-import`, tests passing, CHANGELOG with `QA-J202247`; the root's own check: "the worker's changes were not committed to the branch it reported … the initial setup branch remains in its initial state … CHANGELOG.md is missing". The worker's report even says it *copied the project files from the previous run's folder into its worktree* because the worktree was empty (M-111) | U/U/**c: P** | P | P | P | PUSH U with reason. 12 m 25 s |
| 09-16 20:05 | `arboslife/demo` (restarted build) | #339 branch | P (seed 135 s) | P (worker at 100 s) | P | U (turn ended before the line; the root answered both follow-ups from the worker's report) | **F** (model: no clock, 6/6) | EYE | **F on the merits, new shape**: the worker *reported* branch `fix/mathlib-enhancements`, tests passed, a CHANGELOG with `QA-J200557` — and the project repo has none of it (`git branch` shows only the seed branch, no CHANGELOG). The root then did the work itself in the checkout. Candidate **JB-5**: a worker's report that the project does not bear out | U/U/**c: P** | P | P | P | PUSH U with reason. 13 m 23 s; **recording** of the photo and the scoped call: `media/mobile/cycle-28/recording-photo-and-scoped-call-66s.mp4` |
| 09-16 19:33 | `arboslife/demo` — **restarted on the newer build** | #339 branch + kind filter | P (seed hit `fatal: Author identity unknown` on the restarted machine; the root set a generic user and committed — M-113) | P (worker at 84 s: it first tried `python3 -m venv` and `sudo apt install python3.12-venv`, was refused, then delegated to a sub-worker) | P | **mid-flight P** (`say` relay, 4th time) and the CHANGELOG carries `Requested by QA-J193334` | **F** (model: no clock, 5/5) | EYE | **F on the merits**: the worker's own verification says `branch=main`, `OK`, `0` commits ahead — the work was committed on `main`, which QA's J7 forbids (the root's guard stops the root, not the worker) | U/U/**c: P** | P | P | P | PUSH U with reason. 12 m 10 s; the phone's half all passed; the two fails are the kernel's environment and the worker's git discipline |
| 09-16 19:01 | `pod` (direct) | #339 branch | P | P (worker at 28 s) | P | U (turn ended before the line — on `pod` the worker finishes in seconds) | U (time ask answered; no Stop) | EYE | **U by the runner's fault**: `branch=fix-mathlib-J190118`, CHANGELOG and `return w * h` are in the paste, but the verify's throwaway `git worktree add` failed because that branch is checked out in the project itself (pod's worker works in place), so no `OK` line. Runner: `--detach`. The work itself is right | U/U/**c: P** | P | P | P | PUSH U with reason. 7 m 22 s |
| 09-16 18:01 | `arboslife/demo` (hub, `efcab58`) | #339 branch | P | P (worker at 21 s) | P | U (turn ended 2 s before the line) | **F** (model: no clock) | EYE | **F** on my regex, P by eye: worker's branch, CHANGELOG, `OK` — the `B=` grep excluded the only branch (`…initial…`); runner picks the newest non-seed branch now | U/U/**c: P** | P | P | P | 7 m 50 s |
| 09-16 18:41 | `arboslife/demo` (hub, `efcab58`) | #339 branch + M-110 | P (seed 70 s) | P (worker at 13 s) | P | **mid-flight P, third time** (`say` to the live worker) | **F** (model: no clock, 3/3 since the update) | **P — away card seen in the run itself** ("root replied") | P by eye, kernel-reported through the worker: branch `feature/mathlib-perimeter-diagonal`, CHANGELOG, `OK`; the runner's regex wanted a raw paste and got a summary | U/U/**c: P** (the calm line only — no red, M-110 holds) | P | P (3/3) | P (3/3) | 8 m 55 s. New finding M-111: the worker's first worktree had no project files — the seed was committed on a side branch because the root's git guard refuses `main`, and the worktree branched from `main` |
| 09-16 17:47 | `arboslife/demo` (hub, `efcab58`) | #339 branch | P (seed 60 s, same git-guard fight) | **P** by the record (spawn 531; the runner mis-scored U — its wait matched an older line; fixed) | P | **mid-flight P, second time** (532 inside the turn → `say` to the live worker; the worker's report quotes the British-spelled entry); after-turn summary answered | **F** — "I do not have access to real-time clock information" again (M-107) | EYE | P kernel-reported, **with a caveat**: the runner's verify picked the seed branch (`fix/J174710_initial_setup`), found no CHANGELOG there, and the root then redid the work in the checkout and pasted `OK`. The worker's own branch (worktree, `demo--…`) was never read. Runner: pick the branch with the newest commit, excluding main and the seed branch | U/U/**c: P** | P | **P** (2/2 since the update) | **P** (2/2) | PUSH U with reason. 10 m 46 s |
| 09-16 17:26 | `arboslife/demo` (hub) — **ArbosLife on `efcab58`** | #339 branch | P (seed took 48 s: the root fought a "main is protected" git guard before committing on a branch) | **P** (worker at 8.6 s, live steps "Investigating tests", "Generating diff" on the line) | P | **mid-flight P — seen for the first time**: the line landed inside the turn (spawn 385 → user 386 → `say` 388 to the live worker → root: "I have steered the worker to add a line to the CHANGELOG…"), no second worker — #317's shape exactly. After-turn half U: the worker was still running when the summary and verify were asked | **F** ("I do not have access to real-time clock information" — the demo root's model changed; no Stop) | **P, checked by hand right after**: a 20 s task sent, app backgrounded — badge 1 on the icon, "While you were away · root replied: away card test done." on return (`media/mobile/cycle-22/02-`) | **P, late**: the worker finished (`say` 420 `OK`); the root's first verify ran in the root checkout while the branch lived in a worktree and it floundered (M-106), but its final paste has it all — CHANGELOG with `QA-J172642` in British spelling, `return w * h`, `OK`, branch `fix/J172642_initial_setup`, 4 commits (`03-`). Runner now reads the branch, not the checkout | U/U/**c: P** | P | **P — first time on `demo`**: "The photo shows a vibrant display of pink, yellow, and purple ice plants" (JB-2 closed) | **P — first time on `demo`**: the call's question and answer in `demo`'s transcript (JB-3 closed) | PUSH **U with a real reason** now: `enabled: false`, "no APNs key on the hub: set [push] apns_key, key_id and team_id in hub-server.toml", `/push/test` 503. 11 m 5 s |
| 09-16 16:46 | `pod` (direct) | #319 branch | P | P (worker; spawn record seen at 46 s) | P | U (line typed at once after the spawn record — which, with `wait:true`, is written only when the call ends, so the worker was already done) | U | P by eye | P | U/U/**c: P** | P | P | P | PUSH U: hub answers 404 to `/push` (predates #333). 7 m 10 s |
| 09-16 16:54 | `pod` (direct) | #319 branch | P | P (worker **running** at 26 s, seen live from the child's `turn` frame) | P | **U, and now understood**: the line was typed at 27 s while the worker ran; the kernel wrote the `user` event at 952, after `turn_complete` 946 — the root, blocked in `spawn wait:true`, takes a line only when the worker returns. That is exactly what #317 changes; neither the pod kernel nor ArbosLife's has it yet. J4 mid-flight stays U on every kernel until #317 is deployed | U | P by eye | P | U/U/**c: P** | P | P | P | 7 m 0 s |
| 09-16 16:33 | `pod` (direct) | #319 branch + M-100 | P | **P** (first worker on `pod`: the heavier challenge made the root delegate; spawn seen at 70 s) | P | U (the root's turn ended with the worker's result before the line landed; the runner polled every 5 s and typed 3 s later — now polls every 2 s and types at once) | U (time ask answered; no Stop) | P by eye | P (7 tests `OK`, CHANGELOG `Requested by QA-…`, branch `fix-mathlib-J163350`) | U/U/**c: P** | P | P | P (`pod`) | 7 m 48 s |
| 09-16 16:21 | `arboslife/demo` (hub) | #319 branch + M-100 | P | **P** (worker at 80 s; heavier challenge: perimeter + diagonal + 6 tests) | P | U — the line was typed 83 s in, while the worker ran, but the root (blocked in `spawn wait:true`) took it only after the worker returned: the `user` event sits after `turn_complete` and starts a new turn with its own worker. On a kernel with #317 the root would yield to the line; this kernel predates it | U (time ask answered in 3 s, and the root folded the British-spelling check into the same reply; no Stop) | P by eye | **P on the merits**: 13 tests, `OK` from the folder, CHANGELOG with `QA-…`, branch `fix/area-and-more` | U/U/**c: P** | P | **F** (JB-2, 6th) | **F** (JB-3, 6th) | 8 m 55 s |
| 09-16 15:44 | `arboslife/demo` (hub) | #319 branch | P | **P** (worker at 50 s) | P | U (root delegated and ended its turn in ~60 s; the line landed 2 s after; a second worker did the CHANGELOG line — legitimate after the turn; summary answered) | U (time ask answered; no Stop) | P by eye | **P on the merits**: worker added `tests/__init__.py`, `python3 -m unittest -q` from the folder → `OK`; CHANGELOG (British, `Requested by: QA-…`), `return w * h`, branch `fix/area-multiplies` with commits | U/U/**c: P** | P | **F** (JB-2, 5th) | **F** (JB-3, 5th) | 7 m 21 s; the challenge now asks for discovery to work from the folder (M-99 lesson) |
| 09-16 15:26 | `arboslife/demo` (hub) | #319 branch + steer fix | P by eye (root seeded with one `bash set -e` script and said "seeded"; the runner's regex missed it — widened) | **P** (worker "Fix area bug" spawned at 70 s) | P | U (the root's turn ended at 224 s, the line landed at 226 s → mid-flight half unverified by QA's rule; the after-turn summary answered; CHANGELOG carries `Requested by: QA-J152618` in British spelling — a second worker did it, which is fine after the turn) | U (time ask answered: "Roughly half past three in the afternoon, UTC"; no Stop on the phone) | EYE | **F on the merits**: CHANGELOG and `return w * h` and branch `fix/area-multiplies` 2 ahead are all there, but QA's own check — `python3 -m unittest -q` in the folder — says **NO TESTS RAN** (no `tests/__init__.py`; the worker ran the test another way and reported OK) | U/U/**c: P** | P | **F** (JB-2, 4th) | **F** (JB-3, 4th) | 9 m 52 s; 30 s recording of the challenge and the worker appearing: `recording-challenge-workers.mp4` |
| 09-16 15:16 | `pod` (direct) | #319 branch + steer fix | P (kernel record: test written, commit) | U (no worker; 27 s) | P | U (turn ended before the line; CHANGELOG carries QA-id in the verify paste; summary answered) | U (time ask answered in 9 s; no Stop on the phone) | P (away card "root replied" on return) | P (kernel-reported: CHANGELOG, `w * h`, `OK`, `fix-area-J151648`, commits ahead) | U/U/**c: P** | P | P | P (pod) | 5 m 42 s; the steer and the ask rendered whole — no stranded word (M-97 fix holds); the photo thumbnail survives the reopen |
| 09-16 15:00 | `pod` (direct) | #319 branch | P by eye (kernel record: `write` ×2, `git init … commit`, "seeded"; the runner's `read`-frame check is wrong — `read` is confined to `.arbos/`) | U (no worker; root did it in 24 s) | P | U (turn had ended before the line; CHANGELOG carries QA-id per the root; summary answered) | **F** as scored — but the time question never reached the kernel (runner typed three lines back to back and the third was lost) → harness | P by eye (away card "root replied" on return) | P by eye: verify paste `OK`, branch `fix-area-J150046`, 2 commits ahead of main | U/U/**c: P** | P | P | P (pod) | 7 m 8 s. **App bug found**: a steer typed mid-stream stranded the reply's first word as its own bubble ("That" … steer … "That line is already…") — fixed on the #319 branch, see M-97. The pod root behaved this time (opus-class answers, `write` worked): JB-4 did **not** repeat |
| 09-16 14:32 | `pod` (direct) | #319 branch | **F** (kernel said "seeded"; the read frame found no `tests/test_math.py`) | U (no worker) | P | **F** (no CHANGELOG at all) | **F** (the time ask never answered) | U (chat intact; this kernel sent no `notify` during the run) | **F** (no OK, no branch) | U/U/**c: P** (calm line under the steer during the cut; turn went on after) | P | P | P (on `pod` — the gateway's own kernel, so JB-3 does not bite here) | 6 m 40 s. The pod root (gemini-2.5-flash) floundered for the whole challenge: 13 `compacted` / `over budget (~19k tokens)` notices, `write` "exit 1" failures, five `rm -rf` + `mkdir` loops, two false "seeded", never a worker. See **candidate JB-4** |

Rates on QA ids after 16 runs (6 `pod`, 7 `demo` — 4 since ArbosLife moved to `efcab58`): since the update, `demo` J2 4/4, J4 mid-flight **3/4** (+1 U by two seconds), J6 2/2 (one by hand, one in-run), P2 4/4, P3 4/4, J8c 4/4, PUSH U-with-reason 4/4; J5 0/4 (the model refuses the clock); J7 4/4 by eye, 2/4 by the runner's regex (the paste comes as the worker's summary, not raw output — the runner should score on the fields, not the shape). Before it: `pod` J2 3/6 (the heavier challenge makes the root delegate every time), J7 5/6, J8c 6/6, P1–P3 6/6; J4 mid-flight 0 scorable on any kernel — blocked on #317 being deployed, not on the phone. `pod` J2 1/4 (needs the heavier challenge), J7 3/4, P1–P3 4/4. `demo` J2 3/3, J7 2/3, J8c 3/3, P2 0/3, P3 0/3. `demo` J2 2/2, J7 1/2 (run 7 on the merits, run 8 pass), J8c 2/2, P2 0/2, P3 0/2; J4 mid-flight still unverified everywhere — on `demo` the root ends its turn as soon as it delegates (#317), so by QA's rule the line always lands after the turn; scoring it needs QA's second shape (root wakes and `say`s to the live worker) and a worker that is still running when the line arrives. J7 on `demo` failed on the merits (unittest discovery), the first real J7 finding; J2 scored for the first time on `demo` (a worker). `pod` rates — J1 2/3, J3 3/3, J6 2/2 (+1 U), J7 2/3, J8c 3/3, P1–P3 3/3; J4 0/1 fail + 2 U (the pod root finishes the challenge in ~25 s, before the mid-flight line lands — QA's rule makes that half unverified, so the phone needs a slower challenge or a worker-spawning one to score it); J5 0/1 + 1 harness + 1 U; J2 3× U (the pod root never spawns a worker for this task); J8a/b U. Run 4's fails were the pod root floundering (candidate JB-4, not repeated since).

## Runs 1–3 (earlier 13-step numbering, `demo` on ArbosLife)



| run (UTC) | build | J1 | J2 | J3 | J4 | J5 | J6 | J7 | J8 | J11 | J9 | J10 | J12 | J13 | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 09-16 13:16 | #309 branch | P | P | P | P | P | P (intact; no away card) | **F** | P | P | P | **F** | **F** (call never started: the screen waits for a tap) | P | 8 m 38 s; J10 auto-check false-positive, corrected by eye |
| 09-16 13:27 | #309 branch | P | P | P | P (45 s) | P | P | **F** | P | P | P | **F** | **F** (call ran; the turn landed in the voice server's own kernel, not `demo`) | P | 9 m 25 s; Jacob typed "Sup" into `demo` mid-run |
| 09-16 13:56 | #319 branch | P | P | P | P (35 s) | P | P | **F** | P | P | H (runner still expected auto-send; the words waited in the field as designed — runner now taps send) | **F** (model searched for the file: not there) | **F** (turn landed in the gateway's kernel) | P | 9 m 48 s |

Rates after 3 runs: J1–J6, J8, J11, J13 **3/3**; J9 **2/2** product (one harness lag); J7 **0/3**; J10 **0/3**; J12 **0/3**. H = harness, not counted against the product.

## Candidate (one run; named if it repeats)

| id | step | what | whose |
| --- | --- | --- | --- |
| **JB-6 — named 01:55 UTC 09-17; 2/2 (runs 29 at 23:07 and 30 at 01:43)** | J2 on `demo` | every spawn with `host: arboslife` is refused in 35 ms: `arboslife: start arbos-kernel serve: No such file or directory (os error 2)` — `worker.rs:285` starts the child from `current_exe()`, and the daemon's binary is no longer at the path it was started from (replaced or moved when `b6e7098` was installed at 22:31). The root then does the work itself, so J7 can pass while JB-5 stays untested | mesh (restart the daemon from the binary now on disk) + kernel (fall back when `current_exe()` is gone: the path the daemon was started with, or `arbos-kernel` on PATH, and say which in the spawn result). Note: `features-inbox/2026-09-17-mobile-spawn-refused-daemon-binary-gone.md` |
| ~~JB-4?~~ (did not repeat in run 5 — the pod root completed the task; watching) | J1/J4/J7 on `pod` | the pod's root agent cannot carry a four-step task: its context budget reads as ~19–21k tokens ("over budget … nothing old enough to compact" 13 times in one run), the `write` tool returns exit 1 for it, and it loops (`rm -rf`, `mkdir`, "seeded" twice) without ever spawning a worker or producing the CHANGELOG. `demo` on ArbosLife (opus, workers) did the same shape of task in every earlier run | pod kernel config (root budget / model) + `write` tool on that place |

## Candidate (one run; named if it repeats)

| id | step | what | whose |
| --- | --- | --- | --- |
| **JB-5 — named 20:36 UTC; 5/5 (runs 19, 20, 22, 24, 26) before #357; diagnosed by the features agent from these tails: the seed on a side branch, the worktree cut from an empty `main`** | J7 on `demo` | the worker reports a branch, passing tests and a CHANGELOG; the project repo shows none of it (run 19). Related: run 18's worker committed on `main` (M-114); run 16's worker found an empty worktree (M-111). Three runs, three ways a worker's work and the project's repo disagree | kernel (worktree/branch model for workers) |

## Named bugs (failed twice running)

| id | step | what | whose |
| --- | --- | --- | --- |
| **JB-1** (superseded: with #317 the root relays the mid-flight line to the live worker via `say` and keeps its promise — run 13) | J7 | the root never delivers the report it was asked for ("one line per worker") once a follow-up arrives mid-flight; it answers the follow-up and the promise is dropped. Both runs. The workers did finish (J11 proves the files) — it is the report that goes missing | kernel / root agent (steer handling) |
| ~~**JB-2**~~ **closed 17:31 UTC** — ArbosLife kernel updated to `efcab58`; the photo reached `demo`'s model on the first run after | J10/P2 | a photo sent to `arboslife/demo` never lands: the kernel answers "nothing at that path", no `written` error, no "unknown frame" — so `put` is accepted and the bytes are lost on the way. The same photo landed on the Mac's own kernel through a rebuilt hub in cycle 6 (M-56 shape: a hub relaying `wire::Frame` drops `data`) | ArbosLife hub / kernel deployment — mesh worker; ask filed `features-inbox/2026-09-16-mobile-journey-photo-and-call-asks.md` |
| ~~**JB-3**~~ **closed 17:25 UTC** (gateway PR #56 + app on #319; a scoped call from `demo` landed in `demo`, `media/mobile/cycle-21/02-`; the next `demo` run scores P3) | J12/P3 | "Call demo" does not talk to `demo`: the voice gateway attaches to one fixed kernel (the phone kernel), so a question asked on the call from any project's chat lands in that kernel's transcript. The call is not project-scoped | voice gateway (`session.start` needs the target) + app (pass it); same ask file |

Harness notes: run 1's call did not start because the call screen waits for a tap on the disc; the runner taps it now. The photo check now reads the reply after the photo line's sequence number and fails on "didn't arrive / nothing at that path".

**PUSH step** (added cycle 19, #333): `~/push-check.sh` reads `GET /push` and calls `GET /push/test`; scored PASS on a 200 (Apple took a test alert to this phone), U on 503 with the hub's reason (expected until Jacob's key exists), FAIL on 502. Today the ArbosLife hub answers 404 — it predates #333 — so the step is U with that note.

## Run 31 — 2026-09-17 12:09 UTC, `arboslife/demo`

- **App** `main@1410`. **Kernel, asked on the attach socket at both ends:** `arbos-kernel 0.2.0 0f2a8bc68cc6 protocol 1`, built 04:39, `binary_gone: false`. First run to carry the kernel's own commit, which is what QA asked for.
- **Scored: J1 pass** (seeded in 170 s, the kernel's own record shows the test written and a commit), **J7v pass** (the verify line landed), everything else **unexercised**.
- **Why unexercised rather than failed** (M-162): six of the run's eight typed lines never reached the kernel. Only the setup (seq 1493) and the verify (1512) landed. The challenge, both follow-ups, the steer, the read-only ask and the phone-only photo and call lines did not.
- **The "spawn refused" in the raw score is not this run's** (M-160). No spawn happened here at all; the refusals quoted belong to runs 29 and 30, matched because an empty anchor let the scorer read the whole transcript. Fixed in #447.
- **JB-6 neither fired nor was disproved, and its premise is in doubt** (M-161): the roster's per-process `builds` for `arboslife` list six kernels and a worker daemon with none reporting `binary_gone`.
- Evidence: `media/mobile/journey/0917-120912/` (23 files including `record.json`, `kernel-version.txt`, `score.txt` and the challenge recording).

## Run 32 — 2026-09-17 22:48 UTC, `pod` — the first run against the fixed `type_send`

- **App** `cursor/mobile-cycle-54-type-send-a4fa` (main + #515). **Kernel, asked on the attach socket at both ends:** `arbos-kernel 0.2.0 efcab58f29e1 protocol 1`.
- **Every typed line reached the kernel — seven of seven, each at "after 0s".** Run 31 landed two of eight. So the steps run 31 had to record as *unexercised* are exercised now, and there is finally a run that measures the app rather than the harness.
- **Scored:** J1, J1s, J2 (both halves), J3 (both halves), J4a, J4m, J5q, J5s, J7, J7v **pass**. J4, J5, J8 **unverified** (standing: the phone has no Stop control; the mid-flight half of J4 needs the turn still running; J8a/b are not reachable from the phone). J6, J6k **eye**.
- **Three FAILs, all phone-only steps, none of them `type_send`:** P1 the dictation line, P2e the photo line, P3 the call. P2e is the harness: the photo picker sheet covers the composer, and the run log now says so in as many words — `no composer to type into … on screen: Application Arbos` — instead of giving up silently.
- **J1 no longer scores a false FAIL.** Its wait read the transcript against an anchor it was itself about to set, so it could never pass; it waits on the line's own seq now.
- **The recording is real this time.** Run 32's first attempt produced no video at all: a recorder left behind by an interrupted run holds the device and every later `recordVideo` is refused with "Host recording is already in progress", silently, because the output went to `/dev/null`. The journey clears stale recorders, logs the refusal and says when no file appeared.
- Evidence: `media/mobile/cycle-55/` — 19 stills, `score.txt`, `run.txt`, and two recordings. `recording-journey-challenge-and-workers-123s.mp4` is the one worth watching: the 490-character challenge typed in about six seconds and arriving whole, sent, the worker appearing and stepping through `Reading mathlib.py` and `creating fix branch`, and the reply at 2:02 — `Branch name: fix-mathlib-J224840`, `OK (7 tests ran)`.
| 09-18 05:11 | `pod` — kernel **`c3247332dc4e`** (asked on the attach socket at both ends) | `main` d3734226 | P (setup line at seq 2421; seeded reply in 15 s) | P (challenge reached the kernel; worker running in 27 s) | P (turn ended clean — no repeated assistant line, no status-as-prose) | **U** (CHANGELOG carries QA-J051151, but the challenge turn had ended before the line, so the mid-flight half is unexercised) | **U** (read-only ask answered; Stop is not exercised by this step — the control exists, M-130) | EYE (chat intact and an away card on return) | P (kernel-reported: CHANGELOG present, `area = w * h`, unittest OK, branch `fix-mathlib-J051151` with commits) | **U/U/c: P** — the link was cut 25 s mid-turn and the turn finished after it returned | P (dictation reached the kernel) | P (the model named the picture: `Ice plant flowers, predominantly magenta`) | P (the call's question landed in this project's transcript; **connect 2635 ms**, reply's first audio 56 ms) | PUSH U (off until the APNs key exists). **14 pass, 2 eye, 4 unverified, no failures.** Stills, recording and console: `media/mobile/journey/0918-051151/` |
| 09-18 08:52 | `pod` — kernel **`c3247332dc4e`** (asked on the attach socket at both ends) | `main` f662b876, the first run since #599's label sweep | P (setup at seq 2756; seeded in 20 s) | P (challenge and worker, 23 s) | P (turn ended clean) | **U** (J4 mid-flight half) | **U** (J5 — and its wording is corrected: *Stop not exercised by this step; the control exists*, where it used to assert the phone has none) | EYE | P (J7: CHANGELOG, `area = w * h`, unittest OK, branch `fix-mathlib-J085213`) | **U/U/c: P** (link cut 25 s mid-turn, turn finished after it returned) | P (dictation) | P (photo described: `Ice plant flowers, predominantly magenta`) | **P (the call — and this is the first run whose close taps `End call` rather than falling through to a coordinate on Back)** | PUSH U. **14 pass, 2 eye, 4 unverified, no failures.** connect 2532 ms, dictation 5 segments with `delta_shrinks=0`. `media/mobile/journey/0918-085213/` |
| 09-19 06:21 | `pod` — kernel **`c3247332dc4e`** (asked on the attach socket at both ends) | app **`main@959e5960`**, the first run whose record names the app at all (M-486) | 15 pass, 2 eye, 4 unverified (J4 mid-flight half, J5 Stop not in that step, J8 needs a kernel restart the phone cannot cause, PUSH off until the key exists). P1–P3 pass; the model named the picture. `media/mobile/journey/0919-062153/` |
