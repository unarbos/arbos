---
cursor:
  subagentId: "bc-2a1318aa-e675-52f4-b3ab-94cb9415aa39"
---

# Cursor symmetry loop — findings ledger

Every gap between Arbos and Cursor, who found it, the cycle, the PR. Target: zero findings from Jacob per cycle. A finding is a pixel gap (spacing, type, colour, placement), a missing control, or a behaviour Cursor would not have (an ask, a narrated command, a refusal, a file written on a read question) — behaviour findings also go to `internal/features-inbox/` the same turn. The catalogue that feeds the loop is `internal/symmetry-prompts.md`.

| # | date | found by | where | finding | fix |
| --- | --- | --- | --- | --- | --- |
| F-01 | 09-15 | Jacob | Project page | no visible way back to the chat (two glyphs, no words); Escape did nothing | Mac PR: "Back to chat" control, Escape, ⌘1, tab click all return; `ui_pass` `project-page-*` |
| F-02 | 09-15 | Jacob | first launch | window restored hanging off the right edge | Mac PR: a frame with < 70 % on any screen is centred (macOS) — Mac worker to confirm |
| F-03 | 09-15 | Jacob | ⌘T new tab | new project landed on the empty Project page; "Start the page…" did nothing | Mac PR: a tab always lands on the chat; the link goes to the chat with the prompt; `ui_pass` `new-project-kickoff`, `panel-start-page` |
| F-03b | 09-15 | Jacob | new project | Cursor shows the new-Project view (header block, kickoff greeting), not "New chat" | Mac PR: kickoff view on an empty root; kernel kickoff turn asked (features inbox) |
| F-04 | 09-15 | Jacob | opener | `~/Code` → "no folders"; `~` alone → nothing | Mac PR: `~`, `~/…`, `/~` expand; Create <path> for a missing folder; `ui_pass` `opener-tilde*`, `opener-create-*` |
| F-05 | 09-15 | Jacob | under-composer | `Changes +6241230 −429` and a `main` branch pill on `~/Code` (inside a dotfiles repo) | Mac PR: pills only when the place itself holds `.git`; none past 400 files / 200k lines |
| F-06 | 09-15 | Jacob | status line | "Planning next moves" a step smaller than Cursor's body size | Mac PR: work lines, thought, heartbeat, worker lines at 14 px; Mac worker to measure at 2x |
| F-07 | 09-15 | Jacob | user bubbles | right edges not aligned (pencil inside widened the card); ask answer as a second bubble with a stray "answered"; answer above the prompt on replay | Mac PR: pencil outside on hover, 70 % max width, 12 pt pad, flush with the composer; the pick lives in the folded card; echo bubbles skipped |
| F-08 | 09-15 | Jacob | root chat, own command | Cursor shows "Running 1 command", "Ran <description> ⌄" with the card; Arbos showed "Ran 1 command ›" and a narrated line | Mac PR: root's own calls drawn live, "Ran <label>" from #225; doc rule added |
| F-09 | 09-15 | Jacob (behaviour) | root chat | "answer refused: no question is pending for root · Retry" as a free line | kernel #225; desktop draws a refusal inside the question card |
| F-10 | 09-15 | Jacob (behaviour) | root chat, ask mode | an ask card for the root's own `plan` call | kernel #225 (auto default, no ask cards); features inbox |
| F-11 | 09-15 | loop (cycle 9) | reopen | reopened root lost the date line, "Worked 1s", one Done line of three | #223 |
| F-12 | 09-15 | loop (cycle 9) | transcript | scrolling up while a turn streams snapped back | #223 |
| F-13 | 09-15 | loop (cycle 8) | fan-out | no Working card over the pills (Jacob read it off the cycle-7 pair) | #209 |
| F-14 | 09-15 | loop (cycle 10) | approval card | labels not Cursor's (Skip · Always Run · Run ↵), Enter did not run | #224 |
| F-15 | 09-15 | loop (cycle 10) | worker chat | two "Thought briefly" rows split by a hidden status call | #224 |
| F-16 | 09-15 | loop (cycle 11, cold p5) | user bubble | right edge still 7 px short of the composer's plate (a negative margin clipped by the scroll pane) | #226 (`abff24d`): the row sits COMPOSER_PAD_X right — flush to the pixel (`cycle-11/bubble-composer-edges-after.png`) |
| F-17 | 09-15 | loop (cycle 11, cold p5) | root chat | a streaming thought opened its raw tail text on the root ("**Completing Task Updates** …") | #226: the root keeps the "Thinking" line; a worker's opens |
| F-18 | 09-15 | loop (cycle 11, cold p5) | panel | Ongoing Tasks readout shows raw `[PR 1](https://…)` | #226: readouts show the link words |
| F-19 | 09-15 | loop (cycle 11, cold p5, behaviour) | root chat | after the worker's done the root woke again with no prompt, wrote "I have completed the following steps:", then an empty reply, then a nudge, then kept Working — a second turn with nothing to say; and the worker "opened a PR" on a repo with no remote (`https://github.com/dmaydan/cold-p5/pull/1`, invented) | features inbox §3 → fixed in #236 (a done wake names who reported and who still works; a PR link no tool opened gets one reminder) |
| F-20 | 09-15 | loop (cycle 11, cold pp2, behaviour) | root chat | the coordinator restated each worker's sentence in prose as it arrived and gave the combined answer twice | features inbox §3 → fixed in #236 (the same paragraph repeated ends the turn) |
| F-21 | 09-15 | Mac worker (cycle 11, `11-approval`) | ask mode | the bash approval renders as a Question card (allow / deny / Other…, Skip · Continue) and Enter skips | #224 (the approval-shaped ask is Cursor's card, Enter runs); #225 makes auto the default |
| F-22 | 09-15 | Mac worker (`07-working-live`) | live turn | no "Working <step> ⌄" headline, no "Running 1 command" — only "Running <description>" | cycle 11: live headline over the rows; the count while live, the words once settled |
| F-23 | 09-15 | Mac worker (`14-small-window`) | 900×600 | the right panel does not fold | cycle 11: folds under 1000 pt |
| F-24 | 09-15 | Mac worker (`02-kickoff`) | new project | the tab sheet opens over the new-Project header | cycle 11: the sheet sits below the header block |
| F-25 | 09-15 | Mac worker | notices | `mode: ask — …` one unwrapped line off the right edge | cycle 11: notices wrap |
| F-26 | 09-15 | Mac worker (`01-first-launch`) | window | no frame persisted, so #226's centring was untestable | cycle 11: frame in state.toml, restored when 70 % on a display, else centred |
| F-27 | 09-15 | Mac worker (cap table) | Working card | title one step smaller (18 px vs 21 px cap at 2x) | cycle 11: prose size |
| F-28 | 09-15 | loop (Mac stills `10-panel-rows`) | prose | half-bold words on a fresh install — bionic reading defaulted on; Cursor's prose is plain | cycle 11: off by default, still a Settings switch |
| F-29 | 09-15 | loop (cycle 11, kickoff) | root chat | the kickoff greeting landed under the user's first prompt when typed during the kickoff turn (steer-by-default) | cycle 11: words typed during the kickoff wait for it |
| F-30 | 09-15 | loop (remote track, arboslife) | remote connect | updating a stale remote kernel failed every time: scp ran through the probe's mux socket while its master (ControlPersist=no) was closing → "Connection closed"; the window flapped connecting/lost with "scp … failed" | cycle 11: scp on its own connection, one retry, scp's words in the error |
| F-31 | 09-15 | loop (remote track) | remote connect | `REMOTE_BIN` `$HOME/.cargo/bin/arbos-kernel` reached scp as a literal (SFTP has no remote shell): "dest open $HOME/…: No such file or directory" | cycle 11: `$HOME`/`~` resolved on the host before scp |
| F-32 | 09-15 | loop (remote track) | remote connect | no visible state while the app checks, installs, updates or starts the remote kernel — only "connecting…" | cycle 11: the step by host in the composer placeholder, the machine row and the kickoff shimmer ("Updating Arbos on arboslife…") |
| F-33 | 09-15 | loop (remote track) | under-composer | no branch pill for a remote place that is a repo (branch read from a local `.git` only) | open |
| F-34 | 09-15 | loop (remote, templar, behaviour) | kickoff | on a fresh machine with no provider key the kickoff turn ran and failed, twice ("No API key…"), and a later key does not re-run it (root has a turn on record) | remote-states PR: the window asks for the kickoff once the provider frame says key; kernel ask: a kickoff that failed for want of a key is not "a turn on record" |
| F-35 | 09-15 | loop (remote, templar) | remote connect | "Starting the kernel failed: did not start within 60 s" — `~/.arbos` on templar is a symlink to a folder that is gone, so the launch's `>>~/.arbos/web.log` failed silently | remote-states PR: the launch logs under `~/.cache/arbos`, fails at once if that cannot be made |
| F-36 | 09-15 | loop (remote, templar, behaviour) | workers | the two spawned workers were "started read-only and couldn't write their sentence files" — the brief asked for an output file and forbade writing | features inbox |
| F-37 | 09-15 | loop (remote, templar, behaviour) | root chat | "run this command and show the output" became a `subscribe kind=shell` ("Subscribed the shell output…") once, and a `bash` call that returned after the first line of a 24 s loop the next time ("stopped after the first line, still running as a job") — Cursor streams the whole output | features inbox — fixed in #244 (`background:true` was honoured for a plain loop) |
| F-38 | 09-15 | loop (remote) | tunnels | `ssh -N -L` tunnels leak when the window is killed rather than quit (two per host after a few runs) | open — desktop: reap on launch by ControlPath/port ownership |
| F-39 | 09-15 | loop (remote, templar) | notices | the same failure notice twice in a row | remote-states PR: identical consecutive notices read once |
| F-40 | 09-15 | loop (cycle 12, p13 steer) | transcript | a steer typed into a running turn opened a second turn: its bubble, then a new "Worked", the first turn left as a bare "Ran … ›" fold with no answer — Cursor keeps one turn with the card inside | cycle 12: `UserMessage.steer` (set at send, and on replay for a user line inside a turn); a steer opens no turn, its bubble sits in the work |
| F-41 | 09-15 | loop (cycle 12, p10 ask) | ask card | typing the option's name ("alpha") sent the pick and the words: the model read "alphaalpha" | cycle 12: words naming an option are that pick, once |
| F-42 | 09-15 | loop (cycle 12, p10 ask) | root chat | a bare `ask "Which name…"` tool row above the question card | cycle 12: `ask` is a kernel call; the card is its face |
| F-43 | 09-15 | loop (cycle 12, p13, behaviour) | root chat | `bash` for a 12 s loop returned after the first line, so a steer at +3 s found no running tool and became a plain second turn (F-37 again, on parity-proj) | features inbox (already filed as F-37) |
| F-44 | 09-15 | loop (cycle 12, harness) | Cursor side | Cursor's frames were taken scrolled up ("N New Messages ↓"): the Project chat keeps the reader's scroll | harness: wheel to the bottom before every Cursor shot |
| F-45 | 09-15 | loop (cycle 13, app tour) | root chat | a refused tool call draws as its success form — "Edited main.py" with the refusal text in mono beside it; Cursor shows a failed call as a failed card | **closed cycle 40** ([#573](https://github.com/unarbos/arbos/pull/573)): reproduced on 1741 with the coordinator's fenced `write` — the run line read *Edited hello2.txt +1* and the row was a diff card of words never written. Now the run line counts it as a failure (*Could not edit hello2.txt*), the row's verb says what did not happen with the error's first line beside it, no diff card, no ± (`cycle-40/failed-call/`) |
| F-46 | 09-15 | loop (cycle 13, behaviour) | root chat | the coordinator tried to edit `main.py` and `math_utils.py` itself on a code task (refused by the kernel: "as coordinator you write only the project store") instead of spawning a worker | features inbox — fixed in #245 (coordinator tool descriptions say spawn first) |
| F-47 | 09-15 | features agent (cycle-12 video) | root chat | the running row collapsed to "Ran …" the instant a steer was sent, before any output: the live-run test wanted the run to be the last segment, and the steer's inline card came after it | step PR: a run stays live while only inline cards follow it (`media/cursor-reference/cycle-13/f47-steer-keeps-running-after.png`) |
| F-48 | 09-15 | loop (cycle 13, p24 cold) | root chat | a prompt typed in the kickoff's first quiet seconds (before any kernel event made the chat busy) landed its card above the kickoff's work, so the greeting drew under the user's bubble | step PR: `kickoff_running` counts from the ask, not from `busy()` (`f48-prompt-in-kickoff-quiet-before/after.png`) |
| F-49 | 09-15 | loop (cycle 13, p24) | root chat | the kickoff's "Worked Ns" read 1–2 s for a 40 s kickoff: no flight clock was opened for the kickoff turn | step PR: the kickoff opens a flight when asked |
| F-50 | 09-15 | loop (cycle 13, p24, Gemini) | root chat | a settled `thinking` record arriving after `turn_complete` drew a second "Thought briefly" under the greeting and pushed the greeting into the fold | step PR: step maps live until the next turn boundary; a late settled thought stamps the item with the same words |
| F-51 | 09-15 | Jacob (Mac) | opener | a folder whose name is a prefix of a sibling could not be opened: `/Users/const/Code` listed `Code/` and `Code2/`, Enter or a click only completed the text, no Open row; `~/Code` reachable only with the trailing slash (same area as F-04) | #258: the first row always says what Enter does — "Open <name>" for the folder the text names (slash or not, siblings or not) or the one match; folder rows step in; `ui_pass` `opener-prefix-sibling-*`, `opener-dir-row-steps-in` (`media/cursor-reference/cycle-13/f51-opener-*.png`); closed on Linux (the rig, Jacob's decision 09-16): `ui_pass` opener checks 13/13 |
| F-52 | 09-15 | Jacob (Mac, `181b657`) | root chat prose | half-bold prose again after #228 (`media/mac/update-bar/update-bar-879-2026-09-15.png`): every build before #228 defaulted bionic reading on and wrote `bionic_reading = true` back into `state.toml` on each save; #228 flipped only the default, and `main.rs` applies the file's value at boot, so the leaked `true` survived | #264: `state.toml` gains `version = 2`; a `true` from an unversioned file is the old default and is dropped on restore; driver state shows the flag; `ui_pass` `bionic-leak` + toggle round-trip |
| F-53 | 09-15 | loop (while fixing F-52) | Linux rig | the Linux leg is blind to weight bugs: `sans-serif` → Noto Sans with no SemiBold face on the VM, so gpui draws SEMIBOLD runs at regular and bionic prose looks plain — the reason F-52 came from Jacob and not the loop | #266: Inter 4.1 (8 static faces + OFL text) shipped inside the binary and named in the theme on every platform; Linux now shows the weight (`cycle-13/f53-inter-bionic-visible-linux.png`, 5,863/40,200 prose pixels differ off→on, was 0); `ui_pass` `weight-visible` proves it each run; closed: #266 merged; the AWS Mac's font-metric reply (`qa-results:inbox/cycle-13/inter-reply.md`) gave the size step (F-55) |
| F-54 | 09-15 | AWS Mac loop (bc-08d8261b), on the Inter brief | Mac leg | **no Mac can render the desktop.** Jacob's MacBook is off limits; on the AWS EC2 Mac the app launches, writes `state.toml`, spawns the kernel and opens the driver socket, but never paints — gpui's macOS window draws only from a `CVDisplayLink` bound to the window's screen (`bezel-gpui-macos` `window.rs` `start_display_link`: returns early when `occlusionState` is not visible or the screen has no display id; `CVDisplayLinkStart` on the EC2 console's virtual framebuffer never ticks). Every Retina check is blocked — the leg that found the last round | closed by decision (Jacob, 09-16): the Linux build is the rig for desktop work — no Mac leg, no renting a Mac. Bundled Inter (#266) makes Linux type findings real. What Linux cannot show is listed below under "Untested by decision" |
| F-55 | 09-15 | AWS Mac loop (font metrics) | prose | Inter's lowercase reads 7 % larger than SF at the same size (x-height 0.546 em vs 0.508; at 14 px @2x 15.3 vs 14.2), so Arbos prose read bigger than Cursor's after #266 | #266 `ea6be27`: prose 13 px Inter (rendered @2x from the bundled face: x 15 / cap 20 vs SF 14's 14.2 / 19.7), 23 px line box kept; the UI ladder measured and kept — Body 13: x 15 vs SF 13.2 (+0.9 px @1x, watch), Callout 12: 13 vs 12.2, Subheadline 11: 12 vs 11.2, Caption 10: 11 vs 10.2 (all +0.4 px @1x; a step down would take captions to 9 px) — `cycle-13/inter-prose-14-vs-13-linux.png` |
| F-56 | 09-16 | loop (cycle 14, long-form l2–l11, behaviour) | workers | every worker of a 13-turn project ran read-only: the model kept `kind: explore` and dropped `output` to get past the spawn guard; seven workers, zero files (`cycle-14/arbos-l2-three-workers-readonly.png`) | features inbox `2026-09-16-long-form-project-cycle-14-kernel-asks.md` §1 (kernel: #285 puts `agent_kind`/`readonly` on the tree); desktop #288: a mark after a read-only worker's name on its line and in the roster, tooltip "read-only: reads and reports, writes nothing", a low-contrast kind chip (`cycle-15/readonly-mark-worker-lines-and-roster.png`) — Cursor draws no such distinction on its sub-agent chips (cycle-14 l2/l10 frames); a deliberate divergence, since it is the failure F-56 exists to catch |
| F-57 | 09-16 | loop (cycle 14, l10, behaviour) | root chat | asked to list running and archived workers, the coordinator said it had none while the panel showed 6 archived; Cursor lists every archived sub-agent as a chip with a one-line summary (`cycle-14/cursor-l10-archived-workers-listed.png`) | features inbox §2 |
| F-58 | 09-16 | loop (cycle 14, l11, behaviour) | workers | a re-spawn with a used name failed the turn on a worktree branch-name collision | features inbox §3 |
| F-59 | 09-16 | loop (cycle 14, l12) | root chat | on a 12-turn chat the right edge shows a stack of a dozen short dashes (the turn rail's markers); Cursor has no rail — one thin scrollbar | open — cycle 15: hide the rail's marks until hover, or drop them for a plain scrollbar |
| F-60 | 09-16 | loop (cycle 14, l2) | bottom bar | `0.2.0 (886) · check failed` sits in the bottom bar for the whole session on a rig with no update feed; Cursor shows nothing when there is no update | open — cycle 15: no words for a failed check (a dot on hover at most), the words only for an update that exists |
| F-61 | 09-16 | loop (cycle 14, l2) | panel | after `Edited notes.md, started 3 workers, kept its checklist +6` the panel's Project section still read "Nothing on the project page yet. Start the page…" until a later turn | open — cycle 15: reproduce; likely the panel's page read happens on `changed` for `notes.md` only when the file existed at attach |
| F-62 | 09-16 | loop (cycle 14, l1 vs Cursor) | root chat | Cursor's Project chat splits one user turn into several "Worked Ns" blocks — one per worker report that woke the coordinator — with prose between them (`cycle-14/cursor-l1-project-chat-worked-segments.png`); Arbos folds the whole turn under one Worked | #292: `ChatItem::Wake` — a kernel wake (`done`/`say`/`serve`/`job`) cuts its own segment with `Worked Ns`, the worker's report line under the header (chip, done —, last words), one footer per run (`cycle-15/f62-wake-segments-after.png`) |
| F-63 | 09-16 | features agent (phone, `arboslife/subnet120`) → desktop half | root chat | tool-call markup a model wrote as prose showed raw in the live stream until the step settled, and stayed for good when the settled line was empty (a markup-only reply); Cursor never shows its tool markup | #279: `markup::strip_live` on the streamed body and the settled line; an empty settled line drops the item — verified live: markup-only reply → no bubble; prose + markup → prose only (`cycle-14/f63-markup-as-prose-cut-after.png`) |
| F-64 | 09-16 | loop (cycle 14, Cursor l12 scrolled to top) | root chat | Cursor's kickoff reads as two lines — `Environment ready 12s` then `Worked 44s` + the greeting; Arbos folds both into one `Worked Ns ›` (`cycle-14/cursor-l12-scrolled-to-top.png`) | closed (not applicable): Cursor's `Environment ready 12s` is its cloud VM coming up before the agent's first turn; Arbos has no such step — the kickoff *is* the agent's first turn, so one `Worked Ns` is the honest label |
| F-65 | 09-16 | loop (cycle 14, Cursor l9) | root chat | Cursor's coordinator asks its one question as plain prose ("For list --json, should each habit be …?") and waits; ours in auto mode does the same since #225 — confirmed matching; no ask card in a Project chat | closed (parity confirmed) |
| F-66 | 09-16 | loop (cycle 15, r1, Gemini) | root chat | the kickoff greeting showed twice: the model called `say(to: user)` with it (drawn as a From block "Hey — this Python → user") and then wrote it as its reply (`cycle-15/f66-say-to-user-duplicate-before.png`) | desktop #288: a settled paragraph equal to the agent's own `say` drops the say block, live and on replay; kernel ask: the coordinator's `say` should not accept `user` as a target (features inbox, next note) |
| F-67 | 09-16 | loop (cycle 15, r1, Gemini) | root chat | the turn's summary paragraph appeared twice, one step before and one after a `plan` call, near-identical wording (`cycle-15/f67-repeated-paragraph-before.png`); Cursor shows a paragraph once | desktop #288: an exact repeat of the turn's earlier prose is dropped at settle and on replay; near-repeats stay the kernel's (`repeat_near_dup`) |
| F-68 | 09-16 | loop (cycle 15, l2) | composer strip | a worker's done report waiting in the coordinator's inbox showed as `1 follow-up queued — Turn ended. Last words: (no reply)` with Send now / Edit / Remove (`cycle-15/f68-worker-report-as-followup-before.png`); Cursor's queue holds only the user's follow-ups | #292: the queue lists inbox nodes whose `origin` is the user (the kernel files every non-steer message as `agent`) |
| F-69 | 09-16 | loop (cycle 15, remote attachment check) | composer tray | a 21 MB non-image file went into the tray (only images counted toward the 20 MiB cap) and on a remote place fell back to sending a path the kernel could not read — silently | #292: every file's size counts in the tray (`Attachments in one message must total at most 20 MiB`, `cycle-15/f69-too-large-refused-in-tray-after.png`); past the tray, the kernel's `written {error}` is the message |
| F-70 | 09-16 | loop (cycle 15, Cursor attachment check) | prompt card | Cursor's user card for a pasted image showed the prompt text only — the image surfaced as `Explored attach-test.png` in the work line (`cycle-15/cursor-attachment-card-no-thumbnail.png`); Arbos draws the thumbnail chip inside the card. The Cursor paste was via xclip and its tray was not captured, so this needs one more look before it counts as a gap | open — Cursor's file attach on the rig needs a native dialog Xvfb has not got; the paste path gave one card with no thumbnail; decide with a Mac still when one exists |
| — | 09-16 | loop (cycle 15) | remote attachments (#273) | verified on Templar: the PNG landed under `.arbos/attachments/`, the user line named it, the model answered `7` (`cycle-15/remote-attachment-templar-answer-7.png`). Two rig facts on the way: the desktop re-installs its *own* kernel binary on the host on every connect (an old rig kernel overwrote the new one — `frame_rejected: unknown frame type put`), and the remote kernel's own `config.toml` model (gpt-5.4-mini, blocked) fails turns until changed on the host | closed |
| F-71 | 09-16 | loop (cycle 16, long-form d6) | project page + panel | the model pushed section titles through the `plan` tool, so `notes.md` reads `- [ ] ## Goal` and the page drew "## Goal" behind a hollow checkbox (`cycle-16/f71-headings-as-items-before.png`) | #294: a checklist item whose text is `#…` reads as a heading (`…-after.png`); kernel half (plan tool lifts or refuses it) in the features inbox |
| F-72 | 09-16 | loop (cycle 16, long-form) | project page | `Kickoff — ready; waiting for the first ask` stays at the top of the page after ten turns | features inbox (kernel: tick or remove it once the first user turn lands) |
| F-73 | 09-16 | loop (cycle 16, d10) | root chat prose | `*   1. What…` renders as an empty bullet, a paragraph gap, then the numbered line (`cycle-16/arbos-d10-open-questions-fanout.png`); nested-list spacing wider than Cursor's | open |
| F-74 | 09-16 | loop (cycle 16, coverage: search) | search | Cursor's ⌘K/Search is a wide centred palette with filter tabs (All / Agents / Files / Actions / Settings), sectioned rows with snippet, workspace and age, footer hints (`cycle-16/f74-cursor-search-palette.png`); ours is a ~280 px box over the transcript matching chat titles only (`f74-arbos-search-palette.png`) | open — cycle 17: widen and centre, sections Chats (title, snippet, place, age) and Files (store docs), footer hints |
| F-75 | 09-16 | features agent (#293) → desktop half | notifications | a reply, question or failure landing while the window was unfocused or another tab in front was silent; Cursor posts an OS notification and badges the chat | #297: OS notification (`notify-send` / `osascript`) for a chat not in view, the tab's dot while any is unseen, `seen` sent when the chat is looked at (`cycle-16/notify-*.png`); open: clicking the notification does not focus the chat (libnotify actions need a listener) |
| F-76 | 09-16 | Jacob (new project) | root chat, first line | the very first line of a new project was a raw provider error — two model ids, "Policy Violation", a `platform.openai.com` link (the kernel's `switched to <model> for this turn: <reason>` notice, and behind it the failed turn's text). Cursor shows a short line and puts the detail behind a disclosure | #299: a notice's news is the line — `Switched to gpt-5.6-terra for this turn.`, `The model provider refused the request.`, `Connection failed; retrying (attempt 4/5).` — with a `Details ›` disclosure holding the raw text (`cycle-16/f76-provider-refusal-*.png`); retry lines replace each other instead of stacking five URLs; the kernel's wording is the features agent's |
| F-77 | 09-16 | Jacob (new project) | kickoff | a first turn producing nothing for 98 s showed only the shimmer; he gave up ("Stopped by you 1m 38s"). Worse on a silent provider: after 20 quiet seconds the new-project view fell to the static "ready" greeting | #299: the shimmer carries its clock after 20 s (`Setting up environment · 1m 10s`) and after 60 s a line saying what to do (`Nothing has arrived in 1m 10s. Stop to try again, or check the model key in Settings.`), in the kickoff view and in the transcript heartbeat; a kickoff that has not ended never falls to the greeting (`cycle-16/f77-stalled-kickoff-clock-and-hint-after.png`) |
| F-78 | 09-16 | loop (gate) | gate | `notify-unseen` reads unseen 0 with `seen_through` advanced inside the full pass, while the same sequence passes standalone three times: a `seen` reaches the session from somewhere the gate's earlier phases set up | open — the row records `unverified` in that case; find the sender in cycle 17 |
| F-79 | 09-16 | loop (gate `stop-notice` still) | root chat | a worker's report line under its wake segment drew twice — once as the segment's line, once again in the turn's tail (#292 left the tail starting at `answer_from`) | #299: the tail starts where the body ended |
| F-80 | 09-16 | loop (cycle 16, Gemini kickoff) | root chat | Gemini wrote its tool call as a Python code block — ```` ```python print(default_api.bash(command='ls -la…')) ```` — as the kickoff's first reply; the markup cut (#279) knows the XML and marker families, not this one | features inbox: the kernel's `strip_tool_markup` should take the `default_api.<tool>(…)` code-block form too; desktop follows once the family is named |
| F-81 | 09-16 | phone worker's finding, checked on the desktop | connection state | with the tunnel to Templar frozen mid-turn the desktop stayed `live`, streaming, and said nothing for the whole 75 s of the cut — it keys "connected" on the socket, which does not notice a cut for 20 s or more; Cursor shows a dropped connection visibly | not-answering PR: every frame restarts a liveness clock; while a turn is owed, a `list` probe goes at 10 s of silence and at 20 s the heartbeat reads `templar is not answering — waiting · 20s`, cleared by the next frame (appeared at 19 s in the cut test, cleared 1 s after restore; `cycle-16/f81-not-answering-after-20s.png`) |
| F-82 | 09-16 | Jacob (Mac, "write and run bubble sort") | root chat, one worker | one worker drawn three times while the root waited on it — a live `Working Starting worker… ⌄` header whose chevron did nothing, a Working / Stop All card over the composer, the `Working 1` pill — with `status: Delegating to worker…` as a paragraph between them and a long empty gap above the card. Cursor, run live on the same prompt: prompt, prose, one line `1 Working  Delegating bubble sort task`, the `Working 1` pill; with three workers one line `3 Working  Waiting on three writers`; **no card** — it sits behind the pill (`cycle-17/f82-cursor-one-worker-live.png`, `f82-cursor-three-workers-one-line-no-card.png`). F-13's card (cycle 7 pair) was the pill's disclosure, not the default | one-worker-turn PR: one `N Working  <step>` line (the one worker's live step, else the coordinator's) with the shimmer; the live header only over rows it can fold, and its chevron folds them; the card opens on the pill or the line and closes on ×; a streamed `status:` line is the step from its first chunk (`cycle-17/f82-pair-before-…`, `f82-pair-after-…`) |
| F-83 | 09-16 | QA (project isolation), checked on the desktop | reconnect | after a kernel respawn on a fresh place a typed line sat twice on the kernel's transcript — once written by the window's `seed_transcript` on reconnect (the card was already on the transcript, its words still queued), once by the kernel from the frame the queue then sent. Reproduced: keyless place, kernel killed under a typed line → two `user` records, the first without a channel | one-worker-turn PR: the seed skips cards whose words are still queued; the `reconnected` notice is written after the seed, so it is never a seeded record. Reproduced fixed: one record |
| F-84 | 09-16 | QA (keyless first line) → features agent #312 | kickoff, keyless | on a keyless place the kernel now answers `kickoff` with an `error` (`kickoff not started: …`) and `turn idle`, keeps a typed `user` line as a pending inbox row and says so once with a failed `notice` + `error`. Checked on the desktop before #312: the first typed line *is* delivered (the desktop never sends `kickoff` without `provider.key`), the kernel burned a failed turn on it. After #312 the desktop should draw the `kickoff not started` error as the setup card's cue, not a red line, and a kept `user` line as its pending row, not a failure | open — cycle 17, once #312 is on `main` |
| F-85 | 09-16 | loop (journey run 1, J05 still) | root chat | the worker line `1 Working  Running tests again` sat between the `Worked 4s` header and the prose "I've spawned two workers…"; Cursor draws the prose first and the worker line under it (`cycle-17/f82-cursor-one-worker-live.png`: "On it — writing a bubble sort…" then "1 Working  Delegating…"; Jacob's `layout/jacob-cursor-inline-working-2.png`) | #315: worker lines after the turn's prose; the footer last |
| F-86 | 09-16 | loop (journey run 2, J05) | root chat | two workers just spawned drew as `Waiting  Build Todo App` / `Waiting  Build Todo Tests` under a `Working 2` pill — the line and the pill disagreed, and Cursor says "2 Working" from the spawn | #315: a worker not yet on its first token counts as working in the line while the root's turn runs, as the pill already did |
| F-87 | 09-16 | loop (journey run 3, J02 still) | kickoff | for the seconds the kickoff's first `ls` ran, the transcript showed the project head, the date line and nothing else — the heartbeat stood down for a running tool, and the kickoff view had already handed over | #315: "Setting up environment" is the kickoff turn's line whatever is running |
| F-88 | 09-16 | loop (journey run 2, J11 still) | notifications | on relaunch an OS notification "root replied" fired for a reply the window had shown in front before the quit — `seen` had not reached the kernel, so the unseen replay on attach posted it | open — cycle 17: send `seen` on quit / when the reply lands in the front chat, and never post for the chat in front on attach |
| F-89 | 09-16 | loop (journey run 2, J11 still) | root chat, replay | after relaunch the same worker's report showed twice under one segment — `Build Todo Tests done — I have created…` and `Build Todo Tests done — I have successfully implemented…` — two `say` records from one worker, both drawn | open — cycle 17: check whether the kernel wrote two reports (features inbox) or the replay pairs one report with two wakes |
| F-90 | 09-16 | loop (journey run 4, J08 still) | root chat | `Done  Todo app worker` / `Done  Todo test worker` lines repeated under three consecutive wake segments of one fan-out — the workers reported questions, not "done", so nothing folded them | open — cycle 17 |
| F-91 | 09-16 | loop (journey run 4, J08) | coordinator, behaviour | after a worker asked for GitHub auth the coordinator ran `gh auth login` itself — an interactive prompt — and its turn hung on `Running 1 command` for the rest of the run (7 min +); Cursor's coordinator never runs a command that waits on a TTY | features inbox (re-filed note §F-91): refuse or time out a command that reads a TTY; the desktop shows the hang honestly already (the stall clock, F-77). journey.py puts the stand-in `gh` on PATH as the gate does |
| F-92 | 09-16 | QA (journey J1/J3, `qal-j01`) | root chat, kickoff | the model writes `status "Setting plan"` as a reply line — the prompt's own example form — and the window hid only the `status:` colon form, so a new user saw three code-looking bubbles over the kickoff greeting | #315: `status_line` takes every form a model writes (colon, quoted, parenthesised, `=`, with markdown around the word), live, settled and on replay; kernel half with the features agent |
| F-93 | 09-16 | QA (journey), Jacob's habit | launch | every launch landed on the Home tab by design, so "close it and come back" dropped him somewhere he was not | #315: a launch lands on the tab that was in front; Home only when that place is gone (journey step J11a scores it) |
| F-94 | 09-16 | loop (journey runs 2 and 5, J11) | relaunch | after quit and relaunch two prompts showed twice — `Continue where you stopped.` and the follow-up — each once with the kernel's clock and once without; the kernel's transcript holds each once. Both were typed while the root was idle and its workers ran; the kernel held them in its inbox past the window's two-minute echo match, and the late record landed a second card (`cycle-17/f94-root-record-after-relaunch.json`) | #323 (merged): the echo is matched by the words the window awaits, however long the kernel held them; journey J11 clean on three runs since |
| F-95 | 09-16 | loop (gate on #315's head, `fork-turn` + `recover`) | turn footer | fork and rewind went dead: F-85 moved the footer under the worker lines and out of the answer's hover group — the group that reveals the two on hover — so they stayed hidden and un-clickable; the gate's fork click fell through and the turn after it read busy (`media/qa-ui/one-worker-turn-2/033-recover-stuck.png`). Reproduced with the driver and with a hand-like hover-then-click: no fork, no notice | #315 `82cc1d9f`: the worker lines sit inside the tail before the footer, the footer back in the group; the probe forks again (a `… copy` session and its folder) |
| F-96 | 09-16 | loop (journey f94 runs 1–2, J08/J09, behaviour) | coordinator, behaviour | after Stop, "Continue Working" woke the coordinator with the stopped workers' `ended badly — stopped` reports; it read them as failures ("Both workers ended badly. I will…"), ended its turn and spawned nothing, so the disk stayed empty until the next follow-up. Cursor's Continue resumes the job | kernel #324 (merged): a stopped worker reports a pause, and Continue carries the reports in |
| F-97 | 09-16 | loop (F-95 probe, root idle with a worker running) | composer | while only a worker runs the root's composer shows the mic, not the stop disc; Cursor keeps ■ while any of the project's work runs (`cycle-17/f82-cursor-one-worker-live.png`), and Stop there stops the workers | open — cycle 17 |
| F-98 | 09-16 | loop (notification driver work) | gate | the gate's `notify-unseen` read `unseen=None` since #297 (F-78's flake): the check looked the chat up by the session id active at send time, which the tab switch changed; and a notification check with no daemon record behind it could not fail | #329: the driver exposes `tab_dot`, `unseen_kinds` and every posted OS notification with its result; the gate's four `notify-*` rows find the root by tab index and check dunst's own history (popups closed first, a nonce in the reply); journey J10b does the same. F-78 closed by this |
| F-99 | 09-16 | QA (`qal-j03`, built on the F-98 surface) | notifications, relaunch | a reply that landed while the app was shut showed no badge on relaunch when the project came back in front (F-93): the restore counted the front chat as looked at and sent `seen` at once — kernel replay proven right by its e2e, the clearing was the desktop's | #329: "looked at" needs the window touched (a press, a key, or a return to it after its first five seconds); the driver says `touched`. Reproduced three times: badge on relaunch, the first click clears it and sends `seen` |
| F-100 | 09-16 | loop (driving #336's review sheet by hand on the rig, first click) | feedback sheet, hang | the window froze the instant the sheet opened: `collect_feedback` runs inside the workspace's observer and took the bundle through `with_session`, which notifies the workspace, which ran the observer again, without end — the driver's click never returned and the main thread sat in `sync_composer` | my slice on #336 (`cursor/thumbs-down-sheet-aa39`): look before taking; the sheet opens, the note takes typing, Send writes the report |
| F-101 | 09-16 | loop (same drive, Linux) | feedback sheet, screenshot | the picture never attaches on the rig: with `import`/`xwd` missing the row says "nothing to send" and the report carries `screenshot_error`; with ImageMagick installed the Linux path photographs the whole 1920×1200 display (1.97 MB PNG, other windows included) and the sheet says "the window is too large to send as one picture". A Retina Mac window (2880×1800 at 2x) is ~2.2× the pixels, so the same branch will fire for Jacob — no report from a Mac will carry a picture until it is scaled or re-encoded under the 1 MiB cap | filed to the sheet's owner (`features-inbox/2026-09-16-feedback-sheet-driven-on-the-rig.md`) |
| F-102 | 09-16 | loop (same drive, against Cursor's 👎) | feedback sheet, parity | Cursor's 👎 opens a small "Share feedback" popover anchored over the footer — one optional text box, ×, "Submit ⏎", plain Enter sends (`cycle-19/cursor-thumbs-down-share-feedback.png`); ours is a centred modal titled "Report a problem" with the parts list, ⌘⏎ sends, Escape closes. The weight is the design's (consent needs the list); the copy has "1 calls" for one call | note, not a bug; the plural to the owner |
| F-103 | 09-16 | loop (gate `rewind-turn`, the flaky row: pass/fail/pass across nine runs) | rewind, kernel wire | "Rewind here" named the turn by counting the window's prompt cards; the kernel counts `user` transcript lines, and the two differ whenever the transcript holds a user line the window folds — an ask's answer (`alpha`), the kernel's "skipped this question" line, a fork's copied history. The gate's click asked for user turn 6 when the prompt was line 57 (turn 9); the kernel said "no checkpoint for user turn 6" and nothing moved. A fork also carried no checkpoints at all: `fork_chat` copied the transcript and not `checkpoints.jsonl` | cycle-20 PR: `Frame::Rewind` carries `line` (the prompt's own `seq`, kept since #341), the kernel resolves by line when given and by count otherwise; a fork copies its checkpoints with its transcript |
| F-104 | 09-16 | loop (gate 030-work still) | Worked header | a finished turn that delegated everything draws "Worked 6s ˅" over nothing foldable — the chevron toggles nothing; Cursor's header over a delegating turn is bare ("Worked", no chevron; `cycle-22/cursor-worked-bare.png`) | cycle-22 PR: `work-bare` headline — no chevron, no pointer, no click; the gate skips it |
| F-105 | 09-16 | loop (gate cycle-20 `queue-survives-relaunch`; not reproduced in two isolated runs, root and fork) | follow-up queue, relaunch | ⇧⌘↩ while busy gave `held=1`; after quit + relaunch `held=0`, never ran, and the words were nowhere on disk — no inbox file, no transcript line, no kernel log (the keyless phase that followed restarted the kernel and its log). In isolation the kernel outlives the quit, the inbox file stays, and the line runs after relaunch | **two causes, both real.** (1) kernel: `hooks.rs::stop_work` deletes every waking inbox file on Stop by design — the gate's `recover()` pressed Stop on the long turn (cycles 20, 21); Cursor's Stop keeps the queue. Ask filed: `features-inbox/2026-09-16-stop-drops-the-queued-follow-up.md`. (2) desktop, cycle 22 with the kept place: no Stop this time — the follow-up *ran* (`turns/t0017/cause.md`, transcript 143–146) in the gap between quit and relaunch, and the window showed only its answer "restart" under the previous turn: the record ended at the quit and the live stream began at the attach, so lines 141–144 were never drawn. Cycle-22 PR: `adopt_kernel_tail` on attach takes in whole turns the kernel wrote after the record's newest prompt; echo stamps are flushed. Re-driven with a 40 s gap: prompt card and answer both present |
| F-106 | 09-16 | loop (cycle 20, search side by side: `cycle-20/*search-palette*`) | ⌘K search palette | Cursor's palette is 630 wide and centred, with filter chips (All / Agents / Files / Actions / Settings), a "Recent Agents" section (status dot, name, project, age in columns), an "Agent" actions section with shortcuts (Go Back, New Agent, Create Project, Dictate) and a footer of key hints; typed, each hit is a bold title over a snippet with the match lit, plus "New agent: 'query'". Ours is bezel's 420-wide list of "project › title — first words" lines, no sections, no actions, no hints. Also: the empty palette's first paint came a beat after ⌘K (a 1.2 s still showed nothing; 2 s showed it) | cycle-21 PR #351: rebuilt in Cursor's shape (chips, Recent chats in columns, Actions with key caps, hints, lit match) — `cycle-21/arbos-search-palette-*.png` |
| F-107 | 09-16 | loop (cycle 20/21, model menu and the old palette on the rig) | popovers, Linux renderer | every frosted popover (bezel `Material::UltraThick`, tone grey 47) paints as a light card on the dark theme on this rig — the model menu, the mode menu, the old palette — where Cursor's are opaque dark panels. The material's tone is dark; the non-Metal paint path is what lightens it. Not seen on Jacob's Mac stills | open — bezel/renderer; the new palette sidesteps it with a plain opaque card |
| F-108 | 09-16 | loop (cycle 21, composer attachments paired against `composer-attachments/`) | attachments, behaviour | a file attached from outside the place (/tmp, ~/Desktop) went to a local kernel as an absolute path; the agent's fence refuses it and the model answered "I can only access files within the workspace… so I cannot read it" where Cursor read the file and answered from it | cycle-21 PR: any attachment outside the place goes as bytes (`put`) into `.arbos/attachments/`, as remote ones did; re-driven, the agent read the file |
| F-109 | 09-16 | loop (same pairing) | composer tray, sent bubble | Cursor's image attachment is a bare rounded thumbnail (~100×70) with an ✕ badge on hover and no name; its file chip sits inline in the text row with no ✕ at rest; a sent image is a small square above the text. Ours: a chip with a 44px thumb + filename + ✕ always, and the same chip in the sent bubble | cycle-21 PR: images draw as bare thumbnails (64 tall, ✕ badge on hover), sent images as a 40px square, the file chip's ✕ on hover. Still open: the file chip inline in the text row; **closed cycle 39** ([#565](https://github.com/unarbos/arbos/pull/565), on main `f99ba703`): the file is a token in the sentence — `≡ name` in the link colour after the `+`, ✕ in the glyph's place on hover — and leads the sent bubble's words, as Cursor's does (`cycle-39/attachments/`) |
| F-110 | 09-16 | loop (cycle 22, a stopped worker's chat) | fold headline | a stopped turn without a stamped time read "Stopped by you the project page ˅": the summary's verb phrase ("explored the project page") lost its verb to the interrupt label and kept its object. Cursor: "Stopped by you" alone | cycle-22 PR: the label stands alone unless the rest is a time |
| F-111 | 09-16 | loop (cycle 22, classic worker chat side by side: `cycle-22/cursor-worker-chat-*.png` vs `arbos-worker-chat-*.png`) | worker chat | Cursor opens a worker in its own tab (italic title beside the project's); the brief is a full-width faded card; the headline reads "Worked for 46s" (with *for*, unlike the project chat's "Worked 25s"); open, the timeline is Thought 3s / Explored ran 1 command / Edited todo.py, ran 3 commands +88, each with its prose; under the footer a "1 File Changed · Review" card listing files with +88; pills "Open Desktop", "Create PR", ↓. Ours: same tab with a breadcrumb (a decision from cycle 9), brief as a plain bubble, no Files Changed card on a stopped worker, no pills | cycle-24 PR (#363): "Worked for Ns" on a worker's headline; a Files Changed card from the worker's own edits (`cycle-24/arbos-worker-chat-after-f111.png`); the tab-vs-breadcrumb stays |
| F-112 | 09-16 | loop (cycle 22, gate 22b `rewind-turn` + a targeted drive) | rewind | (a) the window refused "Rewind here" with "stop the turn before rewinding" whenever a *worker's* spawn row was still running — `busy()` counts tool rows, and a worker's stays running until it reports; the chat itself was idle. (b) on a place that is not a git repo the kernel answers "root has no checkpoints yet": checkpoints need a HEAD, so Rewind here never works on a plain folder, where Cursor's restore does | (a) cycle-22 PR: the guard is the chat's own turn (streaming/turn_open); driven: rewind over a running worker cuts the chat and returns the prompt. (b) open, kernel-side: a transcript-only cut when no checkpoint exists |
| F-113 | 09-16 | loop (cycle 23, Cursor Journey leg: `cycle-23/cursor-08-after-stop.png`) | after Stop | Cursor draws nothing under the prompt after a Stop that produced no output — no header, no "Stopped by you"; the composer's disc becomes the mic and that is the whole signal. Ours draws "Stopped by you" | kept on purpose: a prompt with nothing under it does not say whether the turn failed, was stopped, or is still coming. Listed so the choice is a choice |
| F-114 | 09-16 | loop (cycle 23, `cycle-23/cursor-04-working-12s.png`, `cursor-06-working-50s.png`) | live shimmer | Cursor's provider trouble reads as a faint second line under the shimmer, "We are having difficulties reaching the AI provider. Retrying automatically…", then the shimmer itself turns into "Taking longer than expected…". Ours: "Connection failed; retrying (attempt 4/5)." — counts attempts, Cursor does not | cycle-24 PR (#363): "Having trouble reaching the model provider. Retrying automatically…", attempt behind Details |
| F-115 | 09-16 | loop (cycle 23, `cycle-23/pair-live-*.png`) | Project chat, live fold | the live "Working <step> ⌄" fold started open, the Todos card under it; Cursor's Project chat shows the headline alone until the chevron is clicked (its worker chat streams tool rows open). And under a live headline the workers' line repeated the headline's step: "Working Updating the plan" over "2 Working Updating the plan" | cycle-23 PR: the Project chat's live fold starts shut (a worker's, open); a hand toggle still wins. Under a live headline the workers' line names the workers ("Implement todo app · Implement todo tests") |
| F-116 | 09-16 | loop (cycle 23, J08 after Continue: `cycle-23/arbos-j08-before.png`) | Project chat, worker reports | a worker paused by Stop (kernel #324) reported "Turn stopped by the user. Last words: stopped by the user (Stop) (transcript: …) This is the user pausing, not a failure … `say to=… mode=request` …" — and the Project chat drew all of it, twice, as a worker's block: the kernel's advice to the coordinator in the user's chat. Cursor's line for a finished sub-agent is one dim line | cycle-23 PR: `done_report` knows three verdicts; a stopped worker lands as "⊙ <worker> stopped by you", the file pointer and the advice dropped |
| F-117 | 09-16 | loop (cycle 23, J08 still) | Project chat | a turn with no time to its name (the fallback notice, a brief thought, prose) drew "Thought briefly" bare over the answer — the Project chat is meant to show no thoughts at all | cycle-23 PR: with no headline drawn, a thought in the Project chat is not drawn |
| F-118 | 09-16 | Jacob (his Mac: "the top bar and the bottom bar can't be seen as a separation" — second time on the bottom-left) | window chrome | the tab strip and the bar under the window took a chrome tone (the thinner material; in light, `surface` grey around the content's white) with a rule at each seam: two trays laid over the chat. Cursor's strips are the chat's own background, controls floating, separation by spacing | cycle-23 PR: `chrome_bg` is `content_bg` — tab strip, bottom bar, panel and the macOS title band one surface; the tab strip's bottom rule, the bottom bar's top rule and the panel foot's rule gone. Stills `cycle-23/one-surface-*.png`, triple `pair-one-surface-before-after-cursor.png` |
| F-119 | 09-16 | Jacob (his stuck screen, kernel half #362) | steers, stall hint | (a) "run it" typed four times into a silent turn drew four bubbles; (b) the heartbeat's hint read "Nothing has arrived in 2m 26s … check the model key" — its clock was the turn's age, not the quiet, and a running command is not the model | cycle-23 PR: (a) the same words again while the first steer still waits are one bubble; the kernel's "Already queued" notice hangs under it, once. Driven: 3× "run it" into a streaming reply → 1 bubble, 1 notice (`stuck/02-run-it-x3.png`). (b) `progress_at`: the quiet since the last visible progress — tokens, tool rows, `job` output count; `Alive`/probes do not. Over a running command the hint names it: "`python3 slow.py` has printed nothing in 1m 10s. Stop ends it." |
| F-120 | 09-16 | loop (cycle 23, driving #362's kernel) | process rows | the kernel now opens a process row for any attached command past 20 s (#362); the desktop brought that row to the column — the chat, composer and all, replaced by a `python3 slow.py` board while the person typed (`stuck/01-running-25s.png`) | cycle-23 PR: a process row never takes the column on its own; it lands in the panel's Processes, one click away |
| F-121 | 09-16 | loop (cycle 23, own regression from F-115, caught the same cycle) | live fold | with the Project chat's live fold shut: the steer bubbles typed into the turn were inside it and vanished; the heartbeat drew under the headline saying the same step twice; a steer typed after the answer began streaming was dropped from the tail, so the "Already queued" notice stood alone | cycle-23 PR: steers draw under a shut headline; the headline silences the heartbeat open or shut; a steer in the answer range draws its bubble |
| F-122 | 09-17 | loop (cycle 24 gate, twice in a row: `qa-ui/cycle-24/033-rewind-turn.png`) | rewind | "Rewind here" on an idle root over a stopped worker: no state change; the kernel log shows no rewind frame, so the refusal is the desktop's (the footer under the last answer is not in the still either). Passed on cycle 23's final build an hour earlier, same kernel | cycle-25 PR (#367): a `working` heartbeat after `turn_complete` reopened the turn; same lag rule as `turn_alive` now. `rewind-turn` passed ×3 (cycles 25, 27), then failed once on cycle 28b. Traced in cycle 29 (a refused rewind logs its state): `turn_open=true, turn_ended=None, progress=0s` — the kernel's settled records landed past the 1 s tail lag and `turn_alive` reopened the finished turn (F-122b). Fixed in #391: a record after the turn's end never reopens it; a new turn announces itself first (a prompt, a wake, another client's line, the thinking pulse). Turn phase ×2 on that build: zero fails, rewind passing |
| F-123 | 09-17 | loop (gate `work` row, cycles 23–25) | settled fold | the first click on a settled "Worked" fold reads "no state change" (the footer under it does not move); the second click passes. One cause found and fixed — the toggle recomputed its default from `chat.busy()` instead of the drawn one — but the row still fails first-click on cycle 25c | #367 (toggle default) + #378 (the gate measured the footer's y; the transcript is bottom-anchored, so a fold opening above the viewport's bottom moves nothing below it — rows are measured now). Closed in #381 (cycle 27): the P_EDIT fold *is* bare, correctly; the gate was clicking the kickoff's fold scrolled out of view and reading nothing as the app's fault. The driver now refuses clicks on clipped targets; the row clicks the last fold on screen or gaps. Gate 109 · 0 |
| F-124 | 09-17 | Jacob (feedback report `2026-09-17-1`, "Forked the chat mid run no response from sub agent"; -2 is its echo) | fork | a fork is a whole-log copy, so one taken mid-run ended on the prompt the original was still answering — nothing under it, idle composer — and its model then saw a history without the workers and returned empty twice (-2) | cycle-25 PR (#367): a calm line under the copied prompt says what it is. Kernel note filed on the fork's history (in the feedback ledger) |
| F-125 | 09-17 | loop (driving #372 for its author) | bar: stranger kernel | the "Kernel from another build" plate appears for a busy and for a too-old stranger, the idle one is swapped silently, the tooltip is right — but the click runs the update path, never `restart_kernel` (`plate()`'s `on_click` ignores its `action`) | filed to the author: `features-inbox/2026-09-17-stranger-kernel-control-driven.md`; not fixed here (their PR) |
| F-126 | 09-17 | loop (cycle 26, panel beside Cursor's sidebar, `cycle-26/cursor-sidebar-dark.png` vs `arbos-panel-dark.png`) | panel foot | two gears in one window: the bar's bottom-left (Cursor's place) and the panel foot's | cycle-26 PR (#378): the panel foot keeps search and new-chat only |
| F-127 | 09-17 | loop (cycle 28, d11 settled vs `cycle-23/cursor-13`) | settled fold | the newest turn's Worked fold stayed open once settled (cycle 3's rule); Cursor's settled headline stands shut over its timeline in both chat styles | cycle-28 PR (#384): `auto_work_open` is `running` alone |
| F-129 | 09-17 | loop (cycle 32, `cycle-32/cursor-create-project-sheet.png` vs `arbos-opener-create-*.png`) | new project | Cursor's "+" opens a **Create Project** sheet: an editable name ("New Project") under a glyph, a Workspace (repo) picker, a Model picker, one blue button. Ours opens the folder picker: a path field, machines, folder rows, a Create row; the name is the folder's and the model is the composer's later | design difference, Jacob's call: our project is a folder, Cursor's a chat scoped to a repo. Noted, not changed |
| F-130 | 09-17 | loop (cycle 32, same pair) | glass on Linux | the Opener's glass surface, without the blur primitive, painted only its tint — the project header's text stayed legible *through* the card and the typed path sat over "parity-proj". F-107's family. **The rig cannot see the Mac's blur, so this is how Linux users see it, not how Jacob does** | cycle-32 PR: the non-lensed fallback paints an opaque `surface_raised` floor under the tint (`vendor/bezel-ui/src/surface.rs`) |
| F-131 | 09-17 | loop (cycle 32, worker chat beside Cursor's worker tab: `cycle-24/cursor-worker-chat-open.png` vs the 07:34 drive — its still was overwritten by the rerun; the drive log's `text 'Worked for': False` and the headline read from the still at the time are the record) | worker chat, headline | the settled headline read its summary phrase — "Edited 2 files, explored slow.py, updated the project page 3 times ›" — where Cursor's reads "Worked for 46s"; F-111's "Worked for" had regressed. The turn had no time: a worker's first turn opens on its `plan` wake, not a `user` line, and (a) the replay parser skipped `plan` wakes when starting the clock (`kernel.rs session_history`), (b) a window that joined the worker mid-turn had no flight to stamp from (`stamp_worked`), (c) `TurnEndedAt` on replay stamped only `User` openers | cycle-32 PR: the clock starts at the plan wake on replay; `stamp_worked` falls back to the opener's own `sent_at`/`at`; `TurnEndedAt` stamps a `Wake` opener too. Driven: "Worked for 17s ›" live and after relaunch (`cycle-32/arbos-worker-chat-after.png`, `-reopened.png`) |
| F-132 | 09-17 | loop (same pair) | worker chat, brief | the brief drew as a prompt bubble the length of the page, pushing the work below the fold; Cursor's worker tab draws the brief as a full-width bordered card folded to its first three lines under a fade, opened by a click (`cycle-22/cursor-worker-chat-collapsed.png`) | cycle-32 PR: the first prompt of a chat with a parent is that card — column-wide, hairline border, three lines, the last under a fade into the card's colour, click to open and fold; no pencil (a worker's words come from its parent). Driven: folded / open / folded (`cycle-32/arbos-worker-brief-open.png`) |
| F-133 | 09-17 | loop (reading `session_history` for F-131) | worker chat, relaunch | `session_history` read only `.arbos/agents/<id>/transcript.jsonl`; a finished worker is moved to `.arbos/archive/agents/<id>/`, so a worker chat opened after a relaunch with no local copy of its items would show the brief alone (read from the tree: `find . -name transcript.jsonl` on the drive's place lists the worker's only under `archive/`) | cycle-32 PR: the archive path is the fallback. **Not driven end to end** — the drive's relaunch had the desktop's own copy of the items; the case that needs it is a worker that ran and archived while the app was shut |
| F-134 | 09-17 | QA `af-03`, handed over as desktop work; driven `af03_drive.py`, `af03b_drive.py` | after-failure: folder moved | a project's folder renamed under its kernel: typing into it drew "this agent is archived: its history stays, but it takes no more messages" (false) and dropped the line — the fourth swallowed-message path. `agent_gone()` read a missing agent folder as an archive whatever had become of the place | cycle-33 PR: `place_gone()` is its own state; the line's card lands, its words are kept in the queue (the pending row shows them), and one notice says "This project's folder is gone or was moved: expected `<path>`. Your line is kept and goes when the folder is back or the project is reopened."; the same line at the moment the socket drops for this reason; when the folder is back a new line queues behind the held ones so they go in the order typed (driven: three answers in order, no duplicate cards). "Archived" is said only when `.arbos/archive/agents/<id>` holds the agent; a missing agent folder names its path and says "not sent". **Limit, honest:** the queue is this window's; a relaunch while the folder is away keeps the cards, not the words (F-105's family) |
| F-135 | 09-17 | loop (the af-03 drives, both runs) | replay into a live pane | when the folder came back with the kernel still alive, the kickoff greeting and the first prompt drew a second time between the held cards and the new one; the kernel's record has each once. Lead: the kernel log shows an `attach_open`/`attach_close` one second after the rename back — a listing or history read whose replay reached the live session, where `foreign_prompt` compares only the newest card and a `kickoff` wake resets the step table so a replayed assistant text appends | open — mechanism read from the tree (cycle 33, 10:15): a recorded `assistant` line whose step is not in `step_items` is appended unless it equals the *last* item (`session.rs` `Event::AssistantFinal`, "No deltas seen (an older kernel, a replay): the line stands on its own, once"), and a recorded `user` line is matched only against the *newest* User card (`foreign_prompt`); a `kickoff` wake resets the step table first. So any recorded replay reaching a pane that has grown since duplicates the record's head. Fix to make: carry `seq` on `AssistantFinal`/`Woke` as `UserLine` already does, and drop any record whose seq the pane already holds — a record the pane has is not news. Then find the short attach (the kernel log's `attach_open`/`attach_close` one second after the rename back) that replays into a live stream |
| F-136 | 09-17 | loop (deleted-build drive `binary_gone_drive.py` with main's kernel `7e19f9e9`) | stranger plate | with two strangers (the Home place's, idle and already swapped, and the busy tab's) the plate was the first found and its click restarted the other place's kernel while the person looked at this one; the label named no place | cycle-33 PR: the active project's stranger first; with several, the label carries the place ("Kernel running a deleted build · bgone-092810"); driven end to end: plate within 8 s of the replacement, tooltip names the replacement and `turns running: root`, click → "Restarting…" / "Stopped." / "Kernel … restarted on this build (…)", new pid on the new image, plate gone (`cycle-33/arbos-stranger-deleted-build-*.png`). F-125 (the click ran the update path) closed with it. **For the update owner:** `gate.say()` starts lowercase mid-tooltip ("never be reported. turns running: root.") |
| F-137 | 09-17 | Jacob (feedback 2026-09-17-6, still); the coordinator's capture `media/desktop-feedback/f137-sessions/` (98 files) | worker rows, panel | a **Delegate 1 · Working** row for `chat-1789473184580`: the window's session (15 Sep) filed it as root's delegate, the kernel's `agent.md` names no parent and no role (the shape New Chat makes), its transcript is one "reconnected" line, and no status file backed the Working. Separately, the three worker sessions were written at 06:42:45/:49/:54 against spawn results at :53 — a spawn that failed would have left a row with nothing under it | cycle-33 PR (`4cf27bcb`): `kernel::agent_parent` reads the kernel's record; `ensure_child_agent` makes no row for a local agent the kernel has no record of, and parents by the record — a parentless agent is a chat of its own with no delegate number, whatever adopted it; `resolve_parents` lets the record override the filed parent at restore (the log says so); `child_state` draws Working only from evidence (a live socket, a tool seen to start, the kernel's activity). Driven on Jacob's own seven records laid into a rig place: `chat-…` restores parentless, "Delegate 1" drawn nowhere, the five real delegates keep their parent (`f137-sessions/after-rig-restored-on-jacobs-records.png`). **What the rig cannot see:** which frame set the phantom's `Working` on his machine — no status file, so a flag on a live socket; the evidence rule covers it either way |
| F-138 | 09-17 | Jacob (feedback 2026-09-17-8) | steer in a running turn | his *where is my response* drew above the three *Done <worker>* lines, as if they answered it: the worker lines were appended after the whole tail, so a steer card typed later came first | cycle-33 PR: worker lines under the prose that spawned them and before any later steer card; driven (`media/desktop-feedback/2026-09-17-8/after-rig-steer-below-worker-lines.png`) |
| F-139 | 09-17 | Jacob (feedback 2026-09-17-6) | live headline | the model's `status` step *Waiting on three sorting workers* stood for minutes over three Done lines while the coordinator itself sat in `sleep 75` — the true line was *Running sleep 75; echo waited* | cycle-33 PR: `live_status()` — a Running tool item newer than the last status call outranks the status; the headline, the worker line's own step and the heartbeat read it; an attached command running as a *job* (a process row, not a tool item until it returns) counts too (`running_job`). **Second half (#432's author):** a status that stood while a worker worked is about the workers and goes when the last of them finishes, settled from the children's states before each draw — the kernel writes no waiting line over the agent's own status, so its clear cannot be the signal. Driven: "Working  Running sleep 100; echo waited ›" over "Done  Write bubble sort" (`2026-09-17-6/after-rig-status-cleared-running-command.png`). The cause (a coordinator sleeping on its workers) is the kernel's: `features-inbox/2026-09-17-coordinator-sleeps-on-its-workers.md` |
| F-140 | 09-17 | loop (cycle 33, cold kickoff pair: `cycle-33/cursor-cold-scratch-settled.png` vs `arbos-cold-settled.png`) | kickoff, cold | Cursor's Start-from-scratch project ends its kickoff with a **Create repo** pill (no repository yet) and a greeting that names the person ("Hey Jacob — ready when you are. Drag in chats or files…") in two paragraphs; ours ends with no pill on a non-git folder and a greeting that describes the folder ("an empty directory with just the .arbos folder") | design/kernel: the pill is a cloud action (GitHub repo creation); the greeting's words and the person's name are the kernel's kickoff prompt (`user.md`). Noted, not a desktop change. Shape otherwise matches: header, date line, *Setting up environment*, *Worked Ns ›*, greeting, footer |
| F-143 | 09-17 | Jacob (feedback 2026-09-17-12) | run lines | every fetched page drew as a bare *fetch* / *Fetched fetch*: the label builder never read the `url` argument | cycle-34 PR: the host, as Cursor's *Fetched en.wikipedia.org* |
| F-142 | 09-17 | Jacob (feedback 2026-09-17-11) | artifacts | the agent screenshotted its drawn cat twice on the way and embedded the last shot in its reply — three cats on the pane | cycle-34 PR: an artifacts row whose files the answer's prose embeds is skipped; the first attempt stays as the record of the work |
| F-144 | 09-17 | Jacob (feedback 2026-09-17-14) | document view | the Kickoff document opened on its TOML front matter (`+++ owner = "root" +++`) drawn as prose | cycle-34 PR: the document view drops a `+++`/`---` block, as the project page already did |
| F-145 | 09-17 | Jacob (feedback 2026-09-17-15, -16); reproduced on the rig | links in prose | every URL and doc chip in an answer was dead to a click. Root cause read from the app's own log: bezel-markdown reports the release to every prose item and the items above the pressed one hear it first; `point()` cleared `dragging` on the first release it saw, so the pressed item's own release found the gesture over. And a doc chip's relative path (`docs/brief.md`) became `https://docs/brief.md` for the browser | cycle-34 PR: a release on an item that does not hold the press leaves the gesture alone; a path in the place (`place:<path>`) opens in the column via `open_shown`. Driven with a fake browser on PATH: the URL reaches it, the doc chip opens `brief.md` beside the chat. **Rig:** the gate had no link-click row — it does now want one (R18) |
| F-146 | 09-17 | Jacob (feedback 2026-09-17-19) | dictation | on stop, dictation sent at once and had the reply read aloud (the voice side's "same two-step as the web composer's dictation final") — "the wrong way to do things" | cycle-34 PR: the words land in the field at the caret and wait for Enter; the reply is read, not spoken; a call is where speech answers speech. Voice owner told (`features-inbox/2026-09-17-dictation-lands-in-the-field-not-sent.md`) |
| F-147 | 09-17 | Jacob (feedback 2026-09-17-17); reproduced on the rig | fork, replay | a fork lost every earlier answer: the copy reads its transcript from the file, and the replay parser flagged a `user` line as a steer whenever a turn was open — the kernel writes `wake user` before every prompt, so every prompt of the copy folded into the turn before it and the answers sat behind shut folds | cycle-34 PR: a prompt following its own wake is the turn's prompt. Driven: the copy shows its turns with their footers and the date line (`/tmp/c23/fork/02-fork.png` → `cycle-34/arbos-fork-after.png`) |
| F-148 | 09-17 | Jacob (feedback 2026-09-17-18) | rewind on a fork | his kernel log: the rewind ran on **root** (`agent=root target=Line(75) dropped=21 files=true`) while he was in the copy — 21 lines of root's transcript archived by a click in a fork | kernel side, filed with the line quoted (`features-inbox/2026-09-17-rewind-on-a-fork-cut-roots-transcript.md`). Desktop: on the rig a copy's rewind is refused by the kernel with *no checkpoints yet* on a non-git place (F-112b's family) |
| F-149 | 09-17 | loop (link drive, first run) | steer echo | three lines typed in quick succession while the first ran: the second and third drew **twice** each (`/tmp/c23/link/01-answer.png`, first run) — F-83/F-94's family on main's kernel `7e19f9e9` | open — a steer typed while the previous steer's echo is still owed; reproduce with two fast steers and read `awaiting_echo` |
| F-150 | 09-17 | Jacob (feedback 2026-09-17-27) | heartbeat, Project chat | between the kernel's *Thinking* heartbeat and the answer's first words the pane went blank: F-117 hides a thought with no headline, and the heartbeat stood down for that same thought as "live" | cycle-34 PR: a hidden streaming thought says *Thinking* |
| F-151 | 09-17 | Jacob ("Text highlighting is not correct", via the coordinator); reproduced with a real pointer | selection | per-row bands with gaps, ragged right edges, the selection hidden behind inline-code washes | cycle-34 PR (bezel-markdown patch): wrapped rows extend to the layout's right edge and abut; the selection paints over the code and chip washes, the caret last (`cycle-34/arbos-selection-*.png`). Cross-item selection (prose into a tool card) is not possible — one item at a time — a design limit noted. **Rig R19:** the driver's synthetic drag cannot select (all events in one frame; `dragging` read at render); the gate has never selected text |
| F-152 | 09-17 | Jacob (opener on ArbosLife at `/`, via the coordinator) | opener | the folder step opened at `/` on every machine — `bin/`, `boot/` as the first rows | cycle-34 PR: `~/` on every machine; "Open home" for `~`; #258's typed-path and Create behaviours unchanged |
| F-153 | 09-17 | Jacob (feedback 2026-09-17-28) | system lines | the kernel's *Correction not kept* nudge drew as prose with a loop glyph, indistinguishable from the agent's words | cycle-34 PR (#438): a system line — left hairline, faint italic caption, reworded (*Project page not updated*, *Correction not kept: …*) |
| F-154 | 09-17 | Jacob (feedback 2026-09-17-26) | model picker | *astra is not available on this key* and Astra still offered in the picker | cycle-34 PR (#438): a model the kernel names as unavailable to the key leaves the picker for that chat; the catalogue-vs-entitlement half is the kernel's |
| F-155 | 09-17 | Jacob (feedback 2026-09-17-13) | type size | default prose 13 pt against Cursor's 14 px | cycle-34 PR (#438): default 14, state v3 migrates an untouched 13 |
| F-156 | 09-17 | d12 drive, panel main row | chat title | the sidebar label was the first prompt's first four words, cut mid-clause: *This project is a*; Cursor titles a chat with a model summary and, before one arrives, the prompt's opening words to a width | `cursor/chat-title-fallback-aa39`: a prompt title is cut at its first clause, six words / 40 chars, and never ends on a function word → *This project is a research notebook*; the model-titled label is the kernel's — inbox note filed |
| F-157 | 09-17 | mesh worker's survey (Jacob's three-hour dead tab), `features-inbox/2026-09-17-client-honesty-when-the-mesh-is-unavailable.md` asks 1–3 | connection notice, machine pill | `short_error` collapsed every `connection failed: <reason>` to *Connection failed.*; a fatal fault posted once and never retried, the pill read a counter and never why; the remote bootstrap's step was hidden behind *reconnecting, try N* on every retry, and a mid-install failure's step was recorded and never shown | [#467](https://github.com/unarbos/arbos/pull/467): the reason in plain words on the notice and on the pill for as long as the state lasts (`connect_fault`), a 60 s re-check for a fatal fault, the step outranks the counter, *Installing arbos-kernel 0.2.1 (27 MB)*; driven on no-binary and unresolvable-host (`cycle-35/connection-honesty/`) |
| R21 | 09-17 | QA's fresh machine: `-- desktop main: app build failed` cost fifteen scenarios | rig | my gate read the kernel's `--version` and never the desktop's; a failed `cargo build` leaves the old binary in `target/`, `cp` copies it, the run is green on a build that is not the PR's | [#470](https://github.com/unarbos/arbos/pull/470): `desktop` row from `arbos-desktop --version` against the tree's HEAD, fail + stop on mismatch (driven: `42cb975-dirty` vs `382efcb` → exit 2); `arbos-launch.sh` stops on a failed build with its log; `desktop/BUILDING.md` |
| F-159 | 09-17 | Jacob (feedback 2026-09-17-29) | prose, markdown | two prices in one sentence parsed as inline math: *61bnprivateUSdefence* in italics | [#482](https://github.com/unarbos/arbos/pull/482): money and prose between two dollars stay text (bezel-markdown `reads_as_prose`); driven |
| F-160 | 09-17 | Jacob (feedback 2026-09-17-33, -34) | Project chat, live turn | prose after the last tool drawn as the answer while the turn ran: a model looping sentence/command every second flashed text / shimmer / text for 40 s on a kickoff | [#482](https://github.com/unarbos/arbos/pull/482): the kickoff shows its shimmer alone until it ends; a running Project turn holds the tentative answer once the pattern shows twice; the heartbeat says Working over it. Not reproduced — read against the report's 51 events |
| F-158 | 09-17 | gate phase T (cycle 35, three runs incl. main's build) | follow-up rows | a queued line the kernel already took (bash yields since #362) left a stale row; Remove did nothing; the refusal `plan op cancel … no longer in the inbox` drew as FAILED | [#489](https://github.com/unarbos/arbos/pull/489): a cancel marks the row cancelled as the click lands; the refusal drops the row and says nothing for a cancel, one calm line otherwise |
| R22 | 09-17 | cycle-35 gate (twice) | rig | the settings walk (rewritten for the inline tab) listed the whole window's ids and clicked `tab-0`, `tab-close-0` — closed the project tab under itself; every later phase without a composer | [#470](https://github.com/unarbos/arbos/pull/470) `38770f31`: scoped to `.settings-body.`; 45 · 0 on rerun |
| R23 | 09-17 | dead-event pass | rig | `permission-strip` read not-reachable in every gate since it was written: the approval card it waited for could never open (its two events were never emitted) | [#486](https://github.com/unarbos/arbos/pull/486): row removed with the card; the `ask-*` rows drive approvals |
| F-161 | 09-17 | cycle-35 items (1)(2), `cycle-35/bars-and-sizes/` | bottom bar, window sizes | **paired, mostly matched.** Arbos keeps a 28 px status bar (gear, `0.2.0 (1509)`, the update/stranger plates) on the same surface as the content (#1615 14 vs #141414); Cursor's Agents window has no bar at all — its foot is the composer's own row (branch · Cloud · spinner). At 900×600 both drop the side panel (Cursor its right Project/Browser panel, keeping the 255 px sidebar; Arbos its panel) and the chat fills: composer 612 px in Cursor's 645 px column, ~700 px in Arbos's 900; at 1440 Cursor's composer 720 vs Arbos 700; at 1600, 745 vs 700 — within 5 %. | no change; the bar is a deliberate addition (plates need a home). **Rig:** `panel_open` stays `true` in the driver while the panel is not drawn at 900 wide — the state says open, the window shows none (R1-class; a `panel_shown` field would settle it); **re-read cycle 43** on main + #652 (`cycle-43/sizes/`): composer 708 px at 900, 1440 and 1600 (Cursor 612 / 720 / 745); the 28 px bar at every size; the panel closed by default at all three — unchanged since 35 |
| F-162 | 09-17 | cycle-35 item (3), `bars-and-sizes/mic-pair.png` | composer mic | Cursor's mic disc rests as a light filled disc (#F0F0F0) with a dark glyph, right of the model picker; Arbos's `theme.solid` disc renders dark grey with an accent glyph on the rig and on Jacob's Mac (report -33's still). Click: Cursor showed no change on the rig (no microphone device); Arbos says *Dictation (macOS only for now)*. The listening state is unpaired on Linux for both | theme token question for the theme owner (`solid` vs Cursor's light plate), not changed here; the Mac's listening state needs the Mac |
| F-163 | 09-17 | cycle-35 item (4), `cycle-35/themes/` (p8 in both apps, both themes) | themes | **Light:** Cursor's page is #F8F8F8 with a darker rail #E5E5E5 and body text #141414; Arbos is pure white #FFFFFF everywhere (page and panel one tone) with body text #222222 and a tinted table header #F5F5F5 where Cursor's header is the page colour. **Dark:** Cursor #141414, Arbos #161514 (a warm step) — near enough. Headings, table rules, bullets, inline code: the same shapes at the same weights in both | theme token numbers for the theme owner (light page and rail); not changed here. Colour accuracy caveat stands (Xvfb, no colour management) but these are rendered values read from the same display for both; **re-read cycle 42 on main `dc633b7d`** (`cycle-42/light/`): page #FFFFFF, panel #FFFFFF, tab strip #FFFFFF, composer #FFFFFF, body text #222222, inline-code plate #F0F0F0, user bubble a warm dark #372F2B — unchanged since 35; Cursor #F8F8F8 page / #E5E5E5 rail / #141414 text. Still the theme owner's numbers |
| F-164 | 09-17 | cycle-35 item (5), f1 cold kickoff (`cycle-35/cold/f1-settled.png`) | notices | the refusal notice read *This key cannot use openai models (openai/gpt-5.* — `short_notice` cut a long notice at the first `.` anywhere, and `gpt-5.4-mini` has one | `cursor/notice-cut-aa39` (`8d82eb06`, unpushed — GitHub credentials absent from the VM since ~19:35): a stop counts only when whitespace or the end follows it; unit-tested |
| F-165 | 09-17 | gate `cycle-35d` phase D (1 of 3 runs today) | after-failure: folder moved | after the rig renamed the scratch place, the old path came back as a whole `.arbos/` tree without `agents/`; the window saw the place present and the agent gone and dropped the line. **Correction (23:40):** the maker was the desktop, not the kernel's late writes as first written here — `attach_or_spawn` bootstrapped the workspace on every attach, and the sidecar's `project::init` recreated `<old>/.arbos/desktop/` so the gone path could be attached at all (the features agent's note, from QA's 0-of-4 on a directly attached kernel) | **desktop half** [#496](https://github.com/unarbos/arbos/pull/496) merged (the line is kept); **the maker** [#530](https://github.com/unarbos/arbos/pull/530): bootstrap only where `.arbos/project.toml` or `agents/` is, `init` never makes the project folder; driven — rename, two lines, 30 s, the old path stays gone. The kernel note stands corrected in `features-inbox/2026-09-17-kernel-writes-recreate-a-moved-place.md`; **re-checked cycle 41 on main `dc633b7d`** (kernel with QA's af-04 fix, `80e69942`): phase D 5 of 5 clean, `world-back-in-order` back to pass, and no ghost folder at the moved name after runs 3, 4 and 6 (0 of 3 checked) — the desktop path holds |
| F-166 | 09-17 | cycle-36 item (1), `cycle-36/launch/` | launch, first run | with no state file, Arbos lands at (2,85) at 1100×761 — the WM's default for a window it will not grant 1600×1000 — while Cursor's first run centres a 1280×800 window. Cursor's first frame is a sign-in card (CURSOR / Log In / Sign Up), no permissions ask; ours opens Home with its history and the Permissions sheet over it | Linux WM placement; the Mac's own rule was set in cycle 20 (restore or centre). Not changed; a `centre if the WM moved us` fallback on Linux is cheap if wanted |
| F-167 | 09-17 | cycle-36 item (2), `cycle-36/tabs/tabs-pair.png` | tab strip | the metaphors differ: Cursor's top row is *project label · chat pills (dot) · +new chat* with ← → at the left and IDE ↗ ⋯ at the right; ours is *Home · project pills (×) · +new project*. Pill: Cursor 22 px, `#202020` on `#141414`, active text `#F0F0F0`, inactive label `#969696`; ours 24 px, `#222120` on `#161514`, active `#F0F0F0`, **inactive `#BBBBBB`** — brighter than Cursor's by 37 levels, so an inactive tab reads nearly as loud as the active one | theme token for the theme owner (`text_muted` on the strip); the metaphor is Jacob's design (tabs are projects) |
| F-168 | 09-17 | cycle-36 item (4), `cycle-36/picker/` vs `cycle-35/bars-and-sizes/cursor-model-popover-crop.png` | model picker | Cursor's popover is four settings rows — Thinking (toggle), Context 300K ›, Effort High ›, Model Claude Fable 5.1 › — with the model list one level down; ours opens the full list at once (115 models, search, provider chips, *vision* tags) with a *Context —* row at the foot | design difference, for the picker owner; F-154 already removes key-unavailable models. `/mode ask` drew no mode chip on 1509 (`composer-mode` absent) — the chip appears only when the agent pins a mode, by design since #? — noted, not a fault |
| F-169 | 09-17 | cycle-36 item (4)'s panel (`picker/01`) | chat titles | the kernel-started chat was titled *This place was just opened* — the kickoff wake's text taken as the first prompt | [#498](https://github.com/unarbos/arbos/pull/498): only a `User` event names a chat |
| F-170 | 09-17 | d14 (`cycle-36/d14/03`) | worker report block | the report drawn under *Worked 25s* carries the worker's **id** as its caption (`add-sources-to-project-context`) where the panel shows its name (*Add Sources to Project Context*); Cursor names the agent | [#531](https://github.com/unarbos/arbos/pull/531): `who_label` names an unstamped worker from its id as a person reads it; the worker-card path already named it (*Report Writer done — done*, `cycle-38/caption/`); the wake-segment caption to be read on the next long-form run |
| R24 | 09-17 | gate `cycle-36` | rig | two settings controls below the fold read as fails: the driver's R13 refusal *not on screen … clipped away* was not in `check()`'s unreachable class | [#505](https://github.com/unarbos/arbos/pull/505); next: scroll the settings body to a control before clicking it |
| F-171 | 09-17 | cycle-37 item (1), `cycle-37/project-tab/` | the drawer (new panel) on build 1616 | the Project page is now the drawer's Project tab, closed by default; its row reads *+ · ▢ Project · ⤢ · ×* (Cursor: *▢ Project · ⊕ Browser · +* then ⚙ ⤢ ▣ — the + after the tabs, not before); Escape and × close it, ⌘B reopens; **Expand (⌘\\) changed nothing the driver can read at 1100 wide** (width 280 → 280) | for the side-panel owner: the + position against Cursor's, and what Expand does at a width where the chat's minimum binds; the tooltip *Show panel ⌘B* stayed on screen after the click that opened it (a stale tooltip in `11-panel-open.png`) |
| R25 | 09-17 | gate `cycle-36` | rig | every `project-page-*` row read not-reachable since the drawer rewrite and the gate said nothing louder | [#505](https://github.com/unarbos/arbos/pull/505): the rows open the drawer and drive its ways out (Escape, ×, the Project tab first and active); Expand recorded with widths |
| F-172 | 09-17 | d15 relaunch mid-run (`cycle-37/d15/`) | relaunch / replay | five workers sleeping in a bash read as idle rows after a relaunch (no spinner, no step, no *5 working*, no *waiting on*) while the kernel's `status.toml` said *Sleeping for 40 s* and the sockets were live — the tool record is filed at its end, so `busy()` had nothing | [#512](https://github.com/unarbos/arbos/pull/512): a live socket plus a kernel step counts as Working; `child_state` in the driver; driven on a rerun. The fold's summary also changed across the relaunch (*Worked 9s* → *Started 5 workers*), both true, not judged |
| F-173 | 09-17 | QA `desktop-user-message-card` (qal-j24), reproduced | user card | *hi* in a New Chat took the column (692 of 708 px): the chat is filed under the root, and any first card of a chat with a parent drew as a worker's brief | [#511](https://github.com/unarbos/arbos/pull/511): a brief is the parent's words (the spawn wake's card carries `channel = brief`, or the kernel records a parent); New Chat *hi* 36 px, worker brief still 692. `StoredMessage` also lost channel/worked_secs/sent_at/feedback/seq on every reload — carried now |
| R26 | 09-17 | gate `cycle-37` | rig | phases R and W crashed on a panel row scrolled out of the drawer after a run that opened many chats | [#505](https://github.com/unarbos/arbos/pull/505): reveal or skip the row; the `reveal-*` settings controls (they open Thunar over the display) skipped |
| R27 | 09-17 | gates `cycle-37`, `cycle-37b` | rig | one kernel event (Stop held over a waiting spawn) became 17 fails: every recover failed the same way and every later phase ran in an unstoppable chat | [#505](https://github.com/unarbos/arbos/pull/505): recover names who is busy; on the second failure the run is marked as the kernel's and later phases read not-reachable with the reason |
| R28 | 09-18 | gate `cycle-39b` | rig | `connect-first-spawn` (phase X) counted every *connection failed* notice on the project's transcripts — phase D just before it takes the kernel's file away under a running chat and earns one that stays | [#549](https://github.com/unarbos/arbos/pull/549): the row counts only the notices the connect itself adds; `LX` re-run passes |
| F-174 | 09-17 | cycle-38 item (1) / d16, `cycle-38/fork-rewind/05-rewound.png` | rewind | *Rewind here* on the second-to-last prompt (card 117 of 141) cut only the last turn (141 → 119 items, *41 transcript lines cut*) and left card 117 and its answer on the pane, while the composer was refilled with **117's** text — the cut and the refill disagree by one turn | **closed on build 1733** (`cycle-39/rewind/`): *Rewind here* on the second-to-last card (ix 164 of 168, seq 257) cut the pane to before it — last prompt on the pane is card 162, the composer holds card 164's text, two notices appended (*rewound: 8 transcript lines cut…*, F-175's *files not restored*). The 1657 reading was on a session reloaded without `seq` (#511 carries it now); not reproduced since ; **re-driven on 1741** (kernel `9bb49c6`, `cycle-39/rewind-1741/`): same result — cut to before the clicked card, composer refilled, OK |
| F-175 | 09-17 | same still | rewind notices | *rewind: transcript cut, files not restored: .arbos/ is tracked by the project re…* — the kernel's refusal to restore files draws with Details/Retry like a failure; Cursor's restore says *Restored checkpoint* or nothing. The fork itself matched Cursor's shape: a new tab, the copy answers on its own, the original untouched (141 items before and after) | wording is the kernel's; whether *files not restored* is a failure to draw red is a question for the kernel owner — a non-git place cannot restore and the person should know, once |
| F-176 | 09-17 | cycle-38 item (2), `cycle-38/search/` (⌘K *manifest* vs Cursor's Search *quick*) | search palette | same width (~630 px), same footer shape (↑↓ Select · ↵ Open), same grouping by kind. Differences: Cursor's filters *All · Agents · Files · Actions · Settings*, ours *All · Chats · Actions* (no Files, no Settings); Cursor lights the match in the **accent** on a one-line row, ours bolds it and adds a grey snippet line (rows 44 px vs ~30); Cursor's footer offers *Ctrl+[ / Ctrl+] Change Filter*, ours *esc Close*; Cursor's card is a step darker (#151515 on a #0C0C0C scrim) than ours (#1E1E1E) | for the search owner (F-106): Files and Settings as filter kinds, the accent match, a filter chord; not changed here; **cycle 45**: the accent match taken ([#676](https://github.com/unarbos/arbos/pull/676)) — re-read of Cursor's still: a content match *does* get a second snippet line, so the 44 px row stands; filters and the chord remain the search owner's |
| F-177 | 09-17 | cycle-38 item (3), `cycle-38/pills/` | under-composer pills | Cursor's row above the composer in the parity chat: *Changes +14 −3 · Push · ↓* (a scroll-to-bottom disc when scrolled up); ours: *Agents · Changes +6 −1 · Commit & Push*. Cursor has no Agents pill (workers live in its Working pill); its button says *Push* because the cloud agent has committed already, ours *Commit & Push* because ours has not — both true to their kernels | design, noted; the ↓ scroll-to-bottom disc is the one thing ours lacks that a person meets (long transcripts) — for the transcript owner |
| F-178 | 09-18 | cycle-38 item (4), `cycle-38/reconnect/pill-states.png` | connection pill, recovery | the place's kernel killed under a live chat: *This Computer · reconnecting, try 1 in 1s* → *reconnecting, try 1…* → *This Computer* in 3.2 s, the composer's placeholder *reconnecting… → connecting… → Send follow-up*, no transcript line, the next line answered. `connect_fault` stays empty — a drop that recovers on the first try is not a fault. Cursor is equally silent on a brief drop. The fault states (no binary; a host that does not resolve) were paired in cycle 35 (F-157) | matched; nothing to change. What the rig cannot see: a real remote's tunnel drop (ArbosLife not touched — no install) |
| F-179 | 09-18 | gates `cycle-37`, `37b`, `38`, `38b` (4 of 8 full runs today; never phase T alone, never the probe) | Stop over a waited spawn | after P_LONG (a `wait=true` spawn that sleeps 45 s) Stop leaves the root and its worker busy for minutes: `recover` names them (*Spawn one sub-agent…* / *Sleeper*, *Sleep and report*) and the kernel's transcripts end in `turn_complete` only after the sleep — in the full runs the sessions stayed `turn_open` long after that. Whether the kernel never ended the turns or the window missed the end is unsettled: the two runs that could have shown it were reseeded before the files were read | **desktop half fixed** [#549](https://github.com/unarbos/arbos/pull/549); kernel half open (filed 2026-09-17-stop-waits-for-a-blocking-spawn). Run 11 of the probe (`cycle-39/stop-hold-fixed/run-11.log`) held for real and showed three window-side holds: the poll's tail probe read only `.arbos/agents/<id>` and a finished worker is archived — *not ended* forever; a tool card left Running by a turn the kernel ended over it kept the probe off; and the root, whose turn a worker's report re-opened, heard `Turn idle` once at the Stop and never again — 55 s Working past its own `turn_complete`. The probe now reads live then archive, runs for any chat with an open turn and nothing of this window's in flight, and fails the cards the end left running. Measured: sleeper-2 ended t+51.9 → `done` t+56.2; sleeper-3 archived ~t+61 → `done` t+70.1 (before: never through t+120). Root probe by reasoning from the same trace; the hold did not recur in 17 tries after |
| F-180 | 09-18 | F-174 re-drive on 1741 (`cycle-39/doubled-record/`) | chat history doubled at launch | on kernel `9bb49c6` about half the launches of a place with history doubled every chat: 167 cards → 266, every prompt and worker report twice, tool cards doubled on the next launch, and the saved record kept it. The kernel's tail cursors start at line one and its first tick broadcasts the whole record as live `event` frames; a window attaching during that tick takes them, and a record from before cards kept `seq` (F-135's mark primed at 0) read every line as news; nudges, tool records and says had no guard at all | [#560](https://github.com/unarbos/arbos/pull/560): the mark is primed at the handshake from the file's line count (and `history_end`'s `to`) for a pane with a record; every recorded line's events are bracketed (`RecordLine(seq) … RecordLineEnd`) and dropped when held. 8 of 8 launches clean after; worker tabs still fill live. Kernel half filed: `features-inbox/2026-09-18-tail-cursors-replay-the-whole-record-to-an-early-client.md`; **kernel half closed** [#563](https://github.com/unarbos/arbos/pull/563) (the tail cursors stand at each record's end at boot) — F-180 closed on both sides |
| F-181 | 09-18 | cycle-39 item (3) drive, `cycle-39/attachments/03-file-mid-send-crop.png` | status line | *Working jev* under the shimmer for 1–4 s on every ordinary turn: the engine's router sets its internal name as the agent's step (`jev.rs:292 kernel_step("jev")`); the kickoff turn hides it behind *Setting up environment* | kernel wording, filed `features-inbox/2026-09-18-jev-step-word-and-ask-mode-gate.md` — features' PR, not this loop's ; **kernel answered** [#569](https://github.com/unarbos/arbos/pull/569): the step says *Choosing the next step* |
| F-182 | 09-18 | cycle-39 item (4), `cycle-39/ask-mode/` | ask mode | `/mode ask`'s notice promises an allow/deny card for every writing call; `echo hi > hello.txt` through bash ran with no card (the bash gate asks only for wipe-class commands), and the coordinator's `write` is refused by its fence in every mode — so the ask card per mode has nothing to pair against Cursor's approval row | kernel, same inbox file — features' PR; the pairing waits on it ; **kernel answered** [#569](https://github.com/unarbos/arbos/pull/569): mode follows the tree — a worker in an ask-mode tree raises the card for its write; paired cycle 41 (`cycle-41/ask-mode/`): *write: hello.txt · Skip · Run ↵*, Cursor's row minus *Always Run* (no such answer in the ask protocol — noted for features) |
| F-183 | 09-18 | d17, `cycle-39/d17/03-win.png` | side panel, archived | the *22 archived* list opens at two indents: rows with a report line, then six rows indented a step further with no report (*Add Sources to Outline*, *Key Differences Doc*…) and no header saying what the step means | **fixed cycle 41** ([#636](https://github.com/unarbos/arbos/pull/636)): the second indent was `archived_only_row` — folders under `archive/agents/` with no chat in this window, drawn at `TREE_STEP * 2` with the title alone. Now one list: the same indent as the rows with a chat, and the last words read from the archived transcript (`cycle-41/archived/after-one-list.png`) |
| F-184 | 09-18 | cycle-40 panel pair, `cycle-40/panel/` vs `side-panel/cursor-sp-02-panel.png` | side panel, tab row | Cursor's row is *tabs · + 16 px after the last tab · controls at the right edge*, × on hover only; ours led with the `+` before the first tab and the front tab showed its × at rest | [#583](https://github.com/unarbos/arbos/pull/583): strip bounded, `+` after it, spacer before expand/close; × hover-only on every tab |
| R29 | 09-18 | gate `cycle-40a` | rig | two threshold false positives: `ask-skip follow-through` failed on *"I will choose 'alpha' as the default since no answer was provided"* — the bad-word list catches *no answer* though the model carried on; `weight-visible` read 0.19 % against a 0.2 % floor with a shorter chat on screen | [#640](https://github.com/unarbos/arbos/pull/640): the ask-skip row fails only when the gap is named and nothing says the model carried on; `weight-visible` left at 0.2 % |
| R30 | 09-18 | gate `cycle-41a`, the rebuilt box | rig | a box rebuilt from nothing off BUILDING.md's apt list built the desktop, then `notify-os-posted` failed: *could not start notify-send* — `libnotify-bin` (the app's own notifier) and `python3-pil` (the rig's compares) were not on the page | [#609](https://github.com/unarbos/arbos/pull/609): both named, with the one apt line for a rig box |
| R31 | 09-18 | gates `43a`, `44c`, `45a` (fork), `41b`–`45a` (kickoff), `41c`, `44c` (notify) | rig | on the rebuilt box under a full gate, rows whose effect the kernel delivers read *no state change* / *view absent* inside the rig's short windows and pass every time their phase runs alone: `fork-turn` (1.5 s settle), `new-project-kickoff` (2 s), the `notify-*` trio | [#678](https://github.com/unarbos/arbos/pull/678): `check(wait=)` re-reads until the assertion holds — fork 10 s, the kickoff row 12 s; the notify trio next |
| F-186 | 09-18 | cycle-41 tabs drive on the rebuilt box, `cycle-41/tabs/` | tab strip, Home | the Home tab read *.arbos* with a folder glyph, and every project tab came up the palette's first blue: the kernel's bootstrap writes `project.toml` with only `name = "<folder>"`, and the desktop read it as the project's face — the folder's own name over *Home*, `serde(default)` icon and colour over the house and the path-hashed colour | [#611](https://github.com/unarbos/arbos/pull/611): `Identity::load_for` keeps what the file names, fills the rest from the defaults, drops a name equal to the folder's |
| F-187 | 09-18 | cycle-41 ask-mode pair, `cycle-41/ask-mode/01-root-while-asking-win.png` | Project chat, worker line | while the worker waits on the person's allow, the root's line reads *1 Working · Creating hello.txt*; the panel says *waiting on create hello.txt*. The wait is for the user, not work | observation; the root line could say *Waiting for you* — not changed |
| F-188 | 09-18 | same pair | notices | the `/mode ask` notice drew as the one word *mode* with *Details ›*: `short_notice` cut the 160+-character *mode: ask — ask: every call that writes…* at its first colon | [#623](https://github.com/unarbos/arbos/pull/623): a *key: value — rule* notice keeps *key: value* on the line |
| F-189 | 09-18 | cycle-41 after-failure drive `keygone.py`, `cycle-41/key-gone/` | notices, key gone mid-session | the key goes bad in `config.toml` while the kernel runs: the kernel re-reads it per turn and refuses (*did not accept the API key*), the window drew *no reply from the kernel* above it — its own guess for a turn that ended on the prompt, a tail-tick before the reason | [#631](https://github.com/unarbos/arbos/pull/631): a failed notice arriving right after the guess takes its place |
| F-190 | 09-18 | cycle-41 f2 frame on main `dc633b7d`, `cycle-41/kickoff/f2-refused.png` | chat header | [#629](https://github.com/unarbos/arbos/pull/629) put a bare **Clear** word-button in the chat header beside the panel toggle; Cursor's header right side is *IDE ↗ · ⋯ · ⊟* — actions live behind the ⋯, never as a text button on the bar | **by design — Jacob's ask in #629**, not a defect; not to be paired against Cursor to undo it |
| F-191 | 09-18 | same frame | under-composer pills | #629 dropped the *This Computer* machine pill; only *master* remains. Cursor keeps *main · Cloud* under its composer — the machine is part of the row | **by design — Jacob's ask in #629**, not a defect; the machine pill stays gone |
| F-192 | 09-18 | cycle-42 voice pair, `cycle-42/voice/mic-hover-and-click.png` | composer, voice on Linux | clicking the mic disc on Linux writes *voice dictation is macOS-only for now* above the field in the **danger red**; it is information, not a failure — Cursor's *not available here* notes are muted text | observation for Jacob; `composer-voice-note` colour, not changed |
| F-193 | 09-18 | cycle-43 d18, `cycle-43/d18/02b-settled-win.png` | Project chat, turn headline | one turn read *Edited project-context.md ›* between neighbours reading *Worked 4s*: the prompt was typed as the workers' reports woke the root, and the wake turn's end stamped the newest opener — that prompt — with 0 s; its own end then found it stamped | [#652](https://github.com/unarbos/arbos/pull/652): the end stamps the oldest opener still without a time (last hour), never a steer; the driver dumps `worked_secs` |
| F-194 | 09-18 | cycle-44 Project page on the d18 place, `cycle-44/page/01-before.png` | Project page, panel Project section | lines the model pasted from the plan tool's listing — `[ ] 1 [Kickoff](docs/…) — ready…`, a box and an ordinal with no dash — drew raw as prose, link syntax and all, while the one dashed item drew as a chip row | [#661](https://github.com/unarbos/arbos/pull/661): a boxed line is an item, the ordinal stripped. The kernel's plan reader most likely drops the same lines — noted in the inbox, not filed |
| F-195 | 09-18 | cycle-44 kickoff f1, `cycle-44/kickoff/f1-empty-reply-nudge.png` | kickoff, nudges | *Your reply was empty. Continue the task, or say what is blocking you* drawn as an aside over the greeting — the kernel steering the model about its own output; Cursor's pane shows nothing of the kind | [#666](https://github.com/unarbos/arbos/pull/666): the event carries the kernel's reason; *empty reply*, *repeated reply*, *output owed* stay off the pane, the page and correction nudges stay |
| F-196 | 09-18 | gate `cycle-44b` phase D (*Directory not empty* on the rename back) | after-failure: folder moved — the ghost | the old path grew a whole bootstrapped store again. Timestamps: `.arbos/agents/root/` first (the desktop's transcript seed, which read the moved folder as one that stored nothing), then the attach canonicalized the remade path, bootstrapped, and spawned a kernel there 20 s later | [#672](https://github.com/unarbos/arbos/pull/672): the seed, the kernel spawn and the face file refuse a folder that is not a directory; phase D 0 ghosts in 4 after. F-165's remaining maker; desktop, as the coordinator said |
| F-197 | 09-18 | cycle-45 search pair, `cycle-45/search/` | search palette | the palette indexes a chat's title and first prompt only: *newcomer* and *branch*, words from later turns of the open chat, find nothing; Cursor's Search reads the transcript (its *Parity* row quotes a mid-chat line) | for the search owner, with F-106's filter kinds; the accent match taken in [#676](https://github.com/unarbos/arbos/pull/676) → desktop [#703](https://github.com/unarbos/arbos/pull/703) (cycle 50): ⌘K matches both sides' lines and shows the words around the match; rig `search-by-content`, `search-open-hit` |
| F-198 | 09-18 | cycle-45 settings pair, `cycle-45/settings/` vs `cursor-settings-interior-measured.md` | settings interior | ours on main today: column **left-aligned** at the rail's edge (x 200–872, 673 wide — the width matches Cursor's 669) at 1440 and at 1900, where Cursor centres the column in the area right of the rail; rows on the page colour with full-width hairlines, **no group surface** (Cursor: `#1C1C1C` on `#141414`, radius 6, hairlines 12 px short of the edges); row pitch **≈ 56 px** (246→302) where Cursor's is 60; captions *This app* / *Updates* at the same muted weight as Cursor's *Startup* | for the settings-tab owner who asked for the measurement (bc-6d9c3785…): centre the column, lift the groups, 60 px rows. Not changed here |
| F-199 | 09-18 | cycle-46 journey on main `00cc5ba8`, `cycle-46/journey/08-J08-continue-to-done.png` | Project chat, notices | *nothing to compact yet: the whole working set is recent* drawn as a dim line between two worker reports — the kernel's manual-compaction answer, written five times in the run after tool calls nobody asked to compact (kickoff included); Cursor's pane never carries a compaction status | [#686](https://github.com/unarbos/arbos/pull/686): shown only within a minute of a `/compact` this window typed, live and on replay. Kernel half (the manual path firing unasked) noted in the inbox |
| F-200 | 09-18 | cycle-46 kickoff f2 on main `00cc5ba8` | kickoff, refused key wording | the kernel's refusal now reads *Jev did not choose the next step: 401 bad API key: User not found.. The turn stopped. The chat model did not run.* — the router's name and a raw 401 in a person's line, and a double stop; the earlier kernel said *google/gemini-2.5-flash did not accept the API key. Check the API key (api_key or api_key_env in ~/.config/arbos/config.toml)*. The pane's short line — *No working model key on this kernel.* with Details — covers it | kernel wording, for features (inbox §5); the desktop line is fine |
| F-201 | 09-18 | cycle-48 `worker_badkey.py` on main `f7da9f06`, `cycle-48/keyless/` | after-failure, pending row | the model key removed *after* the handshake, then a prompt: the kernel keeps the line (*Your message is kept and runs once a key is in place*) and sends no `Provider` frame, so `provider_missing` stays unset — the pending row reads *1 follow-up queued · runs when this turn ends* over no turn, and the key-offer bar never shows; Cursor's queue strip says only what the message waits for | desktop #696: the kept-for-key refusal sets `provider_missing` from the handshake's provider (or the provider named in the line) — row *runs once a model key is in place*, offer bar on; `provider_missing` in the driver dump; rig `keyless-mid-session` |
| F-202 | 09-18 | cycle-48 d19 `d19_drive.py`, `fork_sub.py` on main `f7da9f06`; `cycle-48/d19/` | panel, chat titles | a chat opened with ⌘N is labelled *Delegate 1* in the panel, and a fork of it *Delegate 1 copy* — the label path for a nested chat falls to a number when the kernel id (`chat-<stamp>`) carries no brief, before the kernel's title or the first prompt; Cursor names a new chat from its content | desktop #696: a person-opened chat (`chat-…` id) takes its title or first prompt before *Delegate N*, and keeps its whole name (a title's colon is not a brief's); `label` in the driver dump; rig `subchat-title` |
| F-203 | 09-18 | cycle-48 `fork_sub.py`, `cycle-48/d19/arbos-panel-before-relaunch-after-fix.png` (before the fix the copy was crowned *main*) | panel, fork | a fork was ranked at the front of the roots, so `main_session()` — the lowest root — became the copy: the panel crowned *X copy* **main** and the real main chat read as a finished worker under it (*New chat — Hey — this place is ready…* with a check) | desktop #696: the copy is parented beside its source (under the source's parent, or under the source when it is a root) at the front of that row; rig `fork-beside-source` |
| F-204 | 09-18 | cycle-48 d19 relaunch leg; `pk` probe (record `parent: None` for a ⌘N chat) | panel, relaunch | a ⌘N chat is nested under the main chat in the window, but `resolve_parents` took the kernel's empty `parent:` as the word and emptied `parent_kernel` (F-137's rule), so the record filed no parent and a relaunch put every ⌘N chat beside the main one as a peer root | desktop #696: for a person-opened chat the kernel's silence says nothing against the nest; the filed parent stands. F-137's ghost (a parentless chat adopted under root by a panel click) is refused at its source since #438 (`adopt` returns none for a local record without a parent) |
| F-205 | 09-18 | cycle-48 `fork_steps2.py`: ⌫ on the emptied composer closed the chat (`closed=True`, active moved to the sibling) | composer, keys | ⌫ in an empty composer archives the current chat when the Chat pane shows — the panel need not be open, the row need not be seen. One key past the last letter of a draft and the chat is gone from the list; Cursor's composer never deletes a chat (its history rows do) | desktop #696: the highlighted row exists only while the panel is open; ⌫ with the panel shut is a no-op on an empty composer; rig `empty-backspace-keeps-chat` |
| F-206 | 09-18 | cycle-49, d19 relaunch leg on main `f7da9f06`; `cycle-49/relaunch-state/arbos-before-fork-hollow-ring.png` | panel, relaunch | after a relaunch one nested chat read *✓ — its answer* and its twin a hollow ring with no summary, both idle and answered: the probe that stamps a turn's end read the transcript's last record only, and the kernel writes after-lines past `turn_complete` (its *nothing to compact yet* answer, F-199; folds; nudges) — a file ending on one was never stamped and `child_state` stayed *Waiting* for the window's life | desktop [#703](https://github.com/unarbos/arbos/pull/703): `transcript_ended` reads back past notice/compaction/fold/nudge/image/window-reset records to the turn's body; a failed notice ends a turn too; rig `relaunch-ended-reads-done` |
| F-207 | 09-18 | cycle-49 d20 `d20_drive.py` on main `1a0b17aa`; `cycle-49/d20/` | panel, worker states | a worker parked on an `ask` card read **done** (check mark) in the panel while its question stood — `child_state` counted a plan question and plan ask-nodes but not the standing ask card (`questions`); a person in another chat had no word that a worker was waiting on them | desktop [#703](https://github.com/unarbos/arbos/pull/703): the ask card counts as *Asking*; the row's line is the question itself; rig `ask-row-state`. Kernel half filed: `internal/features-inbox/2026-09-18-spawn-returns-no-report-when-the-worker-parks-on-an-ask.md` (`spawn` told the root *the child's turn ended without a report*, and the root steered a worker that was waiting on the person) |
| F-208 | 09-18 | cycle-50 d21 `d21_drive.py`/`d21b.py` on main `1a0b17aa`; `cycle-50/d21/arbos-reopen-mid-worker-before.png` | Project chat, relaunch | the window closed and reopened while a worker ran: the root's pane drew the prompt with nothing under it for the whole run (25 s+ measured) — the kernel sends the root no frame while it waits in `spawn`, `busy()` was false, and the step read from `status.toml` at attach (*waiting on Sleepy — Running sleep 75*) drew nothing; the panel row knew (F-172) but the pane did not. Cursor's chat comes back mid-turn in its working state | desktop [#703](https://github.com/unarbos/arbos/pull/703): a step on file at attach opens the turn (the kernel clears the file at the turn's end; the transcript probe closes it again if the file says so) — the pane draws *1 Working · Running sleep 75* under the prompt (`…-after.png`); rig `relaunch-open-turn-drawn` |
| F-209 | 09-18 | cycle-50 d21, `cycle-50/d21/arbos-rewind-no-checkpoint-before.png` | prompt card, rewind | in a place with no `.git` the prompt card offered *Rewind here* and every click answered *Nothing to rewind to yet: checkpoints are written when a turn starts* — they are not: the kernel's `snapshot_turn_record_with_mark` returns none without `.git` (`tools/git.rs:159`); Cursor draws Restore checkpoint only where a checkpoint exists | desktop [#703](https://github.com/unarbos/arbos/pull/703): no rewind control where the local place has no `.git` (fork stays; remote places keep it); rig `no-rewind-off-repo` |
| F-210 | 09-18 | cycle-50 `steer_worker.py` on main `1a0b17aa`; `cycle-50/steer-worker/` | worker chat, steer | a line typed into a running worker's chat went as a plain prompt with no steer mark: `send()` steered only on a token or a tool row in this window (`streaming \|\| has_running_tool()`), and a worker's chat opened mid-run has neither — the kernel files a tool when it ends; the turn was open (`turn_open=True streaming=False` measured). The card then opened a turn of its own over the worker's instead of sitting inside it, as Cursor keeps a mid-turn line inside the turn. A line kept while the socket came up (`drain`) had the same fate | desktop [#703](https://github.com/unarbos/arbos/pull/703): an open turn is steered whatever has reached the window (the kernel takes a steer frame as a plain line when no turn runs); a kept line steers on drain and its card takes the mark; `steer` in the driver's card dump; rig `worker-steer-card` |
| F-211 | 09-19 | cycle-50 ask card on a worker (d20's still), `cycle-50/ask-worker/arbos-worker-ask-card-kernel-parrot.png` | worker chat, prose | the model repeated the kernel's aside in its own reply — *[kernel] The user provided the following answer to your question: red*, twice, as a paragraph of the answer (the engine speaks to the model in `[kernel] …` lines: an ask's answer, a nudge); Cursor never shows a machine's aside in a person's paragraph | desktop [#703](https://github.com/unarbos/arbos/pull/703): a line starting `[kernel]` is cut from agent prose, live and on replay (`markup::tidy`, the cut every settled and streamed line already passes) |
| F-212 | 09-19 | cycle-51 d22 `d22_drive.py` on main `1a0b17aa`; `cycle-51/d22/` | after-failure, notices | the kernel process killed under a running turn: the pane wrote *Stopped.* — a person's act — and folded it under *Worked 11s* with nothing in view; the reconnect (0.6 s, the kernel respawned and resumed — *I restarted while that worker was running*) left no word of what had happened. Cursor's connection error line stands and names the failure | desktop [#703](https://github.com/unarbos/arbos/pull/703): the socket closing under a running turn writes the failed line *The kernel went away mid-turn* (both the Closed event and the pump's end), and a failed line stays in view under a shut fold as steers do; rig `kernel-killed-mid-turn` |
| F-213 | 09-19 | cycle-51 themes re-read, `cycle-51/themes/arbos-dark-tint-max-no-change.png` | settings, themes | Settings › Appearance offers a *Tint* (oklch hue and chroma on the greys), but in the dark theme the palette pins Cursor's greys as fixed values (`view/palette.rs` `cursor_dark`, since cycle 2): at the slider's ceiling (hue 250, chroma 0.046) every surface still reads #161514 — the control does nothing in dark; light still takes it. Cursor has no tint. Dark page #161514 vs Cursor's #141414, unchanged since F-163 | for the settings/theme owner: either the pinned greys take the brand's tint or the rows hide in dark with a word; not paired away here |
| F-214 | 09-19 | cycle-52 Project page notes render, `cycle-52/page/arbos-table-before.png` | project page | a pipe table in `notes.md` drew as lines of pipes — every row raw, the `\| --- \| --- \|` separator too — under its heading; the page parser knew headings, items and prose only. Cursor draws a markdown table as a table (F-163's pair) | desktop [#703](https://github.com/unarbos/arbos/pull/703): `PageBlock::Table` — header dim over a hairline, rows in columns weighted by their longest cell, the separator row dropped (`…-after.png`); rig `page-table` |
| F-215 | 09-19 | cycle-53 classic worker chat / three workers, `cycle-53/three/arbos-worker-b-ran-1-command.png`, `arbos-exit-mark.png` | command card, run line | a command that exited 2 read *ran 1 command* and its card showed `exit 2` as the last line of output, under the kernel's own note to the model (*Reproduction 1 recorded (exit 2). changes re-runs it…*): the kernel files no `error` for a shell call whose command failed, only the exit line. Cursor's card carries the exit code as a mark | desktop [#703](https://github.com/unarbos/arbos/pull/703): the card's header carries *exit N* in danger when N≠0; the `exit <n>` line and the kernel's note are no longer lines of output; the run line stays *ran 1 command* as Cursor's does; rig `command-exit-mark` |
| F-216 | 09-19 | cycle-55 d26 `d26_drive.py`, `cycle-55/d26/` | Project chat, worker lines | a worker parked on an `ask` showed on the root's pane as *Asking greet with choice* — the worker's name and no more; the person in the coordinator's seat had to open the worker to learn what was asked (the OS toast carried the question, the pane did not). Cursor surfaces a subagent's question in the parent chat | desktop [#703](https://github.com/unarbos/arbos/pull/703): the line reads *Asking Greeter — Which greeting would you like to use?* (`ChildSummary.question`), and opens the worker on the press as before |
| F-217 | 09-19 | cycle-56 files grid re-read, `cycle-56/reread/arbos-files-and-standing-before.png` | panel, Files | `.json`, `.csv`, `.py` under `docs/` wore the open-folder glyph beside the documents' file glyph — `FileKind::Other` mapped to `FOLDER_WITH_FILES`; a file read as a folder. Cursor's list gives every file a file glyph | desktop [#703](https://github.com/unarbos/arbos/pull/703): the document glyph for every non-image file (the set has no code glyph) |
| F-218 | 09-19 | cycle-56 d26 re-read, `cycle-56/reread/arbos-opener-no-hosts-and-done-heading-before.png` | Project chat, done line | a worker's report that opened with a markdown heading put the marks on the root's done line: *Greeter done — ### Outcome*. Cursor never shows heading hashes in a summary line | desktop [#703](https://github.com/unarbos/arbos/pull/703): `done_report` drops the leading heading's hashes, keeping its words — *Greeter done — Outcome* (`…-after.png`) |
| F-219 | 09-19 | cycle-56 typography stepper re-read, `cycle-56/reread/arbos-stepper-card-before.png` | typography, command card, worker lines | ⌘= twice scaled the prose, but the command card's mono text, its exit mark and the worker lines kept their fixed pixel sizes (`MONO_SIZE` 11.5, `CURSOR_PROSE_SIZE` 13 as `px`); Cursor's zoom moves the whole chat | desktop [#703](https://github.com/unarbos/arbos/pull/703): `prose_size()`, `mono_size()`, `mono_lead()` follow `base_text_size()`; the exit mark measured 17 → 20 px from 13 to 15 pt (`…-after.png`) |
| F-220 | 09-19 | cycle-58 p6 (a worker on a 34 s `for … sleep 1` loop), `cycle-58/p6/before-30s-no-card.png` | command card, live turn | the kernel emits a `bash` call twice — once as it starts (no `ended`, no body) and once when it returns — and the desktop read both as *completed*: for the whole run the pane held a green, empty card folded under *Running 1 command*, the headline alone saying *Running bash command*; Cursor draws the terminal card at once and the output moves inside it | desktop [#756](https://github.com/unarbos/arbos/pull/756): the call-start event is a running card (`InProgress`); the kernel's job stream, which already carries every attached command's output, fills its 8-line tail (`job_streamed`, matched by the command in the job's `meta.json`); a running command card stays out of the shut run fold; the headline names the command from the first second (`…/after-30s-live-card.png`). Rig `command-live-card` |
| F-221 | 09-19 | cycle-58 p6, `cycle-58/p6/after-30s-live-card.png` (the card under the command) | ask card | a worker's *What bash command should I run?* card — Skip, Continue, composer *Add more optional details* — stood on after the root's `say` answered it and the worker was running the command; and came back at every relaunch, because the kernel keeps the `waiting/ask-…toml` and replays it at attach | desktop #756: a question goes when a new turn starts over it (the heartbeat), when another agent's line lands, and is not offered when its `ask` record is followed by a `From`/`User`/`Wake` (`question_superseded`, at the frame and after the tail is adopted). Kernel half filed: `features-inbox/2026-09-19-a-parked-ask-outlives-the-say-that-woke-the-agent.md` |
| F-222 | 09-19 | cycle-58 long-form d28 (three files over two turns), `cycle-58/d28/t2-review-before.png` | Files Changed › Review | Review wrote the tree's unified diff to `.arbos/desktop/review/changes.diff` and opened it as a text file: `diff --git`, `index`, `@@` lines in one colour, headed by the scratch path and *unsaved*; Cursor's Review is a diff — per file, signs and washes | desktop #756: a `diff` presentation — one card per file (leaf, path when it adds, `+N −M`), the edit card's rows (`review_view`), read-only (no editor is minted for it); `.diff`/`.patch` files open the same way (`…/t2-review-after.png`) |
| F-223 | 09-19 | cycle-58 d28 relaunch, `cycle-58/d28/kickoff-fold-after-relaunch-before.png` | kickoff fold | the kickoff turn's fold read *Worked 7s ›* until the relaunch and *Edited project-context.md, updated the project page ›* after it: `kickoff_secs` was runtime-only, and a turn with no prompt to stamp had no time | desktop #756: `kickoff_secs` on the record; *Worked 9s ›* after a relaunch (`cycle-58/cold-f2/relaunch.png`) |
| F-224 | 09-19 | cycle-58, a worker chat opened from ⌘K (`cycle-58/s50/worker-opened-before.png`) | notices, nudges | *google/gemini-2.5-flash returned nothing twice, so gemini-3.8-flash answers this turn* and *Your reply was empty…* alternating sixteen times down one turn — the kernel's loop (filed 09-19), drawn in full | desktop #756: over a turn's tail the same notice or nudge is one line with a count (*· ×16*), `…-after.png`. The loop itself stays the kernel's |
| F-225 | 09-19 | cycle-59 window sizes, `cycle-59/small/02-900x600-review.png` | Files Changed › Review at 900×600 | under `PANEL_MIN_WINDOW` (1000) the panel folds (F-23, paired), and Review — a panel tab — then does nothing the person can see: the click lands, the tab is added, the chat stays. Cursor drops its right panel at that width too; what its Review does there the rig cannot see | for the panel owner: a person's own open (Review, a file row) at a folded width needs a visible answer — the panel over the column, or a line saying to widen. Not built here; #629's rule (nothing takes the column on its own) stands |
| F-226 | 09-19 | cycle-59 long-form d29 (compaction with `window_tokens` pinned), `cycle-59/d29/t4-recall-before.png` | compaction notices | *compacting 2 turn(s), ~32k tokens…* stayed on the pane after *compacted 2 turn(s): ~31k → ~21k tokens…* landed — the progress line and its result, two rows; Cursor marks a compaction once, after the fact | desktop #756: the *compacted* notice takes the place of the *compacting* line (`cycle-59/d29/t3-read-b-after.png`) |
| F-227 | 09-19 | cycle-61 update plate on the dev channel (`ARBOS_UPDATE_CHANNEL=dev`, a tree copy in `/tmp/upd-bin`), `cycle-61/update/offer-click-failed-before.png` | bottom bar, update plate | the dev feed offered 0.2.0 (2283): a blue *⭳ Update* whose click went red at once — *Update failed*, tooltip *this copy of Arbos was not installed from a release — its kernel is not beside it*. The bar's own rule (a build that cannot update itself says so rather than offering a button that fails at the last step) held for the quiet state and not for the offer | desktop #756: with `can_install()` set the offer is a muted *0.2.0 (2283) is out* that does nothing on a click, the reason under the notes in its tooltip (`…/is-out-plate-after.png`). Cursor's updater has no such copy to speak of — by principle |
| F-228 | 09-19 | cycle-62 light re-read on a worker's card, `cycle-62/review/worker-review-tree-diff-before.png` | worker chat, Files Changed › Review | the worker's card listed its own file (*c.txt +2*, F-111) while its Review opened `git diff HEAD` for the whole place — two other files, and nothing of c.txt, which the worker had already committed; Cursor's worker tab reviews that worker's changes | desktop #756: a worker's Review draws its own edit records — one card per file, its edits as hunks (`worker_unified_diff`; `…-after.png`). The tree's diff stays the root's |
| F-229 | 09-19 | cycle-62 long-form d32 (a two-level worker tree), `cycle-62/notes/waiting-on-outer-stale-and-raw-table-before.png` | panel, root row | the root's row read *· waiting on Outer* over *2 archived* after Outer had reported and the root had answered: the kernel's waiting line is cleared when the wait ends, not when the worker it named is archived on its way out | desktop #756: the line goes at the root's turn end and whenever no worker is working (`cycle-62/d32/root-row-after.png`). Kernel half not filed: the desktop's rule covers it |
| F-230 | 09-19 | cycle-63 long-form d33 (twelve-item list), `cycle-63/d33/two-digit-markers-wrap-before.png` | markdown render, ordered lists | the marker column was 14 px: *10.* wrapped to *1* over *0.* and every item past nine took two lines; Cursor's list markers sit in one column wide enough for the list | desktop #756 (the vendored bezel-markdown patch): the column grows 7 px per extra digit and never wraps (`…-after.png`); items 1–9 and 10+ differ by that width — Cursor aligns them all on the widest, which would need the run's size at parse |
| F-231 | 09-19 | cycle-63 after-failure (a place renamed while the window was shut), `cycle-63/gone/tab-dropped-silently.png` | launch, tabs | `state.rs::restore` drops a local folder that is not a dir — the renamed project's tab vanished at launch with no word, and the next line typed went to Home; the same rename under a running window draws *This project's folder is gone or was moved: expected … · Details · Retry* and keeps the typed line (F-178). Cursor reopens the window and says the folder is missing | for the launch owner: open the tab on its saved path and let the place-gone path speak, or say once on Home which tabs were not reopened and why. Logged, not built: the drop is a documented rule (*a tab no agent can spawn in*) written before the gone-place path existed |
| F-232 | 09-19 | cycle-64 long-form d34 (a dead key under a running turn, then the key back), `cycle-64/d34/retry-duplicated-prompt-before.png` | refused turn, Retry | the refusal read *No working model key on this kernel · Details › · Retry* (cycle 53's pair); Retry then drew the prompt card a second time under the first and answered under the copy — and a relaunch brought the copy back from the kernel's record. Cursor's *Try again* re-runs under the same message | desktop #756: Retry re-opens the turn under the card already on the pane (the failure line goes; `prepare_retry` + the held-card path), and the kernel's *prompt, failure, prompt* record folds to one turn at load (`fold_retries`; `…-after.png`, `relaunch-folded-after.png`) |
| F-233 | 09-19 | cycle-65 jump-to-end read (a forty-item list streaming, the pane scrolled up), `cycle-65/jump/panic-backtrace-before.log` | transcript, jump-to-end disc | a click on the disc while the answer streamed closed the app: `request_animation_frame` in the click handler — gpui allows it only from layout or paint (`Window::current_view` unwrapped None in release; the debug assert names it). The gate's `jump-to-end` row clicks on a settled pane and never saw it | desktop #756: the click sets the offset, pins the follow and `cx.notify()`s — the repaint is that. Driven: the disc shows over the growing list (`scrolled-up-streaming-disc.png`), the click lands at the end (`after-jump.png`), the app stays up |
| F-235 | 09-19 | cycle-67 opener with a seeded `~/.ssh/config` host, `cycle-67/opener/machine-rows-folder-before.png` | opener, machine stage | *This machine · fakebox · Browse folders…* — the host's row wore the same folder glyph as the local rows; the remote glyph (`remote.svg`, `REMOTE_ICON`) was in the assets and drawn nowhere. Cursor's remote picker marks a remote host apart from local folders | desktop #756: a machine row with a host draws the remote glyph (`…-after.png`); local rows keep the folder |
| F-236 | 09-19 | cycle-68 long-form d38 (three reads and an edit in one turn), `cycle-68/d38/run-split-by-nudge-before.png` | run fold | the kernel's *Your reply was empty…* nudge landed between the second and third read, and the fold read *Explored 2 files* over *Explored c.py* — the nudge is not a row inside the body, yet it cut the run in two; Cursor's is one *Explored 3 files* | desktop #756: a nudge rides inside a run (`segments`); a run needs a tool to be one (`…/one-run-after.png`) |
| F-237 | 09-19 | cycle-69 Files section into the editor, `cycle-69/files/cmd-w-closed-project-before.png` | drawer, ⌘W | a file opened from the panel's Files section and closed by ⌘W took the whole project tab with it — `cmd-w` was `CloseProject` whatever had the focus; Cursor's ⌘W closes the editor tab in front | desktop #756: with the drawer focused on a file or surface tab, ⌘W closes that tab; on the Project tab, or with the focus elsewhere, it is the project's as before (`…-after.png`) |
| F-238 | 09-19 | cycle-69 long-form d39 (Stop All on a worker mid-command), `cycle-69/d39/after-stop-all-done-line.png` | worker line, Stop All | the worker's transcript ends `bash interrupted; job j1 killed · interrupted stop · turn_complete`, and the root's line under its answer read *Done · Sleepy worker*; the kernel's `spawn` returned *(the child's turn ended without a report)* and the model wrote *The worker finished.* Cursor's stopped subagent reads stopped | desktop #756 (cycle 70): `ChildState::Stopped` — the root's line reads *Stopped · <worker>*, the panel row a stop square with *stopped by you*, the driver `stopped` (`cycle-70/d39/stopped-line-after.png`); kernel half filed: `features-inbox/2026-09-19-a-stopped-worker-returns-to-spawn-as-ended-without-a-report.md` |
| F-239 | 09-19 | cycle-71 after-failure (the kernel's file replaced under an idle chat), `cycle-71/kbin/` | reconnect, notices | the kernel saw its file go at once (`binary_gone`), re-exec'd onto the new build and wrote *The kernel's program file was replaced under it (an update); restarting onto the new build now…* to the transcript; the window drew no plate (F-125's rule for an idle kernel — by design) and **no line live** (the tail read on the reconnect did not take it), yet the line was there after a relaunch. Cursor has no such moment | desktop #756 (cycle 72): the reconnect's tail read takes the kernel's trailing notices — the swap line shows live (`cycle-72/kbin/`) |
| F-240 | 09-19 | cycle-71 tabs (close, reopen), `cycle-71/opener/no-recents-before.png` | opener | a closed tab could be reopened only by typing its path again: the opener's machine stage offered *This machine · Browse folders…* and used the recents for remote hosts alone; Cursor's opener leads with recent workspaces | desktop #756: the recent local folders (five) as *Open <name>* rows with their paths under *This machine*, filtered by the query, ↵ opens (`…/recents-rows-after.png`) |
| F-241 | 09-19 | cycle-72 window sizes (900×600, a long draft), `cycle-72/sizes/900x600-long-draft-wraps-at-chip.png` | composer | a five-line draft wraps at the model chip's left edge — the text column shares its row with the chip, the handset and the send disc, so at 900 wide two fifths of the composer are empty beside the words; Cursor's text spans the composer and its chips sit on a row under it | for the composer owner: past one line, the controls move to a row under the text. Logged, not built |
| F-242 | 09-19 | cycle-73 long-form d40 (a worker that says mid-task and then reports), `cycle-73/d40/done-name-only-before.png` | root's worker line | a worker awaited by `spawn` (wait=true) reports inside the call's return — *test-worker reports: …* — not as a `from` line, so the root's *Done · Test worker* row carried no words while its mid-task `say` did; Cursor's finished subagent shows its last words | desktop #756: the Done row takes the spawn return's first line — *Done · Test worker — halfway there* (`…-after.png`) |
| F-243 | 09-19 | cycle-74 thumbs-down sheet, `cycle-74/sheet/tool-io-clipped-before.png` | report sheet | the sentence beside *Sending tool arguments and output* — *1 call carry what they were given and a glance at what came back — including file contents* — ran off the sheet's right edge, cut mid-word: its flex cell had no `min_w_0` to wrap in | desktop #756: `min_w_0` on the cell; two lines inside the sheet (`…-after.png`) |
| F-244 | 09-19 | cycle-75 fresh Home kickoff, live then relaunched, `cycle-75/kickoff-fold/relaunch-before-3calls-over-1call.png` | kickoff fold | after a relaunch the kickoff turn was read as an ordinary one — the window knew it only by the ask it had made (`kickoff_at`, runtime-only) — so its first call stood as the turn's opener: the fold covered the calls after it (*Edited project-context.md, updated the project page* for a three-call kickoff whose live line read *…, ran 1 command, …*), a one-call kickoff had no *Worked 4s* fold at all, and the *Today 9:38 PM* line over the turn went too | desktop #756: the ask time on the record beside its seconds; live and relaunched read the same (`cycle-75/kickoff-fold/live-over-relaunch-after.png`); rig row `kickoff-fold-after-relaunch` |
| F-245 | 09-19 | cycle-75 Stop over a `spawn wait=true` worker, `cycle-75/stop/02-after-stop-line-gone-before.png` | worker lines, Stop | the person's Stop cut the spawn before the worker's id came back, so no turn named the worker: the root's *1 Working · Running sleep and echo* line went with the stop and nothing stood in its place — the worker lived on only in the panel and the *Agents* pill, while Cursor keeps the sub-agent row after a Stop | desktop #756: the newest turn whose spawn never returned takes the unnamed workers — *Stopped · <worker>* under *Stopped by you 22s*, live and after a relaunch (`cycle-75/stop/05-stopped-line-before-after-relaunch.png`) |
| F-246 | 09-19 | cycle-76 rewind on turn 0, `cycle-76/rewind-fork/kickoff-rewind-offered-before.png` | turn footer, rewind | the kickoff turn's footer offered *Rewind here: chat and files back to before this prompt* — a turn with no prompt; the press returned before the kernel was asked and said nothing (`rewind` finds no `User` at or before the turn and returns) | desktop #756: no Rewind on a turn with no prompt at or before it; copy, fork and the votes stay (`kickoff-footer-after.png`) |
| F-247 | 09-19 | cycle-76 remote place on a host that does not resolve, `cycle-76/remote/unreachable-greets-ready-before.png` | kickoff view, remote places | the pane read *remote76-place is ready. Drag in files, or just tell me what you want to build…* over the composer's own line *checking the machine failed: nohost76 is not a name this machine can resolve — retry in 7s* — the greeting fallback for a kernel that never answers (F-77's words) fired for a machine never reached; F-77's lie in a new place | desktop #756: with a connect fault and no live connection the pane above the composer says nothing (`kickoff-unreachable`); the head stays, the line under the composer speaks (`unreachable-after.png`) |
| F-248 | 09-19 | cycle-77 window sizes, 900×600 with ⌘B, `cycle-77/sizes/900x600-hide-panel-tooltip-over-no-panel.png` | panel toggle, window sizes | under the panel's minimum width (1000) ⌘B flips `panel_open` to true while `panel_shown` stays false — nothing opens — and the toggle's tooltip then reads *Hide panel ⌘B* over a pane with no panel on it; a second press says *Show panel* and shows nothing either | for the panel owner (#629's rule holds the fold): the tooltip should say why (*Panel needs a wider window*) or the panel should open over the chat at this width; not built here |
| F-249 | 09-19 | cycle-77 long-form d43 (root asked to archive its live worker), `cycle-77/d43/root-after-archive-ended-badly-before.png` | worker lines, verdicts | the root stopped its own worker as asked; the kernel's done file opened *Turn ended badly. Last words: stopped: stopped by root: Stop* (its `turn_outcome` counts any stop but the person's as not ok) and the desktop drew *Long running command ended badly — stopped: stopped by root: Stop* in the danger colour — a stop the person asked for, read as a failure | desktop #756: a report whose words open *stopped:* is `Verdict::Halted` — *Long running command stopped — by root: Stop*, muted (`halted-line-before-after.png`); kernel half filed (the verdict's own prefix) |
| F-250 | 09-20 | cycle-78 remote place on a local sshd, `cycle-78/remote/did-not-start-home-slash-slash-before.png` | opener, remote places | the opener's folder stage opens with `~/` in the field; an absolute path typed after it became the place `localbox78:~//tmp/remote78-place`, the kernel's launch ran `cd /home/ubuntu//tmp/remote78-place` (*can't cd*, `~/.cache/arbos/web.log`) and two minutes of *Setting up environment* ended in *arbos did not start within 60 s* | desktop #756: `~//x` typed reads as `/x`; the place is `localbox78b:/tmp/remote78b-place` and the retry after the host resolves comes up live in one attempt (`recovered-live-keyless-bar-after.png`) |
| F-251 | 09-20 | cycle-78 long-form d44 (a worker that asks mid-way, `wait=false`), `cycle-78/d44/asking-worker-read-done-no-reply-before.png` | worker lines, ask | the worker parked on its `ask`; the kernel filed a done report at the park (*Turn ended. Last words: (no reply)*, filed 09-19) and the root's flow read *⚙ worker C done — (no reply)* with no *Asking* row — `child_state` counted a parked ask as Asking only through `answering` or a plan ask, never the parked card itself | desktop #756: a parked `questions` is `ChildState::Asking` before anything else; a done report from a worker asking now yields to the *Asking · B* row — live and after a relaunch (`asking-line-live-over-relaunch-after.png`) |
| F-252 | 09-20 | cycle-79 relaunch mid-command on the root, `cycle-79/relaunch/mid-command-relaunch-before-left-after-right.png` | relaunch, live headline, Stop | quit while the root's own `bash` ran (a 60 s loop) and relaunch: the pane read the prompt and nothing under it, the mic where Stop should be, `turn_open` false — while the kernel's `status.toml`, read at attach, said *Running for i in …*; the kernel files a call's record when the call ends, so no running card comes back at attach, and the pane took idle | desktop #756: the kernel's step over an unanswered prompt on a live socket reopens the turn (`reopen_turn_on_step`, at attach and on a status frame) — *Working · Running for i in…* and the Stop square; the turn settles with the tool row and the answer when the command ends. The running card itself needs the kernel to replay the open call (not filed anew: F-220's card comes from the call-start event, which attach does not repeat) |
| F-253 | 09-20 | cycle-79 rewind on a place with no checkpoints, `cycle-79/rewind/rewind-refused-with-retry-before.png` | notices, rewind | the kernel's refusal *rewind: root has no checkpoints yet…* drew as a failed notice with **Retry** beside *Details ›*; Retry re-sends the last prompt (F-232's path), which has nothing to do with the rewind — a press would have run the turn again | desktop #756: a refused control (a `rewind:` line, *stop the turn before rewinding*, *could not fork*) keeps Details and drops Retry (`rewind-refused-details-only-after.png`). The rewind of turn 2 beside a worker itself was not reached: the place had no checkpoints (no git) — carried to a git place |
| F-254 | 09-20 | cycle-79 long-form d45 (read five docs, write an index), `cycle-79/d45/run-rows-gap-before-left-after-right.png` | run fold rows | the open run's rows — *Ran Find*, five *Read x.md L1-3* — had a hole between *glossary* and *manifests*: the kernel's empty-reply nudge filed between two reads (F-236 made it transparent for the fold) still drew an empty element in the gapped column, and so did a `status` call and the root's kernel calls | desktop #756: the column takes only the items that are rows; the pitch is 29 px throughout |
| F-255 | 09-20 | cycle-80 long-form d46 (fork the d45 chat), `cycle-80/d46/fork-kickoff-fold-before-over-after.png` | fork, kickoff fold | the fork's first turn read *Edited project-context.md, updated the project page ›* where the original reads *Worked 10s ›*, and no day line over it: the clone's record is new, with no kickoff stamps, so F-244's rule read the turn as ordinary and the first call as its opener | desktop #756: a fork inherits the original's `kickoff_secs` and `kickoff_at` — *Today 1:27 AM · Worked 10s ›* on the copy |
| F-256 | 09-20 | cycle-81 compaction live (`window_tokens = 9000`, four turns of `seq 1 400`), `cycle-81/compaction/compacted-line-before-over-after.png` | notices, compaction | the result line drew raw — *compacted 1 turn(s): ~15k → ~14k tokens (google/gemini-2.5-flash); full record in .arbos/agents/root/transcript.jsonl:1–11* — two lines of model id and file pointer where Cursor's says *Summarized N messages*; F-226's pair itself held live: *compacting…* at +19.9 s gave way to the result at +22.4 s | desktop #756: *Compacted 1 turn, ~15k → ~14k tokens.* with the rest behind *Details ›* |
| F-257 | 09-20 | cycle-81 long-form d47 (three root chats, two archived workers), `cycle-81/d47/panel-one-row-live-after-plan-check.png` | panel, Agents list | after a turn whose calls were `plan add`, `plan check` (the kernel archived the checked item's worker) and `agents`, the panel's Agents section showed one row — the chat that ran the turn — where a sibling root chat, its worker and the *2 archived* fold had stood; a relaunch drew all of them again (`panel-rows-back-after-relaunch.png`). Not reproduced with an `agents` call alone; the model's answer read *no other chats* from the tool's *0 agents* — the kernel's `agents` tool lists neither sibling chats nor archived workers | open — for the panel owner; the live hide follows the kernel's archive of a worker whose parent is a sibling root; the `agents` tool's empty list is the kernel's |
| F-258 | 09-20 | cycle-82 a worker's ask skipped, `cycle-82/worker/skip-bubble-before-left-after-right.png` | worker chat, ask card | Skip on the worker's card: the kernel delivers *The user skipped this question without answering. Choose a sensible default yourself…* to the agent as a user message, and it drew as a bubble of the person's — words the person never typed — under the folded *Question · … · skipped* row, opening a second *Worked for 3s* turn | desktop #756: the skip's line is an ask echo (drawn nowhere), and the *Question · … · skipped* row stays in view under the shut fold as Cursor's collapsed card does |
| F-259 | 09-20 | cycle-84 Try Live on a remote with no display (the local sshd), `cycle-84/remote/try-live-scrot-stderr-before.png` | live strip, remote | the strip under *Live · cursor · no frame* carried the capture tool's stderr whole — *scrot failed: exit status: 1: scrot: Can't open X display. It \*is\* running, yeah? [NULL] (no DISPLAY or WAYLAND_DISPLAY in this kernel's environment: no screen to capture)* | desktop #756: *No screen to capture: the kernel has no display where it runs.*; any other capture error keeps its closing parenthesis as the line |
| F-260 | 09-20 | cycle-84 the keyless bar's *This session only* on the local sshd, `cycle-84/remote/key-lent-two-lines-before.png` | notices, remote key | one press wrote two lines: this window's *openrouter key in place on this kernel (memory)* from the provider frame and the kernel's *Model key set for openrouter (google/gemini-2.5-flash): kept in memory for this kernel only* | desktop #756: one line — the window's; the kernel's twin is dropped whichever arrives first (`key-lent-one-line-after.png`) |
| F-261 | 09-20 | cycle-84 paste into the composer, `cycle-84/paste/` | composer attachments, paste | the composer has no image paste: `⌘V`/`ctrl-v` with a PNG on the clipboard adds nothing (no `PasteAttachment` path; the field's own `input::Paste` takes words only), where Cursor's composer takes a pasted screenshot as an image attachment | **open — unverifiable on the rig**: neither the driver's keystroke nor a real X `ctrl+v` (xdotool) pastes even *text* into the field on this box (`''` after both, old build and new), so a fix written here could not be measured and was not kept; gpui's `ClipboardEntry::Image` exists on every platform — the change is a `PasteAttachment` action over the field's paste that writes the bytes to a temp file and calls `add_paths`, falling back to `input::Paste`; for the composer owner, to verify on the Mac |
| F-262 | 09-20 | cycle-85 the Project tab's page item and Files rows, `cycle-85/panel/three-files-unsaved-before.png` | panel, file tab | every file opened from the panel — the Kickoff page item's `project-context.md`, `plain.md` (no front matter), `archived.md` — read *<path> · unsaved* in its head before any keystroke: the dirty test compared the editor's text with the file's bytes, and the editor normalises what it loads | desktop #756: *saved* is the editor's own reading of the file at open; the head reads the path alone until an edit (`plain-file-clean-after.png`). The edit half is the same comparison with a different baseline; the driver cannot type into the file editor to show it |
| F-185 | 09-18 | cycle-40 kickoff f2, `cycle-40/kickoff-f2/` | kickoff, refused provider | the refusal line (*did not accept the API key…*) drawn twice with two *Details ›*, one notice on record: the zone drew the notice as the turn's opener and the kickoff body, which starts at the turn's first item, drew it again | [#591](https://github.com/unarbos/arbos/pull/591): the body starts after whatever the zone drew as the opener |
| F-141 | 09-17 | loop (same pair, t2) | composer placeholder | for the first two seconds — before the kickoff turn opens — our composer reads *What are you working on?* with the mic, then flips to *Send follow-up* with the stop disc; Cursor reads *Send follow-up* with ■ from the first frame, since a kickoff is a turn from the moment the project exists | open — small: the placeholder should read *Send follow-up* from the moment a kickoff is wanted, not from its first frame |

## Per-cycle count

| cycle | loop findings | Jacob findings |
| --- | --- | --- |
| 8 | 1 | 1 (Working card, from the pair) |
| 9 | 2 | 0 |
| 10 | 2 | 10 (F-01 … F-10, from the fresh Mac install) |
| 11 | 17 (F-16 … F-20, F-28 … F-39) + 7 from the Mac worker (F-21 … F-27) | 0 |
| 12 | 5 (F-40 … F-44); Mac leg pending | 0 |
| 13 (opened) | 2 so far (F-45, F-46) | 0 |
| 14 | 10 (F-56 … F-65) + 3 around the Inter change (F-53 … F-55) | 1 (F-52, bionic weight) |
| 15 | 5 (F-66 … F-70) | 0 |
| 16 | 9 (F-71 … F-75, F-78 … F-81) | 2 (F-76 raw provider error, F-77 kickoff stall) |
| 17 (opened) | 17 so far (F-83, F-84, F-92, F-93, F-99 from QA; F-85 … F-91, F-94, F-96 from seven journey runs; F-95 from the gate; F-97, F-98 from the probes) | 1 (F-82, the bubble-sort turn) |
| 18 – 22 | F-100 … F-112 (feedback sheet F-100/F-101, hub kinds, F-103 … F-112 from journeys and gates) | 1 (F-102 Jacob's Mac, the frosted popover) |
| 34 (running) | Jacob's seventeen reports of 10:00–11:51: F-142 … F-147, F-150 (fixed), F-148 (kernel, filed), F-149 (open), four routed to the side-panel design, -25 held for his call; F-151 selection, F-152 opener start; F-137 and the status rule landed here from cycle 33 | 0 |
| 33 | F-134 (af-03, fixed), F-135 (open), F-136 (fixed; F-125 closed); Jacob's five reports: F-137 (closed from the capture), F-138, F-139 (fixed), two kernel asks and one design question filed; cold kickoff pair F-140 (design), F-141 (open) | 0 |
| 32 | F-129 (design, noted), F-130, F-131, F-132, F-133 (fixed); rig R3 (settings rows), R17 (`send` reads back) | 0 |
| 31 | 0 app findings; rig: R13 withdrawn, R14 (eight assertions that could not fail), R15, R16; R10 closed — the fold row passes | 0 |
| 30 | 0 (F-128 noted, model-side); ArbosLife on `main`'s kernel | 0 |
| 29 | 0 new; F-122b traced and fixed; R11, R12 | 0 |
| 28 | 1 (F-127); F-122 recurred once; R11 the stale remote kernel (no cycle affected) | 0 |
| 27 | 0 (F-123 closed as a rig fault; rig audit R1–R10) | 0 |
| 26 | 1 (F-126); F-123 narrowed | 0 |
| 25 | 2 loop (F-123 fold, F-125 the stranger click) + Jacob's first four reports (F-124 the mid-run fork; two test sends) | 2 |
| 24 | 1 (F-122 rewind, from the gate; F-111, F-114 closed) | 0 |
| 23 | 7 (F-113 … F-117, F-120, F-121; five fixed, one kept on purpose, one open) | 2 (F-118 the bars, F-119 the stuck screen — both fixed) |

## Untested by decision (Linux is the rig — Jacob, 2026-09-16)

Things the Linux rig genuinely cannot show. Not covered, not pretended to be; if one becomes important, the coordinator asks Jacob for a one-off look.

| what | why Linux cannot | last seen on a Mac |
| --- | --- | --- |
| pixel scale at 2x (Retina hinting, half-pixel edges, cap heights at 2x) | Xvfb runs at 1x; the fonts are identical since #266, the rasteriser is not | cycle 11 stills (`media/mac/cycle-11/`), 2026-09-15 |
| macOS window chrome: traffic lights, titlebar band, tab-strip inset, window shadow | no AppKit | cycle 11 `01-first-launch`, `14-small-window` |
| native menus and the menubar | no NSMenu; the Linux build has none to draw | never captured |
| system dialogs: folder picker (Browse…), save panels, the update prompt | GTK portal or nothing | cycle 11 `05-opener-create` was our own opener, not the panel |
| the permissions sheet's real prompts (mic, screen recording, accessibility, notifications) and TCC state | no TCC; rows show as not applicable | Mac worker's diagnosis on #178/#177, 2026-09-14 |
| macOS text smoothing (font smoothing / no subpixel AA) and Retina weight rendering of Inter vs SF | CoreText vs FreeType | never (F-54: the AWS Mac never painted) |
| Fn-key dictation and in-process mic capture (AVCaptureDevice) | Linux path is cpal/PipeWire | Mac worker's live check on #177 |
| app bundle behaviour: Dock icon, reopen, `arbos://` URL scheme via LaunchServices, Sparkle-style update bar install | no bundle | `media/mac/update-bar/update-bar-879-2026-09-15.png` |
