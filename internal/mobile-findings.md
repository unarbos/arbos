---
cursor:
  subagentId: "bc-08d8261b-fea2-5075-9949-d45f6f9d4acc"
---

> **Partial rebuild, 2026-09-16 12:50 UTC.** The store lost this ledger (with `features-inbox/` and every other `mobile-*.md`) between 12:12 and 12:39 UTC; see `internal/mobile-store-loss-2026-09-16.md`. Rows M-01…M-79 existed in full and are gone; what follows for them is what the loop's context still held (the rows that were open at the loss, verbatim where the text survived, and one line per fixed row from the PR record). M-80 onward is complete.

# iPhone loop — findings ledger

Columns: id · date · source · area · what · state.

## Rows M-01…M-79 (reconstructed summary)

Fixed, by PR — the detail lives in the PR bodies on `unarbos/arbos`:

- **#231 (cycle 1)** M-01…M-10: projects list with face/colour/name from the desktop tab identity; project chat as a navigation destination; worker lines; `Theme.swift` from the desktop palette.
- **#242 (cycle 2)** type scale and spacing retuned to the Cursor reference (17 pt body, 22 pt title); worker pill shows only touched workers.
- **#246 (cycle 3)** M-33…M-40: call screen redesigned — voice first, pulled down; `VoiceOrb`, level meter from an output tap.
- **#260 (cycle 4)** M-41 reply-once (`step` pairing, #247); real project faces from the roster (#233); TestFlight pipeline, app icon, `ExportOptions.plist`, CI workflow.
- **#268 (cycle 5)** M-47 composer as a bottom inset; M-48…M-52 history paging, soft reconnect, resume on foreground, item cap 2000; "Link lost — reconnecting in Ns".
- **#269/#274 (cycle 6)** M-53…M-59: attachments via `put`, dictation into the composer, backward history via `before`, one calm line on a lost link; M-60 baked phone token + `check-secrets.sh` guard; M-61/M-62 tool-markup stripping in the stream.
- **#290 (cycle 7)** M-63 hub URL only moves when the host is truly gone; M-64 build number in Settings; M-65 refusal by name ends the retry; M-66 pill/composer background; M-67/M-68 replayed spawns rebuild finished workers; M-69 harness px→pt.
- **#291** ArbosLife cutover baked into the phone build.
- **#296 (cycle 8)** M-70…M-73: `notify`/`seen`, the away card, local notifications, launch-arg hub URL not clobbered.
- **#302 (cycle 9)** M-74…M-76: APNs registration through the attach socket, `enabled: false` handled honestly, push opens the right project, old-hub `push` error swallowed.
- **#304 (cycle 10)** M-77…M-79: the call's `+` takes a photo or a file.

Still open at the loss (text as it survived):

| id | date | area | what | state |
| --- | --- | --- | --- | --- |
| M-09 | 09-15 | projects list | the hub roster carries no face for a project the phone has never attached to → default folder glyph and a hashed colour | largely closed by #233 + #260; hashed fallback stays for unknown projects |
| M-19 | 09-15 | Agents pill | counted every child ever in the tree, not this session's | addressed cycle 2 (`touched` set); keep an eye |
| M-27 | 09-15 | worker chat | a finished worker's chat shows only "Nothing on record yet" — remote or local | **cause found** (cycle 32): the kernel's `history` for an archived agent answers `total: 0` (probe against the pod kernel); ask filed `features-inbox/2026-09-16-mobile-worker-history-archived-agents.md` |
| M-30 | 09-15 | call | first reply 0.67 s, but the model answered the project question generically | closed by gateway PR #56 (kernel answers) — see M-85 |
| M-31 | 09-15 | call screen | Jacob's reference set has no call screen | closed by cycle 3's own design |
| M-36 | 09-15 | barge-in | 2.4 s through the gateway's echo gate | closed — M-85 |
| M-52 | 09-16 | history paging | `history` paged forward only | closed — kernel #272 |
| M-54 | 09-16 | kernel wire | a photo could not reach the agent (no frame carried bytes) | closed — kernel #270 |
| M-56 | 09-16 | arbos-hub | an older hub dropped `data` and `before` silently | closed — hub redeployed |
| M-58 | 09-16 | pod hub token | 401 to the baked token | closed — M-60, `client-phone` token |
| M-63 | 09-16 | connect | failed attach rewrote `settings.hubURL` silently | closed — #290 |
| M-71 | 09-16 | push | a suspended phone cannot be reached by the app itself | **open — needs Jacob's APNs key + capability**; app half done (#302) |
| M-78 | 09-16 | call / voice server | barge-in on a ~3 s reply not achieved; the reply ignored the project | closed — gateway PR #56, M-85 |

## Rows M-80 onward (complete)

| id | date | source | area | what | state |
| --- | --- | --- | --- | --- | --- |
| M-80 | 09-16 | loop (cycle 10) | harness | the iOS notification permission alert stopped taking `idb` taps in this simulator session (it worked twice in cycle 9); every scripted pass after it is blocked until the alert is answered | open — reboot the simulator before the run, or pre-answer via the Simulator app's GUI |
| M-81 | 09-16 | Jacob (F5) + coordinator | chat | a line that stays "Sending…" while the project's link is down is silent for as long as the link is down — 98 s on the desktop this morning, minutes on the phone; silence reads as broken | fixed #306 (build 994) — after ten seconds without the kernel's echo the card says "demo is not answering — waiting" in muted text (not red); keyed on the missing echo, not on the socket's opinion, since the socket does not notice a cut for ~20 s; verified with a `pfctl` cut: line at +13 s, reply lands and the line clears when the cut lifts (`media/mobile/cycle-11/07-`, `08-`) |
| M-82 | 09-16 | steward via coordinator | chat | the pending-line isolation fix (#306) was reasoned from Jacob's screenshots, not shown live | **proven** cycle 12, two ways: link cut (`pfctl`, port 443) and kernel killed (local hub, two kernels); no marker reached the other project in either — screen, transcript file and kernel log all clean (`media/mobile/cycle-12/01-`–`05-`). With the socket only blocked, TCP delivered the line to its own project when the block lifted; with the kernel dead and a switch, the line is dropped — decision for Jacob whether to hold it instead |
| M-83 | 09-16 | loop (cycle 12) | chat | opening a project while the link is down showed a blank page — no history, no "Reconnecting…" — for as long as the link was down | fixed #309: while connecting the page says "Opening pod…", after ten seconds "pod is not answering — waiting"; **verified** cycle 13 with the cut placed before a cold start (`media/mobile/cycle-13/08-`–`10-`) |
| M-84 | 09-16 | loop (cycle 12) | list | a project whose kernel dies vanishes from the list (hub roster drops it); Jacob's `demo` stayed listed during the hub redeploy because the hub itself was gone | note — a project that was here a minute ago should stay listed, dimmed "not answering", so its chat (and its pending lines) can still be opened; hub or app-side memory, to decide |
| M-85 | 09-16 | gateway + loop (cycle 12) | call | barge-in re-measured on gateway PR #56: 401/404 ms (not achieved in cycle 10, 2.4 s in cycle 3); kernel-answered first audio 2.93 s; small talk 0.70 s; app confirmed not to forward `transcript.final` in duplex; `client.speaking` now carries `route` | done — AirPods leg needs Jacob's phone (`internal/voice/mobile-call-measurements.md`) |
| M-86 | 09-16 | loop (cycle 13) | harness | "the transcript would not scroll back down" was the harness: `idb ui swipe` starting at y ≥ 750 pt begins on the pill/composer inset, not the scroll view; swipes that start inside the transcript (y 150–650) scroll both ways | fixed in `mac-cycle13.sh`; the `scrollPosition` change made on that false reading was reverted before it shipped |
| M-87 | 09-16 | loop (cycle 13) | chat | F12 verified live and recorded: a 20-line reply streamed for 14 s while the view was scrolled up two screens; the view did not move (`media/mobile/cycle-13/02-`, `03-`, `recording-…mp4`) | done — #309 |
| M-88 | 09-16 | loop (cycle 13) | list | after a cold start with the hub unreachable, the list fell from four cached rows to a single `pod · Idle` row and stayed there | **found and fixed** (cycle 15, #319): when the hub fetch failed, `list` held only the pod row, was non-empty, replaced `entries` and was cached — so the shrunken list survived relaunches. Now a hub that does not answer keeps last time's rows marked Off and does not overwrite the cache; verified offline cold start keeps four rows (`media/mobile/cycle-15/01-`), live again on relaunch (`02-`) |
| M-89 | 09-16 | store | ledger | `internal/` lost `features-inbox/` and every `mobile-*.md` (this ledger, coverage, feedback log, TestFlight, Mac host, catalogue) between 12:12 and 12:39 UTC | rebuilt what the context held; `internal/mobile-store-loss-2026-09-16.md`; the loop now keeps a copy of its `internal/mobile-*` and `features-inbox/*mobile*` files on the Mac at `~/mobile-docs/` after every write |
| M-90 | 09-16 | Jacob (F18) + loop (cycle 14) | voice gateway | dictation finals are routed to the gateway's own kernel as a turn (PR #56's answerer routing runs in dictation mode); a note dictated in any project lands unsent in `pod` and gets answered; Jacob saw the loop's own test phrase in his `pod` chat | app stopgap in #319: dictation starts with `answerer: "model"`, `pod` stays quiet; gateway ask filed (`features-inbox/2026-09-16-mobile-dictation-routed-to-kernel-and-tool-output.md`) |
| M-91 | 09-16 | journey JB-3 | call | "Call demo" talked to the gateway's fixed kernel, not `demo`; 0/6 runs landed the spoken turn in the project | **fixed** (gateway PR #56 live 17:17 UTC + app on #319): `session.start` carries `{machine, project}` for a hub target (`.pod` keeps the server default); `session.ready.project` puts the roster's name and glyph under the orb; a `project_unknown/offline/unreachable/no_hub` error is shown as "Can't call demo — its kernel is not running…" with no redial elsewhere. Verified: a call opened from `demo` asked "what are we working on" and the turn and its answer sit in `demo`'s transcript (seq 431–433), "demo" with its glyph under the orb in every state (`media/mobile/cycle-21/02-`) |
| M-92 | 09-16 | loop (cycle 15) | list | after the link returns, rows stayed "Off" until the app was relaunched or pulled | **fixed** (cycle 16, #319): a roster the hub did not answer retries at 10, 20, 40, then every 60 s; verified — Off at +15 s offline, all rows Idle 45 s after the link returned, no touch (`media/mobile/cycle-16/01-`) |
| M-93 | 09-16 | journey run 4 (`pod`) | pod kernel | the pod's root (gemini-2.5-flash) reports "over budget (~19k tokens) with nothing old enough to compact" and compacts every few steps; its `write` tool returns exit 1; a four-step task never completes and no worker is spawned | open — candidate JB-4 in `mobile-journey-runs.md`; for the kernel/pod owner: the root's budget on that place, and the `write` tool there |
| M-94 | 09-16 | journey run 4 | chat | compaction and over-budget notices fill the phone's screen as info lines (four of them between two tool folds) — honest, but on a phone they read as noise | open — fold them with the tool calls, or one dim line "compacted ×N" |
| M-95 | 09-16 | banner check (pod) | notifications | banner-while-away verified on the `pod` kernel after a simulator reboot: badge 1 on the icon at +40 s, away card "root replied" on return (`media/mobile/cycle-15/`); the banner itself fell between stills | done — pod; ArbosLife kernels send no `notify` at all (see M-96) |
| M-96 (**closed** — ArbosLife serves `efcab58`; photo landed, `/push` answers) | 09-16 | banner check + journeys | ArbosLife deployment | `arboslife/demo` and `arboslife/subnet120` replay no `notify` frames and refuse `put` (JB-2): both #270/#296-era kernel features, so the ArbosLife kernels are an older build | open — added to the mesh worker's ask (`features-inbox/2026-09-16-mobile-journey-photo-and-call-asks.md`) |
| M-97 | 09-16 | journey run 5 (`pod`) | chat | a line typed while a reply streamed closed the bubble and sat between the streamed words and the settled text, so the reply's first word ("That") stood alone as a bubble and the full reply landed after the steer as a second one; also the card rode above the reply it interrupted and the reply then read as an answer to it | fixed on the #319 branch: a steer does not close the open bubble; the reply keeps streaming above the card, the card waits at the tail for its echo; a settled reply reaches back past one pending card for the bubble whose words it begins with (`media/mobile/cycle-15/03-`, `04-`) |
| M-98 | 09-16 | journey runner | harness | the kernel's `read` frame is confined to `.arbos/` ("leaves .arbos/"), so the phone cannot read project files; J1/J7 are scored on the kernel's tool records and a verify command's pasted output — "kernel-reported", said so in the score | done — runner |
| M-99 | 09-16 | journey run 7 (`demo`) | challenge on its merits | QA's J7 check — `python3 -m unittest -q` run in the project folder — says **NO TESTS RAN** after the worker's fix: the worker ran the test another way and reported `OK`; the folder has no `tests/__init__.py`, so bare discovery finds nothing. The fix, the CHANGELOG (British spelling, `Requested by: QA-…`) and the branch are all right | open — a product finding for the kernel/worker prompt (prove the tests the way the user will run them), and for QA's rig to compare: does the Linux journey's worker hit the same? |
| M-100 | 09-16 | style pair (cycle 17) | worker lines | finished workers stayed as "Working … · Starting" lines and a "Working N" pill; "Done X" drawn twice | **found and fixed** (cycle 18, #319). Frame log (`kernel.py frames`): an older kernel's spawn record carries `child: null` and names the worker by its brief (`args.name` "Write hello file"); every later frame — `turn`, `status`, `tree`, `say` — carries the id the kernel made from it (`write-hello-file`). The phone kept two entries: the brief's, forever "Starting", and the id's, which went Done. And the spawn record arrives twice (call start and call end, `wait: true`), recreating the brief's entry after the merge. Now a brief-keyed worker is adopted into the id the moment a frame names it (slug match), and the second spawn record recognises it there. Verified: one worker, "Done", no Working line, pill back to "Agents 14" (`media/mobile/cycle-18/01-` before, `02-` after) |
| M-101 | 09-16 | style pair (cycle 17) | chat style | against Cursor's chat still (`media/mobile/cycle-17/01-`): paragraph rhythm, header and composer match; Cursor draws inline code on a soft chip, ours is bare monospace; Cursor's numbered lists indent, ours run flush; one "Worked" line in both | **done** (`cursor/mobile-call-scoped-4acc`): inline code sits on a faint tile, monospaced a point smaller (`media/mobile/cycle-30/01-`) |
| M-102 | 09-16 | style pair (cycle 18) | projects list | against Cursor's All Agents list (`media/mobile/cycle-18/03-`): sections, row shape, search/filter, composer all match; Cursor shows an age on the right of every row ("43m") and a one-line result ("+6,447 −0", "No Changes"), ours only "Idle" | note — an age needs a last-activity time on the roster (`GET /list` carries none today: hub ask, small); the result line is a desktop-parity question for Jacob |
| M-103 | 09-16 | coordinator (#333) | push | the push check no longer waits for a banner: `~/push-check.sh` reads the hub's `GET /push` (enabled / reason / devices as token tails / last attempts) and calls `GET /push/test` — 200 = Apple took a test alert to this phone, 503 = off with the hub's reason, 502 = Apple refused. In the journey as step PUSH. The app shows the `pushed` reply's `reason` in Settings ("push is off: no APNs key on the hub…") instead of a fixed sentence | built; **today the ArbosLife hub answers 404 to `/push`** — it predates #333 (open) — so the step is U with that note; the app's reason line is exercised only on a device build (the simulator has no push token: `media/mobile/cycle-19/01-`) |
| M-104 (**closed** by the ArbosLife update — run 13 saw #317: the line landed inside the turn and the root `say`d it to the live worker) | 09-16 | journey runs 9–12 | kernel (J4) | a line typed while the root waits in `spawn wait:true` is taken only when the worker returns: the `user` event is written after `turn_complete`, and the line starts its own turn (and on `demo` its own worker). Seen on both the pod kernel and ArbosLife's, with the line typed 1 s after the child's `turn running` frame | open — this is #317's case ("spawn wait=true yields to the user's words", merged today); neither deployed kernel has it. QA's J4 mid-flight half cannot score on the phone until it is deployed; the phone's runner now detects the worker live (child `turn` frame) so it will score the moment it can |
| M-105 | 09-16 | style pair (cycle 21) | call | against GPT voice mode (`media/mobile/cycle-21/01-`): orb size and placement, the two round buttons top-left/top-right, and the empty field all match; GPT keeps its composer + mute + close row on screen at all times where ours appears only when pulled down (cycle 3's choice, kept); our call names no project anywhere on the screen — with JB-3 (the call talks to one fixed kernel) that is the line Jacob most needs | done the same hour — name and glyph under the orb from `session.ready.project` (`media/mobile/cycle-21/02-`); voice-first kept |
| M-106 | 09-16 | journey run 13 (`demo`, `efcab58`) | kernel / worktrees | the worker's work lives on a branch in a worktree (`demo--c683687-1`); a verification run in the root checkout sees `main` untouched and `git log main..HEAD` = 0, and this root then tried to "fix" that and broke the tests. QA's J7 wording ("a branch other than main exists with commits ahead") holds — the runner's verify must read `git for-each-ref refs/heads` and `git log main..<branch>`, not the checkout's HEAD | runner changed (verify reads `refs/heads` and tests the branch in a throwaway worktree); the root's own paste got there in the end; note for QA's rig stands |
| M-107 | 09-16 | journey run 13 | demo root | the `demo` root now answers like a small model: fought a "main is protected" git guard for 48 s while seeding, "I do not have access to real-time clock information" for the time ask (it has `bash`), lost the thread on the verify. Earlier runs on `demo` (opus-class) did none of this | note for the mesh worker — which model the ArbosLife roots run after the update |
| M-108 | 09-16 | coordinator (#342) | ask | the phone drew an `ask` frame as a plain agent line: no options, no `id`, nothing sent as an `answer` — a typed reply went as an ordinary `user` line. So a question was never answerable as such, and a question re-offered on attach (#342) would have been a dead line | **built** (`cursor/mobile-call-scoped-4acc`): the `ask` frame is a card — the question, the options as chips, "Tap one, or type an answer", the composer reads "Answer…"; a tap or a typed line goes as `answer {id}`; the card's chips dim once answered; the transcript's own question line, the kernel's "Waiting for your answer" notice and its `answer` echo are not drawn beside the card. Verified on `pod`: card → tap Blue → `answer Blue` → kernel `user Blue` → reply "Blue" (`media/mobile/cycle-23/01-`, `02-`). **Kill-and-reattach against #342 verified** on the Mac (kernel built from `cursor/restart-states-e2e-b027`, real model via OpenRouter, local hub): question open → kernel killed → new kernel on the same place → the app reattaches → the card is back with live chips → tap Owl → `answer Owl` → `user Owl` → reply "Owl" → the kernel's `waiting/ask-….toml` gone (`media/mobile/cycle-23/04-`, `05-`). The re-offered `ask` frame becomes the replay's own question line (no second card) and the replay's "Waiting for your answer" line is dropped |
| M-109 | 09-16 | ask-restart test (cycle 23) | reconnect | **the automatic reconnect never completed**: the countdown task called `reconnect()`, which calls `disconnect()`, which cancels `reconnectTask` — the very task running it — so every attach died with `CancellationError` and the countdown restarted for ever ("Link lost — reconnecting in 13s", twice 15 s apart; hub log: attached / left / attached / left). Leaving and reopening the chat worked, which is why earlier kernel-kill checks (cycle 6, 12) did not catch it: they reopened. On Jacob's phone any kernel restart or hub blip has meant a chat stuck on "reconnecting" until he backs out | **fixed** (`cursor/mobile-call-scoped-4acc`): the task hands the slot back before calling `reconnect()`; verified — kernel killed, new kernel, reattached on the first retry, transcript and the re-offered card in place (`media/mobile/cycle-23/06-` before, `04-` after) |
| M-110 | 09-16 | ask-restart test | chat | while the kernel was down the hub answered `awsmac has no kernel serving "askproj" (live: …)` and the phone drew it as a red error line above the calm "Link lost — reconnecting" | **fixed** (#339): "no kernel serving" is the link still down — the calm line, no red; run 16's cut showed only the calm line |
| M-111 | 09-16 | journey run 16 (`demo`) | kernel / worktrees | the root's git guard refuses to commit on `main`, so the seed went on `fix/…_initial_setup`; the worker's worktree branched from `main` and found no project files ("only README.md"), so the root copied files in and respawned. Every run on `demo` since the update seeds this way | open — kernel/prompt: a worktree should start from the parent's current HEAD (or the guard should allow the first commit on a fresh repo's main); for the kernel team |
| M-112 | 09-16 | coordinator (#346) | projects list | the list hid worker worktrees by a name guess (`--` in the name) and showed the hub's `feedback` service as a project Jacob could open | **fixed** (`cursor/mobile-call-scoped-4acc`): `HubMachine.Project` decodes `kind` (empty by default) and the roster keeps only `kind.isEmpty` — live `/list` today: `demo`, `demo--c206731-1 worktree`, `demo--c311653-2 worktree`, `feedback service`, `phone`, `subnet120`; the list shows pod, demo, phone, subnet120 (`media/mobile/cycle-26/01-`) |
| M-113 | 09-16 | journey run 18 (`demo`, restarted) | ArbosLife environment | the first commit after the restart failed with `fatal: Author identity unknown` — no git `user.name`/`user.email` in the kernel's environment; the root set a generic one and went on. And the worker reached for `python3 -m venv` / `sudo apt install python3.12-venv` and was refused (the task named `unittest`, no venv needed) | note for the mesh worker — a git identity for the kernel's user on ArbosLife; the venv reach is the model's |
| M-114 | 09-16 | journey run 18 | kernel / worker | the worker committed its work on `main` (`branch=main`, 0 ahead) — the root's git guard refuses `main` but the worker's does not, so QA's J7 ("a branch other than main, main unchanged") fails on the merits | open — for the kernel team: the same guard for workers, or the spawn brief saying "on a branch" |
| M-115 | 09-16 | harness | runner | the simulator keyboard's smart punctuation turned the verify command's `--sort` into `—sort` and `'` into `‘`, so the model had to guess; smart quotes, dashes, autocorrection and capitalisation are now off on the loop's simulator | done |
| M-116 | 09-16 | journey runs 19–20 | kernel / workers | **JB-5 named**: a worker's report (branch, tests, CHANGELOG) that the project's repo does not contain, twice running; lineage M-111 (empty worktree) and M-114 (commit on main) | open — kernel team; ask in `features-inbox/2026-09-16-mobile-journey-jb5-worker-work-not-in-project.md` |
| M-117 | 09-16 | call check (cycle 34) | call | with the keyboard up on a pulled-down call, the project's name and glyph under the orb (added cycle 21) printed over the transcript line that rises with the keyboard | **fixed** (`cursor/mobile-call-label-keyboard-4acc`): the name fades out while typing (`media/mobile/cycle-34/02-`). The transcript line still crosses the orb's lower edge with the keyboard up — cycle 3's "orb stays put" choice; note for a later pass |
| M-118 | 09-16 | features agent (queued) | F15 runbook | the next time a sub-agent "just says starting forever" on the pod kernel (F15), file a real `feedback` request against that kernel — the bundle's roster and key state were added for exactly that case, and the earlier exercise ran on a fixture | **queued, one command**: `python3 ~/kernel.py <pod \| machine/project> feedback root [seq=N \| call=ID] [tail=N] <note…>` on the Mac sends the `feedback` frame (agent is `root` — `history` aliases `main`, `feedback` does not) and writes the `feedback_bundle` to `~/mobile-bundles/<utc>-<target>-root.json`, printing the path, the answering build (`git_sha`, `built_at`, model), and the counts. Dry run through the hub against `arboslife/phone` (`efcab58`, 17:17Z): 8.9 KB, 4 anchor events, 6 tail, 54 log lines, nothing redacted — `~/mobile-bundles/20260916-232453-arboslife_phone-root.json` on the Mac. When F15 shows: see the symptom, run it with the spawn's `call=` (or the `seq` of the "Starting" line), copy the JSON to `media/mobile/feedback/`, say where it landed |
| M-119 | 09-16 | feedback dry run + journey run 29 | ArbosLife `demo` | **`arboslife/demo` is not being served by `b6e7098`**: its `hello` carries no `store`, `git_sha` or `built_at` (fields from 09-15 18:13 and 09-16 12:00; `phone`'s hello has all three), it answers any frame it does not parse with `unknown frame type "unknown"` — the kernel-side retyping that a3f89d3 (09-16 02:10) removed — so `feedback` is refused there, and its root transcript is 433 lines ending at the J162115 photo turn (`I serve /home/const/arbos-qa/cycle-11/demo`), where run 27's anchor was 1338. Run 29 (23:05, `main` 1168) started on the new one (seed at seq 1338 at 23:06) and was on the old one by 23:19 | **not a #357 test**; the mesh worker needs to look at which process the hub's `demo` registration points to (two registrations, or the old binary restarted). Also: live `/list` shows `feedback` with no `kind` (the user said #346 returns `feedback service`) while worktrees do carry `kind = worktree` — either the `feedback` place has not declared `kind = "service"` yet or the hub predates it; until then Jacob's list shows it |
| M-120 | 09-17 | cycle 37 (long history, `phone`, 1265 lines) | harness, not the app | after "Show 200 earlier lines" twice, thirty flings toward the tail left the view at the top — it read as a scroll lock after a prepend | **withdrawn**: the flings started at y = 800 pt, on the composer's safe-area inset, not on the transcript; from y = 700 twelve flings reached the tail (`36`→`38` in `~/mobile-out/cycle-37/`). A speculative "release the held row" change was built, found unneeded and dropped before any PR. Paging itself is right: the button says 200, the page lands above, the row that was at the top stays there, a second page goes further back (`media/mobile/cycle-37/04-`, `05-`, recording) |
| M-121 | 09-17 | cycle 37 (list) | projects list | since the ArbosLife cutover the baked direct kernel and the roster's `phone` are one kernel, and the list drew it twice — "pod" and "phone", both opening the same transcript (the C37 seasons sent to `pod` were in `phone`'s chat) — a duplicate Jacob sees on every open | **fixed** (#365): `hello.store` (`arbos://arboslife/phone/`) names the direct kernel's roster twin; the app remembers it (`pod.twin`) and the pod row folds into the roster row on the next refresh — one row, kept across a relaunch (`media/mobile/cycle-37/07-`). When the hub does not answer the pod row stays, being the direct door. The harness now taps `phone` for `pod` runs |
| M-122 | 09-17 | cycle 37 (settings: token editing, never exercised before) | settings | typing a token into any of the four `SecureField`s and tapping Done put up iOS's own "Save Password?" sheet over the projects list, every time — a system sheet the app cannot see or dismiss; the keyboard also offered the Passwords autofill bar. A hub token is not a site password | **fixed** (#365): the fields are `.textContentType(.oneTimeCode)` rather than `.password`; token typed → Done → the list, no sheet, no Passwords bar (`media/mobile/cycle-37/09-`). The edit itself works both ways: a wrong hub token turns every row Off (`10-`), the right one back brings them back Idle by the next refresh |
| M-123 | 09-17 | style pass vs the desktop's one-surface bars (#359) | chat, call | the desktop's tab strip, bottom bar and panel now take the chat's own background with no rule at any seam ("the top bar and the bottom bar can't be seen as a separation") | **already so on the phone**: the chat's header band, transcript and composer inset sample the same background (`0x161514`, ±2 levels under the composer's gradient) with no rule at either seam; the call screen is one field. No change |
| M-124 | 09-17 | store | store | **second loss of every `internal/mobile-*` document and every `features-inbox/*mobile*` note** at about 00:44 UTC (the first: `mobile-store-loss-2026-09-16.md`); a `cat >>` of mine then created a two-line `mobile-findings.md` in the empty slot and my mirror copy overwrote the local mirror with it | **restored from the Mac mirror** (`~/mobile-docs/`, taken 00:31 and 23:05) plus the local copy of the 23:28 demo note; the rows and edits made after 00:31 (M-122, M-123, the cycle-37 coverage lines) were re-applied. Rule from now on: the mirror copy goes store → Mac only after the store file is checked to be longer than the mirror, never the other way |
| M-125 | 09-17 | cycle 38 (`demo` rerun attempt, 01:16) | ArbosLife `demo` | asked to run the `demo` journey "now that its kernel carries the empty-project fix"; checked first: the **machine** row in `/list` is `b6e70980b60a`, built 22:31 — that is the worker daemon — but the kernel process answering `arboslife/demo` still sends a `hello` without `store`/`git_sha`/`built_at`, still retypes unknown frames to `"unknown"`, and its root transcript is 437 lines ending at the 16:2x turn. `phone`'s kernel on the same machine is `efcab58f` with every field. So the daemon is new and the `demo` kernel process is old | **no run** — a journey against it would be void like run 29; the mesh worker needs to find the `arbos-kernel serve` process behind `demo` on arboslife (there may be two, the hub's registration pointing at the old one) and start `demo` from the daemon's binary. Evidence added to `features-inbox/2026-09-16-mobile-demo-served-by-old-image-again.md`; the rerun is the first item the moment `hello` carries a `git_sha` |
| M-126 | 09-17 | cycle 38 (style pass vs desktop one surface, #359) | chat, call | side by side with the Mac's new look (`media/mobile/cycle-38/01-`): the desktop's title band, tab strip, chat, bottom bar and panel are one background with the composer as a lighter field on it | **consistent, nothing to change**: the phone's header band, transcript and composer inset sample one background (`0x161514`; the composer band two levels off under its gradient), the composer and the Agents pill are the same lighter field, and the call screen is one field top to bottom. The two apps read as one family: same background, same "lighter field on one surface" composer, same glyph-and-name identity line |
| M-127 | 09-17 | journey run 30 (`demo`, `b6e7098`) | chat / worker lines | a spawn refused by the machine (JB-6: `arboslife: start arbos-kernel serve: No such file or directory`) drew as "Starting worker …" on the live line while the root did the work itself; the reason sat inside a tool fold. Also the "Running on <machine>" line (F15) read the record's `machine` key, and the mesh writes `host`, so it never fired | **fixed** (#373): a spawn record with `error` drops the worker it started and yields one plain line — `Couldn't start journey_J014026_worker on arboslife: …` — live and on replay; `host` is read as well as `machine`. Verified on `demo`'s replay (`media/mobile/cycle-39/01-`; the run-30 still before: `02-`) |
| M-128 | 09-17 | journey run 30 | kernel / mesh | **JB-6 named**: every spawn with `host: arboslife` refused in 35 ms, ENOENT on `arbos-kernel serve` (`worker.rs:285` starts the child from `current_exe()`, and the daemon's binary is gone from under it) — runs 29 and 30. The root does the work itself, so J7 passes on the merits while JB-5's worktree path stays untested since #357 | open — `features-inbox/2026-09-17-mobile-spawn-refused-daemon-binary-gone.md`: mesh restarts the daemon from the binary on disk; kernel falls back when `current_exe()` is gone. The runner now scores a refused spawn F (it read the record's presence as P) |
