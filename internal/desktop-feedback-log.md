# Jacob's desktop feedback — what he said, what the loop did, which build has it

Written by `deploy/feedback/poll.py`. The rig holds the authoritative copy of
every report; `media/desktop-feedback/<date>-<n>/` is the copy agents read.

| # | build | what Jacob wrote | what it was | done | reaches him in |
| --- | --- | --- | --- | --- | --- |
| 2026-09-16-1 | 0.2.0 (1102) | The worker line says Starting forever. My key is [redacted:openai-key] and the token [redacted:github-token] too. | **FIXTURE — invented words, unrelated events, do not diagnose from it.** The words are made up and the trajectory is an unrelated `cat hello.py` turn, so complaint and evidence do not correspond. Read cold as a debugging exercise it looked undiagnosable, and the gap was attributed to the bundle: [#360](https://github.com/unarbos/arbos/pull/360)'s additions stand on their own merits, but that failure was the fixture's. Also a rig test of the sheet (F-100–F-102) and of pickup on the directory transport | n/a | n/a |
| 2026-09-16-2 | 0.2.0 (1113) | the sheet froze when I opened it, and my key [redacted:openai-key] was on screen | **FIXTURE — invented words and events, do not diagnose from it.** The mesh worker's smoke report: the app's on-demand writer, delivered through the hub, taken with the reader token. Genuine evidence of the hop and of nothing else — everything inside it is made up | n/a | n/a |
| 2026-09-17-1 | 0.2.0 (1185) | Forked the chat mid run no response from sub agent | Real. He pressed fork on the root while its worker ran; the fork is a whole-log copy, so the copy ended on the in-flight prompt `write bubble sort in a subagent and run it` with nothing under it and an idle composer — it read as a chat that never answered. The worker belonged to the original (fork claims no workers, by design). Fix: the copy now carries a calm line under that prompt: *Forked while the original chat was still answering this — it keeps that work. Send a message to continue here.* (`cursor/cycle-25-aa39`, in #367). Kernel-side note: the fork's model then saw a history without the workers and a branch already at `feat/bubble-sort`, and was confused (report -2). | in #367 | next build after #367 |
| 2026-09-17-2 | 0.2.0 (1185) | Nothing returned twice error | Real, and working as designed but read as an error: in the *forked* chat his `hello` made fable-5.1 return an empty reply twice (its thinking shows it puzzled by a copied history whose branch was already at `feat/bubble-sort` with no visible prior work), the kernel fell back to opus-5 and said so with the calm aside *anthropic/claude-fable-5.1 returned nothing twice, so anthropic/claude-opus-5 answers this turn.* The `go` he typed next queued correctly as a follow-up. Root cause is the mid-run fork (-1); the aside's wording is the kernel's — suggested softer form filed with the fork note. | see -1 | — |
| 2026-09-17-3 | 0.2.0 (1185) | Test report | A test send; the path works end to end: hub → poller → rig → store → this ledger, screenshot and trajectory attached. | — | — |
| 2026-09-17-5 | 0.2.0 (1185) | Test | A test send from a place with no kernel attached (`kernel: null`). The report itself was fine; the poller skipped it on every poll with `'NoneType' object has no attribute 'get'` until the null blocks were read as empty (`cursor/poller-null-kernel-aa39`). Same-day note for the poll-error rule: 2026-09-17, poller bug, fixed. | poller PR | — |
| 2026-09-17-6 | 0.2.0 (1335) | Never get response from the 3 subagents. | Real, two halves. **Kernel/coordinator:** he asked for three sorting workers; the coordinator (Opus, standing in for a failing mercury-2.5) spawned them, set its status to *Waiting on three sorting workers*, then — against its own thinking ("better to end this turn and let their reports come") — ran `bash sleep 75; echo waited` with `wait_ms 120000` to wait. The three workers finished within 25 s; their reports queued as wakes behind the coordinator's own sleeping turn, so nothing came back until the sleep ended (`features-inbox/2026-09-17-coordinator-sleeps-on-its-workers.md`). **Desktop:** the live headline kept the model's status *Waiting on three sorting workers* over three *Done* lines for minutes; the true line was *Running sleep 75; echo waited*. Now a running tool of the chat's own outranks an older status (`live_status()`, cycle-33 PR). Also in his still: a phantom **Delegate 1 · Working** row — F-137, explained by the coordinator's capture (`f137-sessions/`): a parentless `chat-…` the window had filed as root's delegate two days earlier, with no status behind its Working; fixed in the cycle-33 PR (rows and parentage from the kernel's record, Working from evidence), driven on his own records | — | — |
| 2026-09-17-7 | 0.2.0 (1335) | Never get response from the 3 subagents. | Duplicate send of -6 (same words, one minute later) | — | — |
| 2026-09-17-8 | 0.2.0 (1335) | The done quicksort and done mergesort worker show below the "where is my response" | Real, desktop. His *where is my response*, typed into the running turn, drew **above** the three *Done <worker>* lines, as if they answered it — the worker lines were appended after the tail, so a steer card typed later came first. Now the worker lines sit under the prose that spawned them and above any later steer card (cycle-33 PR) | — | — |
| 2026-09-17-9 | 0.2.0 (1335) | The question was "waht is your git status" the agent response "Corrected the context" whould just give me the answer right? | Real, model/coordinator behaviour, drawn correctly. He asked *what is the git status*; the model ran `git status`, noticed its own context note was wrong, edited `.arbos/docs/project-context.md`, and its only prose was *Corrected the context note…* — it never answered the question, and in the Project style the `git status` output is hidden work, so he saw no status at all. Filed: answer first, housekeeping after (`features-inbox/2026-09-17-coordinator-answers-before-housekeeping.md`) | — | — |
| 2026-09-17-10 | 0.2.0 (1335) | Is this cant speak to other agents design correct? | A design question, not a bug: he asked whether one place's agent can talk to another local project's; the coordinator tried `say` to a path and was refused (*no agent is named …/everything/.arbos/agents/root*), then explained correctly that messaging reaches this place and registered remote machines only. Whether cross-project talk on one machine should exist is Jacob's and the kernel owner's call — filed with the exchange (`features-inbox/2026-09-17-cross-project-say-design-question.md`) | — | — |
| 2026-09-17-11 | 0.2.0 (1335) | The chat sent the image 3 times? | Real, desktop (F-142). The agent drew a cat as SVG, screenshotted it twice on the way (each a `browser` artifact row) and embedded the last shot in its reply — three cats. An artifacts row whose files the answer's prose embeds is skipped now; the first attempt (a cropped shot) stays as the record of the work. Cycle-34 PR | — | — |
| 2026-09-17-12 | 0.2.0 (1335) | Searching websites is very slow it doesnt search mutiple at once and it shows an error on screen | Two halves. **Kernel:** the search tool ran two queries in sequence and both failed — *OpenRouter web returned no sources; DuckDuckGo answered with a bot check (CAPTCHA)* — then fell back to fetches; slowness and the CAPTCHA are the search tool's (`features-inbox/2026-09-17-search-tool-slow-and-captcha.md`). **Desktop (F-143):** every fetched page drew as a bare *fetch* / *Fetched fetch*; now the page's host. Cycle-34 PR | — | — |
| 2026-09-17-13 | 0.2.0 (1335) | Increase font size needs to be closer to cursor in size. | Real, desktop. Cursor's prose is 14 px on the Mac; bezel's 13 pt body read small beside it. Default body 14 (`DEFAULT_TEXT_SIZE`); a state file from before version 3 holding the old default moves with it, a size he stepped to stays. Cycle-34 PR | — | — |
| 2026-09-17-14 | 0.2.0 (1335) | Kickoff document does not format the owner = root, format is wrong. | Real, desktop (F-144). The Kickoff document opened on its TOML front matter (`+++ owner = "root" +++`) rendered as prose; the document view now drops a `+++`/`---` block as the project page already did. Cycle-34 PR | — | — |
| 2026-09-17-15 | 0.2.0 (1335) | You cant click on the sources links they are not hyper links. | Real, desktop (F-145, root cause). Every link in an answer was dead to a click: bezel-markdown reports the release to every prose item and the items above the pressed one hear it first; `point()` ended the gesture on the first release it saw, so the pressed item's own release opened nothing. Fixed; driven with a fake browser on PATH — the URL reaches it. Cycle-34 PR | — | — |
| 2026-09-17-16 | 0.2.0 (1335) | You cant clikc on the "Canada-EU partnership" document in the chat output | Real, desktop (F-145). Same dead click as -15, plus: the doc chip is a relative link to a file the agent wrote (`docs/canada-eu-partnership.md`), which the opener turned into `https://docs/…` for the browser. A path in the place now opens in the column; driven — `brief.md` opens beside the chat. Cycle-34 PR | — | — |
| 2026-09-17-17 | 0.2.0 (1335) | Forking the chat lost all the previous response outputs. | Real, desktop (F-147). The fork reads its transcript from the file, and the replay parser marked a `user` line as a steer whenever a turn was open — the kernel writes `wake user` before every prompt, so every prompt of the copy folded into the turn before it and the answers sat behind shut folds. A prompt following its own wake is the turn's prompt. Driven: a fork shows its turns with their footers. Cycle-34 PR | — | — |
| 2026-09-17-18 | 0.2.0 (1335) | Rewing to here does not work on a forked chat | Two halves. **Kernel, the worrying one:** his report's kernel log shows the rewind ran on **root** — `agent=root target=Line(75) dropped=21 files=true archive=…/agents/root/transcript.rewound-…` — while he was in the forked chat; filed with the line quoted (`features-inbox/2026-09-17-rewind-on-a-fork-cut-roots-transcript.md`). **Desktop:** before F-147 the copy was one turn and its rewind footer reached nothing; after it, a rewind in a copy on a non-git rig place is refused by the kernel with *no checkpoints yet* (F-112b's family). The desktop half is in the cycle-34 PR | — | — |
| 2026-09-17-19 | 0.2.0 (1335) | When stopping a voice input the message is immediately sent rather than just appearing in the chat box. Also the respons is spoken this is the wrong way to do things, should just appear in teh chat an | Real, product word (F-146). Dictation sent at once and had the reply read aloud, by the voice side's design ("the same two-step as the web composer's dictation final"). Now the words land in the field at the caret and wait for Enter; the reply is read, not spoken; a call is where speech answers speech. Voice owner told (`features-inbox/2026-09-17-dictation-lands-in-the-field-not-sent.md`). Cycle-34 PR | — | — |
<!-- poll.py appends rows above this line; do not remove -->
