---
cursor:
  subagentId: "bc-08d8261b-fea2-5075-9949-d45f6f9d4acc"
---

> Rebuilt 2026-09-16 12:50 UTC from the loop's own context after `internal/` lost this file (with `features-inbox/` and every other `mobile-*.md`) between 12:12 and 12:39 UTC. Content is complete for this log; see `internal/mobile-store-loss-2026-09-16.md`.

# Jacob's TestFlight feedback — what he said, what the loop did, which build has it

Source: App Store Connect beta feedback (`GET /v1/apps/6812503407/betaFeedbackScreenshotSubmissions` and `…/betaFeedbackCrashSubmissions`, read with the `ZV82D3ZWRT` key from the loop's Mac; `~/asc-feedback.py`). Polled every 15 minutes by the `mobile-feedback-poll` timer (quiet when nothing is new); each new submission lands in `media/mobile/feedback/<date>-<n>/` as `screenshot-N.jpg` + `feedback.md` + `feedback.json` (tester email removed). Crash submissions: none so far (13 screenshot submissions on the account).

| # | folder | build | what Jacob wrote | what it was | done | reaches him in |
| --- | --- | --- | --- | --- | --- | --- |
| F1 | `2026-09-16-1` | 956 | "Where does this chat go? Not clear" | the list's composer said "Plan, ask, build…" and named no project | placeholder names the project: "Message phone…" | **994** (#306) |
| F2 | `2026-09-16-2` | 956 | "Can't see the chat box at main screen" | the list's composer was an overlay the keyboard covered on iOS 26.6 | composer is a bottom inset; rides above the keyboard | **994** (#306) |
| F3 | `2026-09-16-3` | 956 | "Response of hello in remote box session is this what?" | `subnet120`'s kernel (another worker's, older build) answered "Hello" with a raw `ls -la`; the model's doing, not the app's | nothing in the app; **for Jacob**: that project's kernel is not the loop's — its owner should update it | — |
| F4 | `2026-09-16-4` | 956 | "Those two messages I did not send in this chat but they got copied over when I went to the other chat" | pending lines typed for `demo` were flushed to `phone`'s kernel on reconnect — and it ran them | pending lines belong to their project; a switch drops them | **994** (#306) |
| F5 | `2026-09-16-5` | 956 | "Secondary agent functions did not get sent" | the two lines stayed "Sending…" because `demo`'s link was down at that moment (the hub was being redeployed ~08:19 UTC); they then went to the wrong project (F4) | F4's fix, plus: a card still "Sending…" after ten seconds says, in muted text, "demo is not answering — waiting" — calm, not red, clears by itself when the reply lands (`media/mobile/cycle-11/07-`, `08-`) | **994** (#306) |
| F6 | `2026-09-16-6` | 956 | "Can't scroll any further to see bottom chat; should be able to click chat and dismiss the keyboard" | no tap-to-dismiss; the tail hid behind the keyboard | a tap on the transcript puts the keyboard away; the tail follows the keyboard | **994** (#306) |
| F7 | `2026-09-15-2` | 817 | "I didn't write that second message and then the response text was all over the place" | a child's spawn brief shown as his card; streamed text broken per token | fixed in cycles 4–6 (M-41, M-14 lineage); already in 956 | 956 — **open until Jacob confirms** (asked 09-16) |
| F8 | `2026-09-15-1` | 817 | "Ummm" (one word per line) | the same streaming break | same | 956 — **open until Jacob confirms** |
| F9 | `2026-09-16-7` | 956 | "Voice does not print the text automatically into the chat section and cuts the history so we only get the end of the voice note" | two bugs in the composer's microphone: the app treated each `transcript.delta` as a replacement instead of an append, so only the last fragment survived; and the words waited in the field for a tap | deltas append and an empty final keeps them; the second tap on the mic sends the line by itself — the words are on screen while he speaks (`media/mobile/cycle-12/06-`, `07-`) | **1021** (#309) |
| F10 | `2026-09-16-8` | 956 | "Images don't work" | `subnet120`'s kernel is an older build (before #270) with no `put`; it answered the photo with a red `unknown frame type "unknown"` and the model said the image never reached it | the app now says in plain muted text: "This project's kernel is an older build and can't take photos or files yet — the words were sent, the file was not. Its machine needs a kernel update." **For Jacob**: `subnet120`'s owner must update its kernel; photos work on `pod`/`demo`/`phone` today | **1021** (#309) |
| F11 | `2026-09-16-9` | 956 | "While sending a photo error" | same as F10 | same | same |
| F12 | `2026-09-16-10` | 956 | "When the chat is printing I can't read because the rollout of text continuously recenters the chat around the rollout of text making this jerky feeling" | every streamed token scrolled the view to the tail with an animation, even when he had scrolled up to read | the view follows the tail only while the tail is in view; scroll up and the stream leaves you alone (verified live and recorded, `media/mobile/cycle-13/02-`–`04-`, `recording-stream-while-scrolled-up.mp4`) | same |
| F13 | `2026-09-16-11` | 956 | "I don't want to see these tool calls on the phone unless maybe I expand" | every `bash · …` line drawn in the root chat | consecutive tool calls fold into one dim line — "4 tool calls · 12s", failures counted — that opens on a tap (`media/mobile/cycle-12/08-`) | same |
| F14 | `2026-09-16-17` | 1021 | "Issue seeing text output" | the root's reply showed three separate folds "2 tool calls / 6 tool calls / 2 tool calls" — empty assistant lines between the calls split one run into three; and an opened fold shows the calls' labels only, never their output (the kernel's tool record carries no output) | empty lines no longer split a run; **for the kernel**: put a short `output` on the tool record so an opened fold can show it | **1099** (#319, uploaded 17:09 UTC) |
| F15 | `2026-09-16-18` | 1021 | "Creating sub agent does not run just says starting forever" | a worker spawned on another machine (`arboslife`) sends no steps back — only its report when done — so the phone said "Starting" for the whole run and its chat "Nothing on record yet" | the line now says "Running on arboslife · reports here when done"; the worker did run (the root's own text confirms it). The empty remote worker chat is M-27, kernel-side | **1099** (#319); F17's swipe is #322, not merged |
| F16 | `2026-09-16-19` | 1021 | "The image sent does not show as an image in the chat section" | the card showed "📎 photo-….jpg" | the photo is drawn on the card (thumbnail, rounded), before and after the kernel's echo and after a reopen (bytes cached by stored name); files keep the paperclip line (`media/mobile/cycle-14/01-`, `02-`) | **1099** (#319); F17's swipe is #322, not merged |
| F17 | `2026-09-16-20` | 1021 | "Swiping to the left should take me back to the Home Screen" | the chat hides the navigation bar to draw its own header, and UIKit switches the edge swipe off with it | the interactive pop gesture is re-enabled whenever there is a screen to go back to. **Not verifiable with `idb`** (it cannot fake a true screen-edge touch); Jacob's phone is the check | **1099** (#319); F17's swipe is #322, not merged |
| F18 | `2026-09-16-21` | 1021 | "The voice note was sent without me clicking send and then still shows in the chat section" | two causes. (a) Build 1021 sent a dictated note by itself on the second mic tap — my reading of F9, and wrong: he wants the words in the field and his own send. (b) Worse: the **voice gateway routes dictation finals to its own kernel as a turn** (PR #56's answerer routing runs in dictation mode too) — a note dictated in any project lands, unsent, in the gateway's project (`pod`) and gets answered there. His `pod` screenshot shows my own test phrase from the simulator in his chat. Proven live: dictate in `demo`, do not send, a new turn appears in `pod` (`media/mobile/cycle-14/04-`, `05-`) | (a) reverted: the words wait in the field for his tap (`03-`). (b) the app now starts dictation with `answerer: "model"`, which keeps the gateway off its kernel (`06-`, `pod` quiet); gateway ask filed to make dictation never route, whatever the answerer | **1099** (#319); F17's swipe is #322, not merged |
| F19 | `2026-09-16-22` | 1021 | "Why?" (under: "Sorry — empty reply on my side, nothing blocking. Not much: validator's chewing through duels…") | `subnet120`'s model answered "Sup" with an empty step; the kernel nudged it, and the model apologised in prose for its own empty reply before answering. The app drew exactly what the kernel sent | nothing in the app. **For the kernel**: after a nudge for an empty reply, the model's apology is noise the user should not see — either drop a leading "Sorry — empty reply…" sentence or make the nudge tell the model not to mention it | — |

**Proof of F4/F5, done live 2026-09-16 12:00 UTC** (the steward asked): (A) port 443 cut on the Mac, a marker line typed into `demo`, switched to another project, link restored — the other project never saw it, and the line ran in `demo` when the socket came back (TCP kept it; `media/mobile/cycle-12/01-`–`03-`). (B) `longproj`'s kernel killed on the local hub, a marker typed, switched to `otherproj`, kernel restarted — `otherproj`'s transcript file and kernel log contain no marker (`grep -c` = 0), and the line was dropped rather than sent anywhere (`04-`, `05-`). **Decision for Jacob**: in case B the words are lost. Should the app hold them for that project and show them as pending cards when he comes back to it, sending when it answers? Recommended yes.

Rules the loop follows from here: new feedback is read within 15 minutes; a fix goes into the cycle in progress, ahead of the coverage rotation; anything that needs Jacob's decision is listed here and in the report, not guessed.
