---
cursor:
  subagentId: "bc-71eb0fc3-658e-5b64-8b2b-9854416c9baf"
---

# Release v0.2.0: merge into `main`, repo polish, README

Done 2026-09-13 21:55 UTC. Repo `unarbos/arbos`.

## Where things are

| Ref | SHA | Note |
| --- | --- | --- |
| `main` | `ab7cf62` | Rust Arbos + `www/`. Merge commits, history kept. `912f740` cherry-picks the Mac worker's `d962931` (bundle-id ad-hoc signature, mic AGC); `ab7cf62` lists `cpal` once in `desktop/Cargo.toml`. |
| `v0.2.0` (annotated tag) | → `ab7cf62` | Moved twice: from `82bc69c` (old integration) to `44d7901`, then to `ab7cf62` for the Mac DMG rebuild. |
| `rust` | `ab7cf62` | Fast-forwarded to `main` (so it now carries `www/` and the joined history too). |
| `release/v0.2.0` | `2a23c72` | Integration `7ab5351` + everything below. |
| `legacy/go` | `e17374a` | The Go-era `main`, kept as a branch pointer. |
| Draft release | https://github.com/unarbos/arbos/releases/tag/untagged-4864da369adb2bc263f9 | Notes grouped Agent model / Desktop / Voice / Mesh / Website / QA. Assets: Linux kernel tarball (workflow); **notarised** `Arbos-0.2.0-arm64.dmg` (sha256 `c7aa8131…`, uploaded by the Mac worker 2026-09-15 21:21 UTC, verified by download) + macOS arm64 kernel (`509c5a36…`, verified). Publishing is Jacob's step. |

## What went into `release/v0.2.0` (on top of #58 `7ab5351`)

- `cursor/project-panel-94d6` (#71 → #101 → #105), fast-forward.
- Voice stack: #111 (contains #56 and #100's slices 1–4), then `cursor/voice-server` tip `bbffd99` (audio normalisation), then #100's tip `40bfacf` (slice 5: dictation mode, auto model voice, `--echo-margin`). Two conflict rounds in `voice_server/{base,echo,server}.py`: kept the later echo-gate refinement (post-gain, margin 1.6, `FLOOR_RMS`) and the normaliser; kept #100's `VOICE_ECHO_MARGIN` env default. Harness: 16/16 scenarios pass after each round.
- #121 (spawn host=local), #120 (cherry-picked `a2b4e0b`; conflict: kept `.arbos/runtime/` for the kernel log, added the spawn counter).
- #119, #122, #123. #119 vs #121 both fixed spawn host=local: kept #121's `choose_host` (tested superset), removed #119's `means_local`/`any_machines`, kept #119's worktree-in-no-repo note. #123 vs layout: dropped `sidebar.rs` (deleted by #71), took the tabs menubar, added `AttachPaths` action; one test needed the new `described` field.
- #5 (iPhone, 8 commits, `ios/` only) — not in the ask, added because the website's install page and the release notes describe `ios/scripts/gen-secrets.sh` and the current app; flagged here.
- #94 and #93 were already contained.

Checks on the final tree: `cargo test --workspace --locked` 363 passed / 0 failed; `cd desktop && cargo build --locked` and `cargo test --locked` (45 passed) on Linux with x11/wayland and `libasound2-dev`; voice harness 16/16. CI (`ci.yml`) green on `rust` and `release/v0.2.0` at `2a23c72`, and on `main` at `62ff0a5`, `44d7901`, and `ab7cf62`.

## `main` merge decisions

- `main` and `rust` had no common ancestor. `git merge --allow-unrelated-histories`, resolved to the release tree. The Go-era code (`cmd/`, `internal/`, `web/`, `go.mod`, Go docs, Makefile, scripts) is removed from the tree; history stays, plus `legacy/go`.
- #125 (`www/` + `pages.yml`) merged into `main` after the release. The old `site/` + `deploy-site.yml` + `scripts/{install.sh,arbos}` were removed in that merge: both workflows deployed to the same Cloudflare Pages project `arbos-life`. Consequence: `curl arbos.life | bash` (Go installer) is gone; the new site already replaced arbos.life.
- Set repo secrets `CLOUDFLARE_API_TOKEN` and `CLOUDFLARE_ACCOUNT_ID` from the vault (rows 15, 19); the Website workflow then deployed `main` successfully.
- Site copy fixes in the merge: DMG is per architecture (arm64 / x86_64), and the docs page no longer links `qa-loop-design.md` (that doc points at `internal/qa/` code that is not in the repo, and names Jacob's server; not imported).

## Repo files added

- `README.md` (147 lines): what it is, two screenshots (`docs/img/project-page.png`, `docs/img/call-mode.png` cropped from `media/call-mode/desktop-spoken-user-card.png`), Install (macOS, source, iPhone, voice server, mesh) reconciled with `www/install/`, Configure, Concepts, Repository map, License (MIT, exists on both lines).
- `.github/PULL_REQUEST_TEMPLATE.md`, `.github/workflows/ci.yml` (kernel build+test, desktop build, Linux), `.github/workflows/release.yml` (Linux kernel+hub tarball attached to the tag's release; creates a draft if none).
- `docs/design/{filesystem-state-design,cursor-vs-arbos-agent-model,arbos-mesh-design,desktop-call-mode-design}.md` — copied from the store (originals left in place; the coordinator owns them), trimmed of `internal/`/`media/` paths, and the mesh doc's deployment section (pod address, tunnel id, vault item id) rewritten as generic guidance.

## GitHub settings

Description set as asked; homepage `https://arbos.life`; default branch `main`; Issues on, Discussions off; topics: agents, ai-agents, agent-coordinator, rust, gpui, desktop-app, voice, full-duplex, speech, ios, openrouter, llm, cursor, coding-agent, multi-agent, swe-bench. Wiki/projects left as they were.

## PRs

- GitHub auto-marked 97 PRs merged when `rust` was fast-forwarded (their heads are ancestors). #125 likewise on `main`.
- Closed with a comment pointing at `44d7901`: #12, #13, #100, #101, #105, #111, #121; #120 pointing at cherry-pick `a2b4e0b`.
- Left open (head not contained): #34, #35, #54 (re-landed as #60, #59, #61 — content is in; branches differ), #55 (sidebar, moot with the tabs layout), #57 (branding, done by #58), #73 (killpg de28 variant; the 52cd variant landed), #95 (UI QA fixes; draft), #124, #126 (new features work).

## For Jacob

1. **License**: MIT exists (`LICENSE`, "Copyright (c) 2026 unconst") on both lines; README links it. Nothing to decide unless a different licence is wanted.
2. **Notarised DMG**: `desktop/Makefile release` needs `.env.release` with `APPLE_SIGNING_IDENTITY` and `APPLE_KEYCHAIN_PROFILE` (Developer ID certificate + notarytool profile). Today's DMG is ad-hoc signed; the README and site tell users to right-click › Open.
3. **Publish the draft** when the Mac assets are final. On publish it becomes "latest".
4. The DMG does not contain `arbos-kernel`; the README says install it from source or take `arbos-kernel-0.2.0-macos-arm64` from the release. Bundling the kernel in `Arbos.app` would remove that step.
5. `www/assets/site.js` picks the newest `Arbos-*.dmg`; with one DMG per architecture it will pick one. Website worker's call.
6. Store hygiene: the four design docs were copied, not removed, from `docs/` in the store.

## Merge steward log

Standing role from 2026-09-13 22:18 UTC: hourly pass over PRs targeting `main` (timer `merge-steward-hourly`). Rules: CI green (from 11:40 UTC that includes the `macOS (check kernel + desktop)` job), verification note in the body, not WIP (from 2026-09-16 00:12 UTC, coordinator's standing adjustment: the draft flag alone is a formality when the PR is green and its body says it is finished; hold only when the body says incomplete, checks are red, or it asks for review first); read diffs touching secrets, deletion, kill/job paths; merge commits; never merge auto-merge policy changes or anything that prints or stores secrets. After each batch: `rust` fast-forwarded, new SHA posted for the Mac worker as a comment on #58 (the PR it built from; the coordinator's "the PR" was not named — change the target if another PR is meant).

| Batch | `main` | Merged | Held | Closed superseded |
| --- | --- | --- | --- | --- |
| 1 (22:25 UTC) | `7ea2bb3` | #129 (desktop: steer-by-default, kernel-held queue, connect retries, no attach-port probing), #124 (model picker chips), #127 (kickoff fixes: `fs::confine` store aliases — confinement check still runs after the alias; absolute worker cwd; coordinator gets `secret list/use`, values stay redacted) | #126 (browser parity; `browser_e2e` fails on the runner), #128 (audit batch 2; `audit_kernel_2_e2e` nudge test fails; body still names the integration base) — both commented | #34, #35, #54, #57, #73 (content re-landed as #60, #59, #61, #58, #74). #102 was already merged into `rust` before the role began. |
| 2 (23:15 UTC) | `e7fb6a7` | #126 (browser parity; the e2e now skips when Chrome cannot start on the runner), #128 (audit kernel batch 2: hidden chores, notes nudge, `grep scope=history`), #131 (environment allowlist: desktop→kernel and kernel→jobs `env_clear` + allowlist + secrets-door grants; `check` names stray secret-looking variables, names only) | #130 (wake-up incident: reap leftover jobs at start — held for the macOS fallback `args.contains(program)` in `process_looks_like`, which could `killpg` a reused pid that is any shell; asked to drop it and rebase) | — |
| 3 (00:10 UTC) | `26e10cc` | #130 (wake-up incident; the hold was answered: `pid_identity` proves a pid by the leash's job-folder argv or a Linux start time, never by program name; unverifiable → left alone and named by `check`), #132 (voice harness mock machine `qa-mock`, never `mac`) | — | — |
| 4 (01:15 UTC) | `ba1589f` | #133 (Try Live; screenshot wire frames, no kill/secret paths), #134 (iCloud store relocation: rename + symlink, refuses when a kernel runs or the target exists, user-prompted; follow-up asked: exclude `.arbos.nosync/` from the project's git), #135 (inline cards; steer is not a follow-up), #136 (nudge once, own event) — #135/#136 conflicted in `transcript.rs` on the nudge renderer: kept #135's dim ↻ line and pointed #136's `ChatItem::Nudge` at it | — | — |
| 5 (02:05 UTC) | `c2bb5e5` | #137 (`.arbos.nosync/` excluded from the project's git at every kernel start and after `relocate`; the follow-up from batch 4) | — | — |
| 6 (02:50 UTC, priority) | `f0b0897` | #138 (desktop OOM: `path_to` stops at a repeated id / the session count; `can_parent` refuses self/descendant links from `resolve_parents` and the kernel child list) | — | — |
| 7 (03:05 UTC) | `888f512` | #139 (SWE-bench loop cycle 2: `[hook] Tests covering this edit` line after source edits via `git diff` + `git grep`; contract paragraph; 37→41/50 on slice 2) | — | — |
| 8 (04:05 UTC) | `e793b03` | #140 (standing pass kernel items: forks drop `child` from copied spawn records and the wire scrubs stale claims; kind-less children get `role: worker` and a worker contract line; empty-reply lines are `nudge`s; `rewound` announced at the cut with `pending`; qa-032 — the cut walks back to the turn's opening wake, bounded by the previous `turn_complete`, cut lines archived) | — | — |
| 9–10 (05:00, 06:00 UTC) | `e793b03` | nothing targeted `main` | — | — |
| 11 (07:20 UTC) | `46437a3` | #143 (qa-017: appends never recreate a deleted agent folder; late reply dropped), #142 (SWE-bench cycle 3 hook per function), #144 (archive_children on by default — moves finished workers to `archive/agents/`, history still reads), #145 (github_ci by branch), #146 (clean worktree removed on archive; `git worktree remove` without force, branch deleted only when it has no commits beyond HEAD), #148 (archived row in the Agents tree), then #141 (Show line in the brief) and #147 (⌘K search, date divider, mic note) after their CI re-runs passed — both had failed on `recreate_e2e::a_restored_chat_folder_streams_its_new_lines`, a 5 s timing wait neither PR touches (passes 3/3 locally on `main`) | — | — |
| 12 (08:15 UTC) | `52d4ae4` | #149 (recreate_e2e flake: one wait for idle + user line), #151 (cache_control for Gemini/Qwen/DeepSeek V3.2; `usage.cached`), #154 (cache_ttl 1h, Claude only), #152 (waited-for workers archived after the parent's turn; `changed` frames), #153 (chat doors: Discord/Slack polling in, reply out; tokens protected in the secrets store; not verified against real Discord), #155 (host-level agent-defs dir), #156 (finished job row keeps its output) | #150 (secrets scope + door redaction — compile error E0061 in kernel CI) | — |
| 13 (09:25 UTC) | `221ea4d` | #150 (secrets scoped by lineage, revoke by subtree, job deltas + subscription output redacted), #157 (agent follows the PRs it opens), #159 (goal subscriptions), #160 (ask wait:false), #161 (Escape/⌘W close Settings), #163 (say mode=stop, descendants only, jobs killed via the jobs API), #164 (run --no-prompts: deny approvals), then #158 (protected files ask in every mode) and #162 (subscription continuity) after CI re-runs — both had failed on `standing_pass_e2e::rewound_arrives_before_the_file_restore`, a timing wait neither touches. Text conflict #157/#159 in `PROTOCOL.md` (both kept); semantic conflict #157/#162: the PR-follow `Subscription` literal lacked the new `continuity` field — fixed in `serve.rs` (`continuity: false`) | — | — |
| 14 (10:20 UTC) | `0bc74c9` | #168 (rewind cut written temp+rename — the flake behind #158/#162), #170 (e2e settle sleeps → polls), #171 (data_policy deny/zdr; free-endpoint tag), #172 (status tool/frame/derived line), #165 (containment: metadata/runtime/credential reaches ask in every mode; merged after resolving `preflight` so the protected-file ask and the containment ask chain, and the PROTOCOL.md lines) | #166, #167, #169 (conflict with `main` in `protocol.rs` + `hooks.rs`/`host.rs`/`check.rs`; GitHub runs no PR CI on a conflicting head, so no green to judge — asked for rebases) | — |
| 15 (11:30 UTC) | `9bb8793` | #166 (/mode pins a skill), #167 (child_model), #169 (result spill to results/), #173 (Mac release polish: icon, embedded kernel, signing/notarize targets, macos-dmg job that skips without the cert secrets), #175 (Cursor's form: worker status lines, link chips, dark default), #176 (spoken lines marked for the model), #177 (Fn dictation instant), #178 (permissions sheet modal). Conflicts resolved: PROTOCOL.md twice (both kept); #169/#176 both appended a test module to `project.rs` (both kept); #176/#177 both plumbed `channel` through the desktop Prompt — took #177's (adds `device`, `Prompt::dictated()`), dropped #176's unused `spoken` field | #174 (mode chip; conflicts, stacked on #166's old head) | — |
| 16 (11:40 UTC, priority) | `63359fa` | #180 (mine: `macOS (check kernel + desktop)` job on `macos-14` in `ci.yml` — now part of the green criterion; plus the one-line `unsafe` fix), #181 (Mac fixups: same compile fix, Notifications `Unavailable` outside /Applications, ScreenCaptureKit capture probe, dictation waits for the final, latency phase reset). Conflict in `permissions.rs`: took #181's SCK probe. The merged tree ran the macOS job green via #180's branch before the push | — | — |
| 17 (12:05 UTC) | `73ad619` | #182 (voice phase stall guard re-armed by every audio frame; Notifications reason names the signed build; all three CI jobs green incl. macOS) | #179 (SWE-bench cycle 4; conflicts in `batch.rs`, checks stale), #174 (mode chip; still not rebased) | — |
| 18 (13:10 UTC) | `52cb63a` | #174 (mode chip beside the model picker; slash commands are not chat titles), #179 (SWE-bench cycle 4: `mechanism` argument recorded in `mechanism.md`, refusal opt-in via `ARBOS_MECHANISM_REQUIRED`, on by default in the harness only) — both rebased, all three CI jobs green | — | — |
| 19–20 (14:00, 15:00 UTC) | `52cb63a` | — | #183 (visual polish cycle 1; green on all three jobs, but the author keeps it draft until the `ui_pass.py` gate runs with the store back — waiting for ready-for-review) | — |
| 21 (16:00 UTC) | `52cb63a` | — | #183 still draft (author's gate pending) | — |
| 22 (17:05 UTC) | `2434332` | #183 (visual polish cycle 1; left draft once the author's `ui_pass.py` gate ran: 92 pass, 1 pre-existing fail; all three CI jobs green) | — | — |
| 23 (17:20 UTC) | `a9557ba` | #184 (visual polish cycle 2; retargeted from #183's branch to `main` at the coordinator's request, CI re-ran green on all three jobs, author's gate 93/0) | — | — |
| 24 (18:10 UTC) | `53c37c5` | #185 (kernel polish asks: plan add de-dups, archive retires the worker's page row, slug names as words, one-line `status:` prose sets the live line, nudge `reason`), #186 (SWE-bench cycle 5: `bash repro:true`, `ARBOS_REPRO_REQUIRED` gate off by default, `changes` re-runs recorded reproductions) — both green on all three jobs | — | — |
| 25 (19:05 UTC) | `2afde2f` | #187 (Cursor symmetry cycle 1; gate 94 pass / 1 pre-existing flaky fail; all three jobs green) | #188 (symmetry cycle 2; draft, was stacked on #187 — retargeted to `main` so CI runs against it; waits for ready-for-review) | — |
| 26 (20:05 UTC) | `6f6e50e` | #188 (Cursor symmetry cycle 2; Commit & Push only prompts the agent, git reads only; gate 93/1-flaky; all three jobs green) | #189 (symmetry cycle 3; draft, retargeted from #188's branch to `main`) | — |
| 27 (21:15 UTC) | `e6e8be1` | #189 (symmetry cycle 3), #190 (coordinator gets `bash`/`terminal`/`secret` for one quick command — a softening of the 'coordinator runs no commands' enforcement; the contract still says edits/builds/tests are a worker's; guards apply; flagged to the coordinator). #190's first CI failure was `archive_children_e2e::archiving_a_worker_retires_its_row_on_the_project_page`, which also fails 1 in 3 on `main` (#185's new test) — re-run green | #191 (symmetry cycle 4; draft, retargeted to `main`) | — |
| 28 (22:10 UTC) | `3fb48c1` | #191 (symmetry cycle 4), #192 (archive page-row e2e polls the row — the flake flagged on #185), #193 (symmetry cycle 5; retargeted from #191's branch to `main` before the merge) | #194 (symmetry cycle 6; draft, retargeted to `main`) | — |
| 29 (23:10 UTC) | `0eadd1d` | #195 (coordinator answer contract; lone worker edits in place, worktree only for 2+; `[done]` carries the worktree state) | #194 (symmetry cycle 6; green, still draft) | — |
| 30 (00:20 UTC, Sep 15) | `e981f67` | #194 (symmetry cycle 6), #196 (coding-task contract paragraphs dropped from the coordinator's prompt; first tool call is spawn), #197 (qa-035: worktree root admitted to the place's `.arbos/` except other worktrees; parent checkout still refused; root-owned files checked against the place), #198 (say title/rename, spawn base, placement + store rules). #196/#198 conflicted in the coordinator paragraph of `prompt.rs` and `PROTOCOL.md` — composed both (note: markers inside raw-string prompt constants compile, so a test run does not catch them; checked by grep before committing) | — | — |
| 31 (01:50 UTC) | `12a0758` | Process-parity stack #200–#207 (helper kinds + area coordinators, `todo`, `kind=chat` subscriptions, page archive/tldr/PR-on-retire, missing-link wake, user store in Cursor's shape, context inventory, `agents`/`transcript`/`delete` tools — `delete` is one file through `resolve_write` with the protected-file ask; each retargeted to `main`, merged in order), then #199 (symmetry cycle 7, two chat styles; its red kernel job was `follow_prs_e2e::a_merged_pr_ends_its_subscriptions`, a desktop-only PR — re-run green, passes 3/3 locally). The stack rewrote the coordinator paragraph at every step: took the stack's text each time and re-inserted #196's two sentences at the end (pinned by #196's unit test) | — | — |
| 32 (02:55 UTC) | `5c94f6a` | #210 (follow_prs e2e flake fix), #208 (spend cap: `[spend] cap_usd`, `spend.toml`, 80 %/cap notices, workers/spawn refused past the cap while the user's own words still run), #211 (`pr` tool over gh — create draft/update/comment/resolve/ci/status/labels; never merges; artifacts pushed to an `arbos-artifacts` branch), #212 (`screenshot target:text` via headless Chrome; Show line no longer suppressed by the word 'capture') | #209 (symmetry cycle 8; draft) | — |
| 33 (03:50 UTC) | `318b57a` | #209 (symmetry cycle 8: Working card with Stop All, todo checklist card, scroll-follow fix; left draft), #213 (qa-033: `say` to an archived worker names when it finished and where its report is), #214 (git guard refuses `git commit` on a protected branch; kickoff rules say fix on a branch + draft PR), #215 (empty page template named in the prompt; whole-page `write` keeps front matter + context link), #216 (status prompt describes the shape, no copyable phrase) | — | — |
| 34 (04:15 UTC) | `06367f4` | #217 (git guard judges later segments in the directory a `cd`/`pushd` moved to; found by the kickoff benchmark on `318b57a`) | — | — |
| 35 (05:20 UTC) | `dd06687` | #218 (qa-035: `Subscription::coerce` — timer+cmd → shell; notify without `{output}` completed) | #219 (Show step + image-owed nudge; its own `show_nudge_e2e` fails on the runner, both cases — likely the headless-Chrome-in-container issue #126 hit) | — |
| 36 (06:25 UTC) | `3fff013` | #219 (Show as a numbered kickoff step; one image-owed nudge at turn end; `screenshot target:text` now renders through the browser hub's CDP with the one-shot Chrome as fallback — the runner fix; stale `arbos-textshot-<pid>` temp dirs swept only when the pid is dead) | — | — |
| 37 (07:30 UTC) | `750abb4` | #220 (`bash::build_or_test`: a coordinator's build/test runner command is refused before the approval prompt — the kernel-side enforcement for the #190 concern flagged in batch 27) | — | — |
| 38 (08:30 UTC) | `984df70` | #222 (brief `Output:` paths owed as files, one `output owed` nudge at turn end; coordinator links a file instead of restating it) | #221 (worker-chat kernel asks: settled `thinking` record with secs, `Wake.brief`; compile error E0063 on `main`), #223 (symmetry cycle 9; draft, stacked on #221) | — |
| 39 (09:20 UTC) | `1d2f57d` | #221 (settled `thinking` record with `secs`; `Wake.brief`; desktop guards) | #223 (symmetry cycle 9; retargeted to `main`, conflicting — rebase asked) | — |
| 40 (10:20 UTC) | `25197c1` | #223 (symmetry cycle 9: settled thoughts + brief on reopen, panel row summaries, row ring, replayed root reads like live). The `main` macOS job at `1d2f57d` failed on a crates.io index download (runner network); re-run green | #224 (symmetry cycle 10; draft, green) | — |
| 41 (11:30 UTC) | `f1c850b` | #225 (Jacob's 2026-09-15 decision: `permission = auto` by default asks nothing — the #158 protected-file ask and #165 containment ask now fire only in ask mode; hard refusals stay (git guard, metadata fetch) plus a new outright refusal for wipe/sudo/mkfs; approval cards resolved by `answer` frames, unknown words deny; protected-file redirect detector distinguishes reads; bash `description` → `label`) — a policy loosening by the user's decision, flagged in the report | #224 (symmetry cycle 10; draft) | — |
| 42 (12:40 UTC) | `c6e7698` | #226 (Jacob's Mac findings: Project page back link, opener `~`, new-Project view, pills only for a real repo and small trees, prose-size work lines, bubbles, root's own commands shown), #224 (symmetry cycle 10: approval row Skip · Always Run · Run ↵, idle Agents pill, ↓ disc, thoughts merged across status) — conflicted with #226 in `transcript.rs` on adjacent helper fns, both kept; #227 (kickoff turn on first open: one bounded root turn, spawn/ask refused during it; bookkeeping never asks) | — | — |
| 43 (13:10 UTC) | `c6e7698` | — | #228 (symmetry cycle 11 — live kickoff turn, Mac worker's seven findings, persisted window frame; draft, was stacked on #227 → retargeted to `main`) | — |
| 44 (14:45 UTC) | `18d1dea` | #228 (symmetry cycle 11: live kickoff turn, Mac findings, persisted window frame), #229 (remote kernel install/update over ssh: semver rule, release tarball with checksum, stop script signals the kernel.json pid once with TERM — unit test pins no pkill/killall/group), #232 (keyless kickoff does not count, readonly spawn with Output refused, watched bash runs to its end). #228/#229 both wrote remote connect progress in `desktop/src/kernel.rs`: kept #229's `Progress` map, left `connect_step(host)` as a view over it for #228's callers | #230 (draft; retargeted to `main`), #231 (iOS cycle 1; draft) | — |
| 45 (15:35 UTC) | `848e003` | #233 (project face in the hub roster and on `hello`; tab-sheet save keeps `[root]`/`[spend]`), #234 (qa-036: plain GET on the attach port → 200 health JSON or 426; auth reported as mode only) | #230 (draft, conflicting — rebase asked last pass), #231 (iOS cycle 1; draft) | — |
| 46 (16:30 UTC) | `b17cccb` | #230 (remote places: kickoff waits for the `provider` frame's key; repeated notice reads once — rebuilt on `main` after the #228/#229 resolution), #235 (stray `agents/` folder without `agent.md` named in the log, not in the tree). `main` kernel job at `848e003` failed once on `standing_pass_e2e::rewound_arrives_before_the_file_restore` (`[]` after the 5 s poll; 4/4 locally) — re-run green; noted on #168 | #231 (iOS cycle 1; draft) | — |
| 47 (17:35 UTC, on request) | `28291de` | #236 (done wakes fold reports; PR link must come from a tool; repeated reply ends the turn), #237 (remote root's status/turn frames mirrored under the local child; `RemoteHub.running`), #238 (hub roster marks worktree places with their parent), #239 (remote kernels leashed `--leash 10m`, stopped with the parent via the one-pid TERM script, `spawn wait=true` waits for a remote report; `forget` now also clears `RemoteHub.running` — the coordinator's one-liner, added after the #237/#239 test-module conflict), #241 (approved policy: no git identity → commits as `Arbos <unarbos@users.noreply.github.com>`, guard no longer asks for an author), #231 (iOS cycle 1, `ios/` only) | #240, #242 (symmetry cycle 12, iOS cycle 2; drafts) | — |
| 48 (17:45 UTC) | `9f552c5` | #240 (symmetry cycle 12: steer inside its turn, typed option = pick once, ask call no row; gate 98 pass) | #243 (remote transcript mirrored mid-turn; conflicts with #237/#239 in `remote.rs` beyond the promised one-liner — four hunks incl. three test modules; asked for a rebase rather than resolving the relay semantics blind), #242 (iOS cycle 2; conflicting with `main` after #231) | — |
| 49 (18:55 UTC, on request) | `42ef648` | #243 (rebased by hand; remote transcript mirrored every 3 s), #244 (background:true honoured for servers/watchers only), #245 (spawn-first in the coordinator's tool descriptions), #247 (`step` on assistant/thinking/tool lines and deltas), #248 (max_children 24/256), #249 (correction-not-kept nudge), #250 (`github_prs` kind), #251 (mesh store addresses, `[share]`, per-recipient access), #242 + #246 (iOS cycles 2–3). VM was rebuilt before this pass (op, apt deps, target caches gone; reinstalled). One semantic conflict: #243's relay tests lacked #247's new `step` field on two `Assistant` literals — fixed; 554 tests green, desktop check clean | — | — |
| 50 (20:10 UTC) | `6b66ce9` | #252 (step pairing, desktop half), #255 (batch timestamps strictly increasing), #256 (voice.rs import cfg), #258 (opener Open row), #253 (coordinator's order, draft waived: dev-channel publishing on green `ci` on `main` with a serialising concurrency group, stable channel from tags, `arbos-update` crate, bundle outputs under `target/bundle.noindex`; secrets flow env→file→keychain, nothing printed) — 588 workspace tests, 50 desktop | #259 (update bar; retargeted to `main`, macOS E0433 `Path`), #254/#257 (mesh read/write + briefs; red — `RunCx.step`) | — |
| 51–52 (20:15–20:45 UTC, on request) | `3f76ec3` | #254 (mesh read/write by `arbos://` address; hub enforces access, private by default from the second user), #257 (remote child briefs by address, deliverables into the parent's store; retargeted to `main`), then #259 (bottom bar + blue Update button; installer is two renames, leftovers `lsregister -u` then removed, open places' kernels get one SIGTERM by pid from kernel.json; draft waived on the coordinator's order; all three jobs green after the `Path` fix). dev-channel published once per `main` push so far (`6b66ce9`, `907c6a5`) — no double publish | — | — |
| 53 (20:30 UTC) | `e8bd1b4` | #260 (iOS cycle 4: `ios-testflight.yml` reusing the notary secrets, reply drawn once via #247's step, per-project faces; `ios/` + workflow only, no Rust) | — | — |
| 54 (21:20 UTC, on request) | `181b657` | #262 (setup script: iOS ASC key secrets, both `.p8` keys named explicitly; feed TestFlight link), #261 (TestFlight workflow on the iOS key; skips with a notice until secrets exist; its kernel job hit the `standing_pass_e2e` runner flake once, re-run green), #263 (update-key.pub gets the public half; draft waived on the coordinator's order). First dev-channel publish that signed: run 35023316187 → `NOTARIZED: true`, stapled, `dev` pre-release holds `Arbos-0.2.0-879-macos-arm64.zip` + Linux tarball + `arbos-dev.json`; one run per push, no double publish | — | — |
| 55–56 (21:19, 22:20 UTC) | `181b657` | — | #264 (bionic-reading leak from an old `state.toml` default; green, draft) | — |
| 57 (22:25 UTC, on request) | `371445e` | #264 (draft waived by the coordinator: `State.version = 2`, an unversioned `bionic_reading = true` is dropped as the old default; author asked to mark PRs ready in future) | — | — |
| 58 (23:30 UTC) | `de5bf88` | #265 (one build number stamped by the packager; a failed update check reads as failed), #266 (Inter 4.1 bundled under OFL 1.1 with its licence, named in the theme on every platform) — 612 + 50 tests | — | — |
| 60 (00:20 UTC, Sep 16) | `20470c5` | #267 (prose 13 px in Inter; gate 106 pass) | — | — |
| 61 (01:40 UTC) | `a146c41` | #270 (`put` with base64 bytes → `.arbos/attachments/`, path confinement + 20 MB cap tested), #271 (Dice ≥ 90 % near-duplicate reply guard), #272 (`history before`), #268 + #269 (iOS cycles 5–6; #269 draft but body finished — merged under the new rule) | #273 (attachments as bytes on remote places; macOS E0063 `acp::Session.remote`; retargeted to `main`) | — |
| 62 (02:40 UTC) | `0a667bd` | #273 (attachments as bytes on remote places; the `acp::Session.remote` test literal fixed), #274 (iOS cycle 6b), #275 (hub relays attach frames as raw JSON, unknown fields kept) — 620 + 50 tests. dev-channel: still one run per push (two runs at 01:32/01:35 were for two pushes); but old build assets accumulate on the `dev` tag instead of being replaced — noted on #253 | — | — |

Left open on purpose (until 2026-09-14 ~08:30, when someone else closed them): #55 (sidebar search — moot with the tabs layout), #95 (draft, diverged after its fixes landed via integration).

Local check before each push: `cargo test --workspace --locked` and `cd desktop && cargo test --locked` on the merged tree (batch 1: 365 + 45 passed).
