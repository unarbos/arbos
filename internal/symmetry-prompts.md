---
cursor:
  subagentId: "bc-2a1318aa-e675-52f4-b3ab-94cb9415aa39"
---

# Cursor symmetry loop — prompt catalogue

The running list of prompt kinds for the "Arbos chat looks exactly like Cursor's" loop. Each cycle takes kinds not yet covered; once every kind is covered the list starts again, and earlier kinds are re-run now and then for regressions. Rule from Jacob (2026-09-14): where his Mac stills (`media/cursor-reference/chat-2026-09-14/`) and the Linux Cursor build differ, the Mac wins.

Both apps run on the Linux VM: Cursor 3.20.21 (Agents window, dark, sidebar hidden, local project `/tmp/parity-proj` on `master`, Auto model) and Arbos (`main` or the cycle branch, dark, GPT 5.4 Mini via OpenRouter), the same 1440×900 frame. Harness: `/tmp/polish/sidebyside.py` on the VM (stills at t+2 s, t+8 s, t+20 s and idle; a 12 s recording of the working state where a still cannot show the motion). Pairs: `media/cursor-reference/cycle-N/<tag>-<prompt>-<moment>-pair.png` (Cursor left, Arbos right), singles beside them, recordings as `*-working.mp4`.

## Scope (widened 2026-09-15, Jacob)

The loop covers the whole app as a new user meets it, side by side with Cursor, not only the transcript. Each cycle walks this list and records what differs (pixels or behaviour) in `internal/symmetry-findings.md`:

1. Launch and first run: window placement (a saved frame off-screen), the permissions sheet, the Home tab.
2. ⌘T and the folder picker: `~`, `~/`, a partial name, a non-existent path (Create), a folder of repos (`~/Code`), a non-repo folder, an iCloud path (`~/Library/Mobile Documents/…`), a remote host.
3. What a new project lands on (the new-Project view, never the Project page).
4. The Project page and every way in and out: panel header, ⌘2, "View Project Page"; Back to chat, Escape, ⌘1, the tab.
5. The panel: every row and click (project head, agents, archived, Project section, Files, Standing, the gear, search, +).
6. Tabs: cycle (ctrl-tab, ⌘⇧[ ]), close, reopen through the opener, two named coloured tabs, one spinning.
7. Settings: every section, open and close, ⌘, and Escape.
8. The mode chip and `/mode`; approval and ask flows in each mode (auto, ask, plan).
9. Under-composer pills on a repo vs a non-repo folder (branch, machine; Changes, Commit & Push, PRs, Working/Agents).
10. Ask / answer cards; approval cards; the refused-answer shape.
11. Reopen after relaunch: root and worker chats read like the live ones.
12. Resize and small windows (900×600), the panel folded; light theme.

Behaviour is in scope: when Arbos does something Cursor would not (asks, narrates a command before running it, refuses an answer, writes a file on a read question), it is a finding and a features-inbox note the same turn.

## Cold track

Every prompt kind below also runs "cold": a fresh place (`/tmp/cold-<id>`, no `.arbos`, a `git init` for the repo kinds), first message, no history — where Jacob meets things. Harness: `sidebyside.py` with `SBS_PLACE=/tmp/cold-<id>` (fresh store each run). Cold frames carry the tag `cold-<id>`. The kickoff view is the cold frame's start state.

## Mac measurement

Type and spacing are measured on macOS Retina, not only Linux. Each cycle a capture script goes to the Mac worker (bc-b337f0b1) through the repo's `qa-results` branch — `qa-results/inbox/cycle-N/capture-script.md` and a copy of this catalogue — and its 2x stills come back under `media/mac/cycle-N/` on the same branch; the layout worker copies them into the store's `media/mac/cycle-N/` and diffs them against the Linux frames. Anything that differs by more than 1 px at 1x is a finding. The Mac worker's rig: the `main` build, the test place `~/arbos-fresh/places/demo`, the driver socket for scripted clicks.

## Remote track (from 2026-09-15, Jacob)

The loop also tests remote places, cold and warm, on ArbosLife and Templar (SSH via the agents' key in the 1Password Arbos vault; aliases `arboslife`, `templar` in the VM's `~/.ssh/config`). Scratch folders only: `~/arbos-qa/<cycle>/…` under the SSH user's home; never other users' processes, never Jacob's validator or subnet dirs, no sudo, no kill of anything the loop did not start; hosts and keys redacted in stills and logs (the alias may show, the address never). Cursor's Agents window has no SSH-remote flow (its Workspace picker offers repos and the cloud), so remote pairs are Arbos before/after unless a Cursor Remote-SSH IDE frame is the better reference.

1. ⌘T → machine → a scratch folder; no `arbos-kernel` there → the app installs it (same os-arch: scp of its own binary; else build) and connects, with a visible "Installing Arbos on <host>…" state; a second open is instant.
2. Remote kernel older than the desktop → "Updating Arbos on <host>…" before connecting; same version → no update; newer → still connects.
3. A normal prompt on the remote, a worker spawned there, a steer, reopen after relaunch; the SSH connection killed mid-turn and the reconnect watched.
4. The remote binary removed between runs to force the install path again.

Media: `media/remote/cycle-N/`. Kernel gaps (install/update over ssh, version handshake, reconnect) go to the features inbox with the failing step; the desktop owns how the states render.

## Coverage (rotating; Jacob's standing order 2026-09-16)

One row per app area. "Last" is the last cycle that looked at it side by side (or the gate that exercises it every run). Anything older than three cycles is due. The Linux rig is the rig (Jacob, 09-16); what Linux cannot show is listed in `symmetry-findings.md` under "Untested by decision".

| area | last | how | due? |
| --- | --- | --- | --- |
| chat: Project style (root) | 81 (a root turn of two reads and prose: *Worked 27s ›* shut, *Explored 2 files* open — Cursor's words; no Thought row from Flash) | `journey.py` beside `cycle-23/` | — |
| chat: classic style (worker) | 82 (a worker's own ask card, Skip: F-258 the kernel's skip line drew as the person's bubble; *Worked for 3s* holds across a relaunch) | `worker_live.py`, `cycle-47/worker-live/` vs `cycle-24/cursor-worker-chat-open.png` | — |
| long-form project (many turns, notes restructure, workers come and go, archived, context re-found, scroll) | 86 (d52: the coordinator's fence refused the write, two workers wrote the file — *Done · … — # Sources*; the d51 place itself was wiped by F-263 before the drive) | l1–l13, d1–d19 | — |
| kickoff / new-project landing — the first thirty seconds, refused and silent providers | 81 (a 60-file tree: the same one-command kickoff, 13 s; a key swapped mid-kickoff changes nothing — F-189's per-turn read) | `cold.py` f1/f2, `new-project-kickoff` gate row | every cycle from now |
| launch, first run, permissions sheet | 80 (a true first run: shimmer, the Home face sheet, the permissions sheet, filled screen by design; F-231 stands — the renamed place is dropped in silence) | gate `launch` phase (Linux rows only) | — |
| tabs: cycle, close, reopen, colours, spinner | 79 (eight tabs at 900 wide fit at 96 px each, ⌘⇧[ ] cycle; no overflow control past that — F-167's metaphor) | `tabs_drive.py`, gate `tabs` phase | — |
| opener: ~, partial, create, prefix sibling, remote host, recents | 80 (the gate's twelve opener rows pass on this build; F-250 the `~//` path) | #258, gate opener rows | — |
| Project page: in/out, back, Escape, ⌘1, files grid, notes render | 85 (a page item opens its file as a panel tab; every file read *· unsaved* at open — F-262) | gate `project-page` rows | — |
| panel: agents tree, archived row, Project section, Files, Standing | 83 (F-257 re-driven with the panel open — rows held; stays open on d47's stills) | gate `panel` phase, `cycle-40/panel/` | — |
| settings: every section, ⌘, / Escape / ⌘W, typography stepper, bionic toggle + eyesight check | 86 (the Model section: three provider chips, key field with Save/Paste, eight picks; *flash* narrows to twelve; the key paste is out of the driver's reach — R45) | gate `settings` phase (`weight-visible`), `cycle-45/settings/` | — |
| mode chip, /mode, approval + ask cards per mode | 86 (`/mode` ↵ ran `/model` — F-264, the picker's exact match now first; `/mode` alone → *No mode is pinned…*; `/mode auto` when already auto says nothing; the hand-off line F-265) | `askmode41.py`, gate `ask-*` rows | — |
| under-composer pills: repo vs non-repo, Push, PRs, Working/Agents | 86 (the Agents pill's click opens the Agents card over the composer — rows, ✕ — holds; the Working count with two workers not re-driven) | gate `pills` rows | — |
| bottom bar (version, Update) | 86 (Stable→Dev on this box: the plate stays *unreachable* — no release feed here; the switch's effect cannot be read on the rig) | Jacob's still `media/mac/update-bar/`; gate has no row | — |
| search (⌘K chat search palette) | 85 (a Chats hit with ↵ lands on that chat and closes the palette — holds) | gate `search-by-content`, `search-open-hit` | — |
| themes: light, dark; tint | 83 (the ask card, the skipped row, the compaction line and the Asking line in light — hold) | cycle-11 `13-light`; gate `appearance-*` | 15 |
| window sizes: 900×600, panel fold, 1600×1000 | 84 (1600×1000 with the panel: panel 591 px, the chat column 708 px shifted left from x 446 to 150 — the column keeps its width, Cursor's shape) | `cycle-43/sizes/` | — |
| remote places: install/update/reconnect states, kernel version handling, attachments as bytes | 84 (Try Live on a display-less remote: F-259 the strip's words; the keyless bar's *This session only* lends the key and the agent answers — F-260 one line for it) | remote track scenarios 1–2; 3–4 open | 16 |
| voice / dictation entry points (Fn, mic button, call strip) | 84 (the mic note's red measured: `#d95e53` on `#212121` — the theme's danger; Cursor's not-available notes are muted grey — F-192's number for Jacob) | gate `composer-voice` row; Fn via driver | **15** |
| relaunch / replay (root + worker read like live) | 85 (a worker mid-command across a relaunch: *Working · Calling bash* and Stop from the step file, no card — F-252's limit holds for a child too) | `journey.py` J11 | — |
| rewind / fork / checkpoint | 80 (rewind beside a worker on a git place: chat cut, composer refilled, INDEX.md restored, the worker to the archived fold; a fork's kickoff fold — F-255) | `rf.py`, `fork_sub.py`, gate `fork-beside-source` | — |
| notices, nudges, refused-answer shape | 81 (compaction live: F-226's pair holds; F-256 the result line's words) | refused `write` on the att place | — |
| composer attachments: file chip, image thumb, hover ×, mid-send bubble | 84 (no image paste — F-261, open; the rig cannot paste at all, even text, by driver or real X key) | `attach_drive.py`, `cycle-39/attachments/` | — |
| **after-failure states — what the window says when the world outside it changed**: folder moved or renamed under a running kernel (QA `af-03`), kernel binary replaced or deleted under a running process (#372/#385 stranger control), key removed mid-session, disk full, tunnel cut (F-81), hub gone | 33 (phase D: folder moved, deleted build — F-134, F-136; key removed / disk full / hub gone still no row) | a `phase_world` in the gate: rename the place, replace the kernel binary, cut the socket — then read the notice, the typed line, the bar, side by side with Cursor's equivalents where it has one (Cursor: a removed workspace shows "folder not found" on the project row) | **33** |

## Kinds and their state

| id | kind | prompt (both apps, verbatim) | covered | cycle | notes |
| --- | --- | --- | --- | --- | --- |
| p1 | one-line answer | Reply with exactly: The quick brown fox. | yes | 1 | footer, headline, bubble |
| p2 | code answer (code blocks, bullets, thought line) | hello write bubble sort | yes | 1 | Cursor edited a file instead; still useful |
| p3 | status question | what are we doing right now? | yes | 1 | agent-link chips on the Mac stills |
| p5 | small multi-file edit | Add a mul(a, b) function to math_utils.py and call it from main.py with mul(4, 5). | yes | 2 | Changes pill, Files Changed card |
| p6 | long-running shell command, streaming output | Run this exact shell command and show me its output as it arrives: `for i in 1 2 3 4 5 6; do echo "step $i"; sleep 2; done` | yes | 3 | recording of the working state |
| p7 | failing test, then the fix | Write tests/test_math_utils.py with a failing test asserting add(2, 2) == 5, run it, show the failure, then fix the test so it passes and run it again. | yes | 3 | error and retry path |
| p4 | sub-agent fan-out | Use parallel sub-agents: one reviews math_utils.py for edge cases, one writes docstrings for every function, one drafts a CHANGELOG.md. Then merge their results. | yes | 3 | working lines, worker chips |
| p8 | long answer with headings and a table | Explain the difference between lists, tuples, sets and dicts in Python. Use headings for each, one comparison table, and finish with three bullet recommendations. | yes | 3 | headings, table, bullets |
| p9 | cancel mid-turn | (p6 sent, Stop pressed 5 s in) | yes | 3 | stop button, interrupted line |
| p10 | ask / clarification | Before doing anything, ask me one multiple-choice question with two options, alpha and beta, about which name to use for a new module. Wait for my answer. | yes | 4 | question card |
| p11 | plan-mode request | Make a plan, do not write code yet: how would you add a command-line interface to this project? List the steps. | yes | 4 | plan card / mode chip |
| p12 | multi-file refactor | Rename the function `sub` to `subtract` everywhere in this project, keeping behaviour, and run main.py to check. | yes | 4 | many edits, Files Changed rows |
| p13 | follow-up steer while working | (p6 running; 3 s in send: "Also print the date at the end.") | yes | 4 | steer card |
| p14 | rewind / checkpoint restore | (after p12: restore the checkpoint before it) | yes | 4 | Cursor's ↺ on the prompt card did not reveal on hover of the pinned card — retry restore icon vs rewind |
| p15 | git commit and push | Commit all current changes with a clear message. Do not push. | yes | 5 | Commit & Push pill |
| p16 | web search | Search the web for the current stable Python version and tell me the number with a source link. | yes | 5 | search tool card, source chips |
| p17 | MCP / tool cards | (tool with a card: browser open, terminal) Open a terminal and run `python3 main.py`. | yes | 5 | tool cards |
| p18 | image / file attachment | (attach media/parity/cursor/public-docs-agent-overview.png) What is in this screenshot? | no | | attachment chip in the bubble |
| p19 | very long thinking turn | Think carefully, step by step, about the fastest sorting algorithm for 10 nearly-sorted integers, then answer in one line. | yes | 5 | thinking fold, "Thought for Ns" |
| p20 | Project / Context panel after a busy session | (after p4 + p12: open the Project page) | yes | 6 | panel vs Cursor's right panel |
| p21 | error path | Run `python3 does_not_exist.py` and tell me what happened. | yes | 5 | failed tool card, retry |
| p22 | coordinator shell — regression check (after #190 lands on `main`) | (a) Run `ls -la` and tell me how many files there are. (b) Run the test suite with `python3 -m pytest -q` and report the result. | yes | 6 | (a) must run in the coordinator itself: a `bash` tool row under the turn, no worker spawned; (b) must spawn a worker (a worker line / panel row), never the coordinator's own shell. Drift on either → `features-inbox/`. Re-run every third cycle. |

## Style track (from cycle 7)

Cursor has two chat styles — the Project chat (root) and the agent chat (a delegated subagent or a classic chat); `docs/project-chat-vs-agent-chat.md` says what each shows. From cycle 7 every prompt kind is captured in both styles: the prompt goes to Cursor's Project *Parity* (No Repo, cloud) and to Arbos's root; the first delegated worker is then opened on both sides. Pairs carry the tag `s<N>`: `s1proj-root-pair.png` (Project chat vs Arbos root), `s1proj-worker-pair.png` (subagent vs Arbos worker). Cost note: the Project side runs on Jacob's Cursor cloud, so it takes the short prompts (pp1, pp2) and one edit kind per cycle, not the whole list.

| id | kind | root style covered | worker style covered | notes |
| --- | --- | --- | --- | --- |
| pp1 | one-line answer | 7 | — (no worker) | |
| pp2 | three-worker fan-out | 7 | 7 | Cursor: workers in the Working pill's popover, the report folded into the woken turn; subagent composer disabled |
| pp3 | todo checklist (root) | 8 | — | Arbos: card from the `todo` echoes; Cursor TodoWrite still to capture live |
| pp4 | titled steer to a worker | 8 | — | title shows as the worker's live line until its first `status`; model copies the protocol example at once (kernel note filed) |
| pp2-reopen | fan-out, then relaunch and read back | 9 | 9 | root and worker after a relaunch must read like the live ones |
| p22a | read-only shell question ("Run ls -la and tell me how many files there are.") | 12 (Project vs root) | — | Cursor Project chat "Worked 13s › … 5 items"; Arbos "Worked 6s ⌄ / Ran List directory contents / There are 5 files" |
| p23 | read-only question, no run ("In one sentence, what does main.py do? Do not run anything.") | 12 | — | Arbos: Explored main.py, one sentence; Cursor frame stale (F-44) |
| p24 | multi-step turn: prose, tool, prose, tool, prose in one turn ("Say the single word first. Then run `ls`. Then say second. Then run `pwd`. Then say third.") | 13 (step pairing) | — | Arbos: first / Ran List current directory contents / second / Ran Print current working directory / third — one line per step, nothing doubled (`cycle-13/p24-multi-step-turn-after.png`); Cursor Project chat folds all but the last |
| m1/m2 | tool markup written as prose: a markup-only reply; prose then markup ("Reply with exactly this text … `<function_calls><invoke …>`") | 14 | — | Arbos: no bubble / prose only (#279); Cursor has no equivalent to capture — it never writes markup as prose |
| l2 (re-run) | fan-out with worker reports — per-wake Worked segments | 15 | 14 | Arbos now: a `Worked Ns` per report with the worker's chip line under it (#292) |
| r1 | read-only explore worker beside a writer | 15 | — | mark + kind chip (#288); Cursor draws no distinction |
| f1 | fresh project, provider refuses (cold place, `openai/gpt-5.4-mini` on the blocked key): the first thirty seconds | 16 | — | Arbos: `Switched to … for this turn.` + Details, then the greeting (#298) |
| f2 | fresh project, provider silent (cold place, api_base at a socket that never answers) | 16 | — | Arbos: shimmer with clock at 20 s, hint at 60 s, never the greeting (#298) |
| d12 | long-form: the notes restructure as one turn (after d1–d10 on the research place) — "The project page has grown in pieces. Restructure notes.md yourself, no workers: sections Goal, Decisions, Done, In flight, Open questions, Next — keep every fact and every link, drop nothing, merge duplicates. Then tell me in the chat, as a short list, what moved where and what you merged." | 34 (`cycle-34/d12/`; Worked 9s, doc chips per moved document, page re-sectioned; F-156 from its panel row) | — | both apps; the Project page after, scrolled to top |
| d13 | long-form: sources audit across the research place's documents, one worker per document without a source (after d12) — "Which of the documents in docs/ cite a source and which do not? Read them, list each with yes or no, then give each document without one a Sources line — one worker per such document." | 35 (`cycle-35/d13/`; the model read `docs/answers/` not `.arbos/docs/`, no workers; say the path next time) | — | workers come and go; archived rows; page updated |
| d14 | long-form: d13 with the path said — "In this project's .arbos/docs/ folder (…): which documents cite a source and which do not? … one worker per such document." — plus a relaunch mid-run | 36 (`cycle-36/d14/`; three workers, Sources landed; relaunch not triggered) | — | F-170 |
| d15 | long-form: d14 + a relaunch while the workers run (fired on a child's `connection == live`) | 37 (`cycle-37/d15/`; F-172) | — | the fold's summary changes across the relaunch |
| d16 | long-form: fork the research chat, ask the fork one line, rewind the original to before d15 | 38 (`cycle-38/fork-rewind/`; F-174, F-175) | — | |
| pp5 | root's own quick command ("what is in this repo") | mac-fixes | — | Cursor: Running 1 command → Ran <description> ⌄ with the card; Arbos matched in the Mac PR (`media/cursor-reference/mac-fixes/repo-question-pair-cursor-vs-arbos-after.png`) |
| p5 | small edit | 2 (classic) | 7 (Arbos) | Cursor Project + worker edit still to capture |
| p4 | fan-out with edits | 3 (classic) | — | |

## The stale remote kernel (mesh sweep, 2026-09-17) — which cycles it touched

The kernel serving `arbos-remote/parity-proj--reply-with-hostname` on ArbosLife (pid 2739473) had run since **Sep 13 18:24 UTC** on a binary deleted from under it at **Sep 14 13:10** (the remote track's install replaced `bin/arbos-kernel`; the old process was left). Read from the place itself, not assumed:

- its transcript's last line is Sep 13 18:24 (the remote worker's brief, cycle 5–6's "reply with hostname" scenario);
- its `runtime/kernel.log` holds two `attach_open`s, the last at Sep 13 18:24, and was last written Sep 13 19:33;
- so **no client attached to it after the binary was deleted**. Nothing measured after Sep 14 13:10 ran against it.

**Affected cycles: none after the deletion.** The cycles that did use that process — the remote worker check of cycles 5–6 on Sep 13 — ran while its binary was current. Cycle 11's remote track (Sep 15) attached to its own fresh places on ArbosLife and Templar with the installed build, not to this worker place. Cycles 12–28 ran on the Linux rig against local kernels built from the branch under test; each PR's gate line names that kernel.

**Restarted** at 04:14 UTC on the on-disk binary: `arbos-kernel 0.2.0 52cb63a04075` (pid 513786, `kernel.json` agrees). That build is itself the Sep 14 install and behind `main` (`d73a25aea876`); the remote track's install path replaces it on the next attach from a newer app, and that is the next remote-track item (coverage row "remote places", owed since 16).

**Rule added to the pairing notes:** every cycle's "what the rig cannot see" note carries the kernel's own build line — `arbos-kernel --version` and `runtime/kernel.json`'s `git_sha` for each kernel the cycle attached to — read, not assumed. The rig audit gains R11: a process serving a deleted binary looks healthy and reports right numbers about the wrong build; check `/proc/<pid>/exe` for `(deleted)` on every remote kernel before a remote scenario.

## What the rig cannot see, per paired area (Jacob's question, 2026-09-17)

The stills are 1x captures of an Xvfb display with no compositor, read by a person or by the driver's geometry. Differences these physically cannot show, by area — so a "matches Cursor" here is a claim about what *was* visible, not about the whole:

| area | invisible to the rig | how it would be seen |
| --- | --- | --- |
| tabs, bars, panel — the one surface | **translucency and vibrancy**: `theme.vibrancy` materials render opaque on Xvfb (no compositor); Cursor's Mac window and ours both blur the desktop through the chrome on macOS. The bundled-font gap lived here too: hinting and weight at 2x | Jacob's Mac, or the Mac worker script at 2x; a still of the window over a busy wallpaper |
| panel / sidebar | **hover and focus rings** on rows (a still is one frame; hover states need the pointer parked); **scroll physics** (rubber-banding, momentum) | a recording with the pointer parked; the Mac |
| tab strip | **the native title-band join on macOS** (`sync_macos_chrome`) — Linux has no title band, so the one-surface fix is unverified exactly where he saw the seam | Jacob's still after Update, or the Mac worker |
| chat: folds, prose | **timing**: shimmer phase, spinner cadence, fade of the "Just now" clock; **selection colour**; **text rendering** at 2x | recordings, and a 2x device |
| themes light / dark | **colour accuracy**: Xvfb is 24-bit with no colour management; a tint that is wrong by a few percent reads as fine | a calibrated display, or numbers from the theme file compared to Cursor's CSS |
| notifications | the **OS banner's own look** (dunst is not Notification Center) | the Mac |
| bottom bar | the **update control's live states** (downloading, restart) need a real update channel; the rig sees only "version" and the stranger plate | a staged release |
| composer, settings key field | **the clipboard**: the driver's synthetic ⌘V / Paste does not reach the X11 clipboard (R45) — image paste (F-261) and the Model section's key paste are unverified here | the Mac, by hand |

Rule adopted with the rig audit (`internal/rig-audit-where-a-pass-can-lie.md`): every pairing note names its invisible column, and a finding that depends on one of these is filed as *needs the Mac*, not as matched.

## Cycle log

- **Cycle 26** ([#378](https://github.com/unarbos/arbos/pull/378), on #367): F-123 measured with its own scenario — the folds toggle, the gate's ruler (footer y) was wrong on a bottom-anchored transcript; rows are measured now; one case still open (a `work-*` over nothing drawn). Panel beside Cursor's sidebar: F-126, one gear. Tabs: decision memo `docs/worker-tabs-vs-breadcrumb.md`. d11 blocked by a keyless kernel the gate left on the long-form place. Gate 101 · 2.
- **Cycle 27** ([#381](https://github.com/unarbos/arbos/pull/381), on #378): F-123 closed — the rig clicked a fold scrolled out of view for three cycles; the driver now refuses clicks on clipped targets and the fold row picks one on screen. The rig audit `internal/rig-audit-where-a-pass-can-lie.md` (R1–R10; three fixed, seven open with next steps). Gate 109 · 0 · 20 · 34 · 7 — the first zero-fail line, and honest about it: `composer-context` is a dead control or a label, `work` a gap until the edit turn has a fold of its own (R10). d11 and themes carry to 28.
- **Cycle 28** ([#384](https://github.com/unarbos/arbos/pull/384), on #381): F-127 (a settled fold is shut, the newest too); R1 `seen()`, R5 `pass-after-recover`, R10's own-turn prompt (not enough yet), R11 the stale remote kernel (read from the place: no cycle measured against it; restarted on `52cb63a04075`); the "what the rig cannot see" table per area; d11 ran on a clean kernel. Gate 107 · 1 (`rewind-turn` once — F-122 reopened as recurred). Carried to 29: the Cursor side of d11, light/dark side by side, the remote-track reinstall so ArbosLife runs `main`'s kernel.
- **Cycle 29** ([#391](https://github.com/unarbos/arbos/pull/391), on #384): the kernel's own `--version` as every run's first row (R11); F-122b traced from the app's own log line and fixed — turn phase ×2 clean, rewind passing; the #385/#388 reading (two gaps found, one claim corrected: R12; #372 is in `main`, checked in the tree). **Not done, and why:** the Cursor side of d11 — Cursor's renderer would not launch on the rig (`launch-failed 1002`, `GpuControl.CreateCommandBuffer` failing) after the hard kill earlier tonight; processes stopped, a clean relaunch is cycle 30's first act. The ArbosLife reinstall through the app's own path and light/dark side by side carry too. Gate 107 · 1 (F-122, since fixed).
- **Cycle 30** (no code; captures and the remote): Cursor relaunched cleanly (GPU cache cleared) and **d11 paired** — Cursor: prompt → `Worked 29s` (shut) → *"Wrote 📄 status: CLI with … not merged to main yet."* → "Three next steps" list; ours: `Worked 5s` (shut since F-127) → prose. One difference, model-side not UI: Cursor's answer names the written file as a doc chip, ours as inline code when the model uses backticks (F-128, noted, not a desktop change). **Rig cannot see here:** nothing beyond the standing list; both stills at 1x. **Light/dark:** Arbos both (cycle 23's one-surface stills); Cursor light **not pairable on this rig** — Cursor Agents has no theme control (Customize is plugins/MCPs) and follows an OS theme xfce does not expose; needs the Mac. **ArbosLife reinstall through the app's own path — done:** with `machines.toml` restored (`build = true`), opening `arboslife:…/parity-proj--reply-with-hostname` had the app `cargo install` `main`'s kernel there (`~/.cargo/bin/arbos-kernel`, 05:23) and, once the old process was stopped, start it: pid 917740, `arbos-kernel 0.2.0 d73a25aea876`, `kernel.json` agrees, `/proc/<pid>/exe` present — read, not assumed. The stale `arbos-remote/bin/arbos-kernel` (52cb63a) stays on disk unused. Cycle timer set (`symmetry-cycle-tick`, 20 min). Carried: R10's fold, the `exists`→`seen` sweep.
- **Cycle 31** ([#395](https://github.com/unarbos/arbos/pull/395), rig only, on #391): the deliberate pass over the gate's 119 assertions (`internal/gate-assertions-what-each-reads.md`) — R14 eight that could never fail, R15 `visible` false on a drawn pill, R16 the window granted is 1100×761 not 1600×1000; R13 withdrawn (my crop); R10 closed — the fold row passes on a real fold. Gate 108 · 1 → PRs phase rerun 6 · 0. **Not done:** the kickoff landing (f1/f2 cold), the worker-chat prompt against Cursor's worker tab, one long-form prompt — carried, and the reason is honest: the cycle went to the rig, as it should have this once.
- **Cycle 42 (opened 12:45 UTC, 09-18)**: (1) ~~chat header and under-composer pills after #629~~ — F-190/F-191 are Jacob's asks, by design, not to be paired away (12:40); (2) **light theme** (last 35): re-read F-163's page/rail numbers on main and hand the theme owner a pair; (3) **voice entry points** (last 35): the mic disc and the call strip, what Linux can show; (4) kickoff f1/f2. Gate on the final build; close.
  **12:50 — (2) light theme** re-read on main (`cycle-42/light/`): page/panel/strip/composer all #FFFFFF, text #222222, code plate #F0F0F0 — unchanged since F-163; the theme owner has the Cursor numbers (#F8F8F8 / #E5E5E5 / #141414). **(3) voice, what Linux shows** (`cycle-42/voice/`): the mic disc at rest is the filled disc (F-162's match); hover *Dictation (macOS only for now)*; click writes the macOS-only note above the field — in danger red (F-192, noted); the panel head's call glyph hovers fine. Listening and the call strip stay unpaired on Linux by decision. **(4) kickoff f1/f2** done 12:37 on `dc633b7d` (logged under 41's close; same build). **13:16 — closing gate `cycle-42a` on main `dc633b7d`: 132 · 2 · 18 · 22 · 7** — `composer-context` (old), `world-back-in-order` (answers [0,0,2]: back inside the full gate though 5 of 5 alone — load-dependent on this box, the kernel's after-move path under other kernels; per the coordinator not a desktop finding and no kernel bug filed), and `new-project-kickoff` once more *kickoff=False* at t+2 s (timing; passes by hand). **Cycle 42 closed**: no product PR — F-190/F-191 by design, F-163 numbers with the theme owner, F-192 noted; #640 held as a draft.
- **Cycle 43 (opened 13:25 UTC, 09-18)**: from the coverage table's oldest rows — long-form (39), bottom bar and window sizes (35), remote track scenarios 3–4 (38, still untouched); kernel binary replaced is phase D's `world-deleted-build-*` and passes. Kickoff f1/f2 every cycle.
  **13:45 — long-form d18** (`d18.py`, fresh place: two-worker fan-out, Decisions section, question from the notes, restructure, one worker with a table, relaunch, summary; `cycle-43/d18/`): the Project style holds — prompts as bubbles, *Worked Ns ›* folds, worker lines *⚙ name done — …*, the page nudge as a dim aside, the relaunch reads back 32 items with the same shape, the summary answers from the notes. Found **F-193** (one headline *Edited project-context.md* where *Worked 4s* belonged — the prompt typed as the reports woke the root took the wake turn's end); [#652](https://github.com/unarbos/arbos/pull/652). **14:40 — gate `cycle-43a`** on it: 132 · 3 · 18 · 23 · 7 — `composer-context`, and `fork-turn` + `queue-survives-relaunch`, both cleared by re-running their phases alone on the same build (T once, X twice). **14:45 — window sizes and bar** re-read (`cycle-43/sizes/`): composer 708 at 900/1440/1600, bar 28 px — F-161 unchanged, no change. Remote scenarios 3–4 need a real remote host; ArbosLife is off limits, so they stay open. **Kickoff f1/f2** on the final build (main + #652): f1 *Choosing the next step* → greeting at 9.6 s; f2 one refusal line. **Cycle 43 closed**: F-193 ([#652](https://github.com/unarbos/arbos/pull/652), draft); d18 paired clean otherwise.
- **Cycle 44 (opened 14:50 UTC, 09-18)**: **15:05 — Project page (⌘2) and the classic worker chat** on the d18 place (`cycle-44/page/`): the worker chat holds its shape against `cycle-24/cursor-worker-chat-open.png` — brief card, *Worked for 49s ›* (folded once settled, F-127's rule), answer, Files Changed; Cursor's *Environment ready 7s* and its *Open Desktop · Create PR* pills have no counterpart by design. The page: **F-194** — the model's restructure pasted the plan tool's `[ ] 1 [Kickoff](…)` lines and the page drew them raw; [#661](https://github.com/unarbos/arbos/pull/661). **15:06 — gate `cycle-44a`** on it: **138 · 0 · 16 · 23 · 7** — no fail at all, `composer-context` included (fixed on main by someone else's PR); the first all-clear since the rig began counting. **15:35 — kickoff f1/f2** on the final build: f2 one line; f1 greeted at 12.8 s with a kernel nudge (*Your reply was empty…*) drawn over it — **F-195**, [#666](https://github.com/unarbos/arbos/pull/666) keeps the model-steering nudges off the pane. **16:10 — gate `cycle-44b`** on #666: 135 · 2 — phase D crashed on *Directory not empty*: **the ghost**, caught with timestamps this time (the transcript seed remade the old path, then the attach bootstrapped and spawned there) — **F-196**, [#672](https://github.com/unarbos/arbos/pull/672); and `connect-first-spawn` read one session *lost*, the ghost's kernel. Phase D on the F-196 build: 0 ghosts in 4. **Cycle 44 closed**: F-194 (#661), F-195 (#666), F-196 (#672), all drafts; #652, #640 drafts still.
- **Cycle 45 (opened 16:40 UTC, 09-18)**: **gate `cycle-44c`** on the F-196 build (now main `ca7e9fac`): 133 · 5 — the phase-B cluster again (`notify-*` ×3, `new-project-kickoff`, `fork-turn`), the same rows that clear alone; on this box under a full gate the reply does not land inside the rig's window (R31, to look at next). No ghost: the renamed-back folder only. **17:05 — search palette** re-paired: Cursor lights the match in the accent on the title and on a snippet line for content hits — the accent taken in [#676](https://github.com/unarbos/arbos/pull/676) (`cycle-45/search/`); found **F-197** (the index is title + first prompt only). **17:40 — gate `cycle-45a`** on #676: 133 · 3 — `fork-turn`, `new-project-kickoff` (R31, both waited for in [#678](https://github.com/unarbos/arbos/pull/678)) and `stop-notice` (*no notice* after Stop — once; the kernel's 'Stopped by you' line late). Nothing from the palette. **17:50 — settings interior** paired against the measured doc (`cycle-45/settings/`): width matches (673 vs 669), but the column is left-aligned where Cursor centres it, the groups have no lifted surface, rows 56 vs 60 — **F-198**, for the settings-tab owner who asked for the numbers. **18:07 — the notify window** taken into [#678](https://github.com/unarbos/arbos/pull/678) (the turn must open before *idle* means anything); **gate `cycle-45b`** on main `00cc5ba8` with the R31 rig: **134 · 4** — `fork-turn`, the notify rows and `new-project-kickoff` all pass now; the four fails are `pill-working` (the model spawned nothing that run), `weight-visible` 0.12 % (R29's threshold half), and the two `world-*` rows (the kernel's after-move path under a full gate on this box). **Cycle 45 closed**: #676 (accent match, held), #678 (R31, ready), F-197 and F-198 for their owners. Jacob's chrome list (call on composer, listening strip, four-squares, search, traffic lights, typed clear, spinner) is with dedicated workers from 18:15 — not this loop's surfaces.
- **Cycle 46 (opened 18:18 UTC, 09-18)**: **18:31 — journey leg** on main `00cc5ba8` (`journey.py`, `cycle-46/journey/`): 10 of 14 — J01–J08 and J11 pass (launch, create, kickoff 15 s, two workers, the one live line, steer, stop, continue, reopen with 453→455 items and no doubled prompts); J09 the model's tests fail (its work, not the pane), J10/J12 the follow-ups did not finish inside the step (the root spawned a test worker), J10b the away notification with them. Stage by stage the Project style holds against `cycle-23/`. Found **F-199** (compaction chatter between the worker reports; [#686](https://github.com/unarbos/arbos/pull/686), merged). **19:23 — gate `cycle-46a`** on it: **137 · 2 · 16 · 22 · 7** — `pill-working` (no worker spawned that run) and `weight-visible` (R29's threshold half); the `world-*` rows passed; nothing from the notices. **19:42 — rewind/fork** on the d18 place (`cycle-46/rewind-fork/`): fork on card 28 → a new chat *Use two workers* with 34 items, active; rewind on the same card → 6 prompts to 4, the composer refilled, *Rewound to before this prompt: 14 lines of chat cut, files restored* (F-175's wording landed from elsewhere). The relaunch leg was the journey's J11 (pass). **Kickoff f1/f2** on the final build: f1 greets at 11.8 s; f2 one line, *No working model key on this kernel.* — the kernel's raw text behind it now names Jev and a 401 (F-200, wording for features). **Cycle 46 closed**: F-199 (#686, merged), F-200 noted; the Project-style row paired for the first time since 23.
- **Cycle 47 (opened 19:46 UTC, 09-18)**: **(1) classic worker chat, live** (`worker_live.py`, `cycle-47/worker-live/`): a worker told to read, edit and run, watched for 150 s from its tab — brief card folded, live headline *Working ⟨step⟩ ⌄* with the kernel's step (*Using todo*, *Choosing the next step*), the model's plan prose in full, the run line *Updating the project page, kept its checklist* under it; the shape of Cursor's `cycle-24/cursor-worker-chat-open.png` live (*Working Planning next moves* in F-115's pair). Holds. The worker itself ran past six minutes without writing `todo.json` — 42 lines, then a compaction that freed nothing (*~39k → ~39k*); the model's and the kernel's, the pane honest about it (busy). **(2) kernel binary replaced**: phase D's `world-deleted-build-*` rows pass on every gate; the plate has no Cursor counterpart to pair words against. **(3) rewind on a worker chat**: a worker's brief card carries no rewind or fork control (the chat is read-only by design, *Follow-ups aren't available for this worker*); nothing to drive. **(4) kickoff f1/f2** on this build at 19:42 (cycle 46's close). **Cycle 47 closed** — no product PR; no new ledger row.
- **Cycle 48 (opened 20:09 UTC, 09-18; closed 22:30)**: **(1) after-failure, key removed on a live connection** (`worker_badkey.py`): the kernel re-reads the config per turn, so the root's own turn fails (*No working model key on this kernel.* + Details/Retry, d18's pair) and no worker is born; F-201 — the pending row said *runs when this turn ends* over no turn and the offer bar never came. **(2) long-form d19** (`d19_drive.py`: two chats, a fork, relaunch, a question needing both): the fork read *Delegate 1 copy* and was crowned *main*, the ⌘N chat read *Delegate 1*, the nest was lost on relaunch, and ⌫ on the emptied composer archived the fork — F-202, F-203, F-204, F-205. After the fix the panel comes back from a relaunch as main + the ⌘N chat + its fork, nested and titled, all live (`cycle-48/d19/`). The cross-question: every chat answered all three facts — the place's shared memory, the kernel's design, not a pane matter (Cursor's chats are islands; ours share the project). **(3) opener rows and under-composer pills**: re-read on the gate (phases L/T/`pills` all pass on `keyless-696d`); nothing new by eye. **(4) notices/nudges with the F-195 filter**: #666 is not on `main` yet — deferred to the cycle after it lands. **(5) kickoff f1/f2**: phase K on `keyless-696d` passes; the cold pair holds from 19:42. All in [#696](https://github.com/unarbos/arbos/pull/696), ready; gate `keyless-696d` 141 pass / 1 fail (`world-back-in-order` — the kernel ran three held lines as one turn and the model answered two in one reply; passed on `keyless-696`). Rig: R32 (the store's `results.json` had stopped at phase X on every gate since phase O's kept-place copy — the folder stays local), R33 (`window-expand` row follows #688). What the rig cannot see: the *look* of a fork row against Cursor's history list (Cursor has no nesting; the pair is by structure, not pixels); whether Cursor's ⌫ is truly inert on an empty composer was not driven live this cycle (by knowledge of its composer — no delete binding). **Cycle 48 closed.**
- **Cycle 49 (opened 22:32 UTC, 09-18; closed 23:20)**: progress — child_state on relaunch: the hollow ring was F-206 (the probe stopped at the kernel's after-lines) → [#703](https://github.com/unarbos/arbos/pull/703), gate `relaunch-703` 138 pass / 1 fail (the stale `window-expand-pinned` row, retired in #696). **d20** (a worker asks while the person is in a ⌘N chat): the worker's row read *done* while its question stood — F-207, in #703 with the question as the row's line; the kernel half (spawn returns *ended without a report* for a child parked on an ask; the root then steers it) filed to the features inbox. Gate `relaunch-703b` on `69e9e04`: 139 pass / 2 fail (the stale expand row; `composer-queue` timing, passed twice before). #696 and #703 held per the coordinator (drafts stay drafts). Left in the cycle: light theme re-read, remote 3–4 if the box allows, notices/nudges once #666 lands. **Light theme** re-read on the new chrome (`cycle-49/light/arbos-light-panel.png`): page/panel/strip/composer still #FFFFFF, as cycle 42 measured — unchanged, the theme owner holds Cursor's #F8F8F8/#E5E5E5/#141414; nothing new. **Remote 3–4**: blocked — the VM reset took `~/.ssh/config` (aliases `arboslife`/`templar` gone, *Could not resolve hostname*), and ArbosLife is hands-off by the coordinator's standing line; left for a cycle where that is lifted. **Notices/nudges**: #666 still open, deferred. **Kickoff**: phase K passes on `relaunch-703b`. What the rig cannot see: whether Cursor's history list ever shows a subagent's question (it does not nest; the pair for F-207 is by function — a question waiting is visible from elsewhere); the light theme's exact tints against Cursor's are the theme owner's, measured in F-163. **Cycle 49 closed** — [#703](https://github.com/unarbos/arbos/pull/703) a held draft per the coordinator, not marked ready.
- **Cycle 50 (opened 23:19 UTC, 09-18; closed 01:52, 09-19)**: progress — **long-form d21** first (`d21_drive.py`, `d21b.py`: a 45–75 s worker, the window closed and reopened twice while it ran, then a rewind of the root over the report): the reopen leg found F-208 (the root's pane empty under the prompt for the whole worker run — the kernel says nothing to a root waiting in `spawn`; fixed by reading the step on file as an open turn) and the rewind leg F-209 (a rewind offered where no `.git` means no checkpoint). Both in [#703](https://github.com/unarbos/arbos/pull/703) (held; cycle 49's PR carries them — no force-push to split). Gate `relaunch-703c` on `26fe221`: 140 pass / 4 fail (stale expand row; `composer-stop` and `fork-turn` early-phase timing, fork passes alone; `no-rewind-off-repo` was the row hovering an unanswered turn — moved, confirmed by hand). **Steer cards on a worker chat** (`steer_worker.py`): the line typed into a running worker went as a plain prompt, no steer mark — F-210, in #703 with rig `worker-steer-card` (passes alone). Gate `relaunch-703d` on `2f5ae23`: 141 pass / 2 fail (stale expand row; `composer-queue` timing). **Search palette** (`cycle-50/search/`): F-197 closed — ⌘K now finds *Lisbon* said mid-chat in three chats, the row's line the words around the match (rig rows pass alone). Gate `relaunch-703e` on `ca0a08b`: 143 pass / 2 fail (stale expand row; `recover` — the model spawned a retry worker past the phase's wait and Stop landed over a spawn, F-179's). **Attachments, files half** (`cycle-50/attach/`): a `.txt` from `/tmp` attached by the action lands as bytes in `.arbos/attachments/`, the card shows the inline `≡ name` token, the model reads the word — holds. **Ask card on a worker** (`cycle-50/ask-worker/`): the same Question card as the root's (A/B/C, Skip, Continue); beside it the model had parroted the kernel's *[kernel] The user provided the following answer…* twice into its prose — F-211, cut in #703 (kernel side noted in the inbox file). **Kickoff f1/f2** on `8f1354d`: ready in 11.2 s / 10.7 s. Final gate `relaunch-703f` on `8f1354d`: 142 pass / 2 fail (stale expand row; `world-back-in-order` — the kernel batched the three held lines into one turn, seen before). What the rig cannot see: whether Cursor's ⌘K searches answers as well as prompts (paired by function against its palette's content search, no fresh still); the exact look of a steer card inside a worker's turn against Cursor's mid-turn line (structure paired; stills under `steer-worker/`); the [kernel] line's absence is read from the transcript, not a pixel. **Cycle 50 closed** — [#703](https://github.com/unarbos/arbos/pull/703) a held draft per the coordinator (F-197, F-206–F-211 on it), not marked ready.
- **Cycle 51 (opened 01:58 UTC, 09-19; closed 03:02)**: progress — **long-form d22** first (`d22_drive.py`: the kernel killed under a worker's run, watched through the reconnect, the prompt again): the pane said *Stopped.* and folded it away — F-212, in [#703](https://github.com/unarbos/arbos/pull/703) (rig row passes alone); the kernel itself came back in 0.6 s and resumed the turn (*I restarted while that worker was running*), the worker's report landed — the kernel's half is good. A first attempt with a plain `sleep 30` prompt found nothing to kill: Gemini refuses to sleep in the root (*it would block*). **Themes** (`cycle-51/themes/`): dark page still #161514 (F-163's warm step from Cursor's #141414); the *Tint* slider does nothing in dark — the Cursor palette pins the greys (F-213, for the theme owner); light takes it. Final gate `relaunch-703g` on `b01c95f`: 141 pass / 2 fail (stale expand row; one of three `composer-stop` reads landed after the turn ended). **Voice entry points at rest** (read only, the surface is a chrome worker's): the mic disc and the handset sit on the composer after #688, the gate's `composer-voice`/`composer-call`/`fn-dictation` rows pass — nothing to add by eye. **Launch/first run/permissions** (phase L/N), **Project page ways in and out** (phase P), **mode chip + approval card** (phase Q/S rows): all pass on `relaunch-703g`; no fresh Cursor still this cycle — re-reads only. **Kickoff f1/f2** on `b01c95f`: 11.2 s / 12.9 s. What the rig cannot see: whether Cursor's error line for a dropped connection stays visible under a collapsed turn (paired by function — Cursor's error banner is never folded); the tint's absence in dark is a design matter for the theme owner (F-213), not a pixel to pair. **Cycle 51 closed** — [#703](https://github.com/unarbos/arbos/pull/703) a held draft per the coordinator (F-197, F-206–F-212 on it), not marked ready.
- **Cycle 52 (opened 03:03 UTC, 09-19; closed 04:08)**: progress — **long-form d23** (`d23_drive.py`: two places in two tabs, a worker in each, tabs switched while both ran, beta's folder renamed under it, then back): the strip showed beta's green working dots while alpha was in front; the rename drew af-03's line (*This project's folder is gone or was moved: expected /tmp/d23-beta. The kernel stopped…*, Details/Retry) and the tab kept its plain glyph (Cursor's pills have no lost state either); the folder back, the kernel woke and finished — nothing new for the pane (`cycle-52/d23/`). **Project page notes render**: a pipe table drew as raw pipes — F-214, in [#703](https://github.com/unarbos/arbos/pull/703) (table block; rig `page-table` passes on L+R). A hand-written `- [x] 1 [Kickoff](…)` row kept its brackets — the kernel's own shape (no dash, ordinal) is #661's, held; not re-filed. **Panel archived row**: *1 archived ›* opens to the archived worker's row (`cycle-52/small/arbos-panel-archived-open.png`) — F-183's indent and summary hold. **Standing section**: none on these places (the gate's `standing-once` row not-reachable) — nothing to read. **900×600 with the panel** (`cycle-52/small/arbos-900x600-panel-wished.png`): the panel wished open is not drawn at 900, the *Agents* pill stands in for it under the composer, bar 28 px — F-161's rule holds. Getting there took un-maximizing by window id: the window opens filled since #688 and xfwm4 ignores a resize of a maximized window — every gate since had measured the 1920-wide frame (R34, fixed in `place_window`; phase L now reads *window at 102,116 1600x1000*). **Tabs**: Home · alpha (×) · beta (working dots while its worker ran) · + — F-167's metaphors stand. **Bottom bar**: `0.2.0 (2148)` and the gear, 28 px; read only. **Kickoff f1/f2** on `927178f`: 10.2 s / 12.9 s (f2 took the empty-reply nudge once and greeted). Final gate `relaunch-703h` on `927178f`: **144 pass / 1 fail** (the stale expand row), the cleanest yet. What the rig cannot see: whether Cursor's notes tables would weight columns as ours do (Cursor has no project page; the pair is its chat table, F-163); the archived row's glyph against Cursor's history list is by structure. **Cycle 52 closed** — [#703](https://github.com/unarbos/arbos/pull/703) a held draft per the coordinator (F-197, F-206–F-212, F-214), not marked ready.
- **Cycle 53 (opened 04:09 UTC, 09-19; closed 06:05)**: progress — **long-form d24** (`d24_drive.py`, thirty turns seeded onto a kickoff'd place, opened cold, `cycle-53/d24/`): 64 items drawn at +0.3 s, bottom-anchored on the latest turn as Cursor opens; wheel-scroll climbs about a turn per 800 px; ⌘K finds *ZEBRA17* deep in an answer (F-197). The rewind twenty back was refused by the kernel — *rewind: no user turn 10* — on records my seeder wrote without the kernel's fields; rewind depth on a real long history stays unmeasured (F-174's pair on real records stands). **Kickoff refused/silent**: no key at all → the static greeting and the offer bar *This machine has no OpenRouter key, and neither does this window (Settings › Model)* over the composer, no notice (`kickoff/arbos-no-key-offer-bar.png`); a bad key → *No working model key on this kernel.* with Details, the kernel's F-200 line behind it — the pane's short line holds; F-200 stays the kernel's. **Three workers at once** (`three_workers.py`, `cycle-53/three/`): the root's pane took the three reports in arrival order under one *Worked 6s*, the page's *Updated* line between them — paired; the page itself crossed the workers' reports (kernel, filed: `features-inbox/2026-09-19-worker-reports-land-on-the-wrong-plan-item-when-three-finish-together.md`). **Classic worker chat with a failed command**: F-215 (exit code as an output line, not a mark) → #703 with rig `command-exit-mark`. Re-asking the same line on that root sent the kernel into an endless *no change / empty reply / fallback* loop — 50 nudges in two minutes, killed by hand; filed: `features-inbox/2026-09-19-no-change-verdict-loops-the-empty-reply-nudge-without-end.md`.  **Settings re-read**: phase W rows pass on `relaunch-703j` (F-198/F-213 stay the owner's). **Voice hover**: read only — the composer's disc and handset hover as before (the gate's rows pass). **Kickoff f1/f2** on `5e1ab7e`: 12.3 s / 12.8 s. Final gate `relaunch-703j` on `5e1ab7e` — the first at 1600×1000 since #688 (R34 took): **145 pass / 2 fail** (the stale expand row; `pill-prs`, the pill plainly on screen in the still but `seen()` read it neither visible nor reachable at the smaller frame — cycle 31's false negative again, R15; to fix in the rig). What the rig cannot see: whether Cursor's command card shows the exit code as text or a colour alone (the mark's place and tone are by memory of its terminal card, no fresh still); the three-report order against Cursor's parallel subagents is by structure. **Cycle 53 closed** — [#703](https://github.com/unarbos/arbos/pull/703) a held draft per the coordinator (F-197, F-206–F-212, F-214, F-215), not marked ready. Two kernel notes filed this cycle: worker reports crossed on the page, and the no-change/empty-reply loop.
- **Cycle 54 (opened 06:05 UTC, 09-19; closed 07:05)**: progress — **R35** done: the driver reports `on_screen` (bounds against the window's frame, whatever mask the element was painted under) and `seen()` takes it; the pill under the composer was read as unseen because its paint mask (a deferred layer's) did not meet its bounds. **Long-form d25** (`cycle-54/d25/`: `model = google/gemini-does-not-exist-9`, `fallback_models = [gemini-2.5-flash]`): the kernel fell back and answered, but wrote *…rejected the request, so google/gemini-2.5-flash answers this turn* **three times in the kickoff turn** (once per step; `turn.rs` promises once per turn), and the composer's chip read the dead configured id throughout — the window is never told the effective model. Filed: `features-inbox/2026-09-19-fallback-notice-per-step-and-no-effective-model-frame.md`. On the pane the first notice stands above the kickoff's *Edited…* header and the trailing two under it as answer lines — the shape follows the kernel's repetition; not paired away. Rewind over the fallback turn: not driven (the shape was the finding). **Launch/first run at 1600×1000**: phase L/N rows pass on `relaunch-703k`; the permissions sheet on a fresh XDG as before (d24's cold open showed it). **Project-style root, failed line under a shut fold**: F-212's shape holds (d22 relaunch still). **Classic worker with an edit** (`cycle-54/worker-edit/`): *Worked for 14s ⌄ · Edited greet.py, explored greet.py, updated the project page 2 times +1 −1 · edited · 1 File Changed / greet.py +1 −1 / Review* — cycle 44's pair holds. **/mode on a worker**: the slash palette offers *model* and *mode* on a worker whose composer says follow-ups are not available — the kernel's semantics (mode follows the tree, F-182); the chip shows the model only, as before. **Light theme on the exit mark and the table** (`cycle-54/light/`): the red *exit 2* reads on the light card; the command's own output stays, the kernel's `exit 2` and note gone. **Kickoff f1/f2** on `a09e55f`: 12.3 s / 11.8 s. Final gate `relaunch-703k` on `a09e55f`: **141 pass / 4 fail** — the stale expand row; `world-back-in-order` (kernel batching, seen before); `stop-notice` and `fork-turn` early-phase timing (both pass on `relaunch-703j` and alone); `pill-prs` passes again with R35. What the rig cannot see: whether Cursor names the fallback model in its chip (d25's finding is on the kernel's silence, not a pixel); the worker's *1 File Changed* card against Cursor's is cycle 44's pair, unchanged. **Cycle 54 closed** — [#703](https://github.com/unarbos/arbos/pull/703) a held draft per the coordinator (F-197, F-206–F-212, F-214, F-215; R34, R35), not marked ready. One kernel note filed this cycle (fallback notice per step; no effective-model frame).
- **Cycle 55 (opened 07:05 UTC, 09-19; closed 08:05)**: progress — **long-form d26** (the ask round-trip from the root's seat, `cycle-55/d26/`): the worker's question reached the person as an OS toast and a bare *Asking greet with choice* line; the line now carries the question (F-216, in [#703](https://github.com/unarbos/arbos/pull/703)); on the second run the root model itself relayed the question in prose (*The greeter worker is waiting for you to choose…*), the line under it; the answer given from the worker's card, `greeting.txt` written, the report back on the root. The kernel's *did not return a report* on the first run is the filed spawn note. **Kickoff f1/f2** on `c559df6`: 9.1 s / 11.2 s. Final gate `relaunch-703l` on `c559df6`: **139 pass / 8 fail** — none the branch's paint: the stale expand row; `world-back-in-order` (kernel batching); `fork-turn` and `child-line` (the click hit `child-line-live`, the summary line before the worker's session exists); the `notify-*` trio (R31's window under load); `pill-prs` (the seeded place carried a PR from the run before, so the count did not grow). `relaunch-703k` on the same app code but F-216's line read 141/4. **Not reached this cycle — carried to 56**: the panel's Standing section with a seeded item, the opener's remote-host row by eye, tab colours after a rename, the files grid with five kinds, the typography stepper on the new marks; a wrong panel toggle ate the one attempt. What the rig cannot see: whether Cursor's subagent question surfaces as a card or a line in the parent (F-216 pairs the *reach* — the question readable from the coordinator's seat — not the shape). **Cycle 55 closed** — [#703](https://github.com/unarbos/arbos/pull/703) a held draft per the coordinator (F-197, F-206–F-212, F-214–F-216; R34, R35), not marked ready.
- **Cycle 56 (opened 08:05 UTC, 09-19; closed 09:22)**: progress — R36/R37 in the rig. **Files grid** (five kinds under `docs/`): `.json`/`.csv`/`.py` wore an open-folder glyph — F-217, in [#703](https://github.com/unarbos/arbos/pull/703). **Standing** (`subscribe` to run `date` hourly): the row *clock check · every 1h · next 09:07* under its own heading, the turn's *standing Subscribed #2 (shell · every 1h · next 09:07)* chip — paired by structure (Cursor has no standing work). **Opener, remote row**: with no hosts in `~/.ssh/config` the sheet offers *This machine* and *Browse folders…* and nothing about machines — the remote row appears only when a host exists (F-152's shape); nothing new. **Done line**: a worker's report opening with `### Outcome` put the hashes on the line — F-218, in #703. **Typography stepper on the new marks**: the card's mono and the exit mark did not scale — F-219, in #703 (17 → 20 px at 15 pt). **Tab colours after a rename**: the tab menu offers two entries (F-186's path); not driven further this cycle.  **Kickoff f1/f2** on `fe7232a`: 11.2 s / 11.7 s. Final gate `relaunch-703m` on `fe7232a`: **146 pass / 6 fail** — the most passes of the branch; `pill-prs`, `child-line` and the notify trio pass again with R36/R37. The six: the stale expand row; `composer-queue`/`fork-turn` early-phase timing (pass on `relaunch-703j`); `work`/`tool`/`term-card` in the fold-line screen — my `command-exit-mark` step's fold-back did not take and the fold-line rows then clicked open cards (rig interplay, R38 for 57). **Not done**: long-form d27 (two workers, one failing and one asking, read from the root's seat) — the five re-reads and their three findings took the cycle; carried to 57. What the rig cannot see: whether Cursor's file list gives code files a language glyph (ours is the document, the set has none); the done line's heading strip is by principle (no hashes in a summary), not a Cursor still. **Cycle 56 closed** — [#703](https://github.com/unarbos/arbos/pull/703) a held draft per the coordinator (F-197, F-206–F-212, F-214–F-219; R34–R37), not marked ready.
- **Cycle 57 (opened 09:23 UTC, 09-19; closed 10:32)**: progress — **R38** in: `command-exit-mark` folds the run and the turn back by state; phase T alone passes the fold rows. **Long-form d27** (`cycle-57/d27/`): from the root's seat, *1 Working · Running ls command* over *Asking Worker Ask — Which greeting would you like to use?* — the two lines side by side, then the kernel's *Worker Ask done — (no reply)* wake under them while the worker still asked (the filed spawn note, evidence appended), then *done — written* and the failing worker's report once answered. **Search filters**: *Chats* keeps the two hits, *Actions* empties the chat rows for a content query — as designed. **Model card** (`reread/arbos-model-card.png`): search, provider chips, the list, *Controller Jev*, *Context 1M* — cycle 44's pair holds.  **Kickoff f1/f2** on `09e2b90`: 11.8 s / 11.7 s. Final gate `relaunch-703n` on `09e2b90`: **146 pass / 3 fail** (the stale expand row; `composer-queue`/`fork-turn` early-phase timing, pass on `relaunch-703j`); the fold-line rows pass again with R38. The run's store save hung half an hour on the kept place folder — R32's fix lives in #696, not on this branch. **Not reached — carried to 58**: tab colours after a rename, the update-bar wording, a classic worker chat with a long streaming command (p6). What the rig cannot see: whether Cursor's model picker marks the current model in the list (ours starts alphabetically; the current is not marked — a question for the chrome owner, not paired here); the two-worker seat is by structure. **Cycle 57 closed** — [#703](https://github.com/unarbos/arbos/pull/703) a held draft per the coordinator (F-197, F-206–F-212, F-214–F-219; R34–R38), not marked ready.
- **Cycle 58 (opened 10:41 UTC, 09-19; closed 12:26)**: progress — **p6** (`cycle-58/p6/`): the kernel's call-start `tool` event was read as *completed*: a green, empty card folded under *Running 1 command* for the whole run — F-220, in [#756](https://github.com/unarbos/arbos/pull/756) (running card from the first second, the job stream fills an 8-line tail, a running card stays out of the shut fold). The worker's ask card stood on after the root's `say` answered it — F-221, in #756. `spawn` with `{}` twice in three runs (the model then asks the person for a `brief`) — filed, `features-inbox/2026-09-19-spawn-with-empty-args-…`. **Tab colours after a rename** (`cycle-58/tabs/`): the green holds through the sheet and a relaunch — nothing new. **Update-bar wording**: *0.2.0 (2160) · check failed* is by principle (`status_bar.rs`: visibly, not folded into the quiet state); Cursor's failed check is silent — a departure with its reason written, for the chrome owner. Rig row `command-live-card` in W. **Long-form d28** (`cycle-58/d28/`): turn 1 → *2 Files Changed* (a.txt +1, b.txt +1), turn 2 → *3 Files Changed*; **Review** opened the scratch `.diff` as an unsaved text file — F-222, in #756 (a diff presentation, one card per file); rewind of turn 2 → *Rewound to before this prompt: 9 lines of chat cut, files restored*, c.txt back, the card *2 Files Changed*. After the relaunch the kickoff fold lost its *Worked 7s* — F-223, in #756. **Archived row after a relaunch** (52): *› 2 archived* under the root — nothing new. **⌘K hit in another tab** (50): *Run bash* → the p6 worker's chat in the p6 tab; that pane showed sixteen notice/nudge pairs (the kernel's loop, filed) — F-224, in #756 (one line, *· ×16*) — and the stale ask card again (the kernel replays the `waiting/` ask at attach; filed) — F-221's attach half, in #756. **Kickoff on a 6k-file repo** (41): *Setting up environment* alone, ready at 11 s — the small-place shape. ⌘T with the panel focused opens a panel tab, not the opener — by design (#panel-plus). Kickoff f1/f2 on `0c25f67`: 9.1 s / 11.2 s. Final gate `cycle58-756a` on `0c25f67` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`, main's build): **138 pass / 5 fail** — `composer-stop`/`pill-prs` (the rig's `seen()` without R35/R36, which live in #703), `fork-turn` (early-phase timing), `window-expand-pinned` (R33 in #703), `command-live-card` (the row's first draft looked under the Project chat's shut headline; fixed `a1b8ce1b`, phase D alone `cycle58-756d2` 12/0). What the rig cannot see: whether Cursor's Project chat shows a running terminal card under its shut headline (ours does on a click; the worker chat streams it open — F-115's pairing stands); Cursor's Review is a per-file diff with accept/reject — ours reads, it does not accept; the ×N fold is by principle (Cursor never emits the run). **Cycle 58 closed** — [#756](https://github.com/unarbos/arbos/pull/756) a held draft per the coordinator, held with the kernel's [#762](https://github.com/unarbos/arbos/pull/762) (F-220–F-224; rig `command-live-card`), not marked ready. Planned was: the three carried (tab colours after a rename via the tab menu, update-bar wording read only, classic worker with a long streaming command p6 — the live output card while it streams); from the coverage table's oldest rows, chrome excluded — kickoff on a place that is a big repo (the first thirty seconds' shape, 41), the archived row after a relaunch (52), a search hit opening a worker chat in another tab (50); long-form d28: a worker that edits three files across two turns, the Files Changed card's Review, and the rewind of the second turn — what the card says after the rewind. Kickoff f1/f2. Remote 3–4 when the ssh aliases are back and ArbosLife is not hands-off; notices/nudges when #666 lands.
- **Cycle 59 (opened 12:27 UTC, 09-19; closed 13:13)**: progress — **Opener** (`cycle-59/opener/`): `Co` → *Code/ · Code2/*; `Code` → *Open Code /tmp/op59/Code ↵* over *Code2/*; `newplace` → *Create /tmp/op59/newplace*, made with its `.arbos` — F-152/#258's shape holds. **900×600**: the chat lays out; Review at that width does nothing visible (the panel folds under 1000) — F-225, logged for the panel owner (#629 stands). **Light theme** on the live command card, at 1600 and at 900×600, and the worker's open live fold — holds (`cycle-59/light/`). **Ask mode** (`askmode59`): the worker's *write: hello.txt · Skip · Run ↵* approval card, answered from its pane, `hello.txt` written — holds; with F-220 the pending write reads *Editing hello.txt +1* over the card, not *Edited*. **Long-form d29** (`cycle-59/d29/`, `window_tokens` pinned): read a 40 KB file, a worker's ask answered from its pane, a second read → the kernel's *over budget… nothing old enough* then *compacting 2 turn(s)…* and *compacted 2 turn(s): ~31k → ~21k*: the progress line stayed beside its result — F-226, in #756; the recall turn and the relaunch read the same 24 items. The model never found the NOTE line in either file (its read stopped short; kernel/model, not paired). **Kickoff f1/f2** on `9e2d2bd`: 21.9 s / 12.3 s (f1's model took three steps). Final gate `cycle59-756b` on `9e2d2bd` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **139 pass / 4 fail** — `fork-turn` (early-phase timing), `window-expand-pinned` (R33 in #703), `stop-notice` (the rig's Send-now follow-up opened a new turn two seconds before its Stop; the stop line sits on the earlier fold — passes on `cycle58-756a`), `world-back-in-order` (the three answers landed under the third prompt after the folder came back — seen before, cycle 5x; passes on 756a). `command-live-card`, `composer-stop`, `pill-prs` pass. What the rig cannot see: what Cursor's Review does under its folded right panel at 900 wide; whether Cursor marks a compaction with a line at all (its *Summarized conversation* marker is from memory, not a still). Project page in/out (52) left to the gate's `project-page` rows, which pass. **Cycle 59 closed** — [#756](https://github.com/unarbos/arbos/pull/756) a held draft per the coordinator, with #762 and #764 (F-220–F-224, F-226; F-225 logged for the panel owner), not marked ready. Planned was: from the coverage table's oldest rows, chrome excluded — the opener re-read (48: `~`, a partial path, Create, a prefix sibling); mode chip, `/mode`, the approval and ask cards per mode (50) on the F-221 build; window sizes 900×600 and the panel fold with a live command card on screen (52); Project page in/out, back, Escape, ⌘1 and the files grid (52); themes light/dark on the new Review cards and the live card (54). Long-form d29: a long root chat that reaches compaction with a worker asking mid-way and an edit after — the context re-found, read live and after a relaunch. Kickoff f1/f2. Remote 3–4 when the ssh aliases are back and ArbosLife is not hands-off; notices/nudges when #666 lands.
- **Cycle 60 (opened 13:16 UTC, 09-19; closed 13:42)**: progress — **long-form d30** (`cycle-60/d30/`): two workers at once — from the root's seat *2 Working · Worker Fail · Running cat command* over *Working · Worker Edit · Reading notes.md* (the count line carries the first worker's step, F-115's shape), then *Worker Edit done — done* and *Worker Fail done — cat: /definitely-not-here.txt: No such file or directory* under *Updated the project page*; the fork of the turn landed first in the panel with no parent (F-203, whose fix is held in #703). **Attachments** (`cycle-60/attach/`): a file token inline and an image thumb in the tray, the ✕ on hover, both on the sent bubble, the model read both (*TURNIP-77*, *HELLO IMAGE 42*) — cycle 50's shape holds on the F-220 build. **Mode chip**: hidden until a mode is pinned — Cursor's Agents composer (`cycle-35/bars-and-sizes/cursor-1600x1000-win.png`) has no mode chip either; nothing new. **Light** on the ask-mode approval card (`cycle-60/askmode-light/`): *write: hello.txt · Skip · Run ↵* — holds. **Voice entry points** (`cycle-60/voice/`): the mic's tooltip *Dictation (macOS only)* and its red line *voice dictation is macOS-only for now*; the handset faint with *Call needs a speech server* and a click that does nothing — by design on Linux; Fn down/up through the driver changes nothing here. **Remote** (38): blocked — the ssh aliases are still gone and ArbosLife hands-off; the local drop states are the gate's world rows. No desktop change this cycle: the gate `cycle59-756b` on `9e2d2bd` (139/4) and f1/f2 stand for this build. What the rig cannot see: Cursor's two-subagent live rows (ours by F-115's structure); Cursor's approval card in light (its dark Run pill is from memory). **Cycle 60 closed** — nothing new for [#756](https://github.com/unarbos/arbos/pull/756), held. Planned was: from the coverage table's oldest rows, chrome excluded — voice/dictation entry points (35: the mic disc, the call strip, Fn via the driver — what is pairable on Linux); remote places (38: the local drop and recovery states on the fake remote, since the real one is hands-off); composer attachments (50: a file chip and an image thumb, hover ×, the mid-send bubble on the F-220 build); mode chip and `/mode` on the root (the chip's words per mode); themes light on the ask and approval cards (54). Long-form d30: two workers in parallel, one of which fails a command and the other edits two files, read from the root's seat while both run (the live headline with two lines), then the Files Changed card and a fork of the turn. Kickoff f1/f2. Remote 3–4 when the ssh aliases are back and ArbosLife is not hands-off; notices/nudges when #666 lands.
- **Cycle 61 (opened 13:43 UTC, 09-19; closed 14:20)**: progress — **update plate** on the dev channel (`cycle-61/update/`): the feed at github offers 0.2.0 (2283) and the bar drew a blue *Update* whose click went red at once for a reason it knew in advance (*its kernel is not beside it*) — F-227, in [#756](https://github.com/unarbos/arbos/pull/756): a muted *0.2.0 (2283) is out* with the reason in its tooltip. The driver's `update_channel` reads the settings value, not the env override (rig nit, noted). **Long-form d31** (`cycle-61/d31/`): plan of three, two workers; the relaunch mid-way reads right — *2 Working · Notes · Constructing new notes.md* over *Working · Commit · Appending to a.txt*, *Working 2* pill, Stop; the model then spent five minutes arguing with its workers over `notes.md` ownership (the kernel's plan file) — the commit landed (`dedb61c add three`), the table never did; the pane's wake segments, done lines and *1 call failed* run line read as designed; nothing paired against Cursor here beyond structure. **Pills after a commit**: no remote → no Push pill; *Working 1* stood while a worker stayed live — as designed. **Notes render with a table** (52): not reachable this cycle — the worker never wrote the table (F-214's table render is in #703). **Permissions sheet at 1440×900** (54): left to the gate's phase L (1600×1000) — not re-read. **Light on Files Changed/Review** (54): the drive landed on the worker pane (light settled worker: `cycle-61/light/`); the root's card in light carried to 62. **Kickoff f1/f2** on `3e909d5`: 12.8 s / 13.4 s. Final gate `cycle61-756c` on `3e909d5` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **138 pass / 1 fail** (`window-expand-pinned`, R33 in #703); the three follow-up-row rows not reachable this run (the queued line ran before its row could be read — early-phase timing, pass on 756b). What the rig cannot see: Cursor's updater has no tree copy that cannot take an update — F-227 is by the bar's own principle; the d31 seat is by structure. **Cycle 61 closed** — [#756](https://github.com/unarbos/arbos/pull/756) a held draft per the coordinator, with #762 and #764 (F-220–F-224, F-226, F-227), not marked ready. Planned was: from the coverage table's oldest rows, chrome excluded — the bottom bar's update plate states on the fake channel (43: an update offered, downloading, restart), the Project page's notes render after a worker rewrites notes.md with a table and a checklist (52), launch/first-run permissions sheet on a fresh XDG at 1440×900 (54), themes light on the Files Changed card and the Review (54), the under-composer pills in a git place after a worker commits (Changes → Commit & Push → PRs, 48). Long-form d31: a root that rewrites the plan three times while two workers report, the Project section's plan rows, the notes render, and a relaunch mid-way. Kickoff f1/f2.
- **Cycle 62 (opened 14:24 UTC, 09-19; closed 15:00)**: progress — **light on the card and Review** (`cycle-62/review/`): holds in light; but a worker's Review opened the tree's diff (a.txt, b.txt) while its card said *c.txt +2* — F-228, in [#756](https://github.com/unarbos/arbos/pull/756): a worker's Review draws its own edits, one card per file. **Long-form d32** (`cycle-62/d32/`): Outer spawns Inner; the panel's tree nests two levels (*↳ Outer — Spawning Inner agent*, *↳ Inner — Writing inner.txt*), the root's seat reads *1 Working · Spawning Inner agent*, the grandchild's report reached the root (*outer done, inner said nested written*); after all three were done the root's row still read *· waiting on Outer* — F-229, in #756. **⌘K by keys** (↓, ↵): opens the second hit — holds. **Notes render with a hand-written table** (`cycle-62/notes/`): raw pipes on this branch (F-214's render is in #703), the checklist renders — known. **Not reached, carried**: the permissions sheet at 1440×900, the after-failure states (39). **Kickoff f1/f2** on `4feb2f6`: 9.6 s / 11.8 s. Final gate `cycle62-756d` on `4feb2f6` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **138 pass / 3 fail** — `composer-stop` (`seen()` without R35, in #703), `window-expand-pinned` (R33 in #703), `world-back-in-order` (flaky; passes on 756c). What the rig cannot see: Cursor's worker tab Review is the worker's diff from memory of cycle 22's still, not re-shot; the two-level tree is by structure (Cursor's subagents do not nest). **Cycle 62 closed** — #756 a held draft per the coordinator, with #762 and #764 (F-220–F-224, F-226–F-229), not marked ready. Planned was: carried — light on the root's Files Changed card and Review (54), the permissions sheet on a fresh XDG at 1440×900 (54), the Project page's notes render with a table and a checklist written by hand into notes.md (52, without F-214's render on this branch: what main shows); from the oldest rows — the after-failure states: the kernel binary replaced under an idle chat and a place folder renamed while the window is shut (39); the search palette's Actions filter and keyboard-only open (⌘K, ↓, ↵) (58 → keys). Long-form d32: a root that spawns a worker, the worker spawns its own sub-worker (two levels), read from the root's seat and the panel's tree, then the grandchild's report reaching the root. Kickoff f1/f2.
- **Cycle 63 (opened 15:03 UTC, 09-19; closed 15:34)**: progress — **long-form d33** (`cycle-63/d33/`): eight short turns then a twelve-item list — items *10.*–*12.* wrapped their markers to two lines — F-230, in [#756](https://github.com/unarbos/arbos/pull/756) (the vendored bezel-markdown patch). The ninth turn streams as prose with no headline over it (as Cursor's text answers do); the scroll-up read did not hold against the follow (the tenth answered in three seconds) — the jump-to-end control not reached this cycle. **Permissions sheet** on a fresh XDG at 1440×900 (`cycle-63/perms/`): Microphone *Unavailable* with the Linux packages named, Screen Recording and Files *Granted*, the mic test, *Nothing more to ask for here · Done* — phase L's shape. **Renamed while shut** (`cycle-63/gone/`): the project's tab vanished at launch with no word and the typed line went to Home — F-231, logged for the launch owner (a documented drop from before the gone-place path). **Tab spinner and badge** (`cycle-63/tabs/`): dots while the worker works, a blue dot on the folder glyph when the report lands in a background tab, cleared on the visit — Cursor's dot; holds. **Kickoff f1/f2** on `1517e9a`: 11.8 s / 12.3 s. Final gate `cycle63-756e` on `1517e9a` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **140 pass / 1 fail** (`window-expand-pinned`, R33 in #703) — the most passes of the branch. What the rig cannot see: Cursor's list markers align on the widest number (ours differ by 7 px between 9 and 10); Cursor's missing-folder dialog at launch is from memory. **Cycle 63 closed** — #756 a held draft per the coordinator, with #762 and #764 (F-220–F-224, F-226–F-230; F-225, F-231 logged), not marked ready. Planned was: carried — the permissions sheet on a fresh XDG at 1440×900 (54); the after-failure states: the kernel binary replaced under an idle chat, a place folder renamed while the window is shut (39). From the oldest rows — the Project page in/out: View Project Page, Back to chat, Escape, ⌘1 (52); the tab strip's spinner while a worker works and the tab's badge after a report lands in a background tab (58). Long-form d33: a root chat of eight short turns (answers, one command, one edit, one worker) then a scroll-up read while a ninth turn streams — the jump-to-end control, the day divider, the footer stamps — and the same after a relaunch. Kickoff f1/f2.
- **Cycle 64 (opened 15:37 UTC, 09-19; closed 16:08)**: progress — **long-form d34** (`cycle-64/d34/`): the dead key written under a running turn (the spawn flaked to `{}` first, filed) gave cycle 53's line *No working model key on this kernel · Details › · Retry*; with the key back, Retry drew the prompt a second time and answered under the copy, and the relaunch brought the copy back from the kernel's record — F-232, in [#756](https://github.com/unarbos/arbos/pull/756): Retry re-runs under the card already on the pane, and the recorded *prompt, failure, prompt* folds to one turn at load. **Not reached — carried**: the kernel binary replaced under an idle chat, the jump-to-end control against a long stream and the day divider across a seeded date, the opener's remote row, the Project page in/out. **Kickoff f1/f2** on `fea3ea0`: 14.4 s / 10.7 s. Final gate `cycle64-756f` on `fea3ea0` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **136 pass / 2 fail** — `window-expand-pinned` (R33 in #703), `world-back-in-order` (flaky; passed on 756e); the follow-up-row rows not reachable this run (timing, pass on 756e). What the rig cannot see: Cursor's *Try again* is from memory of its error card; the refusal's words are the kernel's. **Cycle 64 closed** — #756 a held draft per the coordinator, with #762 and #764 (F-220–F-224, F-226–F-230, F-232; F-225, F-231 logged), not marked ready. Planned was: from the oldest rows — the kernel binary replaced under an idle chat (39, the other after-failure half); the jump-to-end control while a long answer streams (a 40-item list, scroll up at once) and the day divider across a date change (seed a record from yesterday) (52 → transcript); the opener's remote row when a host is in `~/.ssh/config` (48, seeded alias to localhost — the row's shape only); the Project page in/out: View Project Page, Back to chat, Escape, ⌘1 (52). Long-form d34: a root chat that hits a provider refusal mid-turn (a bad key swapped in under a running turn), the refused-answer shape, the key-offer bar, the recovery with the key back — live and after a relaunch. Kickoff f1/f2.
- **Cycle 65 (opened 16:11 UTC, 09-19; closed 16:44)**: progress — **jump-to-end** (`cycle-65/jump/`): a forty-item list streaming with the pane scrolled up — the disc shows over the growing list (F-12's hold works); **a click on it closed the app** (gpui: `request_animation_frame` from a click handler; the release build unwrapped None) — F-233, in [#756](https://github.com/unarbos/arbos/pull/756); the gate's `jump-to-end` row clicks on a settled pane and never saw it. Before the stream the pane shows *Planning next moves* at +0.6 s and text at +2 s — no gap. **Day divider**: the seeded yesterday record could not be scrolled to its top in the drive — carried. **Long-form d35** (a worker asking twice, `cycle-65/d35/`): the first ask answered from the worker's pane; before the second, the kernel's *(no reply)* wake (filed 09-18) made the root `say` *Please continue*, which superseded the second question by F-221's rule — the desktop did as designed, the kernel's wake is the fault; the answered ask sits inside the shut *Worked for 4s* fold (cycle 49's design). **Not reached — carried**: the kernel binary replaced under an idle chat, the opener's remote row, the Project page in/out, the day divider. **Kickoff f1/f2** on `cc5dd50`: 11.2 s / 11.8 s. Final gate `cycle65-756g` on `cc5dd50` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **132 pass / 5 fail** — `window-expand-pinned` (R33 in #703), `composer-queue` (early-phase timing), and three from one provider-flaky world phase (*no answer from provider* on its first turn; the Kimi run spawned a worker for the `sleep 120` it was told to run itself — `world-moved-second-line`, `world-back-in-order`, `command-live-card` read the model, not the pane; all pass on 756e/756f). The stable feed now offers 2301 and the bar reads F-227's muted *is out*. What the rig cannot see: Cursor's scroll-up disc while streaming is from memory; the double ask is by structure. **Cycle 65 closed** — #756 a held draft per the coordinator, with #762 and #764 (F-220–F-224, F-226–F-230, F-232, F-233; F-225, F-231 logged), not marked ready. Planned was: carried — the kernel binary replaced under an idle chat (39); the jump-to-end control while a 40-item list streams, scrolled up at once on the transcript pane, and the day divider across a seeded yesterday record (52); the opener's remote row with a seeded `~/.ssh/config` alias to localhost (48, the row's shape only); the Project page in/out: View Project Page, Back to chat, Escape, ⌘1 (52). Long-form d35: a root chat with a worker that asks twice in one task (two ask cards in sequence), answered from the root's seat both times, then the worker's report and the Files Changed card. Kickoff f1/f2.
- **Cycle 66 (opened 16:47 UTC, 09-19; closed 17:26)**: progress — **rig row `jump-to-end-streaming`** (F-233's regression) in phase T: it waits for the answer to grow past the window, wheels the transcript pane up (a positive `dy` is *toward the top* — the first two drafts wheeled the wrong way and read the follow as broken; a suspected F-234 withdrawn before it was written: the old build unpins on a real wheel up during a stream), clicks the disc, and the app stays up at the end. **Day divider** (`cycle-66/divider/`): the seeded yesterday turns sit under *Yesterday 10:10 AM* with *Yesterday* footer stamps — holds; the idle pane wheeled up shows the disc. Seen on the way: a kickoff whose first prompt was typed in its last second has no *Worked Ns* (the stamp went to the prompt) — F-223's rule covers the common case; noted, not fixed. **Long-form d36** (three commands, one failing) not run: its exit mark and *a command failed* line are F-215's, held in #703. **Not reached — carried**: the kernel binary replaced under an idle chat, the opener's remote row, the Project page in/out. **Kickoff f1/f2** on `ec3a4d7`: 11.7 s / 10.2 s. Final gate `cycle66-756h` on `ec3a4d7` (desktop `cc5dd50`, kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **137 pass / 4 fail** — `window-expand-pinned` (R33 in #703), `composer-queue` and `stop-notice` (early-phase timing; pass on 756e), `world-back-in-order` (flaky; pass on 756e); `jump-to-end-streaming` and `command-live-card` pass. What the rig cannot see: Cursor's *Yesterday* divider wording is from cycle 11's still, not re-shot. **Cycle 66 closed** — #756 a held draft per the coordinator, with #762 and #764, not marked ready. Planned was: carried — the kernel binary replaced under an idle chat (39); the day divider across a seeded yesterday record, scrolled to the top by the rail (52); the opener's remote row with a seeded `~/.ssh/config` alias (48); the Project page in/out (52). Rig: a `jump-to-end-streaming` row in phase T (scroll up while the answer streams, click the disc, the app stays up — F-233's regression). Long-form d36: a root chat that runs three commands in one turn (one fails), the run fold's *3 commands, a command failed* line, each card opened by click, the exit mark, then a relaunch. Kickoff f1/f2.
- **Cycle 67 (opened 17:29 UTC, 09-19; closed 17:53)**: progress — **long-form d37** (`cycle-67/d37/`): the first prompt typed the moment the greeting appeared (the kickoff's turn still open) was held for the next turn and the kickoff stamped *Worked 10s*, before and after a relaunch — the d33 case (no stamp) did not reproduce; left as noted. **Opener with a host** (`cycle-67/opener/`): *This machine · fakebox · Browse folders…*, filtered by typing — the host's row wore a folder; the remote glyph existed in the assets and was drawn nowhere — F-235, in [#756](https://github.com/unarbos/arbos/pull/756). **Pills after an edit**: there is no Changes chip and no Commit & Push by design (Jacob, 09-18; #629/#688) — the coverage row's *Changes → Commit & Push* is retired; Push and PRs remain and are the gate's. **Settings/type stepper on the Review cards**: not driven — the Review rows use the fixed `MONO_SIZE` as main's diff rows do; F-219's ladder (in #703) will want `review_view` on it at the merge (noted in #756). **Not reached — carried**: the kernel binary replaced under an idle chat, the Project page in/out. **Kickoff f1/f2** on `28476f8`: 9.1 s / 11.2 s. Final gate `cycle67-756i` on `28476f8` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **140 pass / 3 fail** — `window-expand-pinned` (R33 in #703), `composer-stop` and `pill-prs` (the rig's `seen()` without R35/R36, in #703). What the rig cannot see: Cursor's remote picker glyph is from memory. **Cycle 67 closed** — #756 a held draft per the coordinator, with #762 and #764, not marked ready. Planned was: carried — the kernel binary replaced under an idle chat (39); the opener's remote row with a seeded `~/.ssh/config` alias (48); the Project page in/out: View Project Page, Back to chat, Escape, ⌘1 (52). From the oldest rows — settings: every section by ⌘, / Escape / ⌘W and the typography stepper on the new Review cards (56); under-composer pills in a git place: Changes after an edit, the Commit sheet's words (48, the chat-side half only). Long-form d37: a kickoff whose first prompt lands in the kickoff's last second — the stamp, the fold, the relaunch (the note above) — then five short turns and a fork. Kickoff f1/f2.
- **Cycle 68 (opened 17:55 UTC, 09-19; closed 18:18)**: progress — **long-form d38** (`cycle-68/d38/`): three reads and an edit in one turn — the model read three files, gave an empty reply (the kernel's nudge) and ended the turn without the edit or a word; the fold read *Explored 2 files* over *Explored c.py*, the nudge between the reads cutting the run — F-236, in [#756](https://github.com/unarbos/arbos/pull/756) (one *Explored 3 files*). A turn that ends with tools and no prose draws its fold and no footer — by structure. **Not reached — carried**: the kernel binary replaced under an idle chat, the Project page in/out, the Files section into the editor, the slash menu. **Kickoff f1/f2** on `a25fdad`: 11.7 s / 12.3 s. Final gate `cycle68-756j` on `a25fdad` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **137 pass / 3 fail** — `window-expand-pinned` (R33 in #703), `world-back-in-order` (flaky; pass on 756i), `weight-visible` (0.12 % of pixels against a 0.2 % bar — the bionic toggle's diff depends on how much text is on screen; pass on 756i). What the rig cannot see: Cursor's *Explored 3 files* wording is cycle 23's still. **Cycle 68 closed** — #756 a held draft per the coordinator, with #762 and #764, not marked ready. Planned was: carried — the kernel binary replaced under an idle chat (39); the Project page in/out: View Project Page, Back to chat, Escape, ⌘1 (52). From the oldest rows — the panel's Files section: a file opened from it, the editor's header and its unsaved mark, ⌘W (56); the composer's slash menu: `/` alone, the rows, Escape (48 → composer). Long-form d38: a root that reads three files and edits one in a single turn, the run fold's words (*Explored 3 files, edited 1*), each row opened, then the same after a relaunch. Kickoff f1/f2.
- **Cycle 69 (opened 18:22 UTC, 09-19; closed 18:53)**: progress — **slash menu** (`cycle-69/files/slash-menu.png`): `/` alone lists eight rows, Escape shuts the menu and keeps the `/` — holds. **Files → editor**: a markdown store file opens as a rendered page with its path over it; **⌘W with that tab focused closed the whole project tab** — F-237, in [#756](https://github.com/unarbos/arbos/pull/756) (the drawer's file or surface tab closes; the Project tab and the chat stay). **Project page in/out** (52): the gate's `panel-project-tab`, `project-stays-in-panel`, `panel-escape-closes`, `toggle-panel-closes` rows pass every run — read as covered. **Long-form d39** (`cycle-69/d39/`): Stop All from the Working card stopped the worker mid-`sleep 90` (its transcript: *bash interrupted; job j1 killed · interrupted stop*), but the root's line read *Done · Sleepy worker*, the kernel's `spawn` returned *(the child's turn ended without a report)* and the model wrote *The worker finished.* — F-238: the desktop half (a `ChildState::Stopped`, *stopped by you*) is cycle 70's first item; the kernel half filed (`features-inbox/2026-09-19-a-stopped-worker-returns-to-spawn-as-ended-without-a-report.md`). No Continue Working pill appeared (the root's turn ended normally on the model's word). **R39**: `weight-visible` measures against the ink on screen — passes this run. **Not reached — carried**: the kernel binary replaced under an idle chat. **Kickoff f1/f2** on `0f6c257`: 12.3 s / 12.8 s. Final gate `cycle69-756k` on `0f6c257` (desktop `b2152fd`, kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **136 pass / 3 fail** — `window-expand-pinned` (R33 in #703), `pill-prs` (`seen()` without R36, in #703), `world-back-in-order` (flaky). What the rig cannot see: Cursor's ⌘W on a right-panel tab is from the editor's habit, not a still. **Cycle 69 closed** — #756 a held draft per the coordinator, with #762 and #764, not marked ready. Planned was: carried — the kernel binary replaced under an idle chat (39); the Project page in/out: View Project Page, Back to chat, Escape, ⌘1 (52); the panel's Files section into the editor, its header, the unsaved mark, ⌘W (56); the composer's slash menu: `/` alone, the rows, Escape (48). Rig: `weight-visible`'s bar against the text on screen (a fixed 0.2 % of the window reads the chat's length, not the toggle). Long-form d39: a root chat that stops a worker mid-command (Stop All from the Working pill), the worker's *stopped by you* line, the root's line, Continue Working, then a relaunch. Kickoff f1/f2.
- **Cycle 70 (opened 18:55 UTC, 09-19; closed 19:19)**: progress — **F-238's desktop half** in [#756](https://github.com/unarbos/arbos/pull/756): `ChildState::Stopped` — a worker whose newest turn ended on the interrupt line reads *Stopped · <worker>* on the root's seat, a stop square with *stopped by you* in the panel, `stopped` in the driver (`cycle-70/d39/`); the model's *The worker finished.* over it stays the kernel's (filed). The spawn-`{}` flake struck once more on the way (filed, #764). **Not reached — carried**: the kernel binary replaced under an idle chat, the panel fold at 1000 px and the long draft at 900×600, light on the plates and the opener, long-form d40. **Kickoff f1/f2** on `bec8b32`: 13.4 s / 12.8 s. Final gate `cycle70-756l` on `bec8b32` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **141 pass / 2 fail** — `window-expand-pinned` (R33 in #703), `world-back-in-order` (flaky) — the branch's best. What the rig cannot see: Cursor's stopped-subagent wording is from memory. **Cycle 70 closed** — #756 a held draft per the coordinator, with #762 and #764, not marked ready. Planned was: F-238's desktop half — `ChildState::Stopped`: a child whose records end on `interrupted` reads *stopped by you* on the root's line, the panel row and the driver; carried — the kernel binary replaced under an idle chat (39). From the oldest rows — window sizes: the panel fold at 1000 px exactly and the composer at 900×600 with a long draft (52); themes: light on the update plates and the opener (54). Long-form d40: a root chat with one worker that reports twice (a `say` mid-task and the final report), the two lines on the root's seat, the panel's summary, then a relaunch. Kickoff f1/f2.
- **Cycle 71 (opened 19:22 UTC, 09-19; closed 19:50)**: progress — **the kernel's file replaced under an idle chat** (`cycle-71/kbin/`): the kernel saw it at once (`binary_gone`) and re-exec'd onto the new build; the window drew no plate (F-125's idle rule, by design), and a line typed after went through — but the kernel's transcript line about the swap showed only after a relaunch, not live — F-239, logged for cycle 72 (the reconnect's tail read). **Tabs: reopen a closed tab** (`cycle-71/opener/`): the opener's machine stage offered no recent local place — F-240, in [#756](https://github.com/unarbos/arbos/pull/756): five recent folders as *Open <name>* rows under *This machine*, filtered by the query. The tab order after a drag not driven (the driver's drag call needs a look). **Not reached — carried**: the panel fold at 1000 px and the long draft at 900×600, light on the plates and the opener, the tab drag, long-form d40. **Kickoff f1/f2** on `7e631f7`: 11.2 s / 12.3 s. Final gate `cycle71-756m` on `7e631f7` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **138 pass / 2 fail** — `window-expand-pinned` (R33 in #703), `queue-survives-relaunch` (the held follow-up neither ran nor was held after the relaunch this once; passes on the ten runs before — a race between the quit and the kernel's queue, watched). What the rig cannot see: Cursor's recent-workspaces rows are from memory. **Cycle 71 closed** — #756 a held draft per the coordinator, with #762 and #764, not marked ready. Planned was: carried — the kernel binary replaced under an idle chat (39); window sizes: the panel fold at 1000 px exactly and the composer at 900×600 with a long draft (52); themes: light on the update plates and the opener (54). From the oldest rows — the tab strip: reopen a closed tab from recents, the order after a drag (58 → tabs). Long-form d40: a root chat with one worker that reports twice (a `say` mid-task and the final report), the two lines on the root's seat, the panel's summary, then a relaunch. Kickoff f1/f2.
- **Cycle 72 (opened 19:54 UTC, 09-19; closed 20:21)**: progress — **F-239** in [#756](https://github.com/unarbos/arbos/pull/756): the reconnect's tail read takes the kernel's trailing notices (the swap line shows live after the re-exec, `cycle-72/kbin/`). **Window sizes**: the panel draws at 1000 px and folds at 999 (F-23's line holds); at 900×600 a five-line draft wraps at the model chip's edge, two fifths of the composer empty beside it — F-241, logged for the composer owner (Cursor's chips sit under the text). **Light** on the opener with the new recents rows — holds; the *is out* plate in light not captured (the theme applied a beat late in the drive). **Not reached — carried**: the tab order after a drag, long-form d40. **Kickoff f1/f2** on `469a86d`: 11.2 s / 16.0 s (f2's model took an empty-reply nudge). Final gate `cycle72-756n` on `469a86d` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **136 pass / 5 fail** — `window-expand-pinned` (R33 in #703), `ask-skip follow-through` (the model's wording), and the notify trio (`notify-unseen`, `notify-os-posted`, `notify-seen`: unseen=0 in the full run); phases Q and B alone (`cycle72-756nb`) pass all four — a full-run timing, not the build. What the rig cannot see: Cursor's composer with a long draft is from memory of its two-row shape. **Cycle 72 closed** — #756 a held draft per the coordinator, with #762 and #764, not marked ready. Planned was: F-239 — the reconnect's tail read takes a record written while no turn ran (the swap notice), as a relaunch does; carried — the panel fold at 1000 px exactly and the composer at 900×600 with a long draft (52); light on the update plates and the opener (54); the tab order after a drag (58). Long-form d40: a root chat with one worker that reports twice (a `say` mid-task and the final report), the two lines on the root's seat, the panel's summary, then a relaunch. Kickoff f1/f2.
- **Cycle 73 (opened 20:24 UTC, 09-19; closed 20:54)**: progress — **tab drag**: the strip's tabs cannot be dragged by design (`tabs.rs`: *no tab in this strip can be dragged*) — the coverage item is retired. **The kernel killed −9 under an idle chat** (`cycle-73/kkill/`): *connecting* at +2 s, *live* on a fresh kernel at +4 s, no line (the machine pill's word is the news, by design), and the next typed line answered. **Long-form d40** (`cycle-73/d40/`): the worker's mid-task `say` woke the root — *Started 1 worker · 1 Working · Saying halfway there*, the *Test worker* block, a bare *Working* headline; settled, the Done row read *Done · Test worker* with no words while the report lived inside the `spawn` call's return — F-242, in [#756](https://github.com/unarbos/arbos/pull/756): *Done · Test worker — <its words>*. The queue row's edit/remove are the gate's `followup-*` rows (pass); the relaunch half is `queue-survives-relaunch` (pass this run). **Not reached — carried**: the *is out* plate in light. **Kickoff f1/f2** on `83fd305`: 11.8 s / 11.8 s. Final gate `cycle73-756o` on `83fd305` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **138 pass / 4 fail** — `window-expand-pinned` (R33 in #703), `composer-queue` (early-phase timing), `ask-skip follow-through` (the model's wording), and `ask-typed-text` (a typed answer with no option picked — the multitask phase's ask round; passes on the runs before, read as the model's). What the rig cannot see: Cursor's subagent last-words line is cycle 22's still. **Cycle 73 closed** — #756 a held draft per the coordinator, with #762 and #764, not marked ready. Planned was: carried — the tab order after a drag (58, the driver's drag call first); the *is out* plate in light (54). From the oldest rows — the after-failure states: the kernel killed under an idle chat (not replaced — gone), the reconnect line and the next typed line (39); the composer's queue row: edit and remove a queued follow-up, then the relaunch (48 → composer). Long-form d40: a root chat with one worker that reports twice (a `say` mid-task and the final report), the two lines on the root's seat, the panel's summary, then a relaunch. Kickoff f1/f2.
- **Cycle 74 (opened 20:58 UTC, 09-19; closed 21:24)**: progress — **long-form d41** (`cycle-74/d41/`): the failing-worker scenario did not come off — the model's `spawn` went out with `{}` twice and the worker itself then asked for a `brief` (both filed); the Done row read the worker's words by F-242. **Model picker** (`cycle-74/picker/`): search *kimi* → eight rows with *vision* tags, the provider chips, *Controller Jev*, *Context 1M* — cycle 44's pair holds (F-168's popover shape stands). **Thumbs-down sheet** (`cycle-74/sheet/`): *Report a problem*, the note, the parts rows with *show · ×*, the tool-io chip — and its sentence ran off the sheet's edge, cut mid-word — F-243, in [#756](https://github.com/unarbos/arbos/pull/756). **Not reached — carried**: the *is out* plate in light, the fresh Home's kickoff fold, the failing worker (d41 again). **Kickoff f1/f2** on `f7a6f77`: 11.8 s / 13.4 s. Final gate `cycle74-756p` on `f7a6f77` (kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`): **137 pass / 2 fail** — `window-expand-pinned` (R33 in #703), `world-back-in-order` (flaky). What the rig cannot see: Cursor's report sheet is its own (a form to Cursor's servers) — the sheet is paired by principle, not still. **Cycle 74 closed** — #756 a held draft per the coordinator, with #762 and #764, not marked ready. Planned was: carried — the *is out* plate in light (54). From the oldest rows — the model picker's search and provider chips on the new build (the list, the current model's mark) (44 → picker); the thumbs-down sheet's words and its Submit (the reported mark) (feedback); the Home tab's kickoff fold after F-223 on a fresh Home (kickoff). Long-form d41: a root chat whose worker fails outright (a bad brief: a command that does not exist) — the *ended badly* line, the worker's failed card, the root's answer, then a relaunch. Kickoff f1/f2.
- **Cycle 75 (opened 21:32 UTC, 09-19; closed 22:50)**: progress — **the *is out* plate in light** (`cycle-75/is-out-light/`): the muted plate is `#525252` on the white bar with white words and the download glyph, the tooltip's two lines (the version and its notes; *not installed from a release — its kernel is not beside it*) — the same shape as dark's; holds, no finding. **The fresh Home's kickoff fold** (a fresh `$HOME`, `cycle-75/kickoff-fold/`): live it read *Today 9:38 PM · Worked 4s ›*; after a relaunch a one-call kickoff had **no fold at all**, a three-call one folded the calls after the first (*Edited project-context.md, updated the project page* where live read *…, ran 1 command, …*), and the day line was gone — F-244, in [#756](https://github.com/unarbos/arbos/pull/756): the ask time goes on the record beside its seconds (`kickoff_at`), the zone knows the kickoff turn by either, and a restored ask cannot restamp the seconds from a later wake; live and relaunched read the same to the pixel. Rig row `kickoff-fold-after-relaunch` in phase X (a scratch place's fold rows live and back). R40 on the way: phase X run alone had `go_project()` as a no-op (the layout flag unset), and its queued follow-up landed in the scratch chat. **Long-form d41, plainer brief** (`cycle-75/d41/`): the spawn went out whole this time; the worker ran `ls /definitely/not/here`, reported the exact error and ended *Done* — the root's *Done · Run failing command — I have run the command…* (F-242's words), *Worked 8s* bare over the answer, the relaunch the same. The worker's own card is F-215's state (no exit mark on the header, `exit 2` as an output line — fixed in #703, held). *Ended badly* was not reachable: a bad `model=` on the spawn falls back to the default per step (the kernel's fallback, filed 09-19) and the worker answered *hello*; only a broken key reaches that verdict, and the root would share it. **Stop while a worker runs** (`cycle-75/stop/`): the root's Stop cascades — the worker's transcript takes *Stopped by you* within a second, the composer's Stop gives way to the mic, the pills read *Agents · Continue Working*. But the root's *1 Working · Running sleep and echo* line went with the stop and nothing stood in its place: the spawn (`wait=true`) never returned, so no turn named the worker — F-245, in #756: the newest turn whose spawn never returned takes the unnamed workers, *Stopped · <worker>* under *Stopped by you 22s*, live and after a relaunch. **Continue Working** sends *Continue where you stopped.*; the kernel's note lands as *⚙ Run sleep and echo stopped by you* under that card, the root `say`s the worker on, *1 Working · Continue sleep and echo*, and the worker's report comes back as a *done —* line with the root's answer under *Worked 4s*. R41: the thumbs-down's report sheet left open covered the footer's fork on this run (`fork-turn` read *no state change* with the sheet in the still) — the rig closes it first. **Kickoff f1/f2** on the final build: 11.8 s / 9.1 s (the 401 line as before). **Gate `cycle75-full`** on desktop `8fd76cd-dirty (tree 91111d4)` — the F-244/F-245 build — kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`: **139 pass / 3 fail** — `fork-turn` (R41, rig, fixed after the run started), `notify-seen` (the project's dot from another chat's unseen; cycle 72's flake, passed 73/74), `window-expand-pinned` (R33 in #703). `steer-within-1s` and `ask-typed-text`, which fail with phase X alone, pass in the full run. **What the rig cannot see:** the plate in light — the Mac's bar band and its type at 2x; the kickoff fold — Cursor's own *Worked for* after a Cursor relaunch was paired in cycle 23 (cursor-12/13), not re-run; Stop over a worker — Cursor's sub-agent row after its Stop on the Mac (cycle 23's cursor-08 drew nothing at the stop itself). **Cycle 75 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned was: carried — the *is out* plate in light (54); the fresh Home's kickoff fold after F-223 (kickoff); long-form d41 again with a plainer brief (a worker whose command fails: the *ended badly* line, the failed card, the root's answer, a relaunch). From the oldest rows — the composer's Stop while a worker runs (the root's Stop cascades? the worker's line), and the *Continue Working* pill after a stop (48 → pills). Kickoff f1/f2.
- **Cycle 76 (opened 22:50 UTC, 09-19; closed 23:28)**: progress — **rewind / fork** (`cycle-76/rewind-fork/`, on the stopped place of cycle 75): the kickoff turn's footer offered *Rewind here…* and the press did nothing and said nothing (`rewind` finds no prompt at or before the turn and returns) — F-246, in [#756](https://github.com/unarbos/arbos/pull/756): no Rewind on a turn with no prompt; copy, fork and the votes stay. The stopped turn itself has no footer (Cursor's cursor-08 drew nothing at a stop either). A fork of the stopped chat carries *Continue Working*; pressed, the fork's model `say`-ed to the *original's* worker, whose report woke the original (*Should I continue with the task or start over?*) while the fork waits on a report that cannot reach it — kernel, filed (`2026-09-19-a-forks-say-resumes-the-originals-worker-…`). **The 401 line** (`cycle-76/notices/`): *No working model key on this kernel.* with *Details ›* under the day line, the shimmer gone by t+1 s — cycle 64's pairing holds; nothing new. **Typography stepper** not re-read: F-219's fix is in [#703](https://github.com/unarbos/arbos/pull/703), held, so this branch's build re-finds the same numbers — the owner's re-read goes on #703's build. **Remote place, a host that does not resolve** (`cycle-76/remote/`): the opener's host row, then the composer's placeholder *reconnecting…* and the line under it *checking the machine failed: nohost76 is not a name this machine can resolve — retry in 7s*, the countdown per second, attempts 1…6, a *connection failed* notice on the transcript at the sixth — F-157's pill holds. Above it the pane read ***remote76-place is ready.** Drag in files…* — F-77's greeting fallback for a machine never reached: F-247, in #756 (with a connect fault and no live connection the pane says nothing; the head stays; the line under the composer speaks). **Long-form d42** (`cycle-76/d42/`): two `wait=false` workers, the slow one stopped from its own composer — the root's turn shows *Stopped · Slow* over *1 Working · Running bash command* (F-238 + F-245 together), the pill *Working 1*; when the reports land the lines give way to *⚙ Quick done — …* and *⚙ Slow stopped by you*, and the relaunch reads the same. The model's part: nudged *Your reply was empty. Continue the task*, the root wrote *[kernel] A message from agent quick: quick done* and its twin for slow — reports that did not exist yet — and later called the stopped worker *completed*; kernel, filed (`…the-empty-reply-nudge-after-a-wait-false-spawn-makes-the-model-invent-the-reports.md`). **Kickoff f1/f2** on the final build: 12.8 s / 9.1 s. **Gate `cycle76-full`** on desktop `f944979-dirty (tree 46efad2)` — the F-246/F-247 build, this branch's head — kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`: **140 pass / 2 fail** — `window-expand-pinned` (R33 in #703), `world-back-in-order` (flaky, passes alone since af-04); `fork-turn` passes with R41. **What the rig cannot see:** rewind/fork — Cursor's restore checkpoint on the Mac (cycle 38's pairing); the 401 line — Cursor's provider-error card wants a Cursor with a bad key, not tried on the VM's signed-in copy; remote — a real tunnel drop and the install narration (ArbosLife untouched); d42 — Cursor's two sub-agent rows with one cancelled, on the Mac. **Cycle 76 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned: from the oldest rows — **remote places** (38: F-178's local drop/recovery; the reconnect pill's countdown and recovery on a local host entry, no install on ArbosLife); **rewind / fork / checkpoint** (58: a rewind of the kickoff turn itself — what the card offers on turn 0 — and a fork from a stopped turn, the *Continue Working* pill on the fork); **settings** (56: the typography stepper's third size on the command card and the worker lines, F-219's numbers re-read on this build); **notices, nudges, refused-answer shape** (64: the *turn failed: Internal error — Jev did not choose the next step: 401* line's words beside Cursor's provider-error line, and its Details ›). Long-form d42: a root that spawns two workers, one stopped by the person mid-way and one left to finish — the two lines under one turn, the Continue Working pill with one worker still running, a relaunch. Kickoff f1/f2.
- **Cycle 77 (opened 23:30 UTC, 09-19; closed 00:14, 09-20)**: progress — **settings** (`cycle-77/settings/`, 1440×900): five sections walked (General's *This app / Updates / This machine* rows — the Kernel row names the build `0.2.0 · e504bf2f48ad` from the socket, the channel toggle Stable/Dev, *Not checked yet*; Model's providers, key field, search and picks; Permissions' three status rows and the mic meter; Appearance; Performance). F-198's numbers stand against `cursor-settings-interior-measured.md`: rail 200 px on the page's own grey (Cursor 255 on `#181818`), the column left-aligned at x≈230 and ~640 wide (Cursor 669 centred right of the rail), hairline rows with no group surface (Cursor's `#1C1C1C` lift) — the tab's owner's. Escape leaves the tab in the strip and fronts the project; ⌘, fronts it again; ⌘W closes it — as before. **⌘K search** (`cycle-77/search/`): a worker hit's row — *Run failing command*, its brief as the snippet, the tab, *1h*; a query with no hit reads *No matches*. A chat archived from the desktop (`closed`) is left out of the hits by design; the kernel-archived worker was found. Cursor's search over its archived agents was not verified on the VM. (⌘A in the palette's field did not select all — the rig's `ctrl-a` fallback exists for the composer; Linux mapping, not paired.) **Attachments** (`cycle-77/attachments/`): an image and a file attached while a turn ran — the chips in the composer, then the thumb and the *notes.txt* token on the card above its words, the composer's × gone with the send, the answer reading both — holds from 60. A true mid-turn steer was not reached: the model would not run the long command (the spawn-with-no-brief ask again, filed 09-19). **Window sizes** (`cycle-77/sizes/`): at 900×600 ⌘B flips `panel_open` while `panel_shown` stays false and the toggle's tooltip reads *Hide panel ⌘B* over no panel — F-248, for the panel owner (#629's fold rule), not built. **Long-form d43** (`cycle-77/d43/`, on d41b's place): a live worker spawned beside the archived one — two lines under the turn, the panel's *Agents* row and *1 archived*; *Archive the live worker too, then tell me what both did* — the root `say`-stopped the worker and answered for both; the kernel's done file opened *Turn ended badly. Last words: stopped: stopped by root: Stop* and the line drew red as a failure — F-249, in [#756](https://github.com/unarbos/arbos/pull/756): a report opening *stopped:* reads *Long running command stopped — by root: Stop*, muted; kernel half filed (`…a-worker-the-coordinator-stopped-is-filed-as-ended-badly.md`). The archived list opened shows both workers; the relaunch reads the same. **Kickoff f1/f2** on the final build: 12.3 s / 9.1 s. **Gate `cycle77-full`** on desktop `46efad2-dirty (tree f3ef4cf)` — the F-249 build, this branch's head — kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`: **139 pass / 1 fail** — `window-expand-pinned` (R33 in #703). **What the rig cannot see:** settings — 12 vs 13 px type at 1x and the Mac's own Settings window chrome; search — Cursor's archived-agent hits (needs the signed-in Cursor, not relaunched this cycle); attachments — the drag-in path (the driver attaches by action); sizes — the Mac's window minimum from the WM; d43 — Cursor's cancelled sub-agent row on the Mac. **Cycle 77 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned was: from the oldest rows — **settings** (56: the sections walk on this build beside cycle 35's measured interior — the General section's kernel line and the update channel row, the Permissions rows, ⌘, / Escape / ⌘W); **search ⌘K** (62: the palette on a project with workers and an archived chat — do archived chats' lines come up, and the row's shape for a worker hit); **composer attachments** (60: an image dropped mid-turn — does the thumb ride the steer card; the file token's × after a send); **window sizes** (72: 900×600 with the panel forced open through ⌘B — what the drawer does under `PANEL_MIN_WINDOW`, F-225's owner note re-read). Long-form d43: on a place with an archived worker and a live one, *Archive the live worker too, then tell me what both did* — the archived row's count, the Done lines, the panel's archived list, a relaunch. Kickoff f1/f2.
- **Cycle 78 (opened 00:15 UTC, 09-20; closed 01:06)**: progress — **voice entry points** (`cycle-78/voice/`): the mic disc rests `#f0f0f0` with a dark glyph (Cursor's `#F0F0F0` — F-162 holds fixed), `#d6d6d6` on hover; the click writes *voice dictation is macOS-only for now* over the field, still in red (F-192's observation stands); the handset's tooltip *Call needs a speech server: set voice_url in config.toml ⇧⌘C* and a click that does nothing — right, the tooltip says why; Fn down/up: nothing on Linux, by design. **Remote, the recovery half** (`cycle-78/remote/`, a local `sshd` raised on the rig, key login to self): the host set to a name that does not resolve, then fixed while the countdown ran — the next try came up live in one attempt, fault cleared, attempt back to 0, the keyless bar *This machine has no OpenRouter key · Use my OpenRouter key on this machine · This session only* under the composer, *Try Live* beside it. Before that a first run showed the remote's launch running `cd /home/ubuntu//tmp/remote78-place` (*can't cd*, `~/.cache/arbos/web.log`) and two minutes of shimmer ending in *arbos did not start within 60 s*: the opener's folder stage opens with `~/` and my absolute path typed after it became `~//tmp/…` — F-250, in [#756](https://github.com/unarbos/arbos/pull/756) (`~//x` reads as `/x`). The remote install's copy (`~/.arbos-remote`) removed after. **Project tab** (`cycle-78/panel/`, 1440×900): the panel takes 591 px (Cursor's default 592 — matches); tab row 36 vs Cursor's 40, pill 24 vs 26, `+` after the tab, the ⊟ toggle — the body's Agents/Project/Workers/Files sections differ from Cursor's by design (F-171/F-184); the toggle's tooltip read *Show panel* a second after opening it (F-171's stale tooltip, owner's). **Ask cards** (from d44): the parked card after a relaunch — two options, *Other*, *Skip*, *Continue* — holds; the mode-chip half (`/mode ask`) carried. **Long-form d44** (`cycle-78/d44/`): three workers, one asking mid-way — the root's flow read *⚙ worker C done — (no reply)* and no *Asking* row: the kernel files a done report when a worker parks on `ask` (filed 09-19), and the desktop's `child_state` never counted the parked card itself as Asking — F-251, in #756: a parked `questions` is Asking first; the done line yields to *Asking · B*, live and after a relaunch (`asking-line-live-over-relaunch-after.png`). After the relaunch the pane landed on worker A (the last pane), live mid-command with F-220's running card. The workers' own words again bent by the model (a worker calling `spawn {}` itself; a report whose last words were a kernel notice) — kernel/model, not new. **Kickoff f1/f2** on the final build: 12.8 s / 9.1 s. **Gate `cycle78-full`** on desktop `684d059-dirty (tree 5f967f2)` — the F-251 build, this branch's head — kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`: **137 pass / 4 fail** — `window-expand-pinned` (R33 in #703); `world-moved-notice` (the agent-gone notice fired before the folder-moved guard this run — the words differ, the row reads one), then phase D died on the rename back (*Directory not empty*: the kernel recreated the moved folder, F-165's kernel half, filed) and its `lost` chat took `connect-first-spawn` in phase X with it. **Phase D alone on the same build: 7 · 0** (`cycle78-D`), so the build is not the cause; the pair is timing plus the kernel's late writes. **What the rig cannot see:** voice — the Mac's Fn and a live microphone; remote — a real tunnel drop and the install narration on a machine that is not this one (the local sshd shares the box's files); panel — Cursor's panel on the Mac; d44 — Cursor's sub-agent question flow on the Mac. **Cycle 78 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned was: from the oldest rows — **voice / dictation entry points** (60: the mic disc's resting and pressed states, the call strip's entry, beside cycle 35's pair; the Fn line on Linux by design); **remote places, the recovery half** (76 found the fault; the recovery — a host entry that starts failing and then resolves — on a local sshd if one can be raised, else the local kernel-kill recovery of 73 re-read on this build); **Project page** (69: the drawer's Project tab body — head, Agents, archived, TL;DR, pages, Files — against `cursor-side-panel-measured.md`'s numbers on this build); **mode chip / ask cards** (65: `/mode ask` then a write — the approval row's words and its Skip/Run, F-221's supersede rule on a second ask). Long-form d44: a place with three workers where one asks a question mid-way — the root's *Asking* line, the ask card in the worker's tab, the answer typed from the root's composer, the other two finishing meanwhile, a relaunch with the ask still parked. Kickoff f1/f2.
- **Cycle 79 (opened 01:08 UTC, 09-20; closed 01:49)**: progress — **relaunch mid-command on the root** (`cycle-79/relaunch/`): quit while the root's own `bash` ran a 60 s loop, relaunch — the pane read the prompt with nothing under it, a mic where Stop should be, `turn_open` false, while `status.toml` read at attach said *Running for i in…* — F-252, in [#756](https://github.com/unarbos/arbos/pull/756): the kernel's step over an unanswered prompt on a live socket reopens the turn — *Working · Running for i in…* and the Stop square, the turn settling with its tool row and *ticked* when the command ends. The running card itself does not come back: the kernel files a call's record when the call ends, so attach has no open call to replay (the kernel's side; F-220's card is from the call-start event). **Rewind** (`cycle-79/rewind/`): on a place with no checkpoints (no git) the kernel's refusal drew as *Nothing to rewind to yet…* with *Details ›* and **Retry** — a Retry that would re-send the last prompt — F-253, in #756 (a refused control keeps Details, drops Retry). The rewind of a turn beside a worker needs a git place — carried. **Tabs** (`cycle-79/tabs/`): eight places at 900 wide fit at 96 px each with `+` and the ⊟; ⌘⇧[ ] cycle; no overflow control past the width (F-167's metaphor stands). **`/mode ask`**: the mode notice (*mode: ask — every call that writes…*), no chip pinned; a writing `bash` then ran with no approval card — F-182, kernel, stands. **Long-form d45** (`cycle-79/d45/`): five docs read, an index written — *Worked 43s* over *Explored 5 files, 1 search, could not edit INDEX.md*; the rows *Ran Find*, five *Read x.md L1-3*, *Could not edit INDEX.md* (F-45's shape; the model wrote to the root, not docs/, and the kernel's fence refused — kernel/model); the worker's Done line, *1 File Changed · INDEX.md +7 · Review* opening the panel's review tab; the relaunch the same. A hole between the *glossary* and *manifests* rows: the nudge filed between two reads drew an empty element that took its gap — F-254, in #756 (the run's column takes only the items that are rows; 29 px pitch throughout). **Kickoff f1/f2** on the final build: 11.8 s / 9.1 s. **Gate `cycle79-full`** on desktop `024742b-dirty (tree 0b7873f)` — the F-254 build, this branch's head — kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`: **143 pass / 2 fail** — `window-expand-pinned` (R33 in #703), `world-back-in-order` (flaky; passed alone in `cycle78-D`). **What the rig cannot see:** relaunch — Cursor's own reopen mid-run on the Mac (cycle 23's cursor-12/13 had the prompt back in an edit card); rewind — Cursor's checkpoint restore; tabs — Cursor's sidebar pills at eight projects; d45 — Cursor's read fold at 2x. **Cycle 79 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned was: from the oldest rows — **relaunch / replay** (75: a relaunch mid-turn of the root itself — a long command running on the root, quit, relaunch: the running card, the headline, the Stop; F-220's card across a relaunch); **rewind / fork** (76: a rewind of turn 2 on a chat with a worker — does the worker's line go with the turn; the composer refilled); **mode chip / `/mode ask`** (carried: the chip's appearance, a write's approval row *Skip · Run ↵*); **tabs** (73: the strip at eight places — overflow, the spinner on a busy tab, ⌘⇧[ ] across them). Long-form d45: the research place's docs — *Read every document under docs/, list each with one line, then write docs/INDEX.md* — the read run's fold (*Explored N files*), the edit card, Review on the root, a relaunch. Kickoff f1/f2.
- **Cycle 80 (opened 01:50 UTC, 09-20; closed 02:26)**: progress — **launch / first run** (`cycle-80/launch/`, no state file, a fresh `$HOME`): the first frame at +0.8 s is the Home kickoff view with *Setting up environment* shimmering, the window filling the screen by +4 s (by design since 09-18 — F-166's corner placement is gone), then the Home face sheet, Escape → the permissions sheet (Microphone *unavailable* with the install hint, Screen Recording and Files *granted*, the mic test, *Nothing more to ask for here · Done*), Skip → the composer; the kickoff answered at 12.5 s under it all. **F-231 re-read**: a place renamed while shut is dropped at launch with no word — stands, the launch owner's. **Project tab** (`cycle-80/project-rewind/`): Escape on the drawer's Project tab closes the drawer and gives the composer the keyboard; ⌘1 leaves the drawer as it is; a file row opens as a second panel tab with its ×; a *Resources* section (project-context.md) sits between Agents and Project now. **Rewind beside a worker, git place**: the turn that spawned the index worker rewound — 17 → 5 items, the composer refilled with the prompt, *Rewound to before this prompt: 23 lines of chat cut, files restored*, `docs/INDEX.md` gone, the worker's line gone from the flow and its row under the panel's *1 archived*; the root row keeps its old title (the kernel's, set once — Cursor keeps titles too). **Opener**: the gate's twelve opener rows pass on this build. **Long-form d46** (`cycle-80/d46/`): the index turn re-sent, then a fork from its footer — the copy as *… copy* at the top of the panel's Agents with the original and its worker under it; in the fork the table rewrite (the model wrote through a worker again — the coordinator's fence, filed) with its *1 File Changed · INDEX.md* card and Review; the rewind landed on the fork (my panel click missed the original) and cut the table turn, INDEX.md back to the list — a fork rewinds on its own checkpoints. The fork's first turn read *Edited project-context.md, updated the project page ›* with no day line where the original reads *Worked 10s ›* — F-255, in [#756](https://github.com/unarbos/arbos/pull/756): a fork inherits the original's kickoff stamps (*Today 1:27 AM · Worked 10s ›* on the copy). **Kickoff f1/f2** on the final build: 11.8 s / 9.1 s. **Gate `cycle80-full`** on desktop `0b7873f-dirty (tree 9f0923b)` — the F-255 build, this branch's head — kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`: **142 pass / 4 fail** — `window-expand-pinned` (R33 in #703); `stop-notice` (the Stop landed on the worker and the root's turn ended whole — F-97's shape — while the check read from the last prompt after the queued follow-ups had run; R42: the row reads from where the stop fell and takes that shape); `child-line` and `pill-prs` (**both pass alone**, `cycle80-SG`: a session appeared under the click; the PR count read against a stale base). **What the rig cannot see:** first run — Cursor's macOS first run and TCC prompts; the Project tab — Cursor's panel on the Mac; rewind/fork — Cursor's restore checkpoint and its fork on the Mac; d46 — the fork's tab in Cursor's sidebar. **Cycle 80 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned was: from the oldest rows — **launch / first run** (63: a fresh XDG at 1440×900 on this build — the Home face sheet, the permissions sheet, the first frame before a project; F-231's renamed-place drop re-read); **Project page** (78: ⌘1 and Escape from the drawer's Project tab, the files grid's kinds on this build — F-217 held in #703); **rewind beside a worker on a git place** (carried from 79); **opener** (71: `~` expansion, a sibling prefix, Create — on this build). Long-form d46: the d45 place — *Fork this chat; in the fork, rewrite docs/INDEX.md as a table; then in the original rewind to before the index was written* — the fork's tab, its edit card and Review, the original's rewind (git place: checkpoints exist) and the worker line's fate. Kickoff f1/f2.
- **Cycle 81 (opened 02:27 UTC, 09-20; closed 03:03)**: progress — **root fold words** (`cycle-81/fold/`): a root turn of two reads and prose — shut *Worked 27s ›*, open *Explored 2 files* over the rows, Cursor's words; Flash gives no Thought row to pair against *Thought 4s*. **Kickoff** (`cycle-81/kickoff/`): a 60-file tree gets the same one-command kickoff as an empty one (*ls -la; cat README*…*, 13 s) — no *Explored N files* to count; a key swapped to a dead one a second into the kickoff changes nothing (F-189: the key is read per turn). The model twice called an empty place *a Rust workspace with a kernel and a desktop app* — the kickoff prompt's own words, kernel/model. **Push pill** (`cycle-81/pills/`): a repo two commits ahead of a local bare remote shows *Push* with the tooltip *2 commits not yet pushed*; the click sends *Push the current branch to its remote.* and the model answered with `spawn {}` and *I need a task or brief* (the 09-19 kernel note again) — the pill stays, rightly. Cursor pushes without an agent (F-177's paired difference). **Compaction live** (`cycle-81/compaction/`, `window_tokens = 9000`, four `seq 1 400` turns): F-226's pair holds — *compacting 1 turn(s), ~15k tokens…* at +19.9 s gave way to the result at +22.4 s, one line; the *over budget…* lines fold under their turns. The result line drew raw, two lines with the model id and a transcript pointer — F-256, in [#756](https://github.com/unarbos/arbos/pull/756): *Compacted 1 turn, ~15k → ~14k tokens.* with Details ›. **Long-form d47** (`cycle-81/d47/`): on d45's place (three root chats — the original, two forks — and two archived workers) the turn's calls were `plan add`, `plan check` (the kernel archived the checked item's worker) and `agents`; the model answered *no other chats* from the tool's *0 agents*, and the panel's Agents section showed one row — the chat that ran the turn — where a sibling root, its worker and the archived fold had stood; a relaunch drew all of them back. F-257, **open**, for the panel owner; not reproduced with `agents` alone. The original root had been archived by an earlier turn's `plan check` (kernel). **Kickoff f1/f2** on the final build: 13.9 s / 9.1 s. **Gate `cycle81-full`** on desktop `bb189d5-dirty (tree d3ed50c)` — the F-256 build, this branch's head — kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`: **139 pass / 3 fail** — `window-expand-pinned` (R33 in #703); `weight-visible` (7.4 % of the ink differed against an 8 % bar — 12.5 % and 16.2 % on the two gates before; the chat in front had less prose this run); `pill-prs` (second full run in a row: *PRs 1* is on screen in the still, `pills.prs = 1`, the record written — the row's read is wrong somewhere; passes alone; **R43 to audit next cycle**). **What the rig cannot see:** the fold — Cursor's *Thought Ns* on a thinking model; kickoff — Cursor's setup on a large repo at 2x; Push — Cursor's Push against a real remote; compaction — Cursor's *Summarized N messages* live; d47 — Cursor's sidebar with three chats. **Cycle 81 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned was: from the oldest rows — **chat: Project style** (68: a root turn with thinking, two reads and prose — the fold's summary words against Cursor's *Thought 4s · Explored 2 files* on this build); **kickoff / new-project** (75: f2 with a key removed mid-kickoff — the refusal line's shape; a kickoff on a place with a large tree — the *Explored N files* count); **under-composer pills** (75: the Push pill on a repo ahead of its remote — a local bare remote; the PRs pill's click); **notices** (76: the compaction line pair — *compacting…* → *compacted N turns* on a long chat, F-226's rule live). Long-form d47: the d45 place after d46 — *Archive the index worker, open the fork, then tell me which chats exist here and what each did* — the panel's archived list, the fork's row, the root's answer naming the chats, a relaunch. Kickoff f1/f2.
- **Cycle 82 (opened 03:05 UTC, 09-20; closed 03:40)**: progress — **R43** (`pill-prs`): the row now writes each of its three terms (`grew`, `seen`, the record) on the record; phases S+W+G together and the full gate both pass it this cycle (`n0=0 grew=True … seen=True`) — the two earlier full-run fails left no term to blame; the next fail will name one. **⌘K Actions** (`cycle-82/search/`): the Actions filter's five rows with their chords — *New Tab ⌘T · Open Folder… ⌘O · Project Page ⌘2 · Settings ⌘, · Report a Problem… ⇧⌘R*; a miss reads *No matches*; Enter on none leaves the palette up. Holds beside cycle 38's pairing. **Worker's own ask, Skip, relaunch** (`cycle-82/worker/`): the card in the worker's tab (two options, Other, Skip, Continue); Skip → the kernel's own line *The user skipped this question without answering. Choose a sensible default yourself…* drew as a **bubble of the person's** under the folded *Question · … · skipped* row and opened a second *Worked for 3s* — F-258, in [#756](https://github.com/unarbos/arbos/pull/756): the skip's line is an ask echo (drawn nowhere) and the *Question · … · skipped* row stays in view under the shut fold, as Cursor's collapsed card does; *Worked for 3s* holds across the relaunch. The root then spawned a second asker on its own after the report (model). **Attachments' drag-in** not driven: the driver has no drop; the paste path was paired in 60. **F-257** not re-driven in time (the d47 place's kernel was reaped). **Long-form d48** (`cycle-82/d48/`): *un-archive the worker* — *I cannot un-archive workers.* (the kernel has no such tool; the model said so plainly). The run itself was tainted by the rig: my first drive died with its window up and the second launched a **second window on the same place** — the two rewrote each other's records, and the place ended with one fresh root record, the workers' records gone and the second worker's kernel folder deleted. Not a finding against the app (one window per place is the app's own rule via the Dock); the rig's launcher now kills a leftover window first (R45, `c75lib`). **Kickoff f1/f2** on the final build: 11.2 s / 9.2 s. **Gate `cycle82-full`** on desktop `8ee0da1-dirty (tree c66be49)` — the F-258 build, this branch's head — kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`: **141 pass / 2 fail** — `window-expand-pinned` (R33 in #703); `ask-skip follow-through` (the model said *Since no answer was provided, I will proceed with 'alpha'* — the follow-through itself — and the row's word list read *no answer* as a stall; R44 reads stalls only, landed after the run). **What the rig cannot see:** ⌘K — Cursor's command palette chords on a Mac keyboard; the worker's ask — Cursor's sub-agent question card and its skip on the Mac; d48 — Cursor's archive/unarchive (it has restore for archived agents). **Cycle 82 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned was: **R43 first** — `pill-prs` reads *fail* with the pill on screen: which of its three terms (`pills.prs > n0`, `seen('pill-prs')`, the record) fails in a full run and passes alone; fix the read. Then from the oldest rows — **search ⌘K** (77: the Actions filter's rows and their keys); **composer attachments** (77: the drag-in path if the driver can drop a file; else the paste of an image); **chat: classic style (worker)** (75: a worker's own ask card and its Skip on this build; the worker's *Worked for* stamp after a relaunch); **F-257** re-driven (a plan check that archives a sibling's worker while the panel is open — rows before/after, `closed` flags each second). Long-form d48: the d45 place — *List the archived workers and un-archive the index one* — the kernel's unarchive if it has one, the panel's fold count, the row's return. Kickoff f1/f2.
- **Cycle 83 (opened 03:42 UTC, 09-20; closed 03:55)**: progress — **F-257 re-driven** (`cycle-83/f257/`): a fresh place, a fork beside the root, a `wait=true` worker under the fork, a `plan add` then a `plan check` on its item with the panel open, rows and `closed` flags read every half second — the rows held (*fork · root · 1 archived*); not reproduced a second time; F-257 stays open with the two d47 stills as its only evidence. **Light theme, the new lines** (`cycle-83/light/`): the worker's ask card (Question, A/B/C rows, *Skip*, a dark *Continue*), *Waiting for your answer*, the folded *Question · … · skipped* row with F-258's bubble gone, *Compacted 1 turn, ~15k → ~14k tokens.* with Details, the root's *Asking · <worker>* line in the accent — all read in light; nothing new. **Stranger plate**: an older kernel (`00cc5ba89968`) started by hand serving the place before the window opened it — the window did not see it: it spawned its own (`e504bf2f48ad`, `kernel.json` rewritten) and two kernels served one place until the old one was killed; the bar showed only *check failed* (the dev feed unreachable on this box). The stranger plate needs a kernel the roster knows; not reached this way, and the double-serve is a kernel-side observation (the older build's `serve` left no `kernel.json` the new build reads). **Remote *Try Live*** not driven (time). **Long-form d49** (`cycle-83/d49/`, the p81 repo, two ahead and dirty): no pill on a dirty tree (Changes retired by design, Push only when clean-ahead); the commit-and-push prompt — the kernel refused the coordinator's `git commit` on `master` (*protected*), the run line reads *A command failed* over the answer asking to branch instead; the relaunch the same. Kernel design; nothing new. **No desktop change this cycle** — the build is cycle 82's (`c66be49`), so its gate stands: **141 pass / 2 fail** (`cycle82-full`, kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`); phase Q alone on it with R44: 5 · 0 (`cycle83-Q`, *ask-skip follow-through* passes). Kickoff f1/f2 as at 03:38: 11.2 s / 9.2 s. **What the rig cannot see:** light — the Mac's rendering of the accent on white; the stranger — a kernel of another build the roster lists (a real second machine's); d49 — Cursor's Commit & Push against a real remote. **Cycle 83 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned was: from the oldest rows — **F-257 re-driven** (a fresh place: root + a sibling fork + a worker under the fork; a `plan check` on the worker's item with the panel open; rows and `closed` flags each second); **themes** (75: light theme on the worker's ask card, the Question row and the compaction line — the new lines of 75–82 in light); **bottom bar** (75: the stranger plate on this build — a kernel of another build serving the place, the plate's words and click); **remote places** (78: the *Try Live* link under a remote's composer — what it opens; the keyless bar's *Use my key* on the local sshd). Long-form d49: a place with a git remote — *Commit the current changes with a good message and push* — the pills before/after (Push, Changes), the worker's commit card, the relaunch. Kickoff f1/f2.
- **Cycle 84 (opened 03:57 UTC, 09-20; closed 04:43)**: progress — **remote, Try Live and the keyless bar** (`cycle-84/remote/`, the local sshd): *Try Live* opens a strip over the composer — *Live · cursor · no frame · Close* — and, the remote having no display, carried the capture tool's stderr whole (*scrot: Can't open X display. It \*is\* running, yeah? [NULL] (…)*) — F-259, in [#756](https://github.com/unarbos/arbos/pull/756): *No screen to capture: the kernel has no display where it runs.* The bar's *This session only* lends the key, the bar goes and the agent answers *keyed.* — but two lines said it, this window's *openrouter key in place on this kernel (memory)* and the kernel's *Model key set for openrouter (…): kept in memory for this kernel only* — F-260, in #756: one line, the window's, whichever comes first. **Paste** (`cycle-84/paste/`): an image on the clipboard pasted into the composer adds nothing — the composer has no image paste at all (Cursor takes a pasted screenshot as an attachment) — F-261, **open**: a `PasteAttachment` action was written (gpui's `ClipboardEntry::Image` → a temp file → `add_paths`, falling back to the field's paste) and **reverted unverified** — on this rig neither the driver's keystroke nor a real X `ctrl+v` pastes even text into the field, old build and new, so the change could not be measured; for the composer owner to verify on the Mac (R45: the clipboard is a rig blind spot). **1600×1000 with the panel** (`cycle-84/sizes/`): the panel 591 px, the chat column keeps its 708 px and slides from x 446 to 150 — Cursor's shape. **The mic note's red**: `#d95e53` on `#212121` — the theme's danger over the composer; Cursor's not-available notes are muted grey (F-192's number for Jacob). **Long-form d50** (`cycle-84/d50/`): the worker made `fix/todo`, committed and pushed the change; `gh pr create` hit the real, unauthenticated `gh` — the rig's fake is `fake-gh/gh.sh`, not `gh` on PATH as the gate stages it — so no PR, no pill; the root reported the auth failure plainly. Rig misconfiguration, not a finding; the gate's `pill-prs` row covers the PR path each run. **Kickoff f1/f2** on the final build: 11.8 s / 9.1 s. **Gate `cycle84-full`** on desktop `98311b3` (tree `98311b3`, the F-260 build, this branch's head) — kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`: **139 pass / 1 fail** — `window-expand-pinned` (R33 in #703). **What the rig cannot see:** the clipboard (paste of any kind); Try Live with a real screen behind it (a remote with a display); the Mac's key bar. **Cycle 84 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned was: from the oldest rows — **remote places** (78: the *Try Live* link under a remote's composer and the keyless bar's *Use my OpenRouter key on this machine* on the local sshd — what each does, the bar after); **composer attachments** (77: the paste path — an image on the clipboard, ⌘V into the composer, the thumb; `xclip -t image/png`); **window sizes** (77: 1600×1000 with the panel open — the chat's width cap and the panel's 592 share); **voice** (78: the mic note's red — F-192 re-read with the theme's danger vs Cursor's muted; a number for Jacob). Long-form d50: the p81 repo — *Create a branch fix/todo, commit the change there, push it and open a PR with gh* — the fake gh, the PRs pill, the worker's card, the relaunch. Kickoff f1/f2.
- **Cycle 85 (opened 04:45 UTC, 09-20; closed 05:15)**: progress — **Project tab** (`cycle-85/panel/`): the *Kickoff* page item opens `project-context.md` as a second panel tab (its front matter's *version: 1* drawn as text — the page's own head); the Files rows the same; and every file so opened read *<path> · unsaved* before a keystroke — the dirty test measured the editor's text against the file's bytes, which the editor normalises — F-262, in [#756](https://github.com/unarbos/arbos/pull/756) (*saved* is the editor's reading at open). The driver cannot type into the file editor, so the edit half is by reading. **Tab face on the strip**: not re-driven (no context menu on right-click; the face sheet opens from elsewhere) — cycle 41's coloured glyphs stand. **⌘K Chats hit** (`cycle-85/search/`): *copy* → the two forks; ↵ lands on the second chat and closes the palette — holds. **Relaunch with a worker mid-command** (`cycle-85/relaunch/`): the worker's pane after the relaunch reads *Working · Calling bash ⌄* from the step file with the Stop square, no running card (F-252's limit: the kernel does not replay an open call), and settles with the tool row when the command ends — holds for a child as for the root. **Long-form d51** (`cycle-85/d51/`): three workers, one per doc — three *Working* lines at +5 s, then *⚙ Append to Glossary done — …* ×3 under *Worked 9s*, *Updated the project page 3 times*, one *1 call failed* run line between them (a `plan add` the kernel refused), the answer, a *3 Files Changed* card with ± per file; the relaunch the same. Nothing new. **Kickoff f1/f2** on the final build: 11.3 s / 9.1 s. **Gate `cycle85-full`** on desktop `98311b3-dirty (tree 14c1637)` — the F-262 build, this branch's head — kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`: **142 pass / 2 fail** — `window-expand-pinned` (R33 in #703), `world-back-in-order` (flaky; passes alone). **What the rig cannot see:** the file editor's keystrokes (the driver's type does not reach it); Cursor's Project panel page on the Mac; Cursor's three sub-agent rows at 2x. **Cycle 85 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned was: from the oldest rows — **Project page** (80: the Project tab's Resources section and a page item's click — what opens); **tabs** (79: the face sheet's colour and glyph applied — the strip's glyph tint against Cursor's project colour dot); **search ⌘K** (82: a Chats hit opened with ↵ lands on that chat — the active_session and the pane's scroll); **relaunch / replay** (79: a relaunch with a worker mid-command — the worker's own pane after: F-252's rule for a child). Long-form d51: a research place with three docs — *Spawn three workers, one per document, each adds a Sources line; wait for all; then summarise* — the three lines, the three Done rows, the Files Changed card's count, a relaunch. Kickoff f1/f2.
- **Cycle 87 (opened 06:15 UTC, 09-20)**: from the oldest rows — **tabs** (79: eight tabs at 900 wide re-driven on this build after F-263 — the strip's cycle keys, ⌘W's target with the drawer open and shut, a closed tab's reopen from the opener's recents); **launch, first run** (80: a fresh XDG's first run — the permissions sheet, the Home face, the shimmer's length against the kernel's first frame); **opener** (80: `~//` after F-250 on a local path; a prefix sibling; Create on a path with spaces); **rewind / fork / checkpoint** (80: rewind on a place whose turn ran a worker — the worker's row after; a fork of a chat with a parked ask). Long-form d53: the d51 place, fresh — *Make three notes files with a shared glossary; then rename the glossary and fix every link; then tell me what changed* — the edits' run lines, the Files Changed card across a rename, Review, a relaunch. Kickoff f1/f2.
- **Cycle 86 (opened 05:16 UTC, 09-20; closed 06:20)**: progress — **the d51 place opened to a fresh empty chat** where the root and three workers had been (`cycle-86/delete/`): the drive's `ctrl-a · backspace` to clear the composer had, on an empty field, gone to *the highlighted chat* — Backspace archived the chat in front, the second Backspace deleted it with its workers' kernel folders, no word said; the window's rest-focus Backspace did the same. d47, d48 and d51 lost their chats this way, and F-257's vanished rows were most likely this — **F-263**, in [#756](https://github.com/unarbos/arbos/pull/756): the composer's Backspace/Delete are typing whatever the field holds; the Delete/Backspace binding moves to the drawer's context, where a lit row still archives then deletes (verified both ways on rl85). **Mode chip** (`cycle-86/slash/`): typing `/mode` lit *model* and ↵ completed to `/model ` — two prefix matches of equal rank, the list's order decided — **F-264** (vendored bezel-ui: an exact match ranks first); after, `/mode` ↵ sends and the kernel answers *No mode is pinned. `/mode <skill>` pins one of: (no skills here).*; `/mode auto` with the mode already auto says nothing (kernel). **Settings** (`cycle-86/settings/`): the Model section's three provider chips, key field with Save and Paste, eight picks; *flash* in the search narrows to twelve picks (the catalogue's Flash rows); the key paste is unreachable from the driver (R45). **Agents pill**: its click opens the Agents card above the composer (rows, ✕) — holds. **Bottom bar channel switch** Stable→Dev on this build: the plate stays *unreachable* (no release feed on the box) — the switch cannot be read here. **Long-form d52** (`cycle-86/d52/`): the coordinator's fence refused the write, two workers wrote SOURCES.md (*Done · … — # Sources* rows, the file's first line as the worker's last words), *Explored 3 files, ran 3 commands, started 2 workers, updated the project page, could not edit SOURCES.md*; the mode's hand-off line read */mode auto rejected the request, so anthropic/claude-opus-5 answers this turn.* — **F-265**: *Mode auto passed this turn to claude-opus-5.* with the id behind Details. **Kickoff f1/f2** on the final build: 12.2 s / 9.5 s. **Gate `cycle86-full`** on desktop `247f0272` (the F-265 build; the head `9fc50438` only drops a dead enum variant) — kernel `arbos-kernel 0.2.0 e504bf2f48ad protocol 1`: **140 pass / 3 fail** — `window-expand-pinned` (R33 in #703), `world-back-in-order` (flaky; passes alone), `composer-queue` (flaky: 4 of 45 gates; no key of F-263's in its path). **What the rig cannot see:** the clipboard (the key paste, image paste); a real release feed for the channel switch; the Working pill with two workers on the Mac. **Cycle 86 closed** — #756 a held draft per the coordinator, with #762 and #764. Planned was: from the oldest rows — **settings** (77: the Model section's key field and picks on this build — a key pasted is unreachable; the provider chips and the pick's mark); **under-composer pills** (81: the Agents pill's click — what it opens — and the Working pill's count with two workers); **bottom bar** (83: the update channel switch Stable→Dev on this build — the plate's change and the tooltip); **mode chip** (79: `/mode` with no argument — the list; `/mode auto` back). Long-form d52: the d51 place — *Read the three docs and write docs/SOURCES.md listing each file's Sources line; do it yourself* — the read run, the refused write (the coordinator's fence) or the edit card, Review, a relaunch. Kickoff f1/f2.
- **Cycle 41 (opened 08:20 UTC, 09-18)**: the VM was reset at 08:00 (fresh disk, `/tmp` gone — every rig binary, drive and gate log; the store kept everything). Rebuilding from main `883a47da`; the fresh box wanted the BUILDING.md apt list, `scrot` and `python3-pil` before the desktop would build — the list held. Items, from the coverage table's oldest rows: (1) **tabs** (last 36): cycle, close, reopen, colours, spinner beside `cycle-36/tabs/`; (2) **after-failure states** (last 33): key removed mid-session and the kernel binary replaced, on the new kernel; (3) **themes** (last 35): light theme page and rail beside `cycle-11/13-light`; (4) kickoff f1/f2 every cycle. Gate on the final build; close.
  **08:51 — baseline gate `cycle-41a` on main `883a47da`, rebuilt box: 134 · 3 · 17 · 21 · 7** — `composer-context` (old), `notify-os-posted` (fresh box had no `notify-send`; R30, [#609](https://github.com/unarbos/arbos/pull/609) names the package), `world-back-in-order` (phase D, cold-box timing). Rig back: main binaries at 1741, 1Password key (doppler has no token on this box), display :1, dunst/libnotify installed. Next: (1) tabs.
  **09:35 — (1) tabs** driven (three places, cycle, hover, spinner, close; `cycle-41/tabs/strip-stack.png`): the spinner on a working tab, the hover ×, the close all as in 36; ⌘⇧T reopens nothing (no such chord — noted, not a diff). Found **F-186**: the Home tab read *.arbos* and every tab was blue — the kernel's `project.toml` read as a face; [#611](https://github.com/unarbos/arbos/pull/611). **10:05 — gate `cycle-41b`** on it: 130 · 2 · 17 · 29 · 7 — `composer-context`, `world-back-in-order` (also on the baseline; cold-box phase D). `new-project-kickoff` read *exists, not on screen* at t+2 s; by hand on the same build it is on screen at t+2 — timing. The settings phase stalled 17 min on store copies (the FUSE throttle). Left: (2) after-failure states on the new kernel, (3) light theme, (4) kickoff f1/f2.
  **10:30 — ask card per mode, paired at last** (#569 made mode follow the tree): a worker in an ask-mode tree raises *write: hello.txt · Skip · Run ↵* in its tab — Cursor's approval row minus *Always Run*; answered, it folds to *Question · allow write: hello.txt · allow*. Found F-187 (root line says *Working* while the worker waits on the person) and **F-188** (the mode notice cut to *mode*; [#623](https://github.com/unarbos/arbos/pull/623)). **10:57 — gate `cycle-41c`**: 122 pass through phase D; `composer-context`, `world-back-in-order` (every gate on this box), and a phase-B cluster (three `notify-*`, `new-project-kickoff`) that a phase-B re-run on the same build cleared (37 · 0) — the box was slow under the full gate. Left: (2) after-failure states, (3) light theme, (4) kickoff f1/f2.
  **11:05 — (2) after-failure, key gone mid-session**: the kernel re-reads the key per turn and says why; the window said *no reply from the kernel* first — F-189, [#631](https://github.com/unarbos/arbos/pull/631). Key line removed altogether: the kernel falls back to its environment and answers (no UI state). **11:16 — gate `cycle-41d`** on it: **135 · 1 · 18 · 21 · 7** (`composer-context`) — the first clean full gate on the rebuilt box. Left: kernel binary replaced (phase D covers the delete; the replace is `world-deleted-build-*`), (3) light theme, (4) kickoff f1/f2.
  **11:50 — F-183 fixed** ([#636](https://github.com/unarbos/arbos/pull/636)): the archived list's second indent was the archived-only rows (no chat here); one indent now, with their last words. The two attachment crops that missed the store in cycle 39 (`01b`, `01c` hover states) regenerated on the current build and stored under `cycle-39/attachments/`. **12:14 — gate `cycle-41e`** on the F-183 build: 133 · 2 · 18 · 21 · 7 — `composer-context`, `world-back-in-order` (this box), and R29's `ask-skip` wording ([#640](https://github.com/unarbos/arbos/pull/640) fixes the rule). Left: (3) light theme, (4) kickoff f1/f2 on the final build, close.
  **12:35 — af-04 re-check** on main `dc633b7d` (QA's kernel fix `80e69942` in it): phase D run six times — five clean (11 · 0), one died at `seed-project.sh` before launch (rig, transient); `world-back-in-order`, which failed on every gate of this box since the reset, passes every time now — it was the kernel's af-04. Ghost check after runs 3, 4, 6: only the renamed-back folder under `~/parity-world-tmp/`, no `-moved` leftover (0 of 3). No kernel bug filed.
  **12:40 — (4) kickoff f1/f2** on main `dc633b7d` (`cycle-41/kickoff/`): f1 *Choosing the next step* under the shimmer (#569's words) → *Hey — the place is ready…* at 11 s; f2 one refusal line with *Details ›* (F-185 holds). Two things from [#629](https://github.com/unarbos/arbos/pull/629) landed meanwhile and want a pair next cycle: a **Clear** control in the chat header (Cursor's header has ⋯ and the panel toggle, no Clear), and the *This Computer* machine pill gone from under the composer (Cursor keeps its *Cloud* pill there). (3) light theme: F-163's numbers stand with the theme owner; not re-paired. **Cycle 41 closed**: F-186 (#611), F-188 (#623), F-189 (#631), F-183 (#636), R29 (#640), R30 (#609); F-187 noted.
- **Cycle 40 (opened 05:30 UTC, 09-18)**: from the coverage table's oldest rows — (1) **notices, nudges, refused-answer shape** (last 13; F-45 open): reproduce a refused call on 1741 and pair its card; (2) **panel** (last 12): the tab row and the Project tab beside `side-panel/` and the measured doc; (3) **kickoff f2** (refused provider), every cycle. Gate on the final build; close.
  **05:50 — (1) F-45 closed** in [#573](https://github.com/unarbos/arbos/pull/573): a refused `write` read *Edited hello2.txt +1* with a diff card of words never written; now *Could not edit hello2.txt* on the run line and the row, error beside, no ±. Gate `cycle-40a` on it: 132 · 5 · 18 · 19 · 7 — `composer-context` (old), the Stop-over-spawn trio (kernel, flaked back on `233ee65`), and two rig false positives (R29). **06:25 — (2) panel tab row** paired: F-184 in [#583](https://github.com/unarbos/arbos/pull/583) (`+` after the tabs, × on hover). The Project tab's body differs from Cursor's by design (ours: head with path, Agents, archived, TL;DR, pages, Files; Cursor's: title, Focus card, checklist sections). Gate `cycle-40b` on it: **135 · 1 · 18 · 21 · 7** (`composer-context`). **07:05 — (3) kickoff f2** (bad key, cold place): the refusal line drew twice — F-185, [#591](https://github.com/unarbos/arbos/pull/591); one line after. The rest of f2 as before: no shimmer past t+1 s, the line with *Details ›*, no Retry (no prompt to retry). Gate `cycle-40c` on the f2 build: 76 · 3 (the Stop trio) through phase P when the VM was reset at 08:00 and the log went with it; #591 merged on CI. **Cycle 40 closed**: F-45 (#573), F-184 (#583), F-185 (#591); R29 noted.
- **Cycle 39 (opened 00:45 UTC, 09-18)**: (1) **F-179 caught in the act**: `stop_hold_drive.py` until a run shows the held Stop with both sides logged each second — the kernel's transcript ends and `status.toml` against the window's `turn_open`; then the desktop half (a `turn_open` the kernel has ended is the window's) or the kernel half (filed) with the lines quoted. (2) **F-174** re-driven on build ≥ 1718 (#511 carries `seq` through a reload): rewind on card N cuts to before N and refills N. (3) **composer attachments** (last 21): file chip, image thumb, hover ×, mid-send — beside `cycle-21`'s Cursor captures; the drag-in path. (4) **mode chip / ask cards per mode** (the chip half untested since 12): `/mode ask` then a tool that asks — the card's shape against Cursor's approval row. (5) Kickoff cold f1/f2. **Long-form d17**: the research place — *Archive the three Add Footer workers, then tell me which documents changed today and by whom* — archived rows, context re-found. Gate on the final build with the R27 rig; close.
  **01:02 — gate on main's head `a26273c` (build 1733), kernel `arbos-kernel 0.2.0 7e19f9e90947 protocol 1`: 132 · 1 · 19 · 18 · 7** (+4 pass-after-recover), the first clean full gate since 37c; #531 (F-170) and #536 are in it. The one fail is a single held Stop, now self-documenting ([#540](https://github.com/unarbos/arbos/pull/540)): window held *Create report md* busy, the kernel's folder for `create-report-md` had no readable transcript at that second — a worker mid-spawn; recovered on the next try. Item (1) F-179 continues with the probe. **01:12 — the probe caught the shape once at 1 s** (run 4, with the gate's steer/queue/Send-now sequence before Stop): the worker's session `turn_open=True` while the kernel's transcript for it read `turn_complete` — the window's side. Not yet held for minutes under the probe; the desktop's child `turn_open` closing is the thread to pull. Left: F-179's desktop half, (2) F-174 on 1733, (3) attachments, (4) ask card per mode, (5) kickoff cold, d17. **01:22 — (2) F-174 closed on 1733**: cut to before the clicked card, composer refilled with it (`cycle-39/rewind/`). #540 merged (`c3247332`). Left: F-179's desktop half, (3) attachments, (4) ask card per mode, (5) kickoff cold, d17. **02:20 — F-179 desktop half** in [#549](https://github.com/unarbos/arbos/pull/549) (probe reads archive, any open turn, over a Running card); gate `cycle-39b` on the branch: 129 pass, 4 fail — `composer-context` (old), `composer-stop`/`stop-word` (the kernel's Stop-over-spawn, failed in 35r/37b/38/38b too, passed once in 39), `connect-first-spawn` (rig, R28, fixed in the same PR; `LX` re-run passes). Left: (3) attachments, (4) ask card per mode, (5) kickoff cold, d17. **03:15 — F-174 re-driven on 1741** (kernel `9bb49c6` built from main): same clean cut. Found **F-180** on the way: the new kernel doubles a chat's history at launch about half the time (tail cursors from line one + the pane's seq mark at 0 for old records); desktop fix in [#560](https://github.com/unarbos/arbos/pull/560) (#549 had merged at 02:41), kernel ask filed. **03:36 — gate `cycle-39c`** (#549 + #560 desktop, kernel `9bb49c6`): 136 pass, 1 fail (`composer-context`, old). The Stop rows and `recover` passed on this kernel. **04:25 — (3) attachments** re-paired against `composer-attachments/` on the 1741 kernel: image thumb/hover/sent already match; the file chip was the one gap left (F-109) — now a token in the sentence, composer and bubble ([#565](https://github.com/unarbos/arbos/pull/565), merged `f99ba703`; `cycle-39/attachments/`). Found F-181 (*Working jev*). **(4) ask card per mode**: the card never appears for an ordinary write in ask mode (F-182, kernel) and the coordinator's `write` is fenced — nothing to pair; filed for features. **(5) kickoff cold** f1 (`cycle-39/kickoff-cold/`): *Setting up environment* from t+1 s to the answer at 13 s, *Worked 13s* fold — matches the reference; no new diff. **d17** on the research place: the five Add Footer workers were already archived by the kernel; the model logged them as Done and answered which documents changed and by whom from the records; the panel's *22 archived* list opens (F-183 noted). Gate `cycle-39d` on the attachments build: 123 pass, 1 fail (`composer-context`) through phase N, then the VM's disk filled (254 G, 22 G of old gate stills) and phase D died on `No space left on device` — rig, not product; cleared 140 G. **05:21 — closing gate `cycle-39e` on main's head `233ee65` (desktop and kernel both from main; the kernel carries the tail-cursor fix for F-180's kernel half, 2cff4bb9): 133 · 1 · 18 · 24 · 7** — the one fail is `composer-context` (old). Cycle 39 closed: F-174 closed, F-179 desktop half (#549), F-180 (#560), F-109 closed (#565), R28; F-181/F-182 filed for features, F-183 noted.
- **Cycle 38 (closed 00:45 UTC, 09-18)** (PRs: [#530](https://github.com/unarbos/arbos/pull/530) F-165's maker — merged; [#531](https://github.com/unarbos/arbos/pull/531) F-170 — open, watched): fork/rewind (fork matches; F-174 rewind off by a turn, F-175 the files-not-restored line), ⌘K search (F-176), pills (F-177), reconnect through the pill (F-178), F-165 corrected to the desktop and fixed, the kickoff row's Escape. **Gate 32 · 6 · 37 · 2 · 1 twice** on `869eb05` (build 1718), kernel `7e19f9e90947` — both runs ended by R27 after the held Stop (F-179); the last clean full gate is cycle 37c's 134 · 1 on 1657. **What the rig cannot see:** fork/rewind — Cursor's restore card on the Mac; search — Cursor's filter chord on a Mac keyboard; pills — Cursor's Push against a real remote; reconnect — a real tunnel drop (ArbosLife untouched). **Carried:** F-179 (first), F-174 (re-drive on a build with #511), the panel pairing proper, the fold summary across a relaunch, the *root replied* banner.
- **Cycle 38 (opened 23:35 UTC)**: (1) **rewind / fork / checkpoint** (last 20): p14 beside Cursor's restore-checkpoint — rewind by line after two turns, fork mid-run, the checkpoint kept on the fork (F-103); one still each. (2) **search ⌘K** (last 21): the chat search palette beside Cursor's Search (sidebar): open, type, pick, Escape; result rows' shape. (3) **under-composer pills** (last 20): repo vs non-repo, Changes, Commit & Push, PRs, Working — beside Cursor's `Changes +6 −1 · Push` row seen in the parity chat this cycle (`cycle-35/themes/cursor-p8-*`). (4) **remote places** (last 15): reconnect states on a local host entry with the connection-honesty pill (#467) — the fault, the countdown, the recovery — and the attach on ArbosLife read-only (no install). (5) **the kickoff row's Escape** (gate 37c's one fail) and F-170 (the report caption). Kickoff cold each cycle. **Long-form d16**: the research place — *Fork this chat, and in the fork ask for a one-line summary of docs/manifests.md; then rewind the original to before d15's footers.* Owner check first on kernel paths. Gate on the final build, then close.
  **23:45 — F-165's maker corrected and fixed:** the features agent placed it in the desktop (`attach_or_spawn`'s bootstrap on every attach; the sidecar's `init` recreating the path) — [#530](https://github.com/unarbos/arbos/pull/530), driven (old path stays gone 30 s under two flushes). My kernel note stands corrected. QA's `hi` card fix is [#511](https://github.com/unarbos/arbos/pull/511), open, main merged in. **23:50 — (1) + d16 driven** on build 1657 (`cycle-38/fork-rewind/`): the fork opens a new tab whose copy answers on its own and leaves the original untouched — Cursor's shape; the rewind cut one turn fewer than the card clicked and refilled the composer with the clicked card's text (F-174, open, likely the lost `seq` that #511 carries again); the kernel's *files not restored* line draws like a failure (F-175, kernel wording). (5) the kickoff row's Escape fixed in the rig (#505). Kernel `arbos-kernel 0.2.0 7e19f9e90947 protocol 1`. Left: (2) ⌘K search, (3) pills, (4) remote reconnect states, F-170. **23:55 — (2), (3) paired** (`cycle-38/search/`, `cycle-38/pills/`): F-176 (filters, match colour, snippet rows, filter chord), F-177 (Push vs Commit & Push, no Agents pill in Cursor, the ↓ disc). #511 (the *hi* card), #512, #505 merged — QA's `desktop-user-message-card` is on `main`. Left: (4) remote reconnect states, F-170. **23:58 — F-170** [#531](https://github.com/unarbos/arbos/pull/531). Left: (4) remote reconnect states; then the gate on main's head (1657 + #505/#511/#512) and close. **00:02 — (4) done** (F-178: a kernel killed under a live chat recovers in 3 s with the pill saying so and nothing on the transcript; the fault states stand from cycle 35). #530 merged; main's head `869eb05` (build 1718) under the cycle's gate now (`gate38.log`), kernel `arbos-kernel 0.2.0 7e19f9e90947 protocol 1`. **00:45 — closed.** Two full runs (`cycle-38`, `cycle-38b`), both **32 · 6 · 37 · 2 · 1**: the kernel held Stop after P_LONG in each and R27 ended the run after the second failed recover rather than cascade — the 37 not-reachable are that. Phase T alone and the two-sided probe pass on the same build; F-179 open with the probe written.
- **Cycle 37 (closed 23:35 UTC)** (PRs: [#511](https://github.com/unarbos/arbos/pull/511) F-173 the New Chat bubble, [#512](https://github.com/unarbos/arbos/pull/512) F-172 workers after a relaunch, [#505](https://github.com/unarbos/arbos/pull/505) rig R24–R27 — open; #494/#496/#497/#498 merged during the cycle): the Project page paired as the drawer's Project tab (F-171), d15's relaunch (F-172), QA's user-card defect (F-173), the settings walk scrolling, the notices/kickoff items closed against earlier pairings. **Gate 134 · 1 · 20 · 18 · 7** on `dbad816`, kernel `7e19f9e90947`; two earlier runs tainted by the kernel holding Stop (three times today; filed). **What the rig cannot see:** the drawer — Expand's effect at a width where the chat's minimum binds, and Cursor's panel on the Mac; d15 — the Mac's rows at 2x during the relaunch; the user card — Cursor's exact bubble padding at 2x. **Carried:** the panel pairing proper once the side-panel work settles, F-170 (report caption shows the id), the unplaced *root replied* banner, the fold summary changing across a relaunch, the kickoff row's Escape timing.
- **Cycle 37 (opened 21:10 UTC)**: from the coverage table's oldest rows (13) — (1) **Project page** (last 13): in/out, Back, Escape, ⌘1, the files grid, notes render — beside Cursor's Project panel tab (`Project` in the right panel: header, *Context sync is not running*, the page's sections) at 1440; the page's scroll and its close (Jacob's -24 said there was no clear way out — check the Back row and Escape read as one). (2) **relaunch / replay** (last 13): root and worker read like live after a relaunch mid-run — d15 (d14's prompt with the relaunch fired on `connection == live` of a child, not a state word). (3) **notices, nudges, refused-answer shape** (last 13): the system-line class from F-153 beside Cursor's — a refused answer (f1's switch notice), a nudge (*Project page not updated*), a plain notice — one still each, both apps. (4) **the settings walk scrolls** (R24's next): scroll the body to a control before clicking so `reveal-*` become rows; and the panel pairing if the side-panel PR has landed. (5) Kickoff cold f1/f2. **Long-form d15** = d14 + relaunch. Owner check first on kernel paths. Gate on the final build, then close. **21:20 — (1) done as the drawer's Project tab** (build 1616 has the side-panel rewrite: drawer closed by default, tab row + | Project | ⤢ ×, a transcript rail): F-171 for the panel owner (the + position, Expand's no-op at 1100 wide, a stale tooltip); R25 the gate's page rows now drive the drawer (`#505`, phase B 38 · 0 · 1). Cursor's side for this is in `cursor-side-panel-measured.md`. Left: (2) d15 relaunch, (3) notices shape, (4) settings scroll, (5) kickoff cold. **21:58 — (2) d15 done**: the relaunch fired while five workers slept; the read-back lost their live state — F-172, [#512](https://github.com/unarbos/arbos/pull/512), driven on a rerun (rows *working* after the relaunch). **QA's `desktop-user-message-card` (qal-j24) taken as it arrived**: F-173, [#511](https://github.com/unarbos/arbos/pull/511) — a New Chat's first line drew as a worker's brief; and the session file's read side had been dropping the card's channel/worked_secs/sent_at/feedback/seq. Kernel under test `arbos-kernel 0.2.0 7e19f9e90947 protocol 1`. Left: (3) notices shape, (4) settings scroll, (5) kickoff cold. **22:07 — (4) done:** `reveal()` scrolls the settings body to a control before the walk touches it (#505, third commit); phase W on build 1616 reads `reveal-0/1/2` (45 · 0, none not-reachable). Left: (3) notices shape, (5) kickoff cold. **22:45 — (3), (5) closed out with what exists:** the notice shapes were paired in cycles 23 (F-114 retry line vs Cursor's) and 34 (F-153 system lines; Cursor has no nudge class — its system words are grey timeline lines); this cycle added only F-164 (the cut on a version dot). Kickoff cold runs in every gate as `new-project-kickoff` and was paired against Cursor in cycle 33/35. **Gate on main `dbad816` (build 1657) tainted:** 70 · 17 · 22 · 8 · 1 — the waiting-`spawn` holding Stop (third time today) cascaded through nine `recover`s, and the pile of recover chats pushed the drawer's first row off screen, which crashed phases R and W (R26, fixed in #505). Rerunning (`gate37b.log`). #494/#496/#497/#498 merged; #505/#511/#512 open (the red on #511 is the `standing_pass_e2e` flake; main merged in). **23:35 — closed.** `gate37b` held Stop again; an A/B of phase T on 1616 and 1657 passed both (23 · 0, 24 · 0) — the kernel's event, not the build's; R27 built so one such event cannot bury a run. **Gate `cycle-37c` on main `dbad816` (build 1657), kernel `arbos-kernel 0.2.0 7e19f9e90947 protocol 1`: 134 · 1 · 20 · 18 · 7** — the one fail the tab sheet still over the kickoff header when the row read it (rig timing).
- **Cycle 36 (closed 21:10 UTC)** (PRs open, each with its own drive: [#494](https://github.com/unarbos/arbos/pull/494) F-164, [#496](https://github.com/unarbos/arbos/pull/496) F-165 desktop half, [#497](https://github.com/unarbos/arbos/pull/497) `panel_shown`, [#498](https://github.com/unarbos/arbos/pull/498) F-169, [#505](https://github.com/unarbos/arbos/pull/505) R24; kernel asks filed: the moved place recreated by late writes): F-165 driven to its cause; launch/first run (F-166), tabs (F-167), picker (F-168), kickoff title (F-169), d14 (F-170) paired on build 1509 against live Cursor and a fresh-profile Cursor first run. **Gate on main 133 · 2 · 20 · 15 · 7** (`aaf3dbe`, kernel `7e19f9e90947`). **What the rig cannot see, per paired area:** first run — the Mac's own window placement and Cursor's macOS first-run (the fresh profile was Linux, unsigned); tabs — hover states and the drag/reorder feel; picker — Cursor's popover on a paid model set; d14 — the workers' rows on the Mac at 2x. **Carried:** the panel pairing (after the side-panel PR lands), d15 with the mid-run relaunch, F-170, F-165's kernel half, the unplaced *root replied* banner, the settings walk's scroll (R24's next step).
- **Cycle 36 (opened 20:20 UTC)**: from the coverage table's oldest rows (12–13) — (1) **launch, first run, permissions sheet** (last 12): cold launch with no state file beside Cursor's first run — window placement, the permissions sheet's rows and its skip, what the first frame shows before a project; the gate's `launch` phase rows read against the stills. (2) **tabs** (last 12): cycle ⌘⇧[ ], close, reopen from Home, the spinner and the dot, colours — beside Cursor's tab strip in the Agents window (its tabs are per-project pills in the sidebar; say where the metaphor differs). (3) **panel** (last 12): agents tree, archived row, Project section, Files, Standing — beside Cursor's right panel (Project/Browser) as measured in `cursor-side-panel-measured.md`; the driver's `panel_open` lie at 900 wide (F-161) gets a `panel_shown` field. (4) **mode chip, /mode, ask cards per mode** (last 12): p10/p11 in auto and ask modes beside Cursor's mode picker. (5) **F-165 driven alone**: rename the place under an idle kernel, watch who spawns at the old path, fix the desktop half if it is ours. **Long-form d14** on the research place: *In `.arbos/docs/`, which documents cite a source and which do not? …* (d13 with the path said), plus a relaunch mid-run to read the worker rows back. Kickoff cold f1/f2 each cycle as before. Owner check first for anything on a kernel path. Gate on the final build, then close. **20:30 — (5) F-165 driven:** the cause is the kernel's late writes through the absolute path after the move (`kernel.json` at the old path is the *original* kernel; `notes.md`/`spend.toml`/`notifications.jsonl` written after the rename) — kernel half filed; desktop half [#496](https://github.com/unarbos/arbos/pull/496) keeps the line (phase D 11 · 0 on the build). **Owner check:** the recreated folder is the kernel's; the dropped line was ours. **20:40 — (3)'s driver half done:** `panel_shown` in the driver and the gate's two panel rows read it ([#497](https://github.com/unarbos/arbos/pull/497); phase R 11 · 0, `open True->False shown=False`). The panel *pairing* against Cursor's right panel is still to do with (1), (2), (4), d14. **20:50 — (1), (2), (4) paired** on build 1509, kernel `arbos-kernel 0.2.0 7e19f9e90947 protocol 1`: F-166 first-run placement (Cursor centres 1280×800; the WM drops us at the corner at 1100×761), F-167 the tab strip (metaphor differs; our inactive label 37 levels brighter), F-168 the picker (Cursor: four settings rows; ours: the list), F-169 the kickoff wake titling a chat ([#498](https://github.com/unarbos/arbos/pull/498)). Cursor's first run captured from a fresh `--user-data-dir` on `:2` — a sign-in card, nothing else. #494 is MERGEABLE/CLEAN with checks green. Left: (3) the panel pairing (Cursor's right panel is measured in `cursor-side-panel-measured.md`; ours is being rebuilt by the side-panel worker — pair after their PR lands rather than against a moving target), d14. **20:55 — d14 run** (`cycle-36/d14/`): with the path said, three workers, Sources lines landed in README.md and outline.md, *Worked 25s ›* with the report under it, *Updated the project page 3 times*, the answer a list — the shape holds. F-170 (report caption shows the id). The mid-run relaunch did not trigger (the root's turn was 25 s; the drive's check read a field the driver does not have) — carried to d15. An OS banner *root replied* posted while the drive's window was in front; a probe of the rule (`window_active`, `touched`, `posted`) showed it holds when the person is looking, so the banner's cause is unplaced (the active chat may have moved to a worker) — noted, not a finding. Gate on `main` `aaf3dbe` (build 1616) running (`gate36.log`), kernel `arbos-kernel 0.2.0 7e19f9e90947 protocol 1`. #496's red kernel job is the flaky `stop_keeps_the_users_queued_follow_up_held…` e2e (main itself alternates red/green on it today); main merged into the branch to re-run. **21:10 — closed.** Gate on `main` `aaf3dbe` (build 1616), kernel `arbos-kernel 0.2.0 7e19f9e90947 protocol 1`: **133 · 2 · 20 · 15 · 7**; the two are one rig class, R24 ([#505](https://github.com/unarbos/arbos/pull/505)) — 133 · 0 · 22 by its rule.
- **Cycle 35 (closed 20:20 UTC)** (PRs: [#467](https://github.com/unarbos/arbos/pull/467) connection honesty, [#470](https://github.com/unarbos/arbos/pull/470) rig R21/R22 + BUILDING.md, [#482](https://github.com/unarbos/arbos/pull/482) Jacob's -29…-34, [#486](https://github.com/unarbos/arbos/pull/486) dead events, [#489](https://github.com/unarbos/arbos/pull/489) F-158, [#460](https://github.com/unarbos/arbos/pull/460) F-156 — all merged; [#494](https://github.com/unarbos/arbos/pull/494) F-164 open): the five items paired — bottom bar and three window sizes (F-161), mic disc (F-162), themes (F-163), kickoff cold (F-164), d13 run; Jacob's six reports; three coordinator items; Cursor's Settings interior measured. **Gates:** #467 109·5·23·10·2 (`382efcb`), #482 143·3·17·15·7 (`b3cae87`), #486 137·6·19·15·7 (`8d2c17c`), all kernel `7e19f9e90947`. **What the rig cannot see, per paired area:** bar/sizes — the macOS title band's join and the window's own min size from the WM; mic — the listening state (no microphone on the rig, Cursor's too); themes — colour accuracy (Xvfb, no colour management; the numbers are rendered values from the same display), tint at a value Jacob uses; kickoff cold — the Mac's 2x type; Settings interior — 12 px vs 13 px type at 1x. **Carried:** F-165 (open, drive alone), the model-titled chat (kernel ask), R19 (driver drag), -25 (Jacob's call), tint pairing, d13 with the path said.
- **Cycle 35 (opened 13:50 UTC)**: from the coverage table's oldest rows — (1) **bottom bar side by side**, never paired: version plate, the update control's idle state and the stranger plate against Cursor's status bar at 1440×900 and 900×600, with Jacob's `media/mac/update-bar/` still as the Mac reference; a gate row for the bar's plates. (2) **Window sizes** (last 11): 900×600, panel fold, 1600×1000 beside Cursor at the same sizes — what gives way first, the composer's minimum, the panel's minimum (the side-panel measurement's numbers are the reference). (3) **Voice / dictation entry points** (last 9): after F-146 the mic lands text in the field — pair the mic button's resting / listening states and the field landing against Cursor's dictation; the call strip's entry. (4) **Themes light / dark and tint** (last 11): p8 (headings, table, bullets) in both themes beside Cursor's, and the tint at two values; numbers from the theme file where the rig cannot see colour. (5) Kickoff cold f1/f2 (every cycle). **Long-form d13** on the research place after d12: *Which of the documents in docs/ cite a source and which do not? Read them, list each with yes or no, then give each document without one a Sources line — one worker per such document.* — workers come and go, archived rows, context re-found, the page updated. Owner check first for anything found on a kernel path. Gate on the final build, then close. **14:20 — two coordinator items took the tick, both ahead of item (1):** (a) the mesh worker's three desktop asks from Jacob's dead tab — F-157, [#467](https://github.com/unarbos/arbos/pull/467), driven on the two faults the rig can make (no binary; a host that does not resolve), the install narration on a real remote reasoned from the arm order and not driven (it would put a binary on ArbosLife again); (b) the build-failure question — the honest answer was that my gate could not tell, R21, [#470](https://github.com/unarbos/arbos/pull/470), driven against a genuinely stale binary; `desktop/BUILDING.md` written, `build-essential` added to the README's and CI's lists. **Owner check:** the phone's half of (a) is the phone owner's, said in the PR; the hub's half is done (#344, #417, #459). The selection highlight the coordinator names as the live complaint is already on `main` in #438 (F-151). Gate on #467's build next, then (1). **19:25 — the tick went to Jacob and to three coordinator items, none of the cycle's five yet:** (a) #467's gate **109 · 5 · 23 · 10 · 2** on `382efcb`, the five explained in the body (four = one rig fault, R22: the settings walk clicked `tab-close-0` under itself — fixed on #470 and rerun 49 · 0; one steer-timing flake); (b) **Jacob's six reports -29 … -34** (poll 17:54, build 1509): F-159 prices parsed as math, dot-folder links to the browser, links with no hand, the bare *reconnected* line, F-160 a looping kickoff flashing text/shimmer — [#482](https://github.com/unarbos/arbos/pull/482), merged by the steward while its gate ran; gate on its final build **143 · 3 · 17 · 15 · 7**, the three one kernel event (a waiting `spawn` held Stop 49 s — `features-inbox/2026-09-17-stop-waits-for-a-blocking-spawn.md`), phase T clean on the third run; (c) **Cursor's Settings interior measured** for the settings-tab worker — `internal/cursor-settings-interior-measured.md` (rows 60 px, 669 px column centred at both widths, controls centred on the row; Cursor draws a lifted surface, not bare rows); (d) **dead events**: seven `Event` variants handled and never sent, and the approval card only two of them could open — [#486](https://github.com/unarbos/arbos/pull/486), −592 lines; R23: the gate's `permission-strip` row was not-reachable in every run because the card could never appear; (e) F-158 the follow-up row the turn already took — [#489](https://github.com/unarbos/arbos/pull/489). **Owner check:** the Stop-over-spawn is the kernel's (filed); the side-panel wish in -30 is the panel worker's. #467, #470, #460, #482 merged; #486, #489 open. **19:35 — items (1), (2), (3) paired** on main's build 1509 vs live Cursor at 1440×900, 900×600, 1600×1000 (`cycle-35/bars-and-sizes/`): F-161 (bar and sizes match within 5 %; Cursor has no bottom bar; the driver's `panel_open` lies at 900 wide), F-162 (mic disc: Cursor light, ours dark; listening unpaired on Linux). Also captured Cursor's model popover (Thinking / Context 300K / Effort / Model) for the picker owner. **19:45 — (4) themes paired**: p8 (headings, table, bullets) in Cursor's parity chat and Arbos, dark and light (`cycle-35/themes/`), F-163 — Cursor's light page #F8F8F8 with a #E5E5E5 rail vs our pure white; dark within a warm step. Cursor's Appearance settings section captured for the settings-tab worker (`cursor-settings/appearance-1440x900.png`: sliders with a right-aligned value, `− 13 +` steppers, a theme dropdown — the value-control answer, boxed and centred). Tint at two values not done (Cursor's Hue/Intensity sliders sit at 0 %; ours would need a value Jacob uses). **19:55 — (5) kickoff cold f1/f2 on build 1509** (`cycle-35/cold/`): f1 (a model the key refuses) → the switch notice at t+2, *Worked 17s*, the greeting at t+17 — the #298 shape holds; one new fault, F-164, the notice cut on `gpt-5.4`'s dot (fixed on `cursor/notice-cut-aa39`, unpushed). f2 (a socket that never answers) → *Setting up environment · 44s* with the clock at t+45, no text, as designed; the 60 s hint was not waited for (cycle 16 has it). Cursor's cold pair is `cycle-33/cursor-cold-*` (unchanged Cursor build). **d13 run** (`cycle-35/d13/`): the coordinator read `docs/answers/` only, apologised for "misremembering", spawned nobody and touched nothing — `.arbos/docs/README.md` and `outline.md` still have no Sources line. Model/kernel-side (the prompt said `docs/`; the kernel's docs live under `.arbos/docs/`); the pane itself drew correctly (*Worked 16s ›*, prose). Not a desktop finding; noted for the prompt catalogue: d13 needs the path said. **Blocked at 19:45: GitHub auth vanished from the VM** (`gh auth status`: not logged in; `git push`/`ls-remote` ask for a username) — `cursor/notice-cut-aa39` waits; PR bodies cannot be updated until it returns. **20:20 — closed.** Gate on #486's build **137 · 6 · 19 · 15 · 7** (`8d2c17c`, kernel `7e19f9e90947`; the six accounted for in the body: the Stop-over-spawn kernel event, F-165 new and open, one lost session on a two-tab relaunch). Push returned at 20:07; [#494](https://github.com/unarbos/arbos/pull/494) (F-164) open; #486, #489 merged.
- **Cycle 34 (closed 13:50 UTC)** ([#438](https://github.com/unarbos/arbos/pull/438), merged; F-156 in [#460](https://github.com/unarbos/arbos/pull/460)): Jacob's eighteen reports (-11 … -28) read, routed, and thirteen desktop fixes F-142 … F-155 (dead links, lost fork answers, selection highlight, dictation lands in the field, blank thinking gap, system lines, the key-unavailable model off the picker, 14 pt default); F-137 from his capture; a status over workers goes with the last worker; the opener at `~`; d12 long-form; rig R19 (no drag-select in the driver) and R20 (`stop_phase`). **Gate 137 · 0 · 19 · 15 · 7** on `2e2905bd`, kernel `7e19f9e90947`. **What the rig cannot see, per paired area this cycle:** selection (F-151) — the selection *colour* on the Mac, and 2x text under the band; system lines (F-153) — the italic's weight at 2x; the opener at `~` on a real remote (`ArbosLife`) — the drive used a local host entry; the model picker (F-154) — the kernel's catalogue order on Jacob's key; d12 — the page's scroll physics. **Carried:** F-113 (kept), F-114 (open), -25 (Jacob's call on the three dots), the model-titled chat (kernel ask), R19.
- **Cycle 34 (opened 11:12 UTC)** ([#438](https://github.com/unarbos/arbos/pull/438), off `main`): opened by #423's merge with two of cycle 33's commits still on its branch (F-137, the status-over-workers rule) — cherry-picked here, the orphan is not dead. Items: (1) **Jacob's nine reports of 10:00–10:21** (`2026-09-17-11` … `-19`): -12 fetch label (F-143) and -13 body size 14 done; -11 (three cats), -14 (kickoff document format), -15/-16 (sources and doc chips not clickable), -17/-18 (fork lost outputs; rewind on a fork), -19 (voice: stop sends at once, reply spoken) to read and route — **for each, the cycle log asks first whether the kernel side already has an owner** (the coordinator's request after two of tonight's three duplications; #426 fixed the same stranger click as #423). (2) d12 long-form (carried). (3) F-141 the composer placeholder before the kickoff's first frame. (4) F-135 by seq. Gate on the final build, then close. **11:40 — (1) done:** all nine reports read, routed and their ledger rows filled; seven desktop fixes on `cursor/cycle-34-aa39` (`15eff595`), three inbox notes (search tool; rewind on a fork cut root's transcript — the worrying one; dictation, for the voice owner). **Owner check, per the new rule:** -12 search and -18 rewind are the kernel's and filed rather than touched; -19 changes a voice-side design and the owner is told in the inbox before it merges. Gate running on this build. **12:50 — eight more reports (-20 … -27, poll 11:51) read and routed**: -23 already fixed (F-147), -26/-27 fixed (F-150; the key-unavailable model off the picker), -20/-21/-22/-24 quoted verbatim to the side-panel worker (`features-inbox/2026-09-17-jacobs-side-panel-words-from-feedback.md`), -25 held for Jacob's call. Also this tick, from the coordinator: F-151 the selection highlight (fixed, real-pointer reproduction; rig R19 — the driver cannot drag-select), F-152 the opener at `~`, `internal/cursor-side-panel-measured.md` for the panel builder, `internal/project-entry-points-enumerated.md` for the coordinator. **Owner check:** the panel default, browser surface and project-page close are the side-panel worker's now; -26's catalogue-vs-key is the kernel's; -25 is Jacob's. Gate `gate34b.log` running on `e1e4764c`+; the two commits after it (-27/-26) need a rerun before the line. **13:20 — gate on the final build `2e2905bd`: 137 · 0 · 19 · 15 · 7** (kernel `7e19f9e90947`); [#438](https://github.com/unarbos/arbos/pull/438) marked ready. F-149 fixed (a steer's card awaits its echo). **Rig R20, from #451's author's find:** which *phases* can stop without saying so — five sites turned a gap-and-return into `stop_phase`, which records every skipped row and fails when the stop is the app's or the rig's. #451 (Settings as a tab) is open and rewrites the settings phase; whichever merges second rebases; with the window gone my settings phase now fails loudly instead of quitting. **Rule to carry into the side-panel cycle:** a chord acting on a hidden surface (⌘N/⌘K/⌘2 changed the selection behind the settings tab until #451 fixed it) — the panel's own chords and ⌘T will meet it. **13:25 — d12 done** (a notes restructure into Goal / Decisions / Done / In flight / Open questions / Next, no workers, Gemini 2.5 Flash): *Worked 9s* fold, the answer a short list with one doc chip per moved document, the page and the panel's Project section re-sectioned as asked (`cycle-34/d12/01–04`). One new finding from the panel: the main row was labelled *This project is a* — the desktop's four-word cut of the first prompt (F-156, fixed on its own branch so #438's gate line stays true; the model-titled chat Cursor draws is a kernel ask, filed). **13:50 — closed.** [#438](https://github.com/unarbos/arbos/pull/438) merged (`4c0a1315`, read from the tree; #451 merged after it and the tree holds both `stop_phase` and the settings-tab phase). F-156 on its own PR [#460](https://github.com/unarbos/arbos/pull/460).
- **Cycle 33 (closed 11:12 UTC)** ([#423](https://github.com/unarbos/arbos/pull/423), merged): af-03/F-134, the stranger click F-125/F-136, phase_world, Jacob's -6/-8 (F-138, F-139), the cold kickoff pair (F-140 design, F-141 open). **Gate 113 · 0 · 20 · 35 · 7** on `668f02f9`, kernel `7e19f9e90947`. **What the rig cannot see:** the Mac's type at 2x on the cold pair; the frame that set the F-137 phantom's Working on Jacob's machine. **Carried:** d12, F-135, F-141. Overlap to carry as a rule: #426 (update owner) fixed the same click; check the kernel side's owner before fixing a screen report.
- **Cycle 33 (opened 08:35 UTC)**: two findings handed over from the kernel side, both after-failure states the rotation never had a row for — (1) **`af-03`, a renamed or moved folder under a running kernel**: the window says "this agent is archived…" (untrue) and the typed line is lost without a word; make it say the folder is gone or moved and name the path, and keep the typed line (or say plainly it was not sent) — the fourth swallowed-message path; drive it: open a place, rename its folder mid-idle and mid-turn, type, read the notice and the line's fate. (2) **The stranger-kernel click**: with a kernel reported as running a deleted build, what the bar's click does and says is desktop behaviour, untested (F-125 found the click running the update path); pair with the update owner's half before changing it — read their PR, then drive: old kernel serving, binary replaced, click. (3) A **`phase_world`** in the gate for the new coverage row (rename the place, replace the binary, cut the socket; read notice / line / bar). (4) The kickoff / new-project landing cold (f1, f2) beside Cursor's Start new project — owed every cycle since 16. (5) Long-form **d12** (a notes restructure). Cycle 32's PR [#404](https://github.com/unarbos/arbos/pull/404) is on `main`, ready, with both gate lines. **09:32 — items (1) and (2) done** on `cursor/cycle-33-aa39` (off `main`, #404 having merged): F-134 af-03 driven idle and mid-turn (this kernel, `d73a25ae`, does not stop itself on a rename while idle or mid-command — its check is at turn start — so the desktop's own guard is what the person meets; the kernel-stopped wording is written but not driven); F-136 from the deleted-build drive with main's kernel; F-135 found and left open. Stills in `cycle-33/`. Rig: `reap.sh` for the rig's leftover kernels (R9 — 50 of them from the opener rows and old drives were serving when the drive began; one was the second stranger). PR [#423](https://github.com/unarbos/arbos/pull/423) (draft). Gate running on `a6c4e655` (`gate33.log`); then (3) `phase_world`, (4) kickoff cold, (5) d12. **09:55 — (3) done:** `phase_world` (gate phase D) — six rows, 6 · 0 on this build with main's kernel `7e19f9e9`; the full gate on `a6c4e655` 106 · 1 (`rewind-turn`, a wake 1 s before the click; the row now waits for a quiet stretch and passes on rerun). [#423](https://github.com/unarbos/arbos/pull/423) marked ready with both lines. Left: (4) kickoff cold beside Cursor, (5) d12. **10:08 — Jacob's five reports (poll 09:55, build 1335) took the tick:** -6/-7 the coordinator slept on its three finished workers (kernel ask filed) and the headline kept its stale status (F-139, fixed) plus a phantom *Delegate 1 · Working* (F-137, open); -8 his steer drew above the Done lines (F-138, fixed, driven); -9 the model answered a git-status question with its housekeeping (kernel ask filed); -10 a cross-project `say` design question (filed for Jacob and the kernel owner). Ledger rows filled. Fixes on `cursor/cycle-33-aa39` (`668f02f9`); gate for the PR's final build next tick, then (4) kickoff cold, (5) d12. **11:02 — F-137 closed from the coordinator's capture** (rows and parentage from the kernel's record, Working from evidence; driven on Jacob's seven session files) and **#432's drawing half** (a status over workers goes with the last worker; a running job outranks an older status) — both on `cursor/cycle-33-aa39` (`a8685e2d`). **(4) kickoff cold paired:** Cursor Start-from-scratch vs an empty folder, t2/t8/t45/settled in `cycle-33/cursor-cold-*`, `arbos-cold-*` — same shape (header, date, *Setting up environment*, *Worked Ns*, greeting, footer); differences noted below as F-140/F-141. Gate on the final build next, then close; d12 carries to 34.
- **Cycle 32 (closed 08:35 UTC)** ([#404](https://github.com/unarbos/arbos/pull/404), on `main`): F-130 glass floor on Linux; F-131/F-132/F-133 from the worker-chat pair; the remote fallback off `~/.cargo/bin`; R3 settings rows assert their values; R17 `send()` reads the field back; R1/R15 `seen()` in the journey. **Gate 108 · 0 · 19 · 35 · 7** on `daeeddfd`, kernel `d73a25aea876`; settings phase 34 · 0 on the final build. **Not done, carried:** the kickoff cold pair (f1, f2), d12. **What the rig cannot see:** the Mac's blur behind glass and under the brief's fade; the Mac's type at 2x. Old 90-min `symmetry-loop-next-cycle` timer removed — the 20-min tick is the loop's one clock (the feedback poll stays separate).
- **Cycle 32 (opened 06:15 UTC)**: first item done — the new-project landing beside Cursor's Create Project sheet (F-129 design difference; F-130 the glass fallback on Linux, fixed: an opaque floor under the tint; **cannot see:** the Mac's blur — this fix is for Linux eyes, Jacob's Mac never showed it). Gate 102 · 1 (a `recover` race in the rig, hardened); turn phase: `rewind-turn` refused once more — this time rightly: a late worker report woke the root into a new turn at the click (`turn_ended=None`), so the row now waits for that turn to end (rerunning). Then — the kickoff / new-project landing cold (f1, f2) beside Cursor's Start new project; a worker-chat prompt against Cursor's worker tab (classic style); the `exists`→`seen` sweep in `journey.py`; R3's ten most-used `unverified` rows given a state assert; one long-form prompt (d12: a notes restructure). **Turn phase rerun 07:21: 33 · 0** (`rewind-turn` passes once the row waits for the late wake turn). **Worker chat paired** against Cursor's worker tab (07:34–08:02): F-131 the lost "Worked for" (three causes, all in the clock), F-132 the brief as Cursor's folded card, F-133 the archive read back; pairs in `media/cursor-reference/cycle-32/arbos-worker-*.png`. **Store, checked after the coordinator's fault report** (`docs/store-fault-report-2026-09-17.md`): one file of mine fell in the 06:41–06:52 window and existed nowhere else — `internal/arboslife-shared-binary-my-install-2026-09-17.md` (written with #404 at 06:51); rewritten 08:10 from the cycle-30 log line and a fresh enumeration on ArbosLife, marked as such. Everything else of mine is present and current: the ledger (through F-130 at 06:34), this log, `gate-assertions-what-each-reads.md` (135 lines, the cycle-31 version with its header), `rig-audit-…` (R1–R16), the cycle-32 media (06:34). The rig's tooling lives in git (`desktop/qa/parity/`) since the second loss, so `internal/parity/` holding only the launch scripts costs nothing. **None of my earlier episodes has the restore signature** (an older, complete file in place of a newer one): the 09-16 12:20 loss and the second one took the whole `internal/parity/` directory; the cycle-14 kernel-asks note and `docs/project-chat-vs-agent-chat.md` were gone, not old; the `/tmp/symmetry-*.md` copies I keep are from 09-16 10:31 and the store's versions are strictly newer supersets of them. So the ledger stands; nothing to correct.
- **Cycle 31 as opened (05:35 UTC)**: R13 raised and **withdrawn** within minutes — "the window a third off-screen" was my own crop of the still (cut at x=200, window at x=100); the raw still shows the window whole, so **cycle 29's results stand**, F-122's trace included. The geometry read-back stays as a guard (`cursor/cycle-31-aa39`). **The deliberate pass over the gate's assertions** (`internal/gate-assertions-what-each-reads.md`): 119 checks classified; eight could never fail (R14) and now say `unverified`; positive existence meaning "seen" reads `seen()`. Gate on this rig build to follow. Then R10 (the edit turn's fold: a `run-*` line for the root's own command under a bare headline, so the fold row tests the turn under test); the `exists`→`seen` sweep in the gate and journey; the kickoff / new-project landing, owed every cycle (f1/f2 cold); a worker-chat prompt against Cursor's worker tab (classic style, owed since 24); one long-form prompt.
- **Cycle 30 as opened (05:16 UTC)**: relaunch Cursor cleanly and pair d11; light/dark side by side; the ArbosLife reinstall (read its build line after); R10's fold; the `exists`→`seen` sweep.
- **Cycle 29 as opened (04:20 UTC)**: the kernel's `--version` as every run's first row (done, `cursor/cycle-29-aa39`); #385/#372 read from the desktop's side — nothing blanks, two suggestions filed (`features-inbox/2026-09-17-builds-list-and-binary-gone-in-the-desktop.md`; one of them, `unknown` shas, I had backwards — inferred, not traced; corrected, and #388 fixes both). #372 is in `main` (head `6b186378` an ancestor of `main`, checked in the tree — its PR was closed by hand after merging); [#388](https://github.com/unarbos/arbos/pull/388) corrects it on top; F-122's recurrence (log `turn_open` around the click); the remote track — install `main`'s kernel on ArbosLife through the app's own path and read its build line; the Cursor side of d11; light/dark side by side; R10's fold.
- **Cycle 28 as opened (03:45 UTC)**: R1 for asserts (sweep `exists(` used as "seen"), R10 (a second edit prompt with the root's own read so the fold row tests the turn under test), R5 (`recover` marks later rows), then d11 with a clean kernel and the Cursor side, and light/dark side by side.
- **Cycle 27 as opened (03:20 UTC)**: (1) `rows_under` must agree with what draws — the P_EDIT fold; (2) d11 on the long-form place with a clean kernel, and the Cursor side of it; (3) themes light/dark side by side (owed since cycle 11); (4) if Jacob says yes to worker tabs, that is the headline instead.
- **Cycle 26 as scoped (02:15 UTC)**: F-123 by hand first — the gate's leftover state no longer holds the edit turn (phases O/X rewrite it), so the probe needs its own scenario: one delegated edit, then click each `work-*` fold and measure the footer with the driver's `find`. Then the coverage table's oldest rows: tabs (Cursor's italic worker tab beside the project tab, F-111's kept decision) and the panel against Cursor's sidebar, side by side in both themes; a long-form d-series prompt in the rotation. Poller fix #375 and cycle-25's #367 await merge.

- **Cycle 25** (branch `cursor/cycle-25-aa39`, [#367](https://github.com/unarbos/arbos/pull/367)): F-122 (a straggling heartbeat reopened a finished turn — Rewind refused, footer gone); QA's `im-02` answer — the stall hint as its own element, a silent `sleep 80` shows it at 65 s and a streaming command never does, `quiet_secs` in the driver; the kernel's "waiting on <worker>" line (#366) taken into the panel's main row and the driver, answer filed for its author; Jacob's first four feedback reports crossed the hub — the mid-run fork (F-124) fixed with a line under the copied prompt, two test sends, ledger filled; #372's stranger-kernel control driven for its author (F-125: the click runs the update path). Gate 109 · 1. Next: F-123 by hand; the bottom bar / tabs / panel side by side with Cursor, which the coverage table has owed since cycle 12.

- **Cycle 24** (branch `cursor/cycle-24-aa39`, [#363](https://github.com/unarbos/arbos/pull/363), stacked on #359): the two rows the cycle-23 report named next — F-111 (a worker's "Worked for Ns" and its Files Changed card from its own edits, driven: `cycle-24/arbos-worker-chat-after-f111.png`) and F-114 (the retry aside in Cursor's soft wording). Short by design: the poll and the symmetry timers fired together and the cycle-23 PR was still in review.

- **Cycle 23** (branch `cursor/cycle-23-aa39`, [#359](https://github.com/unarbos/arbos/pull/359)): the Cursor leg of the journey, stage by stage, at last — Cursor's *Journey* project given a worker-sized challenge, watched live (its provider was down that hour: `cursor-04` retry line, `cursor-06` "Taking longer than expected…"), Stop mid-flight (`cursor-08`: nothing drawn), a follow-up after the stop, Quit and relaunch (`cursor-12/13`: the project tab back, the worker's tab gone, the prompt oddly back in an edit card — Cursor's own reopen is not clean either). Arbos ran `journey.py` on the same build: 14/14, then 13/14 (J09 `NO TESTS RAN`, the model's file). Findings F-113 … F-121; fixed: F-115/F-116/F-117 (loop), F-118 Jacob's bars (one surface), F-119 Jacob's stuck screen (desktop halves of #362), F-120 process row over the chat, F-121 own regression. Rig: `rig.py` display pulse — a hung X fails the run loudly (proved on a SIGSTOPped Xvfb). Pairs in `media/cursor-reference/cycle-23/`. Rig: display `:1` hung ten minutes mid-run and freed when Arbos and Cursor were killed; `cursor-launch.sh` now returns to its folder before AppRun; `journey.py` stamps its stills folder so a rerun keeps the last set. The composer-with-attachment set the phone loop asked for is in `media/cursor-reference/composer-attachments/` (cycle 22).

- **Cycle 12** (branch `cursor/symmetry-cycle-12-aa39`, on `main` `848e003`): live Cursor (Project *Kickoff*, sidebar open) vs Arbos root on p22a, p23, p13, p21, p10 — the read-only / wrong-shape / steer class. Findings F-40 … F-44: a steer stays inside its turn, a typed option name is the pick once, the `ask` row hidden; F-37 seen again (bash returns after the first line); the Cursor side of the harness now wheels to the bottom before each shot. Mac leg: `qa-results` `inbox/cycle-12/capture-script.md` (ten captures incl. the cycle-11 fixes live). Pairs: `media/cursor-reference/cycle-12/c12b-*-end-pair.png`, before/after `c12-p13-before-cols.png` → `c12after-arbos-p13-end.png`.

- **Cycle 11** (branch `cursor/kickoff-live-aa39`, on #227 + #226): the live kickoff turn replaces the static greeting — header block, "Setting up environment", then "Worked Ns ›" + greeting + footer, the date line over it, typed words wait for it (`media/cursor-reference/cycle-11/kickoff-pair-cursor-vs-arbos.png`); the Mac worker's seven findings (F-21 … F-27, stills in `media/mac/cycle-11/`, reply in `media/mac/cycle-11/reply.md`) and two loop findings (F-28 bionic default, F-29 greeting under the prompt) fixed. Cold track: p1, p5, pp2, p6 on fresh places. Remote track: arboslife 1/2/4 and templar 1/3 run (install from nothing, fan-out + steer, relaunch, tunnel kill → reconnect in 2.8 s); desktop findings F-30 … F-32, F-35, F-39 fixed, F-33/F-38 open, behaviour F-34/F-36/F-37 in `features-inbox/2026-09-15-remote-track-findings.md`; stills `media/remote/cycle-11/`.

- **Mac fixes** (branch `cursor/mac-fixes-2-aa39`, on #225): Jacob's ten findings from the fresh Mac install (F-01 … F-10 in `internal/symmetry-findings.md`), one PR. The loop is widened from this cycle (Scope, Cold track, Mac measurement above); the ledger starts.

- **Cycle 10** (branch `cursor/symmetry-cycle-10-aa39`, on `main`): the approval card takes Cursor's row — "Skip" in plain words, "Always Run" when the agent offers a standing allow, "Run ↵" as the default, Enter in the empty composer runs it (reference `cycle-10/cursor-approval-row-reference.png`, from cycle 5's p16); the Working pill reads "Agents" once the workers are done and opens the same card with a check per worker (`cursor-idle-agents-pill-crop.png`); the ↓ disc sits centred low; two reasoning steps split only by a hidden `status` call read as one "Thought briefly", live and on replay; a worker's `plan` calls are checklist cards like `todo`. Pair: `cycle-10/worker-thoughts-plan-cards-before-after.png`.

- **Cycle 9** ([#223](https://github.com/unarbos/arbos/pull/223), rebased on `main` after #221 merged): the features agent's answers (#221) and the 09-13 report's open desktop rows. Reopen parity: a worker chat read back shows "Thought briefly / Thought for Ns" from the settled `thinking` records (secs kept, consecutive steps merged), the brief from the wake's `brief` field, `todo` calls as checklist cards; a root read back shows the date line, "Worked 29s" from the kernel's clock, three Done lines even for archived workers, "6m ago". Panel rows read "title — summary" (report row 16); the under-composer ring turns while any agent works (row 1; Cursor keeps one there always — `cycle-9/cursor-under-composer-ring.png`). Rows already closed in earlier cycles, confirmed on the stills: "Thought Ns" (3), PRs pill (4), thumbs + time (8), date divider (12), tab spinner on sub-agent work (15, #71 badge), repeated sub-agent block (22). Pairs: `media/cursor-reference/cycle-9/root-reopen-before-after.png`, `root-live-vs-reopen.png`, `worker-reopen-before-after.png`. Model for the thinking runs: `google/gemini-2.5-flash` with `reasoning_effort = "low"` (gpt-5.4-mini streams no reasoning through OpenRouter).

- **Cycle 8** ([#209](https://github.com/unarbos/arbos/pull/209), on `main` with the process-parity slices): the Working card, the `todo` checklist card, the title as the worker's live line (confirmed, nothing to add), `status` as the timeline step (confirmed). Cursor's Project chat shows a card above the pills during a fan-out — "Working", "Stop All ×", a spinner and name per worker — and Arbos showed only the pill (Jacob, from `cycle-7/s1proj-root-pair.png`). Arbos now stacks the same card over the pills on the root: opens by itself at fan-out, × closes it for that fan-out, the Working pill toggles it, a row opens the worker, Stop All cancels them; rows at Cursor's 29 px pitch. Pairs: `media/cursor-reference/cycle-8/working-card-pair-cursor-vs-arbos.png`, `working-card-before-after.png`, `working-card-crop-pair.png`. Also confirmed: a modified tracked `.pyc` raises no Changes pill and no Files Changed row (`cycle-7/s1after-arbos-pyc-only-no-changes.png` vs `s1after-arbos-pyc-and-main-changes-2.png`).

- **Cycle 7** (PR #199): the two-style track opens. Live Cursor Project *Parity* and its subagent vs Arbos root and worker. Root: project header, date line back, fork on hover. Worker: brief as the first card, no duplicated headline, status calls hidden, archived composer in Cursor's words, no pills. Doc: `docs/project-chat-vs-agent-chat.md`. Kernel asks: thought duration stamps, live worker thoughts (`features-inbox/2026-09-15-symmetry-two-styles-kernel-asks.md`).

- **Cycle 1** (PR #187, merged): p1, p2, p3. Prose 14/23, bare code plates, footer order, borderless bubble, no date line, branch/machine pills.
- **Cycle 2** (PR #188): p5. Changes + Commit & Push pills, Files Changed card, capitalised run lines, fontconfig sans on Linux.
- **Cycle 6** (PR #194): p1 and p5 regressions (no drift), p22 (#190 check: (a) as intended, (b) drift — coordinator ran pytest itself, filed), p20 (Project page vs Cursor's Files pane, by design). Kernel calls folded into the run line; Files Changed only after edits or workers.
- **Cycle 5** (PR #193): p21, p17, p19, p16, p15. Prompt card as typed; no bare run line. Cursor's approval card recorded (Skip · Always Run · Run ↵) for a later cycle.
- **Cycle 4** (PR #191): p10, p11, p12, p14, p13. Headline without diff badge; rewind line in plain words. Cursor queues follow-ups (opt-in steer) where Arbos steers by default (Jacob's decision, kept); both models asked in prose, no question card this run.
- **Cycle 3** (PR #189): p6, p7, p4, p8, p9. Fresh turn's timeline stays open, worker lines name their worker, Files Changed without junk and only after real work, Continue Working and Push pills, no bare Worked. Kernel ask filed: the coordinator refuses shell prompts instead of delegating (`features-inbox/2026-09-14-symmetry-cycle-3-coordinator-shell.md`).
