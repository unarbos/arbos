# e76d631847: fm-02-broken-place-config-is-silent-to-the-user (fm-02-a-place-mcp-config-that-does-not-parse-is-skipped-without-telling-anyone)

status: closed — fixed before it was filed; see "Closed" below
scenario: fm-02-a-place-mcp-config-that-does-not-parse-is-skipped-without-telling-anyone
feature: 
rollout: /home/ubuntu/arbos-qa/loop/rollouts/20260918T082246Z-fm-02-a-place-mcp-config-that-does-not-parse-is-skipped-without-telling-anyone
first_seen: 20260918T082246Z

## Detail

`.arbos/mcp.toml` does not parse and root's transcript says nothing about it (2 line(s) went to the kernel's stderr instead: ['mcp: /tmp/arbos-qa-fm-02-a-place-mcp-config-that-does-not-parse-is-skipped-without-telling-anyone-17mmc60g/place/.arbos/mcp.toml: TOML parse error at line 2, column 11']). The place's `notes` server is skipped and the name falls through to the next location, so the person gets a different server — or none — with the only record in a log the window never shows

## Suspected location

arbos-kernel mcp.rs load_servers — a config that does not parse is a thing its author needs told, on the transcript, not on stderr

## Repro

`python3 run.py --kernel <bin> --only fm-02-a-place-mcp-config-that-does-not-parse-is-skipped-without-telling-anyone`

## Closed

Not a live bug. This ran on **f80f0b663bac (2026-09-17 12:26)**, which is older than the fix.

- **Fixed by**: [#613](https://github.com/unarbos/arbos/pull/613), merged `23ef527c` 2026-09-18 09:33
- **Why it matches**: This draft's finding is that a place MCP file which does not parse is **silent to the user** — the two lines went to the kernel's stderr instead of root's transcript. #613 is exactly that fix: *a place file that does not parse is said on root's transcript*.
- **How the ordering was checked**: `git merge-base --is-ancestor f80f0b663bac 23ef527c` succeeds, so the kernel this ran on predates the fix. The draft carried no `kernel:` field, so the version came from the rollout's own recorded banner (`arbos-kernel 0.2.0 f80f0b663bac`) rather than from the clock.

`qal-j31`'s own write-up carries the verification on both arms — `a8678ac1` breaks and `f97bb348`
passes — so nothing here needs re-running. The features agent (kernel) confirmed there is nothing
further to change in the kernel for `fm-02`; the note is at
`internal/qa/inbox/2026-09-18-two-drafts-ran-on-older-kernels.md`.

Closed by QA, 2026-09-18, on that note plus the ancestry checks above.
