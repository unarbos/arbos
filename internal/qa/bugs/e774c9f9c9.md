# e774c9f9c9: fm-02-a-file-after-the-broken-one-handed-the-name-away (fm-02-a-place-mcp-file-that-does-not-parse-is-said-and-does-not-hand-its-name-away)

status: closed — fixed before it was filed; see "Closed" below
scenario: fm-02-a-place-mcp-file-that-does-not-parse-is-said-and-does-not-hand-its-name-away
feature: 
rollout: /home/ubuntu/arbos-qa/loop/rollouts/20260918T131435Z-fm-02-a-place-mcp-file-that-does-not-parse-is-said-and-does-not-hand-its-name-away
first_seen: 20260918T131435Z
kernel: arbos-kernel 0.2.0 a8678ac16636 protocol 1

## Detail

`.arbos/mcp.toml` does not parse and a `notes` server was started anyway: ['mcp: notes: MCP server notes sent no reply to tools/list']. A file after the broken one — `.cursor/mcp.json` here, or the machine's own — gave that name a different server, so the person is told its servers are off while something else answers to it

## Suspected location

arbos-kernel mcp.rs load_from — a place file that does not parse stops the walk there (#613, #644)

## Repro

`python3 run.py --kernel <bin> --only fm-02-a-place-mcp-file-that-does-not-parse-is-said-and-does-not-hand-its-name-away`

## Closed

Not a live bug. This ran on **a8678ac16636 (2026-09-18 12:30)**, which is older than the fix.

- **Fixed by**: [#644](https://github.com/unarbos/arbos/pull/644), merged `f97bb348` 2026-09-18 12:57
- **Why it matches**: The finding is that a file **after** the broken one handed the name away — a `notes` server started from `.cursor/mcp.json` despite the place file failing to parse. #644 stops the walk at the broken place file, so that cannot happen.
- **How the ordering was checked**: `git merge-base --is-ancestor a8678ac16636 f97bb3487540` succeeds: the kernel predates the fix by 27 minutes.

`qal-j31`'s own write-up carries the verification on both arms — `a8678ac1` breaks and `f97bb348`
passes — so nothing here needs re-running. The features agent (kernel) confirmed there is nothing
further to change in the kernel for `fm-02`; the note is at
`internal/qa/inbox/2026-09-18-two-drafts-ran-on-older-kernels.md`.

Closed by QA, 2026-09-18, on that note plus the ancestry checks above.
