# 05aa127160: kf-01-accepted-line-never-arrived (kf-01-a-chat-opened-during-kickoff-keeps-what-you-type)

status: closed — the app bug it caught is fixed on `main` by `1768ec83`; `kf-01` now guards it
scenario: kf-01-a-chat-opened-during-kickoff-keeps-what-you-type
feature: 
rollout: (verification run) /tmp/j42/rollouts/20260918T155343Z-kf-01-a-chat-opened-during-kickoff-keeps-what-you-type
first_seen: 20260918T155447Z
kernel: arbos-kernel 0.2.0 1beec0a1fd98 protocol 1

## Detail

the composer cleared, so the app accepted the line, but chat-1789746824668's transcript never recorded it after 45 s (qal-j43). A cleared composer must mean a delivered line.

## Suspected location

The app's chat-minting path (`desktop/src/kernel.rs::mint_chat` -> `arbos_core::create_chat`) when
the place has no agent record yet because it is mid-kickoff. The app logs the mismatch itself:
`filed parent Some("root"), the kernel's record says None; the record wins`.

## Repro

`python3 run.py --kernel <bin> --only kf-01-a-chat-opened-during-kickoff-keeps-what-you-type`

## Written up

Full reproduction, the four-run split and the two candidate fixes are in
`qal-j43-a-chat-opened-during-a-places-kickoff-turn-silently-eats-what-you-type.md`.

This draft was filed by the verification run on an app built from `e10953fb`, which has the bug.
On current `main` (`fba8688d92d2`) `kf-01` passes. The fix is `1768ec83` — a layout commit that
closed this incidentally, which is why `kf-01` is kept as the guard.
