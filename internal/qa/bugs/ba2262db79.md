# ba2262db79: state:agent-md-missing (inbox:swebench-loop-cycle-4)

status: confirmed, fixed (features agent, 2026-09-15)
scenario: inbox:swebench-loop-cycle-4
feature: swebench-loop-cycle-4
rollout: /home/ubuntu/arbos-qa/loop/rollouts/20260915T053553Z-inbox:swebench-loop-cycle-4
first_seen: 20260915T053817Z

## Detail

agent folder without agent.md; list_agents skips it silently

## Suspected location

/tmp/arbos-qa-inbox:swebench-loop-cycle-4-t43qu1n3/place/.arbos/agents/agentA/agent.md

## Repro

`python3 run.py --kernel <bin> --only inbox:swebench-loop-cycle-4`

## Fix

The folder was not an agent (no `agent.md`), so `list_agents` was right to leave it out; the bug was the silence. PR `cursor/agent-unlisted-b027` (main): at boot the kernel logs one `agent_unlisted` line per stray folder under `agents/` — `agents/agentA: no agent.md — not an agent, not listed; remove the folder or give it an agent.md`. Root's own pre-bootstrap folder is not reported. `arbos-kernel check` already errors on the same set. E2e `agent_unlisted_e2e`.
