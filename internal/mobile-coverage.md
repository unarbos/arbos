---
cursor:
  subagentId: "bc-08d8261b-fea2-5075-9949-d45f6f9d4acc"
---

> Rebuilt 2026-09-16 12:50 UTC from the loop's context after the store lost this file (see `internal/mobile-store-loss-2026-09-16.md`). The table is as it stood at cycle 10 plus cycles 11–13.

# iPhone loop — coverage record

What the loop has exercised and when, so nothing goes long untested. One row per aspect; the last cycle that checked it, how (simulator / Jacob's phone), and what is still open. Rotation rule: every aspect within four cycles; anything older than that goes first in the next cycle. Standing order (Jacob, 2026-09-16): never idle, style pair + still every cycle, a recording every third, long-running projects, every aspect.

| aspect | last checked | how | state / open |
| --- | --- | --- | --- |
|  projects list — faces, rows, sections | 69 (**M-253**: the fourth and last mesh-owned row gained its time when that kernel moved to `c3247332` — `phone · home · 2m` beside `demo 7h` and `subnet120 1h`; the three still blank are kernels nobody on the mesh side owns and are correct as unknown. `media/mobile/cycle-69/02-`), 61 (**M-226**: the row says how long since the project last did anything — `demo — 6h`, `subnet120 — 4m` — closing the gap found at 59 and filled by #538. Absent stays absent, never "now". `media/mobile/cycle-61/08-`), 52 (**M-176**: a project he has opened keeps its row when its machine goes, saying "awsmac is off · fieldwork" instead of vanishing), 41 (**M-134**: a project whose kernel runs a deleted file reads "Restart needed", not "Idle" — `media/mobile/cycle-41/02-`), 37 (**M-121** fixed: the pod row folds into its roster twin) | simulator, loopback hub with a #385 kernel; ArbosLife hub, phone token | five rows before, four after opening `pod` once — `pod` and `phone` were one kernel drawn twice; the fold survives a relaunch (`media/mobile/cycle-37/07-`). `Feedback` still shows: the hub gives it no `kind` (M-119) 
| projects list — search, filter, refresh | 47 (**M-157** fixed: the composer names a project that is on screen, not the last one opened; with a filter matching nothing it offers nothing rather than an invisible project) | simulator, ArbosLife hub | 25 | simulator, ArbosLife hub | search filters as typed ("sub" → subnet120); filter menu All / Live only; a new project (`Feedback`) from the roster appeared by itself (`media/mobile/cycle-25/01-`). Note: with a filter on, the list composer still names the last project, not the one shown |
| list composer → names its project, rides above the keyboard | 57 (**M-192** all four claims measured; **M-191** fixed: a search matching nothing said nothing), 11 | simulator, ArbosLife hub | `Message phone…` with 7 rows; with a filter it names one **on screen** — `sub` → `Message subnet120…` (#433); with no match it offers nothing (`Plan, ask, build…`, send disabled) and now **says so** instead of showing a blank screen under an empty `Read` header; rides above the keyboard, measured off the tree — composer top 774 → 480 points, keyboard top 683, a 182-point gap. `media/mobile/cycle-57/` |
|  project chat — send, prompt card, streaming, Worked line | 62 (**M-236** timed: send → card ~1.0 s (at the method's floor), → the reply's first words **8.9 s**, → `Worked 8s` **9.8 s**; composer resets; kernel holds both, seq 2364–2366. `deploy/mobile/scenarios/the-core-chat-path.sh`, `media/mobile/cycle-62/`), 41 (the deleted-binary line against a #385 kernel: one plain line at the tail, `media/mobile/cycle-41/03-`), 40 (the stall notice and Stop against a #374 kernel) | simulator, local kernel via launch args | a silent turn's notice lands whole with `bash` on a chip 32 s after the send (M-129); Stop ends the turn from the composer's stop square (M-130); `media/mobile/cycle-40/` 
|  project chat — worker lines, Working pill, workers sheet | 61 (**M-219 open**: the kernel's tree lists twelve children and the sheet shows **two** after a relaunch — `publishWorkers()` filters on a `touched` set that only this session's events fill. Diagnosed; the fix needs a decision about which children belong on the sheet), 39 (M-127: a refused spawn is one plain line) | simulator, `demo` replay | "Agents 3" pill → sheet lists three finished workers 
|  worker chat — open from line / sheet, back | 61 (**M-225**: a worker's chat **does** have content — plan, reads and a Done line, and the kernel holds 8 lines for it; **M-224**: the `total` that said otherwise at cycle 46 was answering for the root, and is fixed), 46 (**M-153**: sheet lists workers by goal with Done ticks; a worker's chat opens with its name in the header; still "Nothing on record yet" and the count that would explain it was not obtainable this cycle) | simulator, `qa-cycle-11-demo` (12 agents) | 32 | simulator, `pod` (local workers) | sheet lists three Done workers; a Done worker's chat is "Nothing on record yet" even locally — the kernel's `history` answers `total: 0` for an archived agent; ask filed (`features-inbox/2026-09-16-mobile-worker-history-archived-agents.md`) (`media/mobile/cycle-32/01-`) 
| project chat — long history, scroll, older lines | 60 (opens at the tail in **0.9 s** on **2220** lines; `Show 200 earlier lines` pages in **3.6 s** and holds position; **M-203**, **M-204**), 37 (`phone`, 1265 lines through the ArbosLife hub; recorded) | simulator | opens at the tail in 1.1 s; "Show 200 earlier lines" at the top pages 200 back and holds the row that was at the top; a second page reaches the 16:xx turns; flings back to the tail work (M-120 was the harness swiping on the composer). `media/mobile/cycle-37/04-`, `05-`, `recording-long-history-paging-20s.mp4` 
|  project chat — several workers at once, archived children | 60 (**M-211** holding: four workers together took the pill **2 → 6**, four `Done …` lines, the sheet listed all six by name with the archived children kept, pill survived back-and-reopen; **M-212** fixed: one of the four read `Done say-sentence-about-rivers` where three read the spaced form. `media/mobile/cycle-60/21-`), 37 (`pod`: four workers at once, Agents 3 → 7) | simulator | four "Done say sentence about …" lines in the transcript, "Worked 24 s", Agents 7 pill; the sheet lists all seven with names and Done; kept across back-and-reopen (`media/mobile/cycle-37/03-`). Harness note: with the keyboard up the pill sits higher — tap the words first (F6) 
| call — voice first, orb, colours | journey ×3 (connect 0.28–0.45 s; kernel-answered first audio 5.8–6.8 s on the pod today, was 2.9 s) | simulator, injected clips, gateway PR #56 | kernel-answered first audio 2.93 s, small talk 0.70 s |
|  call — pulled down: type, mute, close, `+` | 61 (**M-216** a crash: closing a call that never started killed the app, `NULL != engine`, fixed; **M-217** the row holds — typed line reaches the kernel (seq 2278), mute inert before a call, close returns to the chat when the call was entered from the chat. `media/mobile/cycle-61/`), 38 | simulator | without an injected clip the simulator has no microphone: the orb goes red with "No microphone input." and the project's name under it; pull-down shows the composer ("Type to pod"), mic and close; close returns to the chat (`media/mobile/cycle-33/01-`). Typing on a live call re-verified cycle 34: "typed heard" from the kernel, shown under the orb (`media/mobile/cycle-34/01-`)  **Corrected at 61:** with no call started the orb reads `Tap to call`, not "No microphone input." — that wording belongs to a call that started without a microphone. And close returns to whichever screen opened the call: the chat from the chat's menu, the list from a launch argument, which is why 38 and 56 disagreed.
|  call — barge-in | 65 (**M-244**: **137 ms** and **185 ms** to speech-started, 179/227 ms to response-done, in two of three runs — against 500–522 ms at 42. The third never armed. `media/mobile/cycle-65/`), 42 (**M-136**: 500–522 ms over four runs, against 401 ms at cycle 12 — but that is the round trip to the gateway; the phone's own part is 2–4 ms and unmoved) | simulator, injected clip | AirPods leg needs Jacob. Candidate of its own: cut playback on the phone's own microphone energy rather than waiting to be told 
| call — AirPods / speaker route, screen off, CallKit | — | needs the phone | **never checked**; Jacob's phone |
|  attachments (`+`), photos, files | 60 (chip with its × in the field, send arrow in place of the mic; **M-203**), 37 (`demo`: `+` → Photo Library → picker → chip with × → line typed beside it; style pair) | simulator, library photo | chip sits inside the field above the text as Cursor's does; send arrow replaces the mic once a chip or text is present (Cursor keeps both). **Sent and proven at cycle 60** (M-214): the model named the picture — `Ice plant flowers, predominantly magenta/pink.` The question is now one only a real photo can answer, in both the scenario and the journey's P2 
|  call — the microphone path | 66 (**M-246**: the path is sound — frames are lost only when connect exceeds the two-second hold. connect 2633 ms → 13 frames gone; 1514 ms and 1936 ms → none. **M-247**: the hold's premise expired with the faster connects it was written for), 42 (**M-138** fixed: the first 320 ms of every call was dropped before the socket was up; **M-140**: `-micWav` is the first rig that can drive capture at all) | simulator, clip into `audio.onCapture` | frames counted at both ends, 50 of 50 after the fix against 42 of 50 before. **M-139** left open for the gateway: the server still loses the first word 
|  call — first word and transcription | 68 (**M-251**: the first word arrives whole — `Hey!` and the full status question, 900/900 frames — but **M-146 is still open**, reproduced today: one request split by a pause comes back as two transcripts and two spoken answers), 43 (**M-144**: the voice server's fix verified on the capture path — one final, whole sentence, small talk 559 ms) | simulator, `-micWav` | closes the first-words thread with M-138. **M-146** open for the gateway: a mid-sentence pause answers twice 
|  voice notes in the composer | 69 (**M-252** on counts: **2411 → 2411 unsent → 2415** on the send, with `Up` beside the words; **M-254** a second run from the committed branch, **2415 → 2415 → 2419**, recorded — three measurements on three builds, one shape. **M-255**: the scenario had been counting `pod` whatever row it was told to tap, right only by luck, fixed in #561. `deploy/mobile/scenarios/voice-notes-wait.sh`, `media/mobile/cycle-69/`), 44 (**M-147** closed on counts: 13 segments `delta_shrinks=0`; kernel total 1526 → 1526 unsent → 1531 on his send) | simulator, injected clip | the words wait in the field with the send arrow beside them. 43 attempted and voided three times by iOS's notifications sheet; 14 (words wait in the field; gateway routing stopgap M-90) | simulator, injected clip | deltas append, whole sentence kept, second tap sends (F9); real mic on Jacob's phone: his report on 956 was the bug, re-check on the next build 
|  settings sheet | 60 (**M-201** its ground was iOS's `#2c2c2e`, not the app's — fixed in #537; **M-206** a hub token that cannot work drops the list to the pod's own row and now **says why**; **M-207** a saved token outlives an app uninstall, so a reinstall does not undo it), 37 (**token editing exercised**; M-122 fixed) | simulator | a wrong hub token → every row Off; the right one → rows back; iOS's "Save Password?" sheet after each edit is gone (`media/mobile/cycle-37/09-`, `10-`). Voice / kernel / hub URLs shown are the interim quick-tunnel names  Cycle 60: wrong token → one row (the pod, which needs no hub), right token → seven back; no Save Password sheet appeared through four token edits, so M-122 holds, though that is shown by its absence rather than checked for.
| notifications (`notify`, push) | 58 (**M-193** fixed: the banner said `arboslife/phone · root replied` where every other surface says `phone`; before and after `media/mobile/cycle-58/01-`, `02-`), 36 (`pod`: the banner itself on the Home Screen, then the badge, then the away card on return: `media/mobile/cycle-36/01-`) | `deploy/mobile/scenarios/notifications.sh` — the ask, the banner, the badge, the tap, the away card; recorded at 58 | banner, badge, tap-to-project, away card, seen both ways all verified; real pushes start when Jacob's APNs key + capability land (no release needed) **M-194**: the banner is SpringBoard's, so `describe-all` cannot see it — the scenario reads the app's console instead; and a reply slower than iOS's half-minute background grace raises no banner at all, only a badge, until push is on |
| background 8 s → foreground | 60 (26 text rows before and after; **M-203**), 37 | simulator | chat as it was; no status-bar loss this time 
| background minutes/hours → resume; what a returning user sees first | 50 (**M-167**: 7 min — 16 elements before and after, last three lines identical by md5, no offline line, a line sent after resume landed) | simulator | hours-long suspension still needs Jacob's phone. 27 (12 min, `pod`) | simulator | the chat as it was, then a line sent straight after resume answered in 7 s — the resume reconnect works with M-109 in (`media/mobile/cycle-27/01-`); hours-long suspension still needs Jacob's phone |
| hub refusals — what the phone says | 70 (**M-257**: all three of `Hub::kernel`'s refusals driven from a verbatim fixture and told apart by counting attaches — `no machine named` stops after one, `is offline` and `has no kernel serving` retry. **M-258** the reason was being erased by the close that followed it; **M-259** an offline machine printed the hub's timestamp and remedy in red once per retry; **M-260** a guess about the path sat above the hub's own answer; **M-261** an empty chat called itself ready. All four fixed in #566. `deploy/mobile/scenarios/refusal-and-transport.sh`, `media/mobile/cycle-70/`), 45 (**M-150**: the reason arrives as an `error` frame, not a close reason; the app already read it and now says it in plain words. **M-151** open: a project whose machine goes off vanishes from the list instead of saying so) | simulator, loopback hub fixture | ahead of #417 |
| network drop → reconnect | 70 (**M-257** closes **M-165**: the refusal half is verified on a device at last, and the pair is handled the right way round — 1 attach for a verdict, 3 in 25 s for the path. `media/mobile/cycle-70/`), 49 (**M-163** fixed: a refusal and a transport failure told apart and handled oppositely — transport half verified from the app's log, refusal half **not** verified on device, M-165; **M-164**: `has no kernel serving` reads as a sentence and still retries) | simulator, loopback hub fixture | 23 (**M-109**: the automatic reconnect never completed — fixed; kernel killed and restarted → reattached first retry, ask card re-offered), J8c ×8 | simulator | one calm line; pending lines never cross projects (M-82, proven both ways); a project opened while down says "Opening…" then "not answering — waiting" (M-83, verified) |
| cold start | 60 (**3.0 s** to a list with seven rows, polled off the tree; **M-203**), 37 | simulator | lands on the list with rows in **3.1 s** (was 6 s at cycle 13) 
| TestFlight build on Jacob's phone | 956 (13 reports), 994 (fixes for F1–F6) | TestFlight | F7/F8 open until he confirms; F9–F13 land with #309 |
| style pair vs Cursor stills | 59 (**M-196** fixed: the chat was showing the model's markdown markers — `## Shapes`, `- Note one`; **M-197** filed: Cursor times every row, Arbos cannot, the hub sends no per-project timestamp. **M-199** links checked and right, both markdown and bare URLs; **M-200** a long list item wraps to the margin where Cursor hangs it — left on purpose, a hanging indent costs twenty points a line on a phone. **M-201** fixed: Settings was 45% iOS's `#2c2c2e` against a list that is 92% `#161514`, sampled not eyeballed; **M-202**: dark-only is deliberate on both sides, the desktop's `palette.rs` uses the same literal `0x161514`. Otherwise the two surfaces agree on shape. `media/mobile/cycle-59/`), 38 (chat + call vs the Mac's one-surface look, **M-126**: `media/mobile/cycle-38/01-`), 37 (composer at rest + photo chip: `media/mobile/cycle-37/01-`; the list after M-121 vs All Agents: `08-`; one-surface check vs desktop #359, **M-123**), 21 (call vs GPT: `media/mobile/cycle-21/01-`), 18 (list), 17 (chat) | pairs in `media/mobile/cycle-N/` against `media/mobile/cursor-reference/`; the reference stills are numbered 2 list+chat, 3 call vs GPT, 4 list, 5 chat with four workers, 6 composer with a chip, 7 list search | pairs in `media/mobile/cycle-N/`; verdict 37: the resting composer matches Cursor's iPhone pill (+, placeholder, mic); the photo chip sits where Cursor's does; one difference left as is — Arbos swaps the mic for the send arrow once there is something to send, Cursor keeps both. Next: worker chat vs Cursor's agent chat **Row repaired at cycle 58**: the reference-still numbers had been sitting in the *last checked* column and the cycles in *how*, so the row read as last exercised at cycle 7 and sent two readers to the wrong place |
|  the loop's own machine | 63 (**M-238**: the claim below was true of the files and not of the paths — the journey ran four committed tools from `$HOME`, and `kernel.py`'s three fixes tonight never reached a run. Fixed in #554), 41 (**M-132**, **M-133**: key lost and rebuilt, disk imaged, harness committed to `deploy/mobile/`) | AWS, EC2 Mac | the private key is in the vault now, and the harness is in the repo, so neither the machine nor the tooling depends on one worker's disk. Swapping a rented Mac costs about two hours, nearly all of it the dedicated host's scrubbing workflow  **Corrected at 63:** a fresh machine could not simply clone and run until #554 — it needed an undocumented step copying four tools into `$HOME`. The check that catches this class: compare each committed tool's checksum against the copy the harness actually invokes.
| style pair vs Cursor stills — worker chat | 46 (**M-154**: same plain header, flat surface and composer pill as Cursor's agent chat; Arbos's Done tick earns its place; the unusable composer replaced by a plain line rather than left dead) | `media/mobile/cycle-46/04-` vs `cursor-reference/01-chat-view.jpg` | the standing style row since 37, now done |
| journey — the kernel's own build in the record | 48 (run 31: `arbos-kernel 0.2.0 0f2a8bc68cc6`, asked on the attach socket at both ends) | simulator, `arboslife/demo` | what QA asked for, working. **M-162 closed** (#515), and shown end to end at cycle 55: journey run 32 landed **7 typed lines of 7**, against 2 of 8 in run 31, so the steps run 31 recorded as *unexercised* are exercised and every J step passes or is a standing unverified. `media/mobile/cycle-55/` |
|  recording | 69 (**the dictated note waiting in the field and going only on his tap**, 53 s, `media/mobile/cycle-69/recording-voice-note-waits-then-sends-53s.mp4`), 65 (**the call and a barge-in**, 38 s, `media/mobile/cycle-65/recording-call-and-barge-in-38s.mp4`), 55 (**the journey's challenge typed whole, sent, the worker running and the reply** — `media/mobile/cycle-55/recording-journey-challenge-and-workers-123s.mp4`; also the photo and call steps), 48 (the journey's challenge and workers, `media/mobile/cycle-48/recording-journey-challenge-45s.mp4`), 46 (the worker chat, sheet to worker and back, 30 s, `media/mobile/cycle-46/recording-worker-chat-30s.mp4`), 42 (the call, barge-in and all, 28 s, `media/mobile/cycle-42/recording-call-barge-in-28s.mp4`), 1, 3, 6, 10, 13, 16, 28 (the photo landing and the scoped call answering from `demo`, 66 s, `media/mobile/cycle-28/recording-photo-and-scoped-call-66s.mp4`), 37 (long history: open `phone`, fling to the top, page 200 back twice, 20 s, `media/mobile/cycle-37/recording-long-history-paging-20s.mp4`) | mp4 | next due cycle 58 
| chat — the call's words read back | 54 (**M-177**, **M-179**: both halves of a call appear marked Spoken, and a delegated turn shows one wording, the spoken one, not the kernel's parallel text) | simulator, injected acceptance clip, `pod` | display only — `spoke()` sends nothing and typed lines still wake the kernel. Kernel seq 1957/1961 wrote the other wording for the same turns and it is not on screen. Spoken rows do not survive a restart; after one the chat replays the kernel's wording, which is the only record that persists. `media/mobile/cycle-54/` |
| the harness — typing into the composer | 54 (**M-180**: the fixed tap point, the caret landing mid-text, and nothing read back — all three fixed and counted) | simulator, `pod` | 6 of 8 lines reached the kernel the old way, 8 of 8 the new way. `ui.py` gained `field` and `focus`; `deploy/mobile/scenarios/type-send-measured.sh old\|new` reruns the count. A dropped line is now named in the run log instead of being scored against the app |
| journey — the phone-only steps P1, P2, P3 | 56 (**M-187**, **M-189**: all three could never have passed — P1 tapped Microphone twice when the second button is Stop; P2 tapped the privacy banner instead of the photo grid, so the picker never closed and swallowed P3 as well) | simulator, `pod` | all three proven end to end: the dictated line reaches the kernel (seq 2167), the photo attaches and the picker closes itself, and the call's question lands in this project's transcript (seq 2175). `media/mobile/cycle-56/` |

**Journey runs** (`internal/mobile-journey-runs.md`): 31 so far — 3 on `demo` (old numbering; JB-1…3 named), 3 on `pod` on QA's ids (run 4 a floundering root, runs 5–6 clean apart from the U steps). Next: `arboslife/demo` on QA's ids once its kernel is updated. Run 30 (09-17 01:40, `demo` on `b6e7098` at last): J1 P, **J2 F — JB-6** (spawn refused by the machine, ENOENT), J3 P, J4 P (root), J5 F (model, 11/11), J6 P (away card in the run), J7 P on the merits (root's own work — JB-5 still untested since #357), P1–P3 P, PUSH U. Next `demo` run when the mesh worker has restarted the daemon.
