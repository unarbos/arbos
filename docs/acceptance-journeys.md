# Acceptance journeys — the whole path a user lives, scored step by step

Owner: the QA loop (`bc-f2e2f30d`). First version 2026-09-16. Jacob's words that set it: *"The upgrade loops need to actually run full cycles of creating a project, running a challenge, doing follow up etc. Right now these things are flaky at best."* We had been testing elements and moments; this is the one path nobody ran start to finish. Three loops measure it against this one definition: the QA loop on Linux (every cycle, `internal/qa/journey_scenarios.py`, scenario `journey-linux`), the desktop loop as the spine of its cycle, the iPhone loop as the phone's version.

## Rules

1. **Every step asserts what the user would see** — the chat items, the tabs, the worker rows, the files on disk, the command's output — never an internal state on its own. Internal files (`transcript.jsonl`, `kernel.log`) may confirm, not replace.
2. **A step that cannot be checked honestly is `unverified`, not `pass`.** The score line says how many were unverified and why; a loop that cannot reach a surface (no notification badge in the driver, no network to drop) says so.
3. **Scored per step, run after run.** One line per run in `internal/qa/journey-history.jsonl`: `{ts, branch, sha, steps: {J1: pass|fail|unverified, …}, evidence}`; the loop reports pass rates per step over the last N runs, so flakiness is a rate, not a shrug.
4. **A step that fails twice in a row is a bug with a name** (`qal-jNN-<step>-<slug>.md` in `internal/qa/bugs/`, with the two rollouts), filed by the loop, fixed as any other.
5. **The challenge is real.** It needs workers, edits files, runs a command, and can fail on its merits (a test that must pass). No toy prompt.
6. One journey run uses one fresh folder that is not `~/.arbos`, the app as a new user has it (scratch config, one model key, no prior projects), and the current `main` kernel and app.

## The journey

Each step: what the user does · what the user must see (the assertion) · how the Linux rig checks it · when it is `unverified`.

**J1 — Create a project from nothing.** Open the app on an empty folder (not `~/.arbos`). *Sees:* one tab for the folder, a chat with the kickoff greeting (or a plain one-sentence notice if the key's model is refused — never a raw provider error), no failed notice. *Check:* driver state has a session for the folder; its items contain an assistant line and no `notice{failed}`; `.arbos/` exists under the folder. *Unverified:* never — this always runs.

**J2 — Give it a real challenge.** The seeded folder holds a small Python project with a failing test (`tests/test_math.py`: `area()` wrong) and no CHANGELOG. Prompt: *"Fix the failing test in this project, add a CHANGELOG.md entry describing the fix, run the tests to prove they pass, and commit the work on a branch (not main). Tell me the branch name and the test output's last line."* *Sees:* the chat goes busy; one or more workers appear (worker rows / tabs, or `spawn` cards in the chat). *Check:* state shows `turn_open|streaming` within 20 s; within 120 s a session other than root exists (live or archived) or a `spawn` tool card is in the items. *Unverified:* if the coordinator does the work itself without a worker — then J2's "workers appear" is `unverified`, not `fail`, and the rest of the journey continues.

**J3 — Watch it work honestly.** *Sees:* worker rows go from working to finished; the chat never shows the same assistant text twice in a row; when the chat says idle, the transcript has ended the turn. *Check:* the workers' turns end (transcript `turn_complete`); no two consecutive identical assistant items; `turn_open == false` ⇒ transcript ends with `turn_complete|interrupted` (nudges allowed after). *Unverified:* never.

**J4 — Follow up mid-flight, and after.** While it works: *"Also add a line to the CHANGELOG saying who asked for this: QA-<tag>."* *Sees:* the line is taken up by the work already running — not by a second, parallel piece of work. With a coordinator root that means one of two shapes, both fine: the line lands inside root's still-open turn; or root's turn had ended when it delegated, root wakes for the line and relays it to the **live worker** (`say`), and no second worker appears. *Fail:* a second worker is spawned for it, the line is ignored, or the result on disk (J7) does not carry it. *Check:* the follow-up `user` line is before the challenge turn's `turn_complete`, or the turn it starts contains `say` and no `spawn` and the worker set is unchanged. Also recorded: how long the line takes to reach root's transcript (`reached_root_s`); #317 makes that about two seconds instead of waiting for the worker to finish — once the tracked build carries #317 the step gates on ≤ 10 s. After it finishes: *"Summarise what you changed in two lines."* *Sees:* a new turn that answers. *Check:* a new `turn_complete` after the second follow-up with an assistant reply. *Unverified:* if the turn had already ended when the mid-flight line was sent (the challenge finished too fast) — the mid-flight half is `unverified`.

**J5 — Steer, interrupt, ask read-only.** Steer: *"Use British spelling in the CHANGELOG."* while it works — *sees* it acknowledged in the same turn (check as J4 mid-flight). Interrupt: press Stop — *sees* the turn end at once with an interrupted marker; the composer is usable; a next prompt runs. *Check:* `interrupted` on the transcript within 5 s of Stop, `turn_open` false, a later prompt gets a `turn_complete`. Read-only ask while working: *"What time is it, roughly? One line."* — *sees* an answer without the work being abandoned (the running task continues or resumes). *Check:* the answer line arrives; the challenge's files still get finished (J7). *Unverified:* Stop when nothing is running; the read-only ask when the turn had ended.

**J6 — Leave and come back.** Quit the app (the kernel keeps serving, as it does for a real user); reopen it — the app's own `state.toml` decides what comes back, the rig does not reseed it. *Sees:* the tab is back, the transcript is intact (same items or more), the workers are still listed (finished ones as archived), the unseen notifications are shown. *Check:* driver state after relaunch: the project's session exists with `len(items) >= before`; worker sessions or archived rows present; `notes.md` unchanged by the relaunch. *Recorded, not scored:* which tab the app lands on. Today it lands on the home tab (`~/.arbos`) at every launch by design (`desktop/src/model/workspace.rs`, "in front at every launch"), not on the tab the user left — so a user who reopens and types straight away posts into `~/.arbos` (that is exactly what the first run's J8 did before the rig learned to click the tab). Jacob decides whether "come back" should mean the tab you left. *Notifications, since 15:30 UTC:* before quitting, the rig asks for a reply that lands 15 s later, so something arrives while the user is away. On reopening — before clicking the tab — the driver's notification state for the project must show it unseen, and clicking the tab must clear it. The rig reads any of `unseen`, `unread`, `notifications`, `badge`, `unseen_count`, `unread_count`, `away` on the project or its chat (the desktop loop is adding the surface; the key it picks is read automatically). Until the driver exposes one, the step is `unverified` — but the late reply must still be in the transcript after reopening, or J6 fails.

**J7 — The result happened on disk.** *Sees/checks:* `tests/test_math.py` passes when the rig runs `python3 -m unittest` itself; `CHANGELOG.md` exists and mentions the fix (and "QA" if J4's line was taken); a branch other than `main` exists with at least one commit ahead of `main`; `main` unchanged. *Unverified:* never — but a `fail` here is the challenge failing on its merits, which is a legitimate product finding, not a harness one.

**J8 — The awkward parts.** (a) Kernel restart mid-turn: the turn is running a command **with a side effect** (`echo ran-<tag> >> side-effects.log; sleep 30`); kill the kernel during the sleep, type a line — *sees* the line answered once after the app respawns the kernel, no duplicate; at most one honest notice about the kernel that died ("no reply from the kernel"), never a stale or repeated one; and `side-effects.log` holds the tag **once**. The features agent's known wrinkle (2026-09-16, left unfixed on purpose): an in-flight tool leaves no record when the kernel dies, so the continued turn's model runs it again — harmless for reads and for `git commit` ("nothing to commit"), a real bug for anything non-idempotent. This step is where it would show; two lines in the log is a `fail` and a named bug, not a shrug. Pinned as an e2e in #316. (b) A second project open at the same time: open another empty folder in a second tab, type a line there — *sees* it answered in that tab and nowhere else; the first project's transcript has no trace of it. (c) A dropped connection mid-turn (network loss, not process death): the kernel is local on the Linux rig and there is no link to drop, so **J8c's verdict is taken from the phone loop's history** — the newest run in `internal/mobile-journey-history.jsonl` within 24 h that scored `steps.J8c` (the phone cuts the link 25 s into a turn). Its `pass`/`fail` counts here with its timestamp and evidence path; no such run within 24 h leaves J8 `unverified`. *Check:* transcripts of both projects and the home store for each tagged line; `kernel.json` pid changed for the restarted one.

## Scoring

**Liveness, and a turn that ends for a reason.** A turn that ends on a cost cap or a provider refusal must leave the app answering the driver within the second and the reason readable in the chat as one plain line (`cp-02`, `fr-*`); a stop that reads as "Internal error" or as the agent apologising fails (qal-j05). A step that only asserts driver state cannot see a hang — a hung window answers nothing, and that class of bug reaches Jacob instantly and reads as the app being broken (the feedback sheet's first real click, 2026-09-16, froze the app while twelve unit tests saw nothing). So after every action that opens or changes something — launch, send, tab click, Stop, the opener sheet — the rig sends the cheapest driver call with a one-second deadline. No answer: the step in progress fails, the rest are not reached, a root-window capture is saved, and the run files `journey-hang`. The slowest pulse of each run is recorded in the evidence (`liveness.slowest_ms`).

**A skipped headline is loud, never silent.** The runner's summary names every skipped scenario with its reason, prints `!! BUDGET BOUND` when the daily model cap ($20 since 2026-09-16, approved) stopped scenarios, `!! MODULE MISSING` when a scenario module failed to import (every scenario it defines is then absent), and `!! HEADLINE NOT RUN: journey-linux (<why>)`; the cycle log ends with an `== ALARMS this cycle` block. The 14:00 and 15:00 cycles of 2026-09-16 dropped the journey silently for two different reasons; that cannot recur unnoticed. If $20 binds, the loop reports it and does not raise the cap itself.

Per run: `passed / 8` with the count of `unverified` steps stated (`J6 notifications`, `J8c` always; `J2 workers`, `J4 mid-flight`, `J5` parts when the timing did not allow the check). A step with several parts is `pass` only when every checkable part passed; `unverified` when at least one part was unverified and none failed; `fail` otherwise. Reported every cycle as `journey: N/8 pass, U unverified, F fail — J1 ✓ J2 ✓ …` and as pass rates over the last 10 runs.

## The three loops

- **QA loop (Linux, Xvfb, the gpui app + the driver):** `journey-linux`, every cycle after the tracked-branch run; history in `internal/qa/journey-history.jsonl`; rollout under `internal/qa/rollouts/<ts>-journey-linux/` with the app log, the driver frames, both projects' `.arbos` snapshots and the seeded folder's git log.
- **Desktop loop (macOS):** the same steps against the Mac build; J8c may be checked with a real network toggle there.
- **iPhone loop:** the phone's version — the project exists on a machine's kernel; J1 becomes "open the project in the list", J6 "background the app and return", J8c "toggle airplane mode mid-turn" (the phone owns the dropped-connection check).

All three record the same step ids so one table compares them.

## Findings so far (the loop appends here each cycle; latest first)

- **2026-09-17 03:12–03:35.** `jl-01` pins the 164 GB incident shape — background flood, `jobs`, `kill <the pid jobs shows>`: on the current kernel (`5017ef45`) the writer survives as an orphan (PPID 1), `out.log` grows 28 → 60 MB in the 3 s after the kill, `jobs` still says running; on #377 (`44dcc70d`) `jobs` says `exited with code 143`, the log is frozen, nothing is left. `sb-01` measures #377's stated cost and it is real: a shell subscription whose command backgrounds a child (`nohup sleep 300 & echo started-bg`) delivers its reading in 0.4 s on `main` and **never** on #377 — 200 s and nothing, `last_fired` set, `next_due` passed unfired; #364 is already in #377's base, so the pair does not close it → **qal-j07**, the more visible of the two, pre-merge. Audit of the rig's own measurement (design doc, "Where this rig could produce a number nobody should believe"): 29 self-skipping scenarios read as `pass` (now `skipped` with the reason); `sb-01`'s first version matched the subscription file echoed in the snapshot and passed in 0.0 s (fixed — and only then did the hold show); J7 could be earned by editing the test (now the seed test must be untouched and the rig computes the answers itself). Journey on `main` `b2dc90df`: 8/8, J7 with `test_file_untouched: true, fix_verified_independently: true`. 19 runs; pass rates J1 19/19 J2 19/19 J3 17/19 J4 14/19 J5 18/19 J6 11/19 J7 17/19 J8 10/19. Budget note for Jacob: the $20 cap bound at 22:00 UTC on 09-16 ($20.52; 159 model scenarios skipped in that cycle) — the first day it did. Store: a sixth loss episode was watched happening live at 03:26–03:27 (counts falling between reads seconds apart), settled by 03:29, 174 files restored from `7d283951` (03:23:40Z) by 03:34:53 — record in `internal/store-docs-loss-2026-09-16-qa-record.md`.

- **2026-09-17 02:33 — the 164 GB job, traced (evidence for #377's authors).** The writer was job `j1` of the 21:26:58Z `inbox:jobs-leash` run (scratch `…-4mdxrd2y`): `bash background:true` `while true; do echo "This is a test line…"; done` — a slow flood (~3.5 MB/s; 65 MB at 18 s) that the runaway kill never trips on, as the leash note itself says. The model then ran **`kill 1060017` — the pid `jobs` shows, which is the wrapper/leash** — and `jobs` reported "killed by a signal from outside the kernel"; the inner `bash -lc … while true` loop (pid 1060027) survived as an orphan (PPID 1) with no leash left to watch the kernel. The kernel did **not** leak: the harness stopped it cleanly at 21:27:41Z (SIGINT, exit 0). The harness then removed the scratch folder, so `out.log` was deleted-but-open and `wc -c < out.log` read 0: cap blind, 164 GB in 4.7 h. The harness's old cleanup matched nothing by argv or cwd — it only SIGKILLed the kernel objects it had started itself (already stopped here) and removed the folder — so it never targeted a leash, a wrapper or a bare writer; the leash's death came from the model's `kill`. The new reaper matches argv substring and `/proc/*/cwd` prefix and records pid, parent, orphan state and how it matched (`rollouts/reaped.jsonl`). Live `inbox:jobs-leash` on `5017ef45` at 02:32: five jobs (two background `sleep 1000`, a `yes` killed by the cap at 64 MB, a `du`, one more sleep), kernel stopped exit 0, **nothing survived its kernel, nothing orphaned, reaper found 0** — because this time the model never `kill`ed a job pid. The remaining hole the evidence points at: `kill <the pid jobs shows>` kills the leash and orphans the writer.

- **2026-09-17 02:11, #374's kernel (`8e91ee53`): `st-01`, `pn-01` pass; both fail on the pre-#374 kernel (`5017ef45`) as the control.** `st-01`: with `ARBOS_STALL_SECS=3`, a `sleep 12` turn gets exactly one plain notice — *"Still working, but nothing has happened for 4s: waiting on `bash` (Running sleep 12) since 02:10. If it is stuck, Stop ends the turn; what ran so far stands."* — not marked failed, said once, and the turn ends normally after; the old kernel says nothing for the whole silence. `pn-01`: with `ARBOS_TEST_PANIC_TURN=root`, the agent goes idle in under a second instead of `running` for good, the transcript ends *"The kernel hit an internal error in this turn and ended it: … What ran before it stands; send again to go on. (kernel.log has the detail.)"* + `turn_complete`, `kernel.log` has `turn_panicked`, and "second" is answered "Fine now."; the old kernel goes idle too on Linux but leaves no reason on the record and no log line — the shape with no error anywhere. Harness note: the generic panic detector now lets a scenario's *expected* panic through by its text. Also tonight: a job from the `inbox:jobs-leash` scenario had outlived its scratch folder and written **164 GB** into a deleted `out.log`, filling the VM's disk (one write of this file was lost to it); 123 leaked scenario processes were running, some a day old. `run.py` now reaps every process under a scenario's scratch folder before removing it and says how many it reaped.

- **2026-09-17 01:50, #371's kernel (`874bb9fa`): `rp-01`, `rp-02` pass; the same two on the pre-#371 kernel (`5017ef45`) as a control.** `rp-01` disables the runtime's child reaper with #371's knob (`ARBOS_TEST_NO_CHILD_WAIT`) and a two-second bash still returns its output in 2.6 s, no zombie under the kernel — the exit-file path delivers on its own. `rp-02` execs the kernel with `SIGCHLD` blocked in the inherited mask (what Jacob's launcher did on the Mac): the #371 kernel says on stderr that it unblocked it and the job ends in 2.1 s; the pre-#371 kernel says nothing and the job still ends — because Linux's `pidfd` reaper hides the mask, which is exactly why a week of green cycles never saw his five hung workers. Honest limit: `rp-01` on a kernel without the knob is vacuous (the knob is ignored, the normal reaper runs); its power is against a future regression of the exit-file path. The rig's blind spots — platform process semantics, signals, native dialogs, filesystem semantics, the real network, Retina rendering — are now stated in `docs/qa-loop-design.md`, "What this rig cannot see".

- **2026-09-16 21:57 and 21:58, #356's branch (`d152c70f`), `fb-01` twice: pass, pass — qal-j06 closed.** The surface the rig had stood in for with a screen capture now exists and is asserted: `feedback.message {ok, text}` (the promise itself), `feedback.outbox {waiting, sent_this_run, last_error, drained}`, `feedback.screenshot {attached, whole_screen}`. The scenario now also puts a *second* place in front when the credentials appear and sends nothing, so the drain has to find the waiting report in a place that is not on screen — it did, within the minute, with no second report written. Kept in mind for this harness, from the fix's own summary: seventeen unit tests passed while the feature quietly did not work, because a unit test cannot reach a timer, a launch or a reconnect edge — the journey and the landing scenarios exist for exactly those edges.

- **2026-09-16 21:17 and 21:27, `main` @ `0c82887b` (#349 cap as a place setting, #345 feedback delivery): journey 7/8 then 8/8.** The 7/8's J4 was a real miss: root took the mid-flight follow-up, answered *"the worker … will include that"*, and called only `plan` — no `say` to the worker — so the CHANGELOG never got the line (first time in this shape; a second in a row names it). `cp-01`/`cp-02` green — **qal-j05 closed** by #349. New `fb-01`, the feedback chain on Linux: thumbs-down opens the sheet and the window answers in 96 ms; the report is on disk before anything is sent (9 KB, the note, `screenshot.b64` — the Linux capture is present); with no credentials it waits with the reason recorded (*"no feedback credentials at … — a report will not be sent under another token"*); with credentials the next report lands in the store through `store put` and the poller picks it up (*kept: log, screenshot, session, tail, tool_io, trajectory*). One break, 2/2 → **qal-j06**: the report that waited never goes by itself — delivery runs only when another report is sent, although the sheet says it will go when the link is back. Marked honestly: the sheet's own words are not on the driver's surface (a root-window capture stands in), and the macOS window capture is the one link Linux cannot prove. 16 runs; pass rates J1 16/16 J2 16/16 J3 14/16 J4 12/16 J5 15/16 J6 8/16 J7 14/16 J8 8/16.

- **2026-09-16 19:46, `main` @ `90a33cb2` (#342, #346, #347 in): journey 8/8.** Beside it, five scenarios for what landed (`landing_scenarios.py`): `cp-01` a turn over the per-turn cap ends with a failed notice naming the cap and the setting, closes cleanly, and the kernel answers the next line the same way — pass; `cp-02` the same in the desktop with the liveness pulse (83 ms) and the reason in the chat — responsive and readable, but the chat tells the one stop three times, once as *"turn failed: Internal error"*, once as the agent apologising, and the kernel's numbers read *"$0.00 over the $0.00 cap"* → **qal-j05** (cp-01/cp-02 now assert against it); `re-01` a parked ask is offered again to a client that attaches later, same id, and its answer completes the turn — pass; `re-02` an approval-blocked write across a kernel death is written up *"not run: the kernel restarted while this waited for the user"* and the file is not written — pass; `sv-01` `kind = "service"` reaches the hub roster's `kinds` — pass. Fourteen runs; pass rates J1 14/14 J2 14/14 J3 12/14 J4 11/14 J5 13/14 J6 6/14 J7 12/14 J8 6/14.

- **2026-09-16 18:03 and 18:09, #340's kernel (`71c5a812`) with the `efcab58f` desktop, two runs: 8/8 and 8/8.** J7 held both times — `CHANGELOG.md` where the user asked, no "not written yet" nudge, nothing moved into `.arbos/docs/` — **qal-j04 closed**. First runs with the liveness pulse: every driver answer after a launch, a send, a tab click, Stop or the opener sheet came back well inside the second (slowest 96 ms, after a tab click). Thirteen runs; pass rates J1 13/13 J2 13/13 J3 11/13 J4 10/13 J5 12/13 J6 5/13 J7 11/13 J8 5/13.

- **2026-09-16 17:24 and 17:33, `main` @ `efcab58f` (#329, #334 and the qal-j03 desktop fix in), two runs.** J6 passed both parts both times: on reopening, before any touch, `touched: false`, `tab_dot: true`, `unseen: 1`, no OS post on relaunch; the click set `touched: true` and cleared it; the away-tab reply badged, posted, and was confirmed by `dunstctl history`. **qal-j03 closed.** J8 passed both (side effect once, second project isolated, phone's J8c pass at 16:54). Run 1 lost J7 — `no CHANGELOG.md` — for the second run in a row, so the twice rule named it: **qal-j04**, the coordinator's brief puts `output: .arbos/docs/CHANGELOG.md` and the kernel's "Output … not written yet" nudge makes the worker *move* the file the user asked for out of the repo. Run 1's J4/J5 read `unverified` because root had delegated and gone idle before the mid-flight lines; the rig now treats a live worker's open turn as work in flight, and run 2 scored 8/8. Eleven runs; pass rates J1 11/11 J2 11/11 J3 9/11 J4 8/11 J5 10/11 J6 3/11 J7 9/11 J8 3/11.

- **2026-09-16 15:07 and 15:13, `main` @ `4b387fce` (#316, #317, #320 in; #324–#326 not yet on `main`), two runs.** Both `6/8 pass, 2 unverified, 0 fail` — the two unverified are the two parts this rig cannot check (J6 notifications, J8c dropped connection). J8a's side effect ran **once** in both runs and in 3/3 headless twins (`journey-j8a-headless`): **qal-j02 closed** by #316. No status bubble in either run: **qal-j01 closed** by #320. Six runs in the history now; pass rates J1 6/6, J2 6/6, J3 4/6, J4 5/6, J5 6/6, J7 6/6; J6 and J8 never `pass` here by construction until the two uncheckable parts get a surface. The J4 follow-up still reaches root at 0.0 s because root is idle after delegating; #317's busy-root case is not yet exercised by this challenge.

- **2026-09-16, `main` @ `c964294c`, four runs on the Linux rig.** Run 1 `4/8` (two harness misreads, corrected: J3 sampled a moment between the worker's report and root's wake; J4 compared against the kickoff turn instead of the challenge turn), run 2 `6/8`, run 3 `5/8` (J3 status-as-prose 11×; J8 harness read the transcript before the continued turn finished — corrected to poll for the answer), run 4 `6/8 pass, 1 unverified, 1 fail` — the fail is real: **qal-j02**, the command in flight when the kernel died ran twice (`side-effects.log` holds the tag 2×; the continued turn has no record of the first call). Pass rates over the four runs: J1 4/4, J2 4/4, J3 2/4, J4 3/4, J5 4/4, J6 0/4 (always unverified: notifications), J7 4/4, J8 0/4 (unverified ×2 for J8c; then the doubled side effect). Follow-up latency (`reached_root_s`) was 0.0 s on this build — the coordinator was idle when the line arrived, so #317's case (root busy) was not exercised yet. Real findings: **qal-j01** — Gemini writes the prompt's own `status "…"` form as a reply line and the chat draws it as a bubble (J3, seen in run 1; the desktop only hides the `status:` colon form); **the home-tab landing** (J6, above) — every relaunch lands on `~/.arbos`, so typing after "coming back" goes to the wrong place. The challenge itself passes on its merits: the worker fixes `area()`, writes `CHANGELOG.md` with the mid-flight QA line, runs the tests (`OK`), and commits on `fix/…`; `main` untouched. Steps never checkable here: J6 notifications, J8c dropped connection.
