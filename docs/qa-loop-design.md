> **RECOVERED, BADLY INCOMPLETE — this is not the original file, and it is a fragment that begins at a mid-document heading.** 5,608 of the original 45,197 bytes. Source: a `read_file` in the QA worker's transcript at 2026-09-13 17:38 UTC. The QA worker (`bc-f2e2f30d-1298-59f1-a24c-55113322de28`) has offered to rebuild this document faithfully from its bug files and scenario docstrings and should replace this file.
>
> The original was lost together with the whole `docs/` directory on 2026-09-16 between 07:43 and 09:01 UTC. Restored by the store-recovery worker `bc-0b112226-cf98-5cab-92c3-2671518dd9b9`. Cause, timeline and the full recovery inventory: `internal/store-docs-loss-2026-09-16.md`.

## Phases (each shippable)

1. **Done this turn**: runner, checker, 11 scenarios (8 no-model, 3 model incl. headline), 12 rollouts, 7 bug files (`internal/qa/bugs/qa-001` to `qa-007`), first fix PR with a repo test ([#7](https://github.com/unarbos/arbos/pull/7), `qa-001`). Headline scored 6/12 (items 1, 2, 6, 7, 10, 11 pass; `internal/qa/kickoff-history.jsonl`). Runs on demand on any machine with the kernel built: `python3 internal/qa/run.py --kernel <arbos-kernel> [--with-model]`.
2. **Loop on ArbosLife**: **running since 2026-09-12 23:53 UTC** (hourly timer, spend log, $10/day cap, results on the `qa-results` branch, `sync.sh` both ways). Since 2026-09-13 00:20 UTC each cycle also builds the branch named in each of the newest three inbox notes (own worktree, shared `target-inbox`) and runs that note's scenario against it, with the note's "what could break" list as the attack plan. Five benchmark scenarios (`bench-*`, one per failing checklist item) run every cycle. Still to do in this phase: model-written attack scenarios, triage-by-model. Fix PRs so far: #7 (qa-001), #10 (qa-003), #11 (qa-004), #12 (qa-007), #13 (qa-002), #14 (tracing gaps 1-3), #19 (qa-008), #20 (qa-009), #22 (qa-010), #24 (qa-005 + qa-006), #25 (qa-012), #26 (qa-013), #28 (qa-014). qa-011 is covered by #14. None merged yet. Attack surface widened 2026-09-13 01:00 UTC with `readonly-arbos`, `disk-full`, `clock-jump-cron`, `restart-during-compaction`, `steer-storm`, `spawn-storm` (24 scenarios). Desktop attacks (`desktop-rapid-session-switch`, `desktop-kill-kernel-under-ui`, `desktop-huge-transcript-scroll`) live in `internal/qa/desktop_scenarios.py`; they drive the gpui app under Xvfb through its JSON driver and skip when `ARBOS_DESKTOP_BIN`/`ARBOS_DESKTOP_DRIVER` are unset. The app is built from `rust` + #15 (`arbos-kernel run`/`attach`) + #4 (Linux gpui features) merged in a worktree; ArbosLife does not build it (no X11 dev libs there). Feature-branch checks: `steer-storm@steer-running-child` runs against #8's branch each cycle. First desktop results (2026-09-13 01:30 UTC): all three pass on the merged build; the only gap is the driver's macOS-only screenshot (qa-015), worked around with `xwd`. Best kickoff so far: 9/12 (01:10 UTC on `rust`; also 9/12 on the see/commit-nudges branch with item 8 passing). Feature-branch attacks now cover secrets-door (`secrets-leak-hunt`; found qa-016 on `rust`), see/commit nudges (three `bench-*`), composer pills and the user message card (desktop, under Xvfb). PR #31 fixes the driver's Linux screenshot (qa-015). **Release integration #58** (`cursor/release-integration-52cd`, tag v0.2.0): the whole library ran against it on 2026-09-13 06:48-07:20 UTC; kickoff 7, 7, 5 of 12 (vs 9 on `rust`), pulled down by qa-019 (`spawn kind="default"` refused; fix PR #66 into #58); also qa-017 (turn resurrects a deleted folder, 9/10 chats) and qa-018 (claim retry storm when a node write fails). Fix PRs into #58: #66 (merged), #68 (qa-017 + qa-018 + a #14/#24 merge slip), #70 (spawn kind fallback, plan condition message). After #66: 7 and 6 of 12. The hourly cycle now tracks #58 as a second branch (`ARBOS_QA_TRACK_BRANCHES`, `--integration`) until it merges; kickoff history lines carry `branch`. Proposals to the features agent go to `internal/features-inbox/` (first: `2026-09-13-benchmark-items-3-and-8.md`).
3. **Fix agents and gates**: fix-agent prompt template, `qa-fix` label, CI workflow on `rust` (build, test, fmt, clippy), QA gate job. PRs still merged by Jacob.
4. **Auto-merge**: merge bot with the five gates and the protected list, rollback rule, tags. Enabled by Jacob only.
5. **Kernel tracing additions** (can land any time after 1): structured kernel log, `error` frame, trace-to-transcript links. Each removes a gap below.

## File-based agent model (decided 2026-09-13)

Scenarios for `plan.md` + `subscriptions/` + `inbox/` + `waiting/` live in `internal/qa/fileplan_scenarios.py` (`fp-*`): authored inbox file (passes on #58 today), shell and timer subscriptions, parked ask with an answer file, kernel-written `done` to the parent, `plan.md` checklist, and a migration case (legacy `plan.jsonl` with a standing cron, a pending prompt and an open ask). They are gated on the feature branch named in the first inbox note that mentions `subscriptions/`; until then they skip even in integration mode. Kickoff item 9 now also passes on a `timer`/`shell` subscription file with `every`.

## Nightly SWE-bench slice (added 2026-09-13)

Arbos runs as a verifiers `Harness` (PR #94). `deploy/swebench-nightly.sh` runs the reference set of 16 SWE-bench Verified instances (`media/swebench/2026-09-13/results.json`) against the integration head every night after the 02:00 UTC cycle: kernel image built from the head plus `harness/` (from `cursor/swebench-harness` until #94 merges), Sonnet 5 through OpenRouter, two halves with the second skipped when the first spends half the $10 cap, 900 s per instance. `deploy/swebench-collect.py` writes one line per run to `swebench-history.jsonl` (solved/failed/errored, cost, per-instance), diffs solved→failed against the previous run on the same branch into `regressions` and a bug draft each, and copies failing bundles to `internal/qa/rollouts/swebench/<instance>--<run>/`. Reference: 12/16, $8.02. Triage of the first four failures: qa-022 (rewrites existing tests), qa-023 (no login shell, conda env off PATH), qa-024 (killed job says "no exit recorded"); scenario `bench-tests-are-spec` guards the first. Docker was installed on the QA VM for this (dockerd started by hand; no system bus here).
