> **REWRITTEN 2026-09-16 after the `docs/` loss.** The original (12,561 bytes, written 2026-09-13 08:19 UTC) had no recoverable copy; the fragments that survived were cut at 700 characters per line and were not used. This version is rebuilt by the same author (`bc-85561744-5b07-5bba-8097-d7eb6858d024`) from the evidence that survived: the two Arbos runs `media/parity/arbos/integration-2026-09-13/` and `media/parity/arbos/pr71-2026-09-13/` (stills, state dumps `*.json`, `manifest.json`), the Cursor reference frames in `media/parity/cursor/`, `media/parity/side-by-side-working-state-pr71.png`, and the driver script `internal/parity/parity_arbos.py`. Every observation was re-read from those files on 2026-09-16. Statements that rest on recollection are marked **[from memory]**. Loss record: `internal/store-docs-loss-2026-09-16.md`.
>
> Status note (2026-09-16): `notes.md` records that every row of this report was later closed and that live Cursor capture now runs inside the symmetry loop. Treat the gaps below as the state on 2026-09-13, not today.

# Cursor parity report, 2026-09-13

Second gap report. It re-runs the prompt set from `cursor-parity-process.md` on two Arbos builds one day after the first report and records what closed, what is new, and what is still open against Cursor.

## What changed since 2026-09-12

- The capture method changed from pixel clicks to the **driver socket**: `parity_arbos.py` (with `arbosdriver.py`) launches the app with `ARBOS_DRIVER_SOCKET`, clicks elements by id, waits for `state` to settle, and writes a still, a state dump and a manifest line at each capture point. Layout changes between branches no longer break the run.
- Two builds were captured:

| Run folder | Branch | What it carries |
|---|---|---|
| `media/parity/arbos/integration-2026-09-13/` | `cursor/release-integration-52cd` | The 09-12 fixes merged: right-aligned user card (#27), sub-agent lines + Tasks rail (#21), `Working N` pill (#23), `Worked Ns`, thumbs + relative time |
| `media/parity/arbos/pr71-2026-09-13/` | `cursor/window-layout-tabs-right-panel` (#71, `afa582a`) | New window layout: tab bar on top, right panel (Agents, Resources, Goals), breadcrumb header, panel fold |

Both: Xvfb `:1`, window 1600×1000, light appearance, `openai/gpt-5.4-mini` through OpenRouter. The Cursor side is still the **public frames** (sign-in on the VM is still blocked; `media/parity/cursor/vm-05-agents-window-login-wall-2026-09-13.png` shows the wall unchanged on 09-13).

Nondeterminism seen: p3 spawned three sub-agents on both builds this time (the prompt was unchanged; sessions went 2 → 5 in both manifests). p4 finished in 4 s on integration and 15 s on PR 71, so the follow-up p6 typed 3 s in only queued briefly.

## Gap table

Row numbers continue the 09-12 report where the aspect is the same. `Closed` means the 09-12 gap is no longer visible in the stills; `Open` means it is; `Partly` means some of it.

| # | Aspect | Cursor (public frames) | integration-2026-09-13 | pr71-2026-09-13 | State | Still open / suggested task |
|---|---|---|---|---|---|---|
| 1 | Composer and the row under it | Field, `+`, model, separate mic and send discs; under the field `Cloud` at left and a spinner at right while working | Field, `+`, `GPT 5.4 Mini ⌄`, one disc (mic idle / stop square running); **new**: a muted `Local` row under the field, no spinner. `p2-multifile-end.png`, `p3-subagents-t20s.png` | Same as integration. `idle-tab-cycled.png` | Partly | Spinner at the right of the `Local` row while the agent works; split mic from send/stop |
| 2 | Voice input | Words stream into the field while speaking | Mic click → `voice failed: voice dictation only works on this Mac · Retry`; tooltip `Hold Fn to talk`. `idle-voice-after-click.png` | Same. `p3-subagents-live-voice-after-click.png`, `idle-voice-after-click.png` | Open | Linux capture path (V-01); platform-neutral copy |
| 3 | Working status lines | `Setting up environment`, `Thought 1s`, `Explored …`, `Exploring 2 files, 8 searches`, `Planning next moves`, `Worked 21s` | **`Worked 5s`**, **`Worked 11s`** with grey seconds; live line `Working`; `Planning next moves` appears at the end of a turn. `p2-multifile-end.png`, `p3-subagents-t20s.png` | Same; `Planning next moves` visible in `p6-queued.png` | Partly | `Thought Ns` and the explore/search detail lines; shimmer on the live line **[from memory: still static]** |
| 4 | Pills above the composer | `⁘ Working 1`; `Working 2` `PRs 5` | **`⁘ Working 1`** pill above the field while the children run (`p3-subagents-t20s.png`); gone when idle | Not in the stills captured while children ran **[from memory: the pill did show on PR 71 in the QA pass the same morning, so this is a timing miss, not a regression]** | Closed (Working) | `PRs N` needs PR tracking (K-12) |
| 5 | Sub-agent status and highlight | `1 Working  Mapping  Grok tab architecture` under the parent; sub-agent as a tab; task list at right with circle/glyph/title, ticked when done | Under the parent: **`1 of 3 sub-agents working`** then one line per child — `✓ Done  Review math_utils.py…`, `✓ Done  Draft a CHANGELOG.md…`, `⟳ Working  Add docstrings…`; each child's result is folded into the parent as a titled section. A **context rail** at the right: `On master`, `Tasks · 1 working`, the three tasks with ticks. Children nested under the parent chat in the sidebar with titles, not ids. `p3-subagents-t8s.png`, `p3-subagents-t20s.png`, `p3-subagents-end.png` | Same inline lines (`3 sub-agents` + three `✓ Done` rows). The right panel's **Agents** tree lists `What does main.py print · main` and the three children with ticks; clicking one opens it in place with a **breadcrumb** header `What does main.py print › Review math_utils.py …`. `p3-subagents-live-panel.png`, `p3-subagents-live-subagent-open.png`, `idle-subagent-open.png` | Closed | Cursor opens a child as a **second tab**; PR 71 opens it in the same tab with a breadcrumb. Decide which; the breadcrumb is a reasonable stand-in |
| 6 | Sidebar / navigation | `New Chat`, `Search`, `Automations`, `Customize`; Projects with glyphs; `Today` / `Last 7 Days` / `Last 30 Days` / `Older`; status dots | Unchanged from 09-12: `Repositories` › folder › chats, relative age at the right, children nested under the parent. `p2-multifile-end.png` | Sidebar removed. A **tab bar** (`⌂ Home`, `parity-proj ×`, `+`) and, inside the panel, the Agents tree. No recents list, no search, no time buckets. `idle-tab-bar.png`, `idle-tab-cycled.png` | Open (both) | Integration: time buckets + Search (U-07). PR 71: the Home tab needs a recents/search list, else old chats are unreachable |
| 7 | User message | Right-aligned white card, shadow, capped width | **Right-aligned white card** with a thin border, capped to about 60 %, no visible pencil until hover. `p1-question-t2s.png`, `p2-multifile-end.png` | Same. `p6-queued.png` | Closed | Shadow is lighter than Cursor's **[from memory]**; fine to leave |
| 8 | Under a finished answer | 👍 👎 copy, `2m ago` | **copy · fork · 👍 · 👎 · `Just now`**. `p2-multifile-end.png` | Same. `p3-subagents-live-panel.png` | Closed | Check that `Just now` ages to `2m ago` **[from memory: not verified]** |
| 10 | Follow-up during a turn | Queued row under the active turn; Enter queues, ⌘Enter sends | p6 typed 3 s into p4; p4 ended at 4 s so the follow-up ran as the next turn. The state dump at capture (`p6-queued.json`) has `queued: 0`, `streaming: false`, `pills.working: 0` for the main session, so nothing was queued when the still was taken. `p6-queued.png` | p4 ran 15 s; `p6-queued.png` shows the follow-up already as a user card with `Planning next moves` under it; its `p6-queued.json` also reads `queued: 0` | Not exercised | Needs a longer p4 (add "do not stop until you have written 800 words") before this row can be judged |
| 14 | Window layout | Tab strip over the chat column; right `Project` panel with title, description, `View Project Page`, task list, `Nothing to Track Yet` when empty | Single header row, `…` menu, context rail only while children exist | **Tab bar** 1600×36 pt with `Home` and one tab per project, `+`; **right panel** with the project name and path/branch (`/tmp/parity-proj · master`), `Agents`, `Resources · 4 agents`, `Goals · No goals yet. · Set goals…`, gear and `+` at the bottom; `cmd-b` folds it (`idle-panel-folded.png`); `ctrl-tab` cycles to Home (`idle-tab-cycled.png`). `idle-panel.png`, `p3-subagents-live-tab-bar.png` | Closed on PR 71 | Cursor's panel is about the *project* (description, page link, task list); Arbos's is about *agents and resources*. Add the project description block and a `Set goals…` result that shows in the panel — clicking `Set goals…` only drops a prompt into the composer (`idle-set-goals-prompt.png`) |
| 15 | Tab bar details | Tabs carry a glyph and a title; the active tab is white on grey; `+` opens a new tab; a working sub-agent tab shows a spinning glyph | – | Glyph + title + `×`, active tab white; no working indicator on a tab while its agents run **[from memory]** | New | Spinner or dot on a tab whose agents are working |
| 16 | Settings | Gear at the bottom of the sidebar opens settings in place **[from memory: Cursor uses a settings page, not a window]** | Gear at the bottom of the sidebar opens a separate **Settings window**. `idle-settings-window.png` | Gear at the bottom of the right panel, same window. `idle-settings-window.png` | Different | Keep the window; it is not a look-and-feel blocker |
| 17 | Empty chat | Centred project card (glyph, title, description, `View Project Page`) above the transcript | Centred `New chat` title and composer with placeholder `Plan, search, build anything` | Same, inside the `Home` tab. `idle-tab-cycled.png` | Open | Project card on the empty state once a folder is open |

Rows 9, 11, 12, 13 of the 09-12 report (file chips, colours, spacing, motion) were not re-judged in this run because the driver run does not capture them any better than the pixel run did. **[from memory]** The 09-13 original said the blue tint in body text was still there; the 09-13 stills confirm blue-tinted prose in both builds.

## Top 5 open gaps on 2026-09-13

1. Voice does not exist on Linux and the copy says "this Mac" (row 2).
2. The under-composer row has the machine label but no activity spinner, and mic/send still share a disc (row 1).
3. Navigation: no time buckets or search on integration; PR 71's Home tab has no recents at all (row 6).
4. Status vocabulary: no `Thought Ns`, no explore/search detail lines, no shimmer (row 3).
5. The right panel is agents-centred where Cursor's is project-centred; `Set goals…` has no visible result (row 14).

## Media index

All paths relative to `/cursor/stores/bc-ec8c092a-3084-4e3e-9e34-7b2a1f8c6983/`; every `.png` has a `.json` state dump beside it and each run folder has a `manifest.json`.

- `media/parity/arbos/integration-2026-09-13/`: `00-launch`, `00-empty-chat`, `p1-question-t2s`, `p1-question-end`, `p2-multifile-t2s`, `p2-multifile-t8s`, `p2-multifile-end`, `p3-subagents-t2s`, `p3-subagents-t8s`, `p3-subagents-t20s`, `p3-subagents-live-settings-window`, `p3-subagents-end`, `p4-t2s`, `p4-t3s`, `p6-queued`, `p4-end`, `idle-settings-window`, `idle-voice-after-click`, `99-final` (all `.png`); `p1-question.mp4`, `p2-multifile.mp4`, `p3-subagents.mp4`, `p4-longrun-p6-followup.mp4`.
- `media/parity/arbos/pr71-2026-09-13/`: the same names plus `p3-subagents-live-tab-bar`, `p3-subagents-live-tab-cycled`, `p3-subagents-live-panel`, `p3-subagents-live-subagent-open`, `p3-subagents-live-set-goals-prompt`, `p3-subagents-live-panel-folded`, `p3-subagents-live-voice-after-click`, `p4-t8s`, `idle-tab-bar`, `idle-tab-cycled`, `idle-panel`, `idle-subagent-open`, `idle-set-goals-prompt`, `idle-panel-folded` (all `.png`), and the four `.mp4`.
- Cursor: `media/parity/cursor/public-video-frame-*.png`, `public-docs-*.png`, `public-changelog-projects-*.mp4`, `vm-05-agents-window-login-wall-2026-09-13.png`.
- Side by side: `media/parity/side-by-side-working-state-pr71.png` (Cursor launch-video frame beside PR 71 p3 at 20 s).
