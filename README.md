# Arbos

# One liner
```bash
curl -fsSL https://arbos.life | bash -s -- web
```

# Usage
```bash
arbos .          # open the web UI for this directory 
# or
arbos            # run in the terminal (TUI)
```
