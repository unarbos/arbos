[package]
name = "arbos-core"
version = "0.2.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Arbos file kernel types and place store"

[dependencies]
anyhow.workspace = true
fs4 = "0.13"
serde.workspace = true
serde_json.workspace = true
# Content hashes for compare-and-swap writes across the mesh.
sha2 = "0.10"
toml = "0.8"
libc = "0.2"

[dev-dependencies]
tempfile = "3"
