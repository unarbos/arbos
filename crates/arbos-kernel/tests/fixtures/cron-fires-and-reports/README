A standing shell node with a report, due one second before the kernel's clock
starts. Expect: one say line "BTC: 42", node done, kernel idle.
The state lives in dot-arbos/ (the repo ignores .arbos/); the runner renames it.
